---
phase: 22-multi-recipe-per-message-topic-1
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [BATCH-01, BATCH-02, BATCH-03, BATCH-04, BATCH-05]
tags: [smoke, manual-checkpoint, requirements-tick, operator-gate]
must_haves:
  truths:
    - "22-SMOKE.md exists with verdict: pending and explicit operator runbook referencing the two EVAL-RESULTS files"
    - "After operator runs the eval harness, 22-EVAL-RESULTS-openai.md (merge gate, >=95% count + >=90% name) and 22-EVAL-RESULTS-ollama.md (informational) are filled and committed"
    - "22-SMOKE.md verdict is pass | pivot | fail (NOT pending) after operator review"
    - "If verdict=pass: REQUIREMENTS.md BATCH-01..05 are ticked Complete and the traceability table is updated"
    - "If verdict=pivot: defensive code cap follow-up is filed; phase does NOT tick BATCH-01..05 yet"
    - "If verdict=fail: phase re-opens planning (do NOT tick requirements)"
  artifacts:
    - path: ".planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md"
      provides: "Operator's final verdict document"
      contains: "verdict:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BATCH-01..05 status (ticked on pass only)"
  key_links:
    - from: "22-SMOKE.md"
      to: "22-EVAL-RESULTS-openai.md (merge gate)"
      via: "explicit cross-reference"
      pattern: "22-EVAL-RESULTS-openai\\.md"
    - from: "22-SMOKE.md"
      to: "22-EVAL-RESULTS-ollama.md (informational)"
      via: "explicit cross-reference"
      pattern: "22-EVAL-RESULTS-ollama\\.md"
---

<objective>
The operator runs the eval harness from Plan 03 against Ollama (dev,
informational) and OpenAI (staging, merge gate >=95% count + >=90% name
per D-04), commits the two filled EVAL-RESULTS files, then commits
22-SMOKE.md with an explicit verdict. On verdict=pass, BATCH-01..05 in
REQUIREMENTS.md flip to Complete.

Purpose: D-15 — the eval is the load-bearing acceptance evidence for
BATCH-01..05. No unit test can measure "does the LLM extract 3 recipes
from a Spanish multi-recipe utterance". Phase 22 stays `human_needed`
until the operator commits 22-SMOKE.md with verdict in {pass, pivot, fail}.

Output: 22-SMOKE.md scaffolded by Claude (Task 1); filled-in EVAL-RESULTS
files + verdict (Task 2 operator checkpoint); REQUIREMENTS ticks (Task 3,
conditional on verdict=pass).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-multi-recipe-per-message-topic-1/22-CONTEXT.md
@.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md
@.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md
@.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 22-SMOKE.md with verdict pending + operator runbook</name>
  <files>.planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md</files>
  <read_first>
    - .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md (template: frontmatter, runbook, verdict structure)
    - .planning/phases/22-multi-recipe-per-message-topic-1/22-CONTEXT.md decisions D-04 (thresholds), D-05 (results file structure), D-15 (operator gate), D-16 (V005 retained)
  </read_first>
  <action>
    Write `.planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` with these sections in order:

    1. Frontmatter (YAML between `---` fences):
       - `verdict: pending`
       - `date: <YYYY-MM-DD — operator fills>`
       - `operator: <name — operator fills>`

    2. `# Phase 22 Multi-Recipe Eval Smoke — Final Verdict` header.

    3. Intro paragraph (<= 3 lines): purpose; cross-references 22-EVAL-SET.md and the two EVAL-RESULTS files; D-15 note that this verdict is the load-bearing acceptance gate for BATCH-01..05.

    4. `## Sources` bulleted list:
       - 22-EVAL-SET.md (30 utterances, 10 classes)
       - 22-EVAL-RESULTS-ollama.md (informational)
       - 22-EVAL-RESULTS-openai.md (MERGE GATE per D-04)

    5. `## Thresholds (per D-04)`:
       - OpenAI staging: >=95% count accuracy + >=90% name accuracy on multi-recipe rows = PASS
       - Ollama dev: informational only (any < 70% noted for future model upgrade)
       - Anthropic: optional, operator's choice

    6. `## Operator runbook` (numbered steps):
       1. Ensure `OPENAI_API_KEY` and `LANGWATCH_API_KEY` are exported. (`OLLAMA_URL` + Ollama daemon with gpt-oss:20b optional.)
       2. Run dev pass: `uv run experiments.multi_recipe_eval --backend ollama` — produces filled 22-EVAL-RESULTS-ollama.md.
       3. Eyeball the per-class breakdown; note anything in the Ollama results "Notes" section.
       4. Run merge-gate pass: `uv run experiments.multi_recipe_eval --backend openai` — produces filled 22-EVAL-RESULTS-openai.md.
       5. Review the OpenAI results table — confirm count-correct and name-correct numbers vs the thresholds above.
       6. Commit both filled EVAL-RESULTS files.
       7. Fill the "Verdict" section of this file; set frontmatter `verdict:` to one of `pass`, `pivot`, `fail`.
       8. Commit 22-SMOKE.md.
       9. If verdict=pass: tick BATCH-01..05 in `.planning/REQUIREMENTS.md` (Task 3 below — operator may delegate to Claude in a follow-up commit).

    7. `## Verdict` section with placeholders the operator fills:
       - `**OpenAI staging (merge gate):** PENDING — <PASS / FAIL — operator notes>`
       - `**Ollama dev (informational):** PENDING — <note>`
       - `**Decision:** PENDING — operator selects one of:`
         - `GO (pass)` — V006 prompt + eval evidence sufficient → tick BATCH-01..05.
         - `PIVOT (pivot)` — OpenAI failed merge gate → file defensive `StartWorkflowTool.max_calls_per_turn` follow-up per CONTEXT Deferred Ideas; do NOT tick BATCH-01..05 yet.
         - `NO-GO (fail)` — re-open phase planning; do NOT tick BATCH-01..05.
       - `**Verdict (set in frontmatter):** pending → pass / pivot / fail`

    8. `## Follow-ups by verdict`:
       - `pass` — Task 3 below (REQUIREMENTS.md ticks).
       - `pivot` — File quick-task: defensive code cap on StartWorkflowTool.max_calls_per_turn = 5; re-run smoke; if pass after pivot, then tick BATCH-01..05.
       - `fail` — Re-open `/gsd:plan-phase 22 --gaps`; iterate on V006 prompt; re-run smoke.

    Do NOT fill the verdict yourself; leave it `pending`. Do NOT tick REQUIREMENTS.md
    in this task — that is Task 3 (conditional on operator verdict).
  </action>
  <verify>
    <automated>test -f .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md && grep -q "^verdict: pending" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` exists
    - `grep -c "^verdict: pending" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` == 1
    - `grep -cE "MERGE GATE|merge gate" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
    - `grep -c "22-EVAL-RESULTS-openai.md" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
    - `grep -c "22-EVAL-RESULTS-ollama.md" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
    - `grep -c "Operator runbook" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
    - `grep -c "uv run experiments.multi_recipe_eval --backend ollama" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
    - `grep -c "uv run experiments.multi_recipe_eval --backend openai" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` >= 1
  </acceptance_criteria>
  <done>
    22-SMOKE.md scaffolded; verdict pending; runbook explicit; both EVAL-RESULTS files cross-referenced.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs eval harness on both backends and fills 22-SMOKE.md verdict</name>
  <what-built>
    Plan 03 created the eval harness (`experiments.multi_recipe_eval`) and the per-backend
    result templates with `verdict: pending`. Plan 04 Task 1 scaffolded 22-SMOKE.md with
    the operator runbook. Tasks 1-2 of Plans 01-02 landed the code + prompt + tests
    backing the V006 behavior under eval.
  </what-built>
  <how-to-verify>
    The operator performs these steps locally (cannot be automated — requires real OpenAI
    API key and operator judgment on PASS/FAIL):

    1. Ensure `OPENAI_API_KEY` and `LANGWATCH_API_KEY` are exported in the shell.
       (`OLLAMA_URL` + Ollama daemon with `gpt-oss:20b` is OPTIONAL — skip dev pass if absent.)

    2. Run dev pass (if Ollama available):
       `uv run experiments.multi_recipe_eval --backend ollama`
       Inspect the generated `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md`.
       Note any patterns in the "Notes" section. Per D-04 this is informational only —
       Ollama failures do NOT block merge.

    3. Run merge-gate pass:
       `uv run experiments.multi_recipe_eval --backend openai`
       Inspect the generated `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md`.

    4. Check thresholds against the OpenAI results:
       - Count accuracy >= 95% (e.g. 28+ correct out of 30)?
       - Name accuracy on multi-recipe rows >= 90%?
       - If BOTH yes → verdict candidate = pass.
       - If count accuracy < 95% → verdict candidate = pivot (file the defensive `StartWorkflowTool.max_calls_per_turn` follow-up) or fail (re-open planning).

    5. Commit both filled EVAL-RESULTS files:
       `git add .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-*.md`
       `git commit -m "test(22): commit eval results — openai=<pass|fail>, ollama=<note>"`

    6. Edit `.planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md`:
       - Fill in the `Verdict` section with PASS/FAIL per backend + the chosen decision.
       - Set the frontmatter `verdict:` field to `pass`, `pivot`, or `fail`.
       - Fill `date:` and `operator:` in the frontmatter.

    7. Commit 22-SMOKE.md:
       `git add .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md`
       `git commit -m "test(22): manual eval smoke — verdict <pass|pivot|fail>"`

    Confirm completion: 22-SMOKE.md frontmatter `verdict:` is NOT `pending`.
  </how-to-verify>
  <files>
    .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md,
    .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md,
    .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md
  </files>
  <action>
    OPERATOR-DRIVEN CHECKPOINT (see <how-to-verify> above for full runbook).

    Summary of operator actions:
    1. Export OPENAI_API_KEY + LANGWATCH_API_KEY (Ollama optional).
    2. Run `uv run experiments.multi_recipe_eval --backend ollama` (informational; skip if Ollama absent).
    3. Run `uv run experiments.multi_recipe_eval --backend openai` (MERGE GATE).
    4. Review both filled EVAL-RESULTS markdown files; check OpenAI vs >=95% count / >=90% name thresholds.
    5. Commit both EVAL-RESULTS files.
    6. Edit 22-SMOKE.md: fill Verdict section + set frontmatter `verdict:` to `pass`, `pivot`, or `fail`; fill `date:` and `operator:`.
    7. Commit 22-SMOKE.md.

    Claude does NOT perform these actions — verdict + threshold judgment is human-only.
  </action>
  <verify>
    <automated>grep -E "^verdict: (pass|pivot|fail)$" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^verdict: (pass|pivot|fail)$" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` == 1
    - `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md` has been committed in a state different from the Plan 03 template (operator filled in observed counts)
    - On verdict=pass: OpenAI results show >=95% count accuracy and >=90% name accuracy on multi-recipe rows
    - On verdict=pivot: operator notes the failing threshold in the Verdict section
    - On verdict=fail: operator notes the systemic issue in the Verdict section
  </acceptance_criteria>
  <done>
    22-SMOKE.md frontmatter `verdict:` is `pass`, `pivot`, or `fail` (NOT `pending`); both EVAL-RESULTS files committed.
  </done>
  <resume-signal>
    Reply with the chosen verdict — one of:
    - `verdict pass` — proceed to Task 3 (REQUIREMENTS.md ticks).
    - `verdict pivot` — Task 3 is SKIPPED for now; file the defensive code cap follow-up task per CONTEXT Deferred Ideas; re-run the smoke after the pivot lands.
    - `verdict fail` — Task 3 is SKIPPED; re-open phase planning via `/gsd:plan-phase 22 --gaps`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Conditional REQUIREMENTS.md ticks for BATCH-01..05 (only on verdict=pass)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md (CONFIRM frontmatter `verdict: pass` before any edits — abort if pivot/fail/pending)
    - .planning/REQUIREMENTS.md lines covering BATCH-01..05 (around lines 34-40 in the requirements list and the Traceability table around lines 146-150)
  </read_first>
  <action>
    PRECONDITION: Read 22-SMOKE.md frontmatter. If `verdict:` is NOT `pass`, STOP and write a note in the plan SUMMARY explaining the skip — do not edit REQUIREMENTS.md.

    If verdict = pass:

    Step 1 — Find the BATCH-01..05 list in `.planning/REQUIREMENTS.md` (around lines 34-40, under `### Multi-Recipe per Message (Topic 1)`). Each line currently reads `- [ ] **BATCH-XX**: ...`. For each of BATCH-01, BATCH-02, BATCH-03, BATCH-04, BATCH-05, change `- [ ]` to `- [x]`. Do NOT change the requirement text.

    Step 2 — Find the Traceability table (around lines 146-150). Each row currently reads:
      `| BATCH-XX | Phase 22 | Pending |`
    Change `Pending` to `Complete` for BATCH-01, BATCH-02, BATCH-03, BATCH-04, BATCH-05.

    Step 3 — Update the "Last updated" line at the bottom of REQUIREMENTS.md to today's date with a brief note:
      `*Last updated: <today ISO> — Phase 22 BATCH-01..05 marked Complete after operator eval smoke verdict=pass (see .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md)*`

    Step 4 — Do NOT touch ARCH-*, WAKE-*, TOOLS-*, EVAL-*, URL-*, IMG-*, DASH-*, EXP-* lines. Only BATCH-01..05.
  </action>
  <verify>
    <automated>grep -E "^- \[x\] \*\*BATCH-(0[1-5])\*\*" .planning/REQUIREMENTS.md | wc -l | grep -q "^5$"</automated>
  </verify>
  <acceptance_criteria>
    - Precondition holds: `grep "^verdict: pass" .planning/phases/22-multi-recipe-per-message-topic-1/22-SMOKE.md` returns exactly 1 line (otherwise this task is correctly skipped and that skip is documented in the SUMMARY)
    - On verdict=pass:
      - `grep -cE "^- \\[x\\] \\*\\*BATCH-0[1-5]\\*\\*" .planning/REQUIREMENTS.md` == 5
      - `grep -cE "^- \\[ \\] \\*\\*BATCH-0[1-5]\\*\\*" .planning/REQUIREMENTS.md` == 0
      - Traceability table: `grep -cE "^\\| BATCH-0[1-5] \\| Phase 22 \\| Complete" .planning/REQUIREMENTS.md` == 5
      - `grep -cE "^\\| BATCH-0[1-5] \\| Phase 22 \\| Pending" .planning/REQUIREMENTS.md` == 0
      - "Last updated" line at the bottom of REQUIREMENTS.md references Phase 22 / verdict=pass
    - On verdict in {pivot, fail, pending}: this task's SUMMARY explains the skip and REQUIREMENTS.md is unchanged
  </acceptance_criteria>
  <done>
    Either BATCH-01..05 ticked Complete (verdict=pass), or skip documented in SUMMARY (verdict != pass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-edited markdown files committed to repo | Manual artifact subject to typos/leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Information Disclosure | 22-SMOKE.md + EVAL-RESULTS files | mitigate | Runbook explicitly instructs operator to review committed files for API keys / PII; templates contain only model names + trace IDs + counts. |
| T-22-10 | Repudiation | Verdict choice | mitigate | Verdict is committed to git with operator identity; D-15 makes the operator gate explicit; rollback path documented (V005 retained per D-16). |
</threat_model>

<verification>
- 22-SMOKE.md exists with `verdict: pending` after Task 1
- After operator runs Task 2: 22-SMOKE.md frontmatter `verdict:` is one of `pass`, `pivot`, `fail`
- After Task 3 (if verdict=pass): all 5 BATCH-XX rows in REQUIREMENTS.md show `[x]` and Traceability shows `Complete`
- `uv run pytest -x` full suite still green at phase close (no test should depend on the smoke verdict directly)
</verification>

<success_criteria>
- Operator-driven eval has been run on at least the OpenAI staging backend (merge gate)
- 22-SMOKE.md committed with a definitive verdict
- On verdict=pass: REQUIREMENTS.md BATCH-01..05 ticked and traceability table updated
- On verdict in {pivot, fail}: follow-up path documented; requirements remain pending until follow-up closes
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-recipe-per-message-topic-1/22-04-SUMMARY.md`
</output>
