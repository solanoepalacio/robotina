---
phase: 22-multi-recipe-per-message-topic-1
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - experiments/robotina/__init__.py
  - experiments/robotina/multi_recipe_eval.py
  - pyproject.toml
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md
  - .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-05]
tags: [eval, experiments, langwatch, harness, smoke]
must_haves:
  truths:
    - "experiments/robotina/multi_recipe_eval.py is runnable via `uv run experiments.multi_recipe_eval --backend <ollama|openai|anthropic>`"
    - "Harness loads V006 prompt via AGENT_REGISTRY['handle-incoming-message']"
    - "Harness stubs Respond / StartWorkflow / Terminate tools so they record calls but do NOT enqueue real workflows or send Telegram messages"
    - "Harness parses 22-EVAL-SET.md utterance table, dispatches each utterance through the agent, counts start-workflow tool calls, captures recipe values, compares vs expected, writes 22-EVAL-RESULTS-<backend>.md"
    - "Harness sets per-utterance LangWatch metadata (experiment, phase=22, prompt_version=V006, utterance_id, backend, model)"
    - "22-EVAL-SET.md has >= 30 Spanish utterances covering the 10 coverage classes from CONTEXT D-03"
    - "22-EVAL-RESULTS-<backend>.md templates exist with the operator-fill table shape and aggregate / go-no-go sections"
    - "pyproject.toml [project.scripts] has experiments.multi_recipe_eval entry"
  artifacts:
    - path: "experiments/robotina/__init__.py"
      provides: "Package marker"
    - path: "experiments/robotina/multi_recipe_eval.py"
      provides: "Per-backend eval harness"
      min_lines: 150
    - path: ".planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md"
      provides: "30+ Spanish utterance eval set across 10 coverage classes"
      min_lines: 60
    - path: ".planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md"
      provides: "Operator-filled Ollama eval results template"
    - path: ".planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md"
      provides: "Operator-filled OpenAI eval results template"
    - path: "pyproject.toml"
      provides: "uv run shortcut for the eval harness"
      contains: "experiments.multi_recipe_eval"
  key_links:
    - from: "experiments/robotina/multi_recipe_eval.py"
      to: "AGENT_REGISTRY['handle-incoming-message']"
      via: "get_agent_config() + LLMBackend.create_agent()"
      pattern: "get_agent_config\\(.*handle-incoming-message"
    - from: "experiments/robotina/multi_recipe_eval.py"
      to: "22-EVAL-SET.md"
      via: "markdown table regex parser"
      pattern: "EVAL-SET\\.md"
    - from: "pyproject.toml [project.scripts]"
      to: "experiments.robotina.multi_recipe_eval:main"
      via: "uv run entry point"
      pattern: "experiments\\.multi_recipe_eval"
---

<objective>
Build the load-bearing acceptance evidence for Phase 22: a 30+ Spanish
utterance eval set and a per-backend harness that runs V006 against the
configured LLM, counts start-workflow tool calls per utterance, compares
against expected counts and names, and emits a per-backend markdown report.

Purpose: BATCH-01..05 reliability is empirical — no unit test can measure
"does the LLM extract 3 recipes from a Spanish multi-recipe utterance".
The harness + eval set are the proof. The operator runs them against
Ollama (dev, informational) and OpenAI (staging, merge gate ≥95% count
accuracy per D-04).

Output: One script, one package marker, one pyproject entry, three doc
files (eval set + two per-backend results templates).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-multi-recipe-per-message-topic-1/22-CONTEXT.md
@.planning/phases/22-multi-recipe-per-message-topic-1/22-RESEARCH.md
@.planning/phases/22-multi-recipe-per-message-topic-1/22-PATTERNS.md
@.planning/phases/22-01-SUMMARY.md
@.planning/phases/22-02-SUMMARY.md
@experiments/recipe_research.py
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval-set doc + per-backend result templates</name>
  <files>
    .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md,
    .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md,
    .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md
  </files>
  <read_first>
    - .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md (template structure for table + runbook + verdict)
    - .planning/phases/22-multi-recipe-per-message-topic-1/22-CONTEXT.md §"Eval set design" D-03 (10 coverage classes, ≥3 utterances each) + D-04 (thresholds) + D-05 (results file structure)
  </read_first>
  <action>
    Step 1 — Create `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` with:

    1a. Frontmatter:
        ```
        ---
        version: 1
        created: 2026-05-20
        coverage_classes: 10
        utterance_count: 30
        ---
        ```

    1b. Body sections:
        - "# Phase 22 Multi-Recipe Eval Set" header
        - Brief intro paragraph (≤ 3 lines) — purpose: empirical guarantee that BATCH-01..05 hold against the production LLM (OpenAI staging is the merge gate per D-04).
        - "## Coverage Classes" — bulleted list mapping the 10 classes from CONTEXT D-03:
          1. Single-recipe (N=1)
          2. Multi-recipe N=2
          3. Multi-recipe N=3
          4. Multi-recipe N=4 or 5
          5. Over-cap N>5
          6. Compound dish (ambiguous 1-vs-2) — prefer-fewer = 1 workflow per D-11
          7. Sauce-on-recipe (1 not 2) per D-12
          8. Sanity / must-NOT-split (multi-word recipe names, English noun phrases)
          9. Ambiguous non-recipe / clarify-via-respond per D-10
          10. URL-mention deflection (URL handling deferred to Phase 23 — V006 must respond "no manejo enlaces" + terminate)

        - "## Utterances" — a markdown table with the following columns:
          `| # | Utterance (Spanish) | Class | Expected N start-workflow | Expected recipe value(s) | Expected respond() | Notes |`

          Populate with EXACTLY these 30 rows (you may rephrase utterances slightly to feel natural Argentine Spanish, but keep the per-class counts as written — at least 3 per class):

          Class 1 (Single-recipe, N=1): 3 rows
            - "agregá lentejas" → N=1, value=["lentejas"], respond=ack
            - "quiero hacer milanesas" → N=1, value=["milanesas"], respond=ack
            - "guardame la receta del puré de papas" → N=1, value=["puré de papas"], respond=ack

          Class 2 (Multi-recipe N=2): 3 rows
            - "agregá canelones y pollo al horno" → N=2, value=["canelones","pollo al horno"], respond=ack-all
            - "guardá lentejas y arroz con verduras" → N=2
            - "querría guardar empanadas de carne y tarta de zapallitos" → N=2

          Class 3 (Multi-recipe N=3): 3 rows
            - "agregá canelones, pollo al horno y arroz pilaf" → N=3
            - "guardá milanesas, puré y ensalada mixta" → N=3
            - "agregame fideos con tuco, salmón al horno y postre de chocolate" → N=3

          Class 4 (Multi-recipe N=4 or 5): 3 rows
            - "agregá canelones, pollo, arroz y lentejas" → N=4
            - "guardá milanesas, puré, ensalada, postre y pan casero" → N=5
            - "agregame empanadas, tarta, sopa, fideos y flan" → N=5

          Class 5 (Over-cap N>5 → 0 workflows + ask-to-split): 3 rows
            - "agregá canelones, pollo, arroz, lentejas, milanesas, salmón, ravioles" → N=0, respond="ask-to-split"
            - "guardá estas: pizza, empanadas, tarta, milanesas, sopa, postre, pan, fideos" → N=0
            - "agregá rápido: lentejas, garbanzos, arroz, pollo, papas, zanahorias, calabaza" → N=0

          Class 6 (Compound dish, prefer-fewer → 1 workflow): 3 rows
            - "agregá pollo al horno con papas" → N=1, value=["pollo al horno con papas"]
            - "guardá milanesas con puré" → N=1, value=["milanesas con puré"]
            - "agregame pescado a la plancha con verduras grilladas" → N=1

          Class 7 (Sauce-on-recipe → 1 workflow): 3 rows
            - "agregá canelones con salsa blanca y boloñesa" → N=1, value=["canelones con salsa blanca y boloñesa"]
            - "guardá ñoquis con salsa rosa y queso rallado" → N=1
            - "agregame ravioles con manteca y salvia" → N=1

          Class 8 (Sanity / must-NOT-split): 3 rows
            - "salt and pepper chicken" → N=1, value=["salt and pepper chicken"]
            - "papa rellena" → N=1, value=["papa rellena"]
            - "pollo a la portuguesa" → N=1, value=["pollo a la portuguesa"]

          Class 9 (Ambiguous → respond clarify + terminate, 0 workflows): 3 rows
            - "hola" → N=0, respond="clarify-what-recipe"
            - "agregá algo rico" → N=0, respond="clarify"
            - "qué hago de cena" → N=0, respond="clarify-or-suggest-not-add"

          Class 10 (URL deflection — Phase 23 scope): 3 rows
            - "agregá esta receta: https://example.com/canelones" → N=0, respond="no manejo enlaces directos"
            - "guardame https://cookpad.com/recetas/123 plis" → N=0
            - "https://recipes.test.example/x — agregala" → N=0

        Per-row "Expected respond()" semantics: short tag like `ack`, `ack-all`, `ask-to-split`,
        `clarify-what-recipe`, `no manejo enlaces` — these become substring assertions the
        harness checks against the recorded `respond(text=...)` payload.

    Step 2 — Create `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md`
    with the operator-fill template:

    ```markdown
    ---
    verdict: pending
    backend: ollama
    model: gpt-oss:20b
    date: <YYYY-MM-DD — operator fills>
    operator: <name — operator fills>
    eval_set_version: 1
    ---

    # Phase 22 Multi-Recipe Eval Results — Ollama (dev backend)

    **Status:** PENDING — operator runs `uv run experiments.multi_recipe_eval --backend ollama`.

    Per D-04: Ollama results are **informational only**. Failures here do NOT block merge.

    ## Threshold
    - Count accuracy: report only (no threshold).
    - Recipe-name accuracy: report only.

    ## Per-utterance results
    [Auto-generated by the harness — table with: # | Utterance | Class | Expected N | Observed N | Observed recipe values | OK? | LangWatch trace]

    ## Aggregate
    - Total utterances: 30
    - Count-correct: __ / 30
    - Name-correct (on multi-recipe rows): __ / __
    - Per-class breakdown: __

    ## Notes
    [Operator narrative — patterns observed, classes that struggle, etc.]

    ## Go / No-Go (informational)
    Pass threshold for record-keeping: ≥ 70% count accuracy is "model is usable for dev".
    Below 70%: note model-upgrade candidate for future tracking.
    ```

    Step 3 — Create `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md`
    with the same shape but:
    - `backend: openai`
    - `model: <operator fills — e.g. gpt-4o-mini>`
    - "**Status:** PENDING — operator runs `uv run experiments.multi_recipe_eval --backend openai`."
    - **Per D-04: OpenAI is the MERGE GATE. ≥ 95% count accuracy required to PASS. ≥ 90% name accuracy on multi-recipe rows required to PASS.**
    - Go / No-Go section ends with explicit verdict line: `verdict: pass | fail | pivot`
      where `pivot` triggers the deferred defensive code cap per CONTEXT Deferred Ideas.

    Use `uv run` directly in the runbook commands (do not introduce new env vars).
  </action>
  <verify>
    <automated>test -f .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md && test -f .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md && test -f .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist
    - `grep -c "^| " .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` >= 31 (30 utterance rows + header row; tolerant to extra column separators)
    - `grep -c "agregá canelones, pollo, arroz, lentejas, milanesas, salmón, ravioles" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` == 1 (anchor over-cap row exists)
    - `grep -c "canelones con salsa blanca y boloñesa" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` == 1 (anchor sauce row exists)
    - `grep -c "salt and pepper chicken" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` == 1 (anchor sanity row exists)
    - `grep -cE "^Class [0-9]+|Coverage Classes" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md` >= 1 (coverage classes listed)
    - `grep -c "verdict: pending" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md` == 1
    - `grep -c "verdict: pending" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md` == 1
    - `grep -c "MERGE GATE" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-openai.md` >= 1
    - `grep -c "informational only" .planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-ollama.md` >= 1
  </acceptance_criteria>
  <done>
    Eval-set doc has 30+ Spanish utterances across 10 classes; both per-backend
    result templates exist with verdict=pending and clear thresholds (informational
    for Ollama, ≥95% merge gate for OpenAI).
  </done>
</task>

<task type="auto">
  <name>Task 2: Eval harness script + pyproject.toml entry + package marker</name>
  <files>
    experiments/robotina/__init__.py,
    experiments/robotina/multi_recipe_eval.py,
    pyproject.toml
  </files>
  <read_first>
    - experiments/recipe_research.py (full file — LangWatch boilerplate, agent build pattern, tracer setup, force_flush, main() shape)
    - src/robotina/agent/agents.py (get_agent_config interface)
    - src/robotina/llm/__init__.py (LLMBackend factory selection by env / backend name — confirm how recipe_research.py picks the backend)
    - src/robotina/agent/tools/respond.py, start_workflow.py, terminate.py (tool class signatures so the harness can swap stubs at the same call sites)
    - pyproject.toml [project.scripts] section (existing experiments.recipe_research / experiments.recipe_load entries)
    - .planning/phases/22-multi-recipe-per-message-topic-1/22-PATTERNS.md §"experiments/robotina/multi_recipe_eval.py" (full skeleton including count_start_workflow_calls helper)
    - .planning/phases/22-multi-recipe-per-message-topic-1/22-RESEARCH.md §"Environment Availability" (the harness MUST stub StartWorkflowTool so it does NOT enqueue real workflows)
  </read_first>
  <action>
    Step 1 — Create `experiments/robotina/__init__.py` as an empty file (package marker).

    Step 2 — Create `experiments/robotina/multi_recipe_eval.py`. The script MUST:

    A. Header (module docstring) — purpose, BATCH-01..05 acceptance role, usage examples,
       D-15 "load-bearing manual gate" note, warning that it stubs tool side effects (no real workflows enqueued).

    B. Imports + setup (mirror experiments/recipe_research.py:1-35):
       - `from dotenv import load_dotenv; load_dotenv()` before any langwatch import
       - `import langwatch; import langwatch.langchain`
       - `from langchain_core.runnables import RunnableConfig`
       - Standard `logging.basicConfig(level=logging.INFO, ...)`

    C. Argument parsing (`argparse`):
       - `--backend` choices = {ollama, openai, anthropic}, default=ollama
       - `--eval-set` default path = `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md`
       - `--out` default path = `.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-RESULTS-<backend>.md`
       - `--limit` optional int (run first N utterances; default = all)

    D. Eval-set parser — a function `parse_eval_set(path: Path) -> list[dict]` that reads
       the markdown table rows after the "## Utterances" heading. For each row, extract:
       `idx` (int), `utterance` (str), `class_name` (str), `expected_n` (int),
       `expected_values` (list[str], parse the comma-separated list inside the brackets-or-quotes column),
       `expected_respond_tag` (str). Use a simple line-by-line scan:
         - locate "## Utterances"
         - skip until a line starting with `|` AND followed by a separator row `|---`
         - for each subsequent `| ... |` row until a blank line or next heading, split on `|`, strip cells
         - parse `expected_n` as int from its cell
         - parse `expected_values` by splitting on `,` and stripping quotes and brackets

       It's acceptable to keep this parser tolerant (e.g. accept either `["a","b"]` JSON-style
       or `a, b` plain). Document the accepted format in the function docstring.

    E. Stub tools — define three minimal LangChain `BaseTool` subclasses
       (`StubRespondTool`, `StubStartWorkflowTool`, `StubTerminateTool`) that:
         - have the SAME `name` and `args_schema` as the production tools
           (import the production schemas where possible; for StartWorkflow,
           reuse `AddRecipeQueryInput` and the top-level args schema from
           `src/robotina/agent/tools/start_workflow.py`)
         - StubRespondTool._run: appends `{"text": text}` to an instance list `self.calls`; returns `"OK"`
         - StubStartWorkflowTool._run: appends `{"workflow_type": ..., "input": ...}` to `self.calls`;
           returns `f"Workflow started. workflow_run_id=stub-{len(self.calls)}"` — same shape as
           the real tool's success return; do NOT actually enqueue, do NOT open a DB session.
         - StubTerminateTool: return_direct=True; returns `""` (same as production); appends
           a sentinel to `self.calls`.

       Each utterance gets a FRESH set of stub instances so call counts are per-utterance.

    F. Agent build — function `build_agent(backend_name: str) -> tuple[agent, config, stubs_dict]`:
         - call `get_agent_config("handle-incoming-message")` (will load V006 via plan 02)
         - select backend by env / explicit factory (mirror recipe_research.py's pattern; if
           recipe_research.py uses `make_backend(config.model_config)`, do the same — when
           `--backend openai` overrides, set the relevant env vars BEFORE
           `get_agent_config` is called OR construct the OpenAIBackend explicitly. Read
           recipe_research.py to confirm the chosen pattern; do not invent a new one.)
         - instantiate the three stub tools, plus the read-skill tool (`build_read_skill_tool`)
           if config.skills is non-empty
         - build agent via `backend.create_agent(system_prompt=<V006 file text + skill index if any>, tools=[stubs..., read_skill_if_any])`
         - return agent, config, dict of stub instances

    G. Tool-call counting — function (per 22-PATTERNS.md §"Tool-call counting pattern"):

       ```python
       def count_start_workflow_calls(stubs: dict) -> tuple[int, list[str]]:
           start_calls = stubs["start_workflow"].calls
           count = len(start_calls)
           recipe_values = []
           for call in start_calls:
               inp = call.get("input") or {}
               val = inp.get("value") if isinstance(inp, dict) else None
               if val:
                   recipe_values.append(val)
           return count, recipe_values
       ```

       (Counting from the stub's recorded calls is more reliable than walking
       `result["messages"]` because the stub captures exactly what the agent dispatched.)

    H. Per-utterance loop:
       For each parsed row:
         - construct fresh stubs + agent
         - build per-utterance LangWatch tracer with metadata:
           `{"experiment": "multi-recipe-eval", "phase": "22", "prompt_version": "V006",
             "utterance_id": idx, "model": <model>, "provider": <provider>, "backend": backend_name,
             "expected_n": expected_n, "class": class_name}`
         - `with tracer:` invoke `agent.invoke({"messages": [{"role": "user", "content": utterance}]}, config=RunnableConfig(callbacks=[tracer]))`
         - on exception: log + record observed_n=-1, respond_text="<error>", error=str(e); CONTINUE (per recipe_research.py error pattern — do NOT abort the whole run on one utterance)
         - else: observed_n, observed_values = count_start_workflow_calls(stubs); collect respond texts (from StubRespondTool.calls) and terminate-was-called flag
         - row_ok = (observed_n == expected_n)
         - name_ok (only for multi-recipe rows where expected_values has > 0 items):
             normalize_strings = lowercase + accent-strip (unicodedata.normalize NFD + filter non-ascii combining marks)
             use difflib.SequenceMatcher(None, a, b).ratio() >= 0.75 to consider a match
             name_ok = all expected_values have a match in observed_values
         - record one result dict
       At the end: force-flush LangWatch (per recipe_research.py pattern).

    I. Report emit — function `write_results(out_path: Path, backend: str, results: list[dict], config_meta: dict)`:
       Write a markdown file with:
         - Frontmatter `verdict: pending`, `backend`, `model`, `date: <today ISO>`, `operator: <env USER or "auto-fill">`, `eval_set_version: 1`
         - "# Phase 22 Multi-Recipe Eval Results — <Backend>" header
         - Aggregate section: count-correct / total, name-correct / multi-recipe-total, per-class breakdown
         - Per-row table: `| # | Utterance | Class | Expected N | Observed N | Observed values | OK? | LangWatch trace |`
         - Threshold reminder: Ollama informational, OpenAI ≥95% count + ≥90% name = merge gate
         - The harness writes `verdict: pending` always — the OPERATOR flips to `pass`/`fail`/`pivot` by hand after reviewing.

    J. `main()` — parse args, parse eval set, build agent, loop, write report, log aggregate, exit 0
       (do NOT exit non-zero on per-utterance failures — that's the operator's call).

    Step 3 — Edit `pyproject.toml`:
       Find the existing `[project.scripts]` block (it has `experiments.recipe_research` and
       `experiments.recipe_load` already per CONTEXT). Add ONE new line below them:

       ```toml
       "experiments.multi_recipe_eval" = "experiments.robotina.multi_recipe_eval:main"
       ```

       Do NOT remove or reorder existing entries. Do NOT touch any other section of pyproject.toml.

    Step 4 — Smoke-test the harness in a no-network mode:
       - Run `uv pip install -e .` if needed for the new script entry (`uv sync` may be enough)
       - Run `uv run experiments.multi_recipe_eval --backend ollama --limit 1 --out /tmp/22-eval-test.md` IF and only if `OLLAMA_URL` env is set and reachable; otherwise just run `python -c "from experiments.robotina.multi_recipe_eval import parse_eval_set, count_start_workflow_calls; print('imports ok')"` to confirm the module imports cleanly.
       - Goal of smoke step is `import-cleanliness`, NOT a real eval pass. The operator drives the real eval per Plan 04.

    No new env vars added. No CI hook registered.
  </action>
  <verify>
    <automated>test -f experiments/robotina/__init__.py && test -f experiments/robotina/multi_recipe_eval.py && grep -c "experiments.multi_recipe_eval" pyproject.toml | grep -qE "^[1-9]" && uv run python -c "from experiments.robotina.multi_recipe_eval import parse_eval_set, count_start_workflow_calls, main; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/robotina/__init__.py` exists (empty file OK)
    - `experiments/robotina/multi_recipe_eval.py` exists
    - `wc -l experiments/robotina/multi_recipe_eval.py` >= 150
    - `grep -c "load_dotenv()" experiments/robotina/multi_recipe_eval.py` >= 1
    - `grep -c "import langwatch" experiments/robotina/multi_recipe_eval.py` >= 1
    - `grep -c "phase.*22" experiments/robotina/multi_recipe_eval.py` >= 1 (LangWatch metadata)
    - `grep -c "prompt_version.*V006" experiments/robotina/multi_recipe_eval.py` >= 1
    - `grep -cE "class Stub(Respond|StartWorkflow|Terminate)Tool" experiments/robotina/multi_recipe_eval.py` == 3
    - `grep -cE "def (parse_eval_set|count_start_workflow_calls|build_agent|write_results|main)" experiments/robotina/multi_recipe_eval.py` >= 5
    - `grep -c '"experiments.multi_recipe_eval" = "experiments.robotina.multi_recipe_eval:main"' pyproject.toml` == 1
    - `uv run python -c "from experiments.robotina.multi_recipe_eval import parse_eval_set, count_start_workflow_calls, main; print('ok')"` exits 0
    - `uv run python -c "from pathlib import Path; from experiments.robotina.multi_recipe_eval import parse_eval_set; rows = parse_eval_set(Path('.planning/phases/22-multi-recipe-per-message-topic-1/22-EVAL-SET.md')); assert len(rows) >= 30, f'got {len(rows)} rows'; print(len(rows))"` exits 0 (the parser walks the eval set from Task 1 and finds ≥ 30 rows)
  </acceptance_criteria>
  <done>
    Harness imports cleanly, parses the 30-row eval set, exposes the stub tools,
    is registered as `uv run experiments.multi_recipe_eval`. Operator can now
    run it in Plan 04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval harness ↔ LLM provider | API keys (OPENAI_API_KEY, LANGWATCH_API_KEY) read from env; sent over HTTPS to provider |
| Eval results files committed to repo | Operator-edited; may inadvertently include API keys or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Information Disclosure | 22-EVAL-RESULTS-*.md committed to git | mitigate | Results template explicitly says "model names + trace IDs only — NO env values". Operator review at commit time. Harness never writes API keys to disk. |
| T-22-07 | Tampering | Stub tools side-stepping real workflow enqueue | accept | Intended behavior — eval is acceptance evidence, not production. Stub tools are clearly named and the harness docstring documents the no-side-effects guarantee. |
| T-22-08 | DoS | Eval running against production household DB | mitigate | Stubs ensure NO real workflows are enqueued and NO real DB writes. Harness has no SessionLocal call. Documented in module docstring. |
</threat_model>

<verification>
- `uv run python -c "from experiments.robotina.multi_recipe_eval import main"` succeeds
- The eval-set parser sanity-test (in acceptance criteria) finds 30 rows
- `uv run pytest -x` full suite still green (no test should depend on the eval harness)
- Manual eyeball of 22-EVAL-SET.md: 30 rows distributed across 10 coverage classes (3 per class)
</verification>

<success_criteria>
- Eval set + per-backend templates committed
- Harness importable and runnable via `uv run experiments.multi_recipe_eval`
- Stub tools prevent any real workflow enqueue or DB write
- LangWatch metadata tags include phase=22, prompt_version=V006, utterance_id, backend
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-recipe-per-message-topic-1/22-03-SUMMARY.md`
</output>
