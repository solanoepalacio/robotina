---
phase: 16
plan: 06
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .env.example
  - src/robotina/gateway/send.py
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-HID-6
  - REQ-HID-7
  - REQ-HID-8
tags:
  - docs
  - config

must_haves:
  truths:
    - ".env.example contains a HOUSEHOLD_ID= line preceded by a comment block calling it required and noting the gateway refuses to start when unset"
    - "src/robotina/gateway/send.py no longer references HOUSEHOLD_ID anywhere (stale docstring removed — code path never used it)"
    - "PROJECT.md Key Decisions table has a new row recording the end-to-end household_id-must-be-non-empty contract introduced by Phase 16"
  artifacts:
    - path: ".env.example"
      provides: "HOUSEHOLD_ID documentation block"
      contains: "HOUSEHOLD_ID="
    - path: "src/robotina/gateway/send.py"
      provides: "Docstring with no HOUSEHOLD_ID reference"
      contains_not: "HOUSEHOLD_ID"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row about household_id end-to-end validation"
      contains: "household_id is required and validated end-to-end"
  key_links:
    - from: ".env.example"
      to: "operator's local .env"
      via: "documentation reference (see comment block)"
      pattern: "^HOUSEHOLD_ID="
---

<objective>
Documentation and cleanup tasks: REQ-HID-6 (document `HOUSEHOLD_ID` in `.env.example`), REQ-HID-7 (remove stale docstring reference in `gateway/send.py`), REQ-HID-8 (record the Phase 16 decision in `PROJECT.md` Key Decisions). All three are zero-risk content-only edits; they group into one plan because no test runs gate any of them — `test_env_example.py` (already written in Wave 0) is the only automated check and covers REQ-HID-6.

Purpose: Close the operator-UX and documentation surface of the bug fix. A fresh-checkout operator must be able to discover `HOUSEHOLD_ID` from `.env.example` without reading source (per CLAUDE.md `feedback_env_example.md`).

Output: 3 small content edits, fully automated.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-fix-empty-string-household-id-propagation-through-gateway-an/16-CONTEXT.md
@.planning/phases/16-fix-empty-string-household-id-propagation-through-gateway-an/16-RESEARCH.md

@.env.example
@src/robotina/gateway/send.py
@.planning/PROJECT.md
@tests/unit/test_env_example.py

<interfaces>
Current `.env.example` (49 lines). Telegram section is around lines 11-12:
```
# Telegram (Phase 3+)
TELEGRAM_BOT_TOKEN=
```

`HOUSEHOLD_ID` block will be inserted as a NEW section. Best location: a new "Gateway (Phase 3+ / 16+)" section AFTER `TELEGRAM_BOT_TOKEN` and BEFORE `# Tavily Web Search (Phase 8+)`. Keep Telegram-related vars grouped.

Current `gateway/send.py` lines 1-14 (docstring):
```python
"""Outgoing Telegram message function for the gateway.

...

Env vars consumed:
  TELEGRAM_BOT_TOKEN  — required (KeyError on missing)
  HOUSEHOLD_ID        — used for Conversation lookup; defaults ""
  DATABASE_URL        — default postgresql://robotina:robotina@localhost:5432/robotina
"""
```

The "HOUSEHOLD_ID" line is FALSE — the code in this file never reads `HOUSEHOLD_ID` (verified). Delete that single line.

Current PROJECT.md Key Decisions table (lines 58-65). Each row uses the format:
```
| Decision | Rationale | Outcome |
| `decision` | `rationale` | `— Active` |
```

The new row goes at the bottom of the table (after the create_agent row).

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HOUSEHOLD_ID block to .env.example</name>
  <read_first>
    - .env.example (full — 49 lines)
    - tests/unit/test_env_example.py (read to verify regex expectations from Wave 0)
  </read_first>
  <files>.env.example</files>
  <action>
Insert the following block into `.env.example` immediately AFTER the existing `TELEGRAM_BOT_TOKEN=` line and BEFORE the `# Tavily Web Search (Phase 8+)` comment header. The two surrounding blank lines below give the file consistent spacing with neighboring sections.

```

# Gateway household identity (Phase 16+)
# REQUIRED. The gateway will refuse to start (sys.exit(1)) if HOUSEHOLD_ID is
# unset, empty, or whitespace-only. Replace the placeholder below with the
# actual household UUID for this deployment. See:
#   - src/robotina/gateway/__init__.py::main (boot guard)
#   - src/robotina/queue/task_types.py::NonEmptyHouseholdId (Pydantic validator)
HOUSEHOLD_ID=replace-with-your-household-uuid
```

Do NOT touch any other line in `.env.example`. Specifically:
- Leave the `# Telegram (Phase 3+)` heading and `TELEGRAM_BOT_TOKEN=` unchanged
- Leave every section below (Tavily, LLM tokens, Household Manager API, Agent overrides, Dashboard) verbatim

After this edit, the lines around the new block should be:
```
# Telegram (Phase 3+)
TELEGRAM_BOT_TOKEN=

# Gateway household identity (Phase 16+)
# REQUIRED. ...
HOUSEHOLD_ID=replace-with-your-household-uuid

# Tavily Web Search (Phase 8+)
TAVILY_API_KEY=
```
  </action>
  <acceptance_criteria>
    - `grep -cE "^HOUSEHOLD_ID=" .env.example` returns `1`
    - `grep -cE "^HOUSEHOLD_ID=replace-with-your-household-uuid" .env.example` returns `1`
    - `grep -ciE "required" .env.example` returns `>= 1` (the new comment block adds at least one "REQUIRED" mention)
    - `grep -B5 "^HOUSEHOLD_ID=" .env.example | grep -ciE "required"` returns `>= 1` (the comment IMMEDIATELY ABOVE the line mentions required — required by `test_household_id_marked_required`)
    - `uv run pytest tests/unit/test_env_example.py -x -q` exits 0
    - File line count increased by ~6-8 (verify with `wc -l .env.example` shows the file grew but no other section was deleted: `grep -c "DASHBOARD_PORT" .env.example` still returns `1`; `grep -c "TELEGRAM_BOT_TOKEN" .env.example` still returns `1`; `grep -c "TAVILY_API_KEY" .env.example` still returns `1`)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_env_example.py -x -q</automated>
  </verify>
  <done>.env.example documents HOUSEHOLD_ID with the placeholder and required-comment block; Wave 0 env-example tests all green; no other section disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale HOUSEHOLD_ID line from gateway/send.py docstring</name>
  <read_first>
    - src/robotina/gateway/send.py (full — 88 lines)
  </read_first>
  <files>src/robotina/gateway/send.py</files>
  <action>
Locate the docstring block at the top of the file (lines 1-14). Remove the single line:

```
  HOUSEHOLD_ID        — used for Conversation lookup; defaults ""
```

After the edit, the "Env vars consumed:" block should read:

```python
Env vars consumed:
  TELEGRAM_BOT_TOKEN  — required (KeyError on missing)
  DATABASE_URL        — default postgresql://robotina:robotina@localhost:5432/robotina
```

Why: per RESEARCH.md Pitfall 3, `send.py` does NOT read `HOUSEHOLD_ID` anywhere in code (verified by grep). The stale line creates false-positive search hits in future audits and contradicts the Phase 16 zero-default policy. Delete the single line; leave every other docstring line and code path untouched.

Do NOT modify anything in `send.py` outside the docstring's `Env vars consumed:` block. The function `send_message`, the `SendResult` dataclass, and all other code remain exactly as-is.
  </action>
  <acceptance_criteria>
    - `grep -c "HOUSEHOLD_ID" src/robotina/gateway/send.py` returns `0`
    - `grep -c "TELEGRAM_BOT_TOKEN" src/robotina/gateway/send.py` returns `1` (still present)
    - `grep -c "DATABASE_URL" src/robotina/gateway/send.py` returns `1` (still present)
    - `grep -c "def send_message" src/robotina/gateway/send.py` returns `1` (function unchanged)
    - `uv run pytest tests/test_gateway.py -x -q` exits 0 (no behavioral regression — send.py code path didn't use the var, this is a pure docstring fix)
  </acceptance_criteria>
  <verify>
    <automated>! grep -i HOUSEHOLD_ID src/robotina/gateway/send.py && uv run pytest tests/test_gateway.py -x -q</automated>
  </verify>
  <done>send.py no longer references HOUSEHOLD_ID anywhere; tests unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add Key Decision row to PROJECT.md</name>
  <read_first>
    - .planning/PROJECT.md (lines 50-80, focusing on the Key Decisions table — lines 56-66)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Locate the Key Decisions table starting at line 56 (`## Key Decisions`). The current last row in the table (line 65) is:

```
| `create_agent` from `langchain.agents` is used for all agents | LangGraph deprecated `create_react_agent` (V1.0; removal in V2.0). The new factory is required to unlock `response_format` (Phase 11) and middleware (Phase 12). Behavior parity verified empirically during Phase 10. | — Active |
```

Append the following NEW row IMMEDIATELY AFTER that line (and BEFORE the `## Evolution` header on line 67):

```
| `household_id` is required and validated end-to-end (Phase 16) | A missing `HOUSEHOLD_ID` env var silently propagated as `""` through Conversation, IncomingMessageInput, WorkflowRun, and the household-manager-api tool, surfacing only as confusing 4xx responses from the backend. Phase 16 added defensive validation at four layers: gateway entrypoint (`sys.exit(1)` on missing/empty/whitespace), Pydantic task-input models (`NonEmptyHouseholdId` alias on 7 models), tool constructors (`HouseholdManagerApiTool`, `StartWorkflowTool` reject empty), and `queue_workflow` (raises ValueError before any DB write). | — Active |
```

Do NOT modify any other row in the table. Do NOT modify any other section of PROJECT.md.

The row uses the same Markdown table format as the existing rows (3 columns separated by `|`, decision / rationale / outcome).
  </action>
  <acceptance_criteria>
    - `grep -c "household_id\` is required and validated end-to-end" .planning/PROJECT.md` returns `1`
    - `grep -c "Phase 16" .planning/PROJECT.md` returns `>= 1`
    - `grep -c "NonEmptyHouseholdId" .planning/PROJECT.md` returns `1`
    - `grep -c "^## Key Decisions" .planning/PROJECT.md` returns `1` (header unchanged)
    - `grep -c "^## Evolution" .planning/PROJECT.md` returns `1` (next section header unchanged)
    - Pre-existing Key Decision rows still present: `grep -c "create_agent.*langchain.agents.*all agents" .planning/PROJECT.md` returns `1`; `grep -c "Centralized task-runner" .planning/PROJECT.md` returns `1`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "household_id\` is required and validated end-to-end" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md Key Decisions table has the new Phase 16 row; all pre-existing rows preserved.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_env_example.py tests/test_gateway.py -x -q` exits 0
- `! grep -i HOUSEHOLD_ID src/robotina/gateway/send.py` exits 0 (REQ-HID-7 closed)
- `grep -c "^HOUSEHOLD_ID=" .env.example` returns `1` (REQ-HID-6 closed)
- `grep -c "household_id\` is required and validated end-to-end" .planning/PROJECT.md` returns `1` (REQ-HID-8 closed)
</verification>

<success_criteria>
- `.env.example` documents `HOUSEHOLD_ID` (REQ-HID-6)
- Stale docstring reference removed from `gateway/send.py` (REQ-HID-7)
- PROJECT.md Key Decisions records Phase 16's end-to-end validation contract (REQ-HID-8)
- Zero behavioral regression — all changes are content/documentation
</success_criteria>

<output>
After completion, create `.planning/phases/16-fix-empty-string-household-id-propagation-through-gateway-an/16-06-SUMMARY.md` documenting:
- 3 content edits (one per task)
- Confirmation that no `HOUSEHOLD_ID` reference remains in `send.py`
- The exact PROJECT.md row added (copy-pasted for the historical record)
- Test transitions: `test_env_example.py` Wave 0 → GREEN
</output>
