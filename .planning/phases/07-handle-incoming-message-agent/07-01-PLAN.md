---
phase: 07-handle-incoming-message-agent
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_household_manager_api_tool.py
  - tests/unit/test_queue_tool.py
autonomous: true
requirements:
  - ROBOT-02
  - ROBOT-03

must_haves:
  truths:
    - "tests/unit/test_household_manager_api_tool.py exists with stubs for all ROBOT-02 behaviors"
    - "tests/unit/test_queue_tool.py exists with stubs for all ROBOT-03 behaviors"
    - "uv run pytest tests/unit/ -x -q collects new test files with SKIPPED results (not FAILED)"
  artifacts:
    - path: "tests/unit/test_household_manager_api_tool.py"
      provides: "Unit test stubs for HouseholdManagerApiTool"
      exports: ["test_household_manager_api_tool_construction", "test_household_manager_api_tool_injects_bearer_token", "test_household_manager_api_tool_raises_runtime_error_on_401", "test_household_manager_api_tool_raises_runtime_error_on_403", "test_household_manager_api_tool_returns_error_dict_for_other_non_2xx", "test_household_manager_api_tool_returns_json_on_success", "test_household_manager_api_tool_household_id_not_in_run_signature"]
    - path: "tests/unit/test_queue_tool.py"
      provides: "Unit test stubs for QueueTool"
      exports: ["test_queue_tool_construction", "test_queue_tool_enqueues_send_notification_with_correct_meta", "test_queue_tool_enqueues_at_back_of_queue", "test_queue_tool_returns_job_id_string"]
  key_links:
    - from: "tests/unit/test_household_manager_api_tool.py"
      to: "src/robotina/agent/tools/household_manager_api.py"
      via: "import at test body (deferred, not top-level)"
      pattern: "from robotina\\.agent\\.tools\\.household_manager_api import"
    - from: "tests/unit/test_queue_tool.py"
      to: "src/robotina/agent/tools/queue.py"
      via: "import at test body (deferred, not top-level)"
      pattern: "from robotina\\.agent\\.tools\\.queue import"
---

<objective>
Create Wave 0 test stubs for the two new tools introduced in Phase 7: HouseholdManagerApiTool and QueueTool. These stubs let the full test suite collect cleanly (SKIPPED not FAILED) before the implementations exist.

Purpose: Establish the Nyquist contract — every behavior that will be implemented has a named test before the first line of implementation code is written.
Output: Two new test files with pytest.skip() stubs covering all ROBOT-02 and ROBOT-03 acceptance criteria.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tests/unit/test_send_notification_tool.py (stub pattern to follow):
```python
# IMPORTANT: pytest.skip() must come BEFORE any imports from the module under test.
# Placing it after causes ImportError before skip → FAILED not SKIPPED.
# (Phase 6 decision logged in STATE.md)

def test_some_behavior():
    pytest.skip("ROBOT-XX: not yet implemented")
```

From src/robotina/queue/task_types.py:
```python
class IncomingMessageInput(BaseModel):
    message_id: str
    platform: Literal["telegram"]
    received_at: datetime
    chat_id: str
    user_id: str
    household_id: str
    text: str
    history: list[Message]

class SendNotificationInput(BaseModel):
    platform: Literal["telegram"]
    chat_id: str
    user_id: str
    text: str
```

HouseholdManagerApiTool interface (to be created in Plan 02):
- Constructor: `HouseholdManagerApiTool(household_id="hh-123")`
- `_run(method: str, path: str, body: dict | None, query: dict | None) -> dict | str`
- Injects `Authorization: Bearer <HOUSEHOLD_MANAGER_API_KEY>` on every request
- `401` or `403` response → raises `RuntimeError`
- Other non-2xx → returns `{"error": status_code, "message": response_text}`
- 2xx → returns `resp.json()`
- `household_id` is a constructor field ONLY — NOT a `_run` parameter

QueueTool interface (to be created in Plan 02):
- Constructor: `QueueTool(chat_id="123", user_id="456", platform="telegram")`
- `_run(text: str) -> str` (returns job_id string)
- Enqueues to `agent-tasks` queue (NOT `at_front=True`)
- `meta={"task_type": "send-notification"}`, `result_ttl=-1`, `failure_ttl=-1`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_household_manager_api_tool.py stubs</name>
  <files>tests/unit/test_household_manager_api_tool.py</files>
  <read_first>
    - tests/unit/test_send_notification_tool.py (stub pattern: pytest.skip before import, mock httpx responses)
    - src/robotina/queue/task_types.py (IncomingMessageInput.household_id field)
    - .planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md (D-02: HouseholdManagerApiTool spec)
  </read_first>
  <action>
Create `tests/unit/test_household_manager_api_tool.py` with the following 7 stub functions. Each function must call `pytest.skip("ROBOT-02: not yet implemented")` as its FIRST statement — before any import from `robotina.agent.tools.household_manager_api` (which does not exist yet). Top-level imports are limited to `pytest` and `unittest.mock`.

```python
"""Tests for HouseholdManagerApiTool.

Covers ROBOT-02: Robotina agent has household-manager-api tool
(auth injected invisibly; 401/403 raise hard errors).
Tests mock httpx responses — never call the real household-manager API.
"""
import pytest
from unittest.mock import MagicMock, patch, AsyncMock


def test_household_manager_api_tool_construction():
    """ROBOT-02: HouseholdManagerApiTool can be constructed with household_id."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_injects_bearer_token():
    """ROBOT-02: _run() sets Authorization: Bearer <HOUSEHOLD_MANAGER_API_KEY> on every request."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_raises_runtime_error_on_401():
    """ROBOT-02: _run() raises RuntimeError when response status is 401."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_raises_runtime_error_on_403():
    """ROBOT-02: _run() raises RuntimeError when response status is 403."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_returns_error_dict_for_other_non_2xx():
    """ROBOT-02: _run() returns dict with 'error' and 'message' keys for non-401/403 non-2xx."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_returns_json_on_success():
    """ROBOT-02: _run() returns parsed JSON dict on 2xx response."""
    pytest.skip("ROBOT-02: not yet implemented")


def test_household_manager_api_tool_household_id_not_in_run_signature():
    """ROBOT-02: household_id is a constructor field only — _run() has no household_id param."""
    pytest.skip("ROBOT-02: not yet implemented")
```
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/test_household_manager_api_tool.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_household_manager_api_tool.py` exists
    - `uv run pytest tests/unit/test_household_manager_api_tool.py -v` exits 0 with all 7 tests showing SKIPPED (not FAILED or ERROR)
    - grep confirms `pytest.skip` appears before any `from robotina` import in each function
    - grep `"def test_household_manager_api_tool_"` returns 7 matches
  </acceptance_criteria>
  <done>7 stub tests collected as SKIPPED, full suite exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Create test_queue_tool.py stubs</name>
  <files>tests/unit/test_queue_tool.py</files>
  <read_first>
    - tests/unit/test_send_notification_tool.py (stub pattern and mock RQ queue pattern)
    - src/robotina/gateway/handler.py (RQ enqueue pattern: meta, result_ttl, failure_ttl, at_front semantics)
    - .planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md (D-01: QueueTool spec)
  </read_first>
  <action>
Create `tests/unit/test_queue_tool.py` with the following 4 stub functions. Each function must call `pytest.skip("ROBOT-03: not yet implemented")` as its FIRST statement.

```python
"""Tests for QueueTool.

Covers ROBOT-03: Robotina agent has queue tool (enqueue a single follow-up
send-notification task directly). Tests mock RQ Queue — never touch real Redis.
"""
import pytest
from unittest.mock import MagicMock, patch


def test_queue_tool_construction():
    """ROBOT-03: QueueTool can be constructed with chat_id, user_id, platform."""
    pytest.skip("ROBOT-03: not yet implemented")


def test_queue_tool_enqueues_send_notification_with_correct_meta():
    """ROBOT-03: _run(text) enqueues 'robotina.queue.jobs.run_task' with meta={'task_type': 'send-notification'},
    result_ttl=-1, failure_ttl=-1."""
    pytest.skip("ROBOT-03: not yet implemented")


def test_queue_tool_enqueues_at_back_of_queue():
    """ROBOT-03: _run() does NOT pass at_front=True — follow-up tasks go to back of queue."""
    pytest.skip("ROBOT-03: not yet implemented")


def test_queue_tool_returns_job_id_string():
    """ROBOT-03: _run(text) returns the job.id string (used for IncomingMessageOutput.queued_task_ids)."""
    pytest.skip("ROBOT-03: not yet implemented")
```
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/test_queue_tool.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_queue_tool.py` exists
    - `uv run pytest tests/unit/test_queue_tool.py -v` exits 0 with all 4 tests showing SKIPPED
    - grep `"def test_queue_tool_"` returns 4 matches
    - Full suite `uv run pytest tests/unit/ -x -q` exits 0 with both new files collected
  </acceptance_criteria>
  <done>4 stub tests collected as SKIPPED, full unit suite remains green</done>
</task>

</tasks>

<verification>
After both tasks:
- `uv run pytest tests/unit/ -x -q` exits 0
- `uv run pytest tests/unit/test_household_manager_api_tool.py tests/unit/test_queue_tool.py -v` shows 11 tests, all SKIPPED
- No FAILED or ERROR entries in output
</verification>

<success_criteria>
- tests/unit/test_household_manager_api_tool.py exists with 7 SKIPPED stubs covering ROBOT-02
- tests/unit/test_queue_tool.py exists with 4 SKIPPED stubs covering ROBOT-03
- Full unit test suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-handle-incoming-message-agent/07-01-SUMMARY.md`
</output>
