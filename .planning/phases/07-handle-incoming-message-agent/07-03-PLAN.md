---
phase: 07-handle-incoming-message-agent
plan: 03
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - src/robotina/agent/skills/household-manager/shared.md
  - src/robotina/agent/skills/household-manager/index.md
  - src/robotina/agent/prompts/robotina/V001.md
autonomous: true
requirements:
  - ROBOT-05
  - ROBOT-06
  - ROBOT-07

must_haves:
  truths:
    - "shared.md contains no Authentication section"
    - "shared.md contains no 401 or 403 rows in the error table"
    - "index.md no longer mentions 'authentication' in the preamble"
    - "robotina/V001.md exists, is non-empty, and states the routing principle"
    - "robotina/V001.md does not contain workflow key names (e.g. 'add-recipe' as literal)"
  artifacts:
    - path: "src/robotina/agent/skills/household-manager/shared.md"
      provides: "Updated skill shared conventions (auth removed)"
      contains: "Base URL"
    - path: "src/robotina/agent/skills/household-manager/index.md"
      provides: "Updated skill index (auth reference removed)"
      contains: "Before any operation, read"
    - path: "src/robotina/agent/prompts/robotina/V001.md"
      provides: "Robotina routing agent system prompt"
      contains: "queue"
  key_links:
    - from: "src/robotina/agent/prompts/robotina/V001.md"
      to: "src/robotina/agent/tools/queue.py"
      via: "prompt instructs agent to call 'queue' tool for direct replies"
      pattern: "queue"
    - from: "src/robotina/agent/prompts/robotina/V001.md"
      to: "src/robotina/agent/tools/start_workflow.py"
      via: "prompt instructs agent to call 'start-workflow' tool for multi-step intents"
      pattern: "start-workflow"
---

<objective>
Rewrite the household-manager skill to remove auth references (D-06), and create the Robotina routing agent prompt (D-05). These are content deliverables with no code dependencies — can run in parallel with Plan 02.

Purpose: Give the agent a clean skill (no auth scaffolding the tool already handles) and a routing prompt that correctly disambiguates direct-reply from workflow intent.
Output: Updated shared.md, updated index.md, new robotina/V001.md.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md
@.planning/phases/07-handle-incoming-message-agent/07-RESEARCH.md

<interfaces>
<!-- Current file content the executor must read before modifying. -->

Current src/robotina/agent/skills/household-manager/shared.md contains:
- ## Base URL section (KEEP)
- ## Authentication section (REMOVE ENTIRELY — tool handles this)
- ## Error codes table with 401 and 403 rows (REMOVE those two rows only)
- ## Error response shape section (KEEP)
- ## Pagination section (KEEP)
- ## Filtering reference lists section (KEEP)

Current src/robotina/agent/skills/household-manager/index.md line 5:
"Before any operation, read `shared.md` to set up authentication and understand error handling."
→ Change to: "Before any operation, read `shared.md` to understand base URL, error handling, and pagination."

Also update the File table entry for shared.md:
| `shared.md` | Authentication, base URL, error codes, pagination envelope |
→ Change to: | `shared.md` | Base URL, error codes, pagination envelope |

From src/robotina/agent/prompts/send-notification/V001.md (prompt structure template):
Sections: Role → Instructions (numbered) → Critical Rules → Failure Modes to Avoid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite household-manager/shared.md (remove auth) + update index.md</name>
  <files>
    src/robotina/agent/skills/household-manager/shared.md,
    src/robotina/agent/skills/household-manager/index.md
  </files>
  <read_first>
    - src/robotina/agent/skills/household-manager/shared.md (current content — read the FULL file before touching it)
    - src/robotina/agent/skills/household-manager/index.md (current content — read before editing)
    - .planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md (D-06: exact spec of what to remove/keep)
    - .planning/phases/07-handle-incoming-message-agent/07-RESEARCH.md (shared.md Rewrite section)
  </read_first>
  <action>
**shared.md — full rewrite.**

The new file keeps all sections EXCEPT `## Authentication` (entirely removed) and removes the 401 and 403 rows from the error codes table. The resulting file is:

```markdown
# Shared conventions

## Base URL

All endpoint paths in this bundle are relative to a configurable base URL.

Default base URL: `http://localhost:3001`

All paths start with `/api` (e.g., `GET /api/recipes`).

## Error codes

| Status | Meaning | Action |
|--------|---------|--------|
| 400 | Validation error — request body or query params are invalid | Check the `message` field (array of strings) for details |
| 404 | Not found — the resource does not exist | Verify the ID or path is correct |
| 422 | Validation error (alternative) — same as 400 | Check the `message` field |
| 500 | Server error | Do not retry immediately; report the issue |

## Error response shape

```json
{
  "statusCode": 404,
  "message": "Recipe abc123 not found",
  "error": "Not Found"
}
```

For validation errors (400), `message` is an array of strings:

```json
{
  "statusCode": 400,
  "message": ["name must be a string", "servingsQty must be a number"],
  "error": "Bad Request"
}
```

## Pagination

List endpoints return a paginated envelope:

```json
{
  "items": [],
  "total": 100,
  "page": 1,
  "perPage": 20
}
```

| Field | Type | Description |
|-------|------|-------------|
| items | array | The page of results |
| total | integer | Total number of matching items across all pages |
| page | integer | Current 1-based page number |
| perPage | integer | Number of items per page |

**Important:** The response field is `perPage` (not `pageSize`). The query parameter for requesting page size is `pageSize`. These are different names — do not confuse them.

## Filtering reference lists

The food and unit reference endpoints accept an optional `?name=` query parameter for targeted lookups.

| Endpoint | Filter param | Behavior |
|----------|-------------|----------|
| `GET /api/foods?name=tomate` | `name` (optional) | Returns only foods whose name contains "tomate" (case-insensitive substring match) |
| `GET /api/units?name=taza` | `name` (optional) | Returns only units whose name contains "taza" (case-insensitive substring match) |

When the `name` parameter is **omitted or empty**, the full list is returned unchanged (backward-compatible).

Example — resolve the food ID for "Huevo":

```
GET /api/foods?name=huevo
```

Response:

```json
[{ "id": "f1f2f3f4-...", "name": "Huevo" }]
```

Use the returned `id` as `foodId` when creating ingredients. This replaces the need to fetch the entire foods list and scan it client-side.
```

**index.md — two targeted edits:**

1. Line 5: Change `"Before any operation, read \`shared.md\` to set up authentication and understand error handling."` to `"Before any operation, read \`shared.md\` to understand base URL, error handling, and pagination."`

2. In the Files table, change the `shared.md` row description from `"Authentication, base URL, error codes, pagination envelope"` to `"Base URL, error codes, pagination envelope"`
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && grep -n "Authentication\|401\|403" src/robotina/agent/skills/household-manager/shared.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Authentication" src/robotina/agent/skills/household-manager/shared.md` returns 0
    - `grep -c "401" src/robotina/agent/skills/household-manager/shared.md` returns 0
    - `grep -c "403" src/robotina/agent/skills/household-manager/shared.md` returns 0
    - `grep -c "Base URL" src/robotina/agent/skills/household-manager/shared.md` returns at least 1 (section kept)
    - `grep -c "Pagination" src/robotina/agent/skills/household-manager/shared.md` returns at least 1 (section kept)
    - `grep "authentication" src/robotina/agent/skills/household-manager/index.md` returns 0 matches (case-insensitive)
    - `grep "Base URL, error codes" src/robotina/agent/skills/household-manager/index.md` returns 1 match
  </acceptance_criteria>
  <done>shared.md has no auth section and no 401/403 rows; index.md updated</done>
</task>

<task type="auto">
  <name>Task 2: Create robotina/V001.md routing prompt</name>
  <files>src/robotina/agent/prompts/robotina/V001.md</files>
  <read_first>
    - src/robotina/agent/prompts/send-notification/V001.md (prompt structure template)
    - src/robotina/agent/tools/start_workflow.py (StartWorkflowTool.description — do not repeat workflow type names)
    - .planning/phases/07-handle-incoming-message-agent/07-CONTEXT.md (D-05: prompt content spec)
    - .planning/phases/07-handle-incoming-message-agent/07-RESEARCH.md (Pitfall 4: do NOT enumerate workflow keys; Prompt Architecture section)
  </read_first>
  <action>
Create directory `src/robotina/agent/prompts/robotina/` and write `V001.md` with the following exact content:

```markdown
# Robotina — V001

You are Robotina, an AI assistant for household management.

## Your Role

You help family members manage their household by answering questions and
initiating tasks on their behalf. You have access to household data via the
`household-manager-api` tool and can dispatch work via `queue` and `start-workflow`.

## Routing: When to use each tool

### Direct reply → use `queue`

Use the `queue` tool when the user's request can be fully answered with a single
text response. Read the necessary household data first, compose the answer, then
call `queue` with that answer.

**Examples of direct-reply requests:**
- "What's on the meal plan this week?"
- "Find me a pasta recipe"
- "When does the meal plan end?"
- "What recipes do we have with chicken?"

### Multi-step workflow → use `start-workflow`

Use the `start-workflow` tool when the request requires multiple sequential operations
that cannot be completed in a single response (e.g. research + load + notify).

**Examples of workflow requests:**
- "Add a recipe for spaghetti carbonara"
- "Save this recipe to our cookbook"
- "Find and add a new dessert recipe"

## Instructions

1. Identify whether the request is a direct reply or a workflow request (see above).

2. For direct-reply requests:
   a. Use `household-manager-api` to fetch any data needed to answer the question.
      Read the `household-manager` skill before calling the API.
   b. Compose a clear, helpful answer.
   c. Call `queue` with the answer text. When `queue` returns, the task is done — stop.

3. For workflow requests:
   a. Call `start-workflow` with the appropriate workflow type and a `shared_context`
      dict containing `reply_context` (platform, chat_id, user_id), `household_id`,
      and any task-specific fields the workflow needs (e.g. `recipe_query`).
   b. Use the `start-workflow` tool description to determine available workflow types.
   c. When `start-workflow` returns, the task is done — stop.

## Critical Rules

- Call EITHER `queue` OR `start-workflow` — never both in the same run.
- When a tool returns a completion signal, STOP immediately. Do not call more tools.
- Do not compose final answers yourself and send them directly — always use `queue`.
- Do not enumerate workflow type names in your reasoning — use what the tool tells you.
- If the request is ambiguous, prefer the direct-reply path and ask for clarification
  in the queued message.

## Failure Modes to Avoid

Wrong: Calling `queue` AND `start-workflow` in the same run.
Wrong: Answering the user directly without using `queue`.
Wrong: Starting a workflow for a question that can be answered directly.
Right: Fetch data → compose answer → call `queue` (for direct replies).
Right: Identify workflow intent → call `start-workflow` with correct shared_context.
```

IMPORTANT: Create the directory `src/robotina/agent/prompts/robotina/` before writing the file. The prompt must NOT contain the literal string `add-recipe` or any other workflow registry key — the agent discovers available workflow types from the `start-workflow` tool description (Pitfall 4).
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && test -f src/robotina/agent/prompts/robotina/V001.md && echo "EXISTS" && wc -l src/robotina/agent/prompts/robotina/V001.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/robotina/agent/prompts/robotina/V001.md` exits 0
    - `wc -c src/robotina/agent/prompts/robotina/V001.md` shows > 500 bytes (non-trivial content)
    - `grep -c "queue" src/robotina/agent/prompts/robotina/V001.md` returns >= 3 (tool name mentioned multiple times)
    - `grep -c "start-workflow" src/robotina/agent/prompts/robotina/V001.md` returns >= 2
    - `grep -c "add-recipe" src/robotina/agent/prompts/robotina/V001.md` returns 0 (no workflow keys leaked into prompt — Pitfall 4)
    - `grep -c "Authentication\|Bearer\|token" src/robotina/agent/prompts/robotina/V001.md` returns 0 (no auth in prompt)
  </acceptance_criteria>
  <done>robotina/V001.md exists with routing principle, examples for both paths, no workflow keys leaked</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -c "Authentication" src/robotina/agent/skills/household-manager/shared.md` returns 0
- `test -f src/robotina/agent/prompts/robotina/V001.md` exits 0
- `uv run pytest tests/unit/ -x -q` exits 0 (no regressions from skill/prompt file changes)
</verification>

<success_criteria>
- shared.md: no Authentication section, no 401/403 rows, Base URL and Pagination sections intact
- index.md: no auth reference in preamble line
- robotina/V001.md: exists, contains routing principle, examples for both paths, no workflow key names
</success_criteria>

<output>
After completion, create `.planning/phases/07-handle-incoming-message-agent/07-03-SUMMARY.md`
</output>
