---
phase: 23-url-ingestion-topic-2
plan: 07
type: execute
wave: 5
depends_on: [23-06]
files_modified:
  - .planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-openai.md
  - .planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-ollama.md
  - .planning/phases/23-url-ingestion-topic-2/23-SMOKE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [URL-06, URL-01, URL-02, URL-03, URL-04, URL-05, EXP-02]
tags: [eval, operator, requirements-tick]

must_haves:
  truths:
    - "23-SMOKE.md exists with verdict: pass | fail | needs-revision line"
    - "23-EVAL-RESULTS-openai.md exists with operator-filled aggregate score"
    - "REQUIREMENTS.md ticks URL-01..06 and EXP-02 to Complete once operator records verdict: pass"
    - "Operator manually verified end-to-end Telegram round-trip against a real recipe URL"
  artifacts:
    - path: ".planning/phases/23-url-ingestion-topic-2/23-SMOKE.md"
      provides: "Final operator verdict for URL ingestion"
    - path: ".planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-openai.md"
      provides: "OpenAI staging eval result (merge gate per D-12)"
  key_links:
    - from: "23-SMOKE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "URL-01..06 + EXP-02 checkbox flip"
      pattern: "URL-0"
---

<objective>
Operator-gated end-of-phase verification (D-24 load-bearing manual gate). Operator runs the eval harness against the OpenAI staging backend (merge gate per D-12), commits the results, runs the end-to-end Telegram smoke test against a real recipe URL, and writes the final `23-SMOKE.md` verdict. On `verdict: pass`, the planner ticks URL-01..06 + EXP-02 in REQUIREMENTS.md.

Purpose: Closes URL-06 (≥85% field-level success threshold) and ticks ALL Phase 23 requirements once the operator approves. This plan is `autonomous: false` because it requires operator action (running real LLM calls against real URLs + verifying Telegram round-trip).
Output: Eval results + smoke verdict + REQUIREMENTS.md ticks.

Schema-push note: No schema changes.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md
@.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md
@.planning/REQUIREMENTS.md

<interfaces>
D-12 pass thresholds:
- OpenAI staging eval ≥ 17/21 URLs pass = merge gate (BLOCKING).
- Ollama dev eval = informational (records for completeness; failures do NOT block merge).
- Anthropic optional.

D-13 file set (final state at end of phase):
- 23-EVAL-RESULTS-openai.md (operator-filled, primary gate)
- 23-EVAL-RESULTS-ollama.md (operator-filled, informational)
- 23-SMOKE.md (final verdict, ends with `verdict: pass | fail | needs-revision`)

REQUIREMENTS.md update format (mirror Phase 22 / Phase 21 pattern):
- Flip `- [ ]` to `- [x]` for URL-01..06 and EXP-02.
- Update the Traceability table rows for URL-01..06 and EXP-02 from `Pending` to `Complete` (or `Complete (≥17/21, see 23-SMOKE.md)` for URL-06 specifically).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs eval + records results + writes SMOKE verdict</name>
  <what-built>
    Phase 23 code + prompt + experiment harness are committed (23-01 through 23-06). The eval set is at `.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md`. Operator now runs the harness against real backends.
  </what-built>
  <how-to-verify>
    Operator-only steps (Claude cannot run these because they require real API tokens + real network + real Telegram round-trip):

    1. **OpenAI staging eval (merge gate per D-12):**
       ```
       uv run experiments.gather_from_url --backend openai --eval-set .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md
       ```
       Output lands in `.planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-openai.md`. Confirm aggregate score is recorded; review per-URL results for any unexpected failures.

    2. **Ollama dev eval (informational):**
       ```
       uv run experiments.gather_from_url --backend ollama --eval-set .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md
       ```
       Output lands in `.planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-ollama.md`.

    3. **End-to-end Telegram smoke test:** Send a real Telegram message to Robotina ("agregá esta receta: https://<a real recipe URL>") and verify:
       a. Robotina respond()s pre-batch ("voy con esa receta…")
       b. `add-recipe-from-url` workflow appears in the dashboard with `shared_context.recipe_url` populated.
       c. The workflow drains through gather-from-url → instructions → ingredients → metadata → load → finalize-outcome.
       d. Wake invocation respond()s post-batch with the recipe name + slug.
       e. The recipe appears in the household-manager backend.

    4. **Write `.planning/phases/23-url-ingestion-topic-2/23-SMOKE.md`** with the following structure:
       ```
       ---
       phase: 23
       date: <YYYY-MM-DD>
       operator: <name>
       openai_score: <X/21>
       ollama_score: <X/21>
       telegram_smoke: pass | fail
       verdict: pass | fail | needs-revision
       ---

       # Phase 23 — URL ingestion smoke verdict

       ## OpenAI staging (merge gate)
       Aggregate: X/21 (≥17/21 required per D-12).
       Notes: ...

       ## Ollama dev (informational)
       Aggregate: X/21.
       Notes: ...

       ## Telegram end-to-end
       URL tested: <url>
       Outcome: <text>

       ## Verdict
       verdict: pass | fail | needs-revision
       Rationale: ...
       ```

    5. Commit the three files (23-EVAL-RESULTS-openai.md, 23-EVAL-RESULTS-ollama.md, 23-SMOKE.md). DO NOT yet tick REQUIREMENTS.md — Task 2 (auto) does that conditionally on `verdict: pass`.
  </how-to-verify>
  <resume-signal>
    Type `approved verdict=pass` (or `verdict=fail` / `verdict=needs-revision`) and confirm 23-SMOKE.md is committed. If `fail` or `needs-revision`, the orchestrator should restart this plan after the underlying issue is fixed (likely a new gap-closure plan).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Tick REQUIREMENTS.md (only if verdict: pass)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/23-url-ingestion-topic-2/23-SMOKE.md (just-committed verdict file)
    - .planning/REQUIREMENTS.md (URL-01..06 + EXP-02 rows + Traceability table)
  </read_first>
  <behavior>
    - Reads 23-SMOKE.md frontmatter; aborts with a clear message if `verdict` is not `pass`.
    - Flips URL-01, URL-02, URL-03, URL-04, URL-05, URL-06, EXP-02 from `- [ ]` to `- [x]` in the v1.1 Requirements list.
    - Updates the Traceability table: each of those 7 rows changes from `Pending` to `Complete` (URL-06 row may use `Complete (≥17/21 OpenAI staging, see 23-SMOKE.md)` for traceability).
    - No other REQUIREMENTS.md edits.
  </behavior>
  <action>
    1. Read `.planning/phases/23-url-ingestion-topic-2/23-SMOKE.md` and parse the `verdict:` line. If not `pass`, exit task with the message "Verdict is not 'pass' — skipping REQUIREMENTS.md ticks. Operator must address gap-closure or re-run eval." (Do not modify any file.)
    2. If `verdict: pass`, in `.planning/REQUIREMENTS.md`:
       - Flip checkboxes for URL-01, URL-02, URL-03, URL-04, URL-05, URL-06, EXP-02 to `- [x]`.
       - Update the Traceability table rows for those 7 IDs from `Pending` to `Complete` (URL-06 row gets the OpenAI staging score reference per the SMOKE.md content).
    3. Append a one-line note to the Phase 23 entry in the per-phase counts section if a "last updated" line exists.
  </action>
  <verify>
    <automated>uv run python -c "import re; body=open('.planning/REQUIREMENTS.md').read(); smoke=open('.planning/phases/23-url-ingestion-topic-2/23-SMOKE.md').read(); m=re.search(r'verdict:\s*(\w+)', smoke); verdict=m.group(1) if m else 'unknown'; print('VERDICT:', verdict); ids=['URL-01','URL-02','URL-03','URL-04','URL-05','URL-06','EXP-02']; [print(i, '[x]' in [ln for ln in body.split('\n') if i in ln][0] if [ln for ln in body.split('\n') if i in ln] else 'missing') for i in ids]"</automated>
  </verify>
  <acceptance_criteria>
    - If verdict is `pass`: `grep -E '^- \[x\] \*\*URL-01\*\*' .planning/REQUIREMENTS.md` matches; same for URL-02..06 and EXP-02.
    - If verdict is `pass`: Traceability table rows for URL-01..06 and EXP-02 show `Complete` (`grep -E '\| URL-01 \| Phase 23 \| Complete' .planning/REQUIREMENTS.md` matches; same pattern for the other IDs).
    - If verdict is NOT `pass`: REQUIREMENTS.md is unchanged (git diff returns empty for this file); task exits cleanly with message logged.
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects the operator-approved final state of Phase 23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → SMOKE.md verdict | Trusted operator action — load-bearing manual gate per D-24 |
| Real recipe URLs → safe_fetch | safe_fetch defenses (23-01) apply at fetch time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-FALSE-VERDICT | T | 23-SMOKE.md verdict line | mitigate | Verdict is operator-authored; Task 2 reads the verdict programmatically and ONLY ticks REQUIREMENTS.md on `pass`; non-pass verdicts halt the tick |
| T-23-FALSE-GREEN-AUTO | T | Task 2 verify script | mitigate | The acceptance criteria grep for `[x]` lines explicitly; missing checkboxes fail the grep |
| T-23-PARTIAL-COMPLETE | T | REQUIREMENTS.md ticks | accept | Task 2 ticks all 7 IDs atomically; partial ticks would only occur on file-edit failure, which the post-edit grep catches |
</threat_model>

<verification>
- 23-SMOKE.md committed with verdict line.
- 23-EVAL-RESULTS-openai.md committed with OpenAI staging score.
- On verdict: pass — REQUIREMENTS.md ticks all 7 IDs and Traceability table updated.
- On verdict: fail/needs-revision — REQUIREMENTS.md is untouched, orchestrator escalates.
</verification>

<success_criteria>
URL-01..06 + EXP-02 marked Complete in REQUIREMENTS.md backed by operator-approved 23-SMOKE.md (verdict: pass), with OpenAI staging meeting the ≥17/21 (≥85%) threshold from D-12.
</success_criteria>

<output>
Create `.planning/phases/23-url-ingestion-topic-2/23-07-SUMMARY.md` per template.
</output>
