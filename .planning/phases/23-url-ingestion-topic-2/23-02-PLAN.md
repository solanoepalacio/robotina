---
phase: 23-url-ingestion-topic-2
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/robotina/queue/task_types.py
  - src/robotina/agent/tools/start_workflow.py
  - src/robotina/agent/workflows.py
  - src/robotina/dashboard/templates/_macros.html
  - src/robotina/queue/workflow_runner.py
  - tests/unit/test_start_workflow_tool.py
  - tests/queue/test_workflow_registry.py
  - tests/queue/test_wake_helper.py
autonomous: true
requirements: [URL-03, URL-05]
tags: [workflow, schema, dashboard]

must_haves:
  truths:
    - "WORKFLOW_REGISTRY contains add-recipe-from-query and add-recipe-from-url keys"
    - "WORKFLOW_REGISTRY no longer contains add-recipe (hard rename)"
    - "StartWorkflowArgs.workflow_type is Literal['add-recipe-from-query', 'add-recipe-from-url']"
    - "StartWorkflowArgs.input accepts AddRecipeQueryInput | AddRecipeUrlInput"
    - "@model_validator enforces workflow_type ↔ input pairing"
    - "StartWorkflowTool._run writes recipe_url to shared_context for URL workflows"
    - "_check_and_dispatch_wake reads recipe_url as fallback when recipe_query absent"
    - "Dashboard renders 'Búsqueda por URL' label for gather-from-url task type"
  artifacts:
    - path: "src/robotina/queue/task_types.py"
      provides: "AddRecipeUrlInput, GatherFromUrlInput models"
      contains: "AddRecipeUrlInput"
    - path: "src/robotina/agent/workflows.py"
      provides: "Two workflow definitions (per-query + per-url)"
      contains: "add-recipe-from-url"
    - path: "src/robotina/agent/tools/start_workflow.py"
      provides: "Multi-variant StartWorkflowTool"
      contains: "_enforce_pairing"
  key_links:
    - from: "StartWorkflowTool._run"
      to: "WORKFLOW_REGISTRY"
      via: "queue_workflow(workflow_type, shared_context)"
      pattern: "queue_workflow\\("
    - from: "workflow_runner._check_and_dispatch_wake"
      to: "WorkflowOutcomeSummary.recipe_query"
      via: "shared_context.get('recipe_query') or shared_context.get('recipe_url')"
      pattern: "recipe_url"
---

<objective>
Land the schema + workflow-registry surface for URL ingestion: rename `add-recipe` to `add-recipe-from-query`, add the new `add-recipe-from-url` peer (inline-duplicated 6-step list), introduce `AddRecipeUrlInput` and `GatherFromUrlInput`, extend `StartWorkflowArgs` to the discriminated union with `@model_validator` pairing enforcement, thread `recipe_url` through shared_context, and wire the wake-helper fallback read. No new Alembic migration (D — `shared_context` gains a JSON key only). Dashboard label added.

Purpose: Implements URL-03 (workflow variant routing) + URL-05 (wake reply surfaces URL via shared_context).
Output: All schema/registry/tool/dashboard plumbing in one atomic commit so the CI guard for AGENT_REGISTRY ↔ overrides and the Literal extension land together.

Schema-push note: This phase introduces NO new tables or columns. RobotinaInvocation already exists from Phase 18. shared_context["recipe_url"] is a new JSON key on the existing column — no migration required. Skip the migration task per the phase-prompt's schema_push_requirement.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md
@.planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md
@.planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md
@src/robotina/queue/task_types.py
@src/robotina/agent/tools/start_workflow.py
@src/robotina/agent/workflows.py
@src/robotina/queue/workflow_runner.py
@src/robotina/dashboard/templates/_macros.html

<interfaces>
PATTERNS.md provides verbatim before/after snippets:
- `AddRecipeQueryInput` (task_types.py:175-183) — clone shape for `AddRecipeUrlInput` (url:str, not value:str).
- `StartWorkflowArgs` (start_workflow.py:57-69) — extend Literal + union + add `@model_validator(mode='after')`.
- `WORKFLOW_REGISTRY` (workflows.py:67-133) — rename `add-recipe` → `add-recipe-from-query`; add peer `add-recipe-from-url`; tail-5 steps read `artifacts["gather-from-url"]` vs `artifacts["gather"]`.
- workflow_runner.py:253 — D-08 one-line fallback: `(r.shared_context or {}).get("recipe_query") or (r.shared_context or {}).get("recipe_url")`.

GatherFromUrlInput fields (RESEARCH.md): `url:str, reply_context:ReplyContext, household_id:NonEmptyHouseholdId`. Mirror RecipeResearchGatherInput shape with `url` instead of `query`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Pydantic models + StartWorkflowArgs union + model_validator</name>
  <files>src/robotina/queue/task_types.py, src/robotina/agent/tools/start_workflow.py, tests/unit/test_start_workflow_tool.py</files>
  <read_first>
    - src/robotina/queue/task_types.py (existing AddRecipeQueryInput + RecipeResearchGatherInput shapes)
    - src/robotina/agent/tools/start_workflow.py (current StartWorkflowArgs + _run + ctor-injected fields)
    - tests/unit/test_start_workflow_tool.py (existing test patterns)
    - .planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md (StartWorkflowArgs extension section)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-01, D-02, D-22)
  </read_first>
  <behavior>
    - `AddRecipeUrlInput(BaseModel)`: ConfigDict(extra="forbid"); fields {url: str}. NO hint field (D-02).
    - `GatherFromUrlInput(BaseModel)`: ConfigDict(extra="forbid"); fields {url: str, reply_context: ReplyContext, household_id: NonEmptyHouseholdId} mirroring RecipeResearchGatherInput.
    - `StartWorkflowArgs.workflow_type` is `Literal["add-recipe-from-query", "add-recipe-from-url"]` — hard rename, no transitional alias (D-01).
    - `StartWorkflowArgs.input: AddRecipeQueryInput | AddRecipeUrlInput` (plain union, no discriminator).
    - `@model_validator(mode="after") _enforce_pairing` raises ValueError if workflow_type/input pair mismatched.
    - `StartWorkflowTool._run` handles both shapes: branches on `isinstance(input, AddRecipeQueryInput)` to write `shared_context["recipe_query"]`; `isinstance(input, AddRecipeUrlInput)` writes `shared_context["recipe_url"]`.
    - Tool description string mentions both workflow_types + both input shapes with worked examples.
    - Test functions added (D-22): test_pair_query_passes, test_pair_url_passes, test_pair_query_with_url_input_raises, test_pair_url_with_query_input_raises, test_old_add_recipe_literal_raises.
  </behavior>
  <action>
    1. In `src/robotina/queue/task_types.py`: append `AddRecipeUrlInput` class (docstring references D-01/D-02 of Phase 23) and `GatherFromUrlInput` class (docstring references URL-03 + Phase 23 workflow registry). Add doc-comment to `WorkflowOutcomeSummary.recipe_query` per D-08: "Holds the query string for add-recipe-from-query workflows OR the URL for add-recipe-from-url workflows (Phase 23 D-08). Rename to `recipe_source` deferred per feedback_avoid_premature_abstraction."
    2. In `src/robotina/agent/tools/start_workflow.py`:
       - Update imports: `from pydantic import BaseModel, ConfigDict, Field, model_validator`; add `from robotina.queue.task_types import AddRecipeQueryInput, AddRecipeUrlInput`.
       - Replace `StartWorkflowArgs.workflow_type` Literal to two members and `input` to the plain union.
       - Add `_enforce_pairing` `@model_validator(mode="after")` method per PATTERNS.md snippet.
       - In `_run`: if input arrives as dict, route to `AddRecipeQueryInput.model_validate(input)` when "value" in input else `AddRecipeUrlInput.model_validate(input)` when "url" in input. Branch shared_context build: for AddRecipeQueryInput → `shared_context["recipe_query"] = input.value`; for AddRecipeUrlInput → `shared_context["recipe_url"] = input.url`.
       - Update tool description string to mention both workflow_types + both input shapes with one-line worked examples ("add-recipe-from-query, input={value:'lentejas'}" / "add-recipe-from-url, input={url:'https://...'}").
       - Keep all constructor-injected identity fields unchanged (chat_id, user_id, platform, household_id:NonEmptyHouseholdId, conversation_id, invocation_id).
    3. In `tests/unit/test_start_workflow_tool.py`: add the 5 D-22 test functions named above. Each test instantiates `StartWorkflowArgs(...)` (or asserts `pytest.raises(ValidationError)`).
    4. Update any existing test that constructs `StartWorkflowArgs(workflow_type="add-recipe", input={"value":"x"})` to use the new literal `"add-recipe-from-query"`. Grep first: `grep -rn '"add-recipe"' tests/ src/` — fix every collision in this task to avoid leaving sibling plans in broken state.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_start_workflow_tool.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class AddRecipeUrlInput" src/robotina/queue/task_types.py` → match.
    - `grep -q "class GatherFromUrlInput" src/robotina/queue/task_types.py` → match.
    - `grep -q "add-recipe-from-query" src/robotina/agent/tools/start_workflow.py` → match.
    - `grep -q "add-recipe-from-url" src/robotina/agent/tools/start_workflow.py` → match.
    - `grep -q "_enforce_pairing" src/robotina/agent/tools/start_workflow.py` → match.
    - `grep -q "model_validator" src/robotina/agent/tools/start_workflow.py` → match.
    - `grep -rE '"add-recipe"' src/ tests/ | grep -v -E '(add-recipe-from-query|add-recipe-from-url|add-recipe-from)' | wc -l` returns 0 (hard rename per D-01; no stale "add-recipe" string remains except as a substring of new names).
    - `grep -cE '^def test_' tests/unit/test_start_workflow_tool.py` increased by at least 5.
    - `uv run pytest tests/unit/test_start_workflow_tool.py -q` exits 0.
  </acceptance_criteria>
  <done>Models, StartWorkflowArgs, _run branch, and tests green; old "add-recipe" literal eliminated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename WORKFLOW_REGISTRY entry + add URL variant + dashboard label + wake fallback</name>
  <files>src/robotina/agent/workflows.py, src/robotina/queue/workflow_runner.py, src/robotina/dashboard/templates/_macros.html, tests/queue/test_workflow_registry.py, tests/queue/test_wake_helper.py</files>
  <read_first>
    - src/robotina/agent/workflows.py (existing "add-recipe" entry, full 6-step list)
    - src/robotina/queue/workflow_runner.py (line ~253 — _check_and_dispatch_wake)
    - src/robotina/dashboard/templates/_macros.html (existing TASK_TYPE_LABELS map)
    - tests/queue/test_workflow_registry.py (existing tests)
    - tests/queue/test_wake_helper.py (existing tests)
    - .planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md (workflows.py block + workflow_runner D-08 one-line)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-01, D-08)
  </read_first>
  <behavior>
    - WORKFLOW_REGISTRY has exactly two add-recipe-* entries: `add-recipe-from-query` (renamed, build_input reads `ctx["recipe_query"]`) and `add-recipe-from-url` (NEW, build_input on gather step reads `ctx["recipe_url"]` and emits GatherFromUrlInput).
    - The 5 tail steps (instructions, ingredients, metadata, load, finalize-outcome) inline-duplicated between the two variants — the only structural diff is the first step (gather vs gather-from-url) AND the lambda artifact key in instructions step (`artifacts["gather"]` vs `artifacts["gather-from-url"]`).
    - _check_and_dispatch_wake populates `WorkflowOutcomeSummary.recipe_query` from `shared_context.get("recipe_query") or shared_context.get("recipe_url")` (D-08).
    - Dashboard TASK_TYPE_LABELS gains `"gather-from-url": "Búsqueda por URL"`.
    - New registry tests (D-21): test_workflow_registry_has_add_recipe_from_query, test_workflow_registry_has_add_recipe_from_url, test_workflow_registry_lacks_legacy_add_recipe, test_url_variant_first_step_reads_recipe_url_from_shared_context, test_url_variant_instructions_reads_gather_from_url_artifact.
    - New wake-helper test (D-08): test_wake_helper_falls_back_to_recipe_url_when_recipe_query_missing.
  </behavior>
  <action>
    1. In `src/robotina/agent/workflows.py`:
       - Rename existing WORKFLOW_REGISTRY key `"add-recipe"` → `"add-recipe-from-query"`. Update the inner `workflow_type="add-recipe"` argument similarly.
       - Add new WORKFLOW_REGISTRY entry `"add-recipe-from-url"` with `workflow_type="add-recipe-from-url"`, first step `WorkflowStepDef(step_key="gather-from-url", task_type="gather-from-url", build_input=lambda ctx, _: GatherFromUrlInput(url=ctx["recipe_url"], reply_context=ReplyContext(**ctx["reply_context"]), household_id=ctx["household_id"]))`.
       - Inline-duplicate the 5 tail steps from add-recipe-from-query into add-recipe-from-url. In the instructions step of the URL variant, replace `RecipeData(**artifacts["gather"])` with `RecipeData(**artifacts["gather-from-url"])`. (The rest of the tail-step keys remain "instructions", "ingredients", "metadata", "load", "finalize" — those reference their OWN step_keys for downstream artifact reads.)
       - Add `from robotina.queue.task_types import GatherFromUrlInput` to imports.
    2. In `src/robotina/queue/workflow_runner.py`: Change line ~253 from `recipe_query=(r.shared_context or {}).get("recipe_query")` to `recipe_query=((r.shared_context or {}).get("recipe_query") or (r.shared_context or {}).get("recipe_url"))` with comment `# D-08 (Phase 22) + D-08 (Phase 23): fallback to recipe_url for URL workflows.`
    3. In `src/robotina/dashboard/templates/_macros.html`: locate `TASK_TYPE_LABELS` map; add `"gather-from-url": "Búsqueda por URL",` entry (alphabetical position consistent with existing entries).
    4. In `tests/queue/test_workflow_registry.py`: add the 5 D-21 test functions named above. Use `from robotina.agent.workflows import WORKFLOW_REGISTRY` and assert presence/absence of keys + assert lambda behavior by constructing a synthetic `ctx={"recipe_url":"https://x", "reply_context":{...minimal valid...}, "household_id":"hh"}` and calling `build_input(ctx, {})`.
    5. In `tests/queue/test_wake_helper.py`: add test_wake_helper_falls_back_to_recipe_url. Construct a WorkflowRun row mock with `shared_context={"recipe_url":"https://example.com/x"}` (no recipe_query); call the helper; assert resulting WorkflowOutcomeSummary.recipe_query == "https://example.com/x".
    6. Run full quick suite to catch any cross-test "add-recipe" collisions: `uv run pytest tests/unit tests/queue -q`.
  </action>
  <verify>
    <automated>uv run pytest tests/queue/test_workflow_registry.py tests/queue/test_wake_helper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"add-recipe-from-query"' src/robotina/agent/workflows.py` → match.
    - `grep -q '"add-recipe-from-url"' src/robotina/agent/workflows.py` → match.
    - `grep -c "WORKFLOW_REGISTRY\\b" src/robotina/agent/workflows.py` ≥ 1 and there are exactly 2 add-recipe-* keys in the dict literal.
    - `grep -q 'artifacts\\["gather-from-url"\\]' src/robotina/agent/workflows.py` → match (URL variant's instructions step).
    - `grep -q 'recipe_url' src/robotina/queue/workflow_runner.py` → match.
    - `grep -q '"gather-from-url": "Búsqueda por URL"' src/robotina/dashboard/templates/_macros.html` → match.
    - `uv run pytest tests/queue/test_workflow_registry.py tests/queue/test_wake_helper.py -q` exits 0.
    - `uv run pytest tests/unit tests/queue -q` exits 0 (no collateral breakage from rename).
  </acceptance_criteria>
  <done>Two workflow variants registered, wake-helper reads URL fallback, dashboard label present, all registry+wake tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-emitted StartWorkflowTool args → StartWorkflowArgs | Untrusted dict shape from LLM must validate against the union + pairing rule |
| shared_context JSON write → Postgres | Untrusted URL string lands in JSON column; no SQL injection risk (JSONB) but value flows downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-PAIR-MISMATCH | T | StartWorkflowArgs | mitigate | `@model_validator(mode="after")` rejects `workflow_type ↔ input` mismatch → ToolMessage(error) → LLM retries with correct shape |
| T-23-LEGACY-LITERAL | T | StartWorkflowArgs.workflow_type Literal | mitigate | Hard Literal rename ("add-recipe" no longer accepted); stale LLM emit → ValidationError → ToolMessage(error) |
| T-23-EXTRA-FIELDS | T | AddRecipeUrlInput | mitigate | ConfigDict(extra="forbid") on every input model; rejects e.g. `hint` field smuggled by the LLM |
| T-23-INJECT-SHARED-CTX | T | StartWorkflowTool._run → shared_context | accept | LLM-emitted URL string is the entire purpose; downstream safe_fetch (23-01) is the validating boundary; storing the URL in JSONB carries no SQL injection risk |
</threat_model>

<verification>
- Both registry entries exist; legacy "add-recipe" literal is gone from src/ and tests/.
- Wake helper fallback covered by a unit test.
- Dashboard label rendered (template grep).
- `uv run pytest tests/unit tests/queue -q` exits 0.
</verification>

<success_criteria>
URL-03 plumbing complete: a `start-workflow(workflow_type="add-recipe-from-url", input={"url":"https://..."})` call validates, queues an `add-recipe-from-url` workflow with `shared_context["recipe_url"]` populated, and on completion the wake reply surfaces the URL via WorkflowOutcomeSummary.recipe_query.
</success_criteria>

<output>
Create `.planning/phases/23-url-ingestion-topic-2/23-02-SUMMARY.md` per template.
</output>
