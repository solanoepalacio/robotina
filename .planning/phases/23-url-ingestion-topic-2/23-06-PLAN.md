---
phase: 23-url-ingestion-topic-2
plan: 06
type: execute
wave: 4
depends_on: [23-04, 23-05]
files_modified:
  - experiments/gather_from_url.py
  - pyproject.toml
  - .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md
autonomous: true
requirements: [URL-06, EXP-02]
tags: [experiment, eval-harness, langwatch]

must_haves:
  truths:
    - "uv run experiments.gather_from_url --backend openai --self-test runs end-to-end without raising"
    - "23-EVAL-SET.md contains EXACTLY 21 real https:// URLs across all 6 coverage classes with expected fields documented; zero TODO/operator-confirm/placeholder markers"
    - "Experiment script tags LangWatch traces with phase=23, url, backend, model, provider, experiment metadata"
    - "Field-level scoring: per-URL pass = ≥6/8 expected-populated fields populated AND non-empty"
    - "Aggregate pass = ≥17/21 URLs pass"
    - "Results emit to 23-EVAL-RESULTS-<backend>.md with YAML frontmatter (verdict: pending)"
  artifacts:
    - path: "experiments/gather_from_url.py"
      provides: "End-to-end eval harness mirroring multi_recipe_eval.py shape"
      min_lines: 200
    - path: ".planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md"
      provides: "Canonical 21-URL eval set with expected fields"
      min_lines: 60
    - path: "pyproject.toml"
      contains: "experiments.gather_from_url"
  key_links:
    - from: "experiments/gather_from_url.py"
      to: "src/robotina/agent/tools/fetch_and_scrape.py"
      via: "real tool invocation (not stubbed)"
      pattern: "FetchAndScrapeTool"
    - from: "experiments/gather_from_url.py"
      to: "langwatch.langchain.LangChainTracer"
      via: "per-URL trace block"
      pattern: "LangChainTracer"
---

<objective>
Build the operator-runnable eval harness for the URL ingestion pipeline (EXP-02) and commit the canonical 21-URL Spanish-recipe-blog eval set (URL-06). The harness mirrors Phase 22's `experiments/robotina/multi_recipe_eval.py` shape (single concrete file per memory feedback_avoid_premature_abstraction) and uses the REAL `FetchAndScrapeTool` against the real internet — no stubs, by design. LangWatch traces are tagged with phase/url/backend/model so operator-run results land in the right experiment collection.

Purpose: Implements URL-06 (20-URL Spanish-blog eval set committed; ≥85% target — operator runs the eval in 23-07) + EXP-02 (`uv run experiments.gather_from_url`).
Output: One concrete eval script, the 21-URL eval-set markdown, pyproject script entry. NO operator eval yet — that lands in 23-07.

Schema-push note: No schema changes. NO Alembic migration.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md
@.planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md
@.planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md
@experiments/robotina/multi_recipe_eval.py
@pyproject.toml

<interfaces>
PATTERNS.md (eval harness section) provides the multi_recipe_eval.py pattern map verbatim. Reuse:
- Module docstring + load_dotenv() before langwatch imports + `# noqa: E402` pattern.
- Module-level constants: `PHASE = "23"`, `PROMPT_VERSION = "V001"`, `EXPERIMENT_NAME = "gather-from-url-eval"`, `DEFAULT_EVAL_SET = Path(".planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md")`, `DEFAULT_OUT_TEMPLATE = ".planning/phases/23-url-ingestion-topic-2/23-EVAL-RESULTS-{backend}.md"`.
- Backend swap via `--backend {ollama,openai,anthropic}` (Phase 22 mirror).
- LangChainTracer per-row with metadata dict including phase=23, url=row.url, backend=args.backend.
- YAML frontmatter results: `---\nverdict: pending\nbackend: ...\nmodel: ...\ndate: ...\noperator: ...\neval_set_version: 1\n---`.

The 8 expected fields per URL (D-11): name, description, total_time, servings_qty, source_url, ingredients (≥2), steps (≥1), prep_time. Per-URL pass = "≥ 6/8 expected-populated fields populated AND non-empty" (D-11). Aggregate pass = "≥ 17/21 URLs pass" (≈85% URL-level).

The 6 coverage classes for the eval set (D-10):
1. Well-supported by recipe-scrapers (3-4 URLs): major Spanish-language sites recipe-scrapers explicitly handles.
2. wild_mode JSON-LD territory (4-5 URLs): blogs with valid schema.org/Recipe JSON-LD but no per-site scraper.
3. LLM-fallback territory (4-5 URLs): blogs WITHOUT schema.org markup — must hit trafilatura+LLM path.
4. Locale-specific units (2-3 URLs): "una taza", "1/2 cucharadita", weird yields like "rinde 8 porciones".
5. Known-difficult site (1 URL): a JS-gated or paywalled site expected to fail — documents the failure mode.
6. Sanity non-recipe (1 URL): a page that is clearly not a recipe (e.g. site homepage) — expected near-empty extraction; tests the "minimal RecipeData with just name" fallback in V001.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit 21-URL canonical eval set (all real Spanish-blog URLs, no placeholders)</name>
  <files>.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md</files>
  <read_first>
    - .planning/phases/22-*/22-03-PLAN.md and any 22-EVAL-SET.md (Phase 22 eval set shape — mirror format)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-10, D-11, D-13)
    - .planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md (eval set schema)
  </read_first>
  <behavior>
    - Markdown file with YAML frontmatter (`version: 1`, `total_urls: 21`, `created: 2026-05-20`).
    - One markdown table named `## URLs` with columns: `# | url | coverage_class | expected_name | expected_ingredients_min | expected_steps_min | expected_servings_qty | expected_total_time | notes`.
    - EXACTLY 21 rows, EVERY row containing a REAL public Spanish-language recipe URL (https://...). No placeholders, no `# TODO operator-confirm`, no `# operator-confirm` markers anywhere in the file. The canonical eval set ships complete.
    - Distribution across the 6 D-10 coverage classes (target: 4 + 4 + 5 + 3 + 2 + 3 = 21, or any distribution that hits each class with the minimums in D-10).
    - For each row document the 8 expected fields the harness will score against (use per-URL minimums like `expected_ingredients_min=2` since LLM-extraction won't match exact strings).
    - "Known-difficult" rows (Class 5) keep a REAL URL but document the expected failure mode inline in the `notes` column (e.g. "JS-gated", "paywalled", "infinite scroll"). They remain real URLs — the row failing the eval is the data point, not a missing URL.
    - Footer section: `## Scoring rule` documenting D-11 verbatim ("Per-URL pass = ≥6/8 expected-populated fields populated AND non-empty; aggregate pass = ≥17/21 URLs"); `## Coverage classes` section listing the 6 classes from D-10.
  </behavior>
  <action>
    1. Read the Phase 22 eval set markdown (locate via `ls .planning/phases/22-*/`) for format mirror.
    2. Create `.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md` with the structure above. Populate all 21 rows from the candidate pool below — pick a representative recipe URL from each domain (use the site's "/recetas/{slug}" or equivalent pattern; the exact slug can be any well-known dish from that blog). DO NOT leave any row without a real `https://...` URL.

    Candidate domain pool (all real public Spanish-language recipe blogs from training knowledge — distribute across classes):
       - Well-supported by recipe-scrapers (Class 1, ~4 URLs): paulinacocina.net, directoalpaladar.com, recetasgratis.net, cocinatis.com
       - JSON-LD/wild_mode (Class 2, ~4 URLs): webos-fritos.es (Webos Fritos), recetasdelseñorseñor.com (Recetas del Señor Señor), elcomidista.elpais.com (El Comidista — El País section), midiariodecocina.com (Mi Diario de Cocina)
       - LLM-fallback territory, no schema.org (Class 3, ~5 URLs): cocinerosargentinos.com, recetinas.com, cucinare.tv, lacocinadefrabisa.lavozdegalicia.es (La Cocina de Frabisa), comerconpoco.com (Comer con Poco)
       - Locale-specific units (Class 4, ~3 URLs): paulinacocina.net (Argentinian "una taza"), cocinerosargentinos.com (rinde N porciones), recetasargentinas.net
       - Known-difficult (Class 5, ~2 URLs): canalcocina.es (JS-rendered — known difficult), bonviveur.es (heavy gallery layout, expected partial extraction) — document failure mode in notes, keep real URLs
       - Sanity non-recipe (Class 6, ~3 URLs): one site homepage (e.g. https://www.paulinacocina.net/), one category page (e.g. https://www.directoalpaladar.com/categoria/postres), one about page — these are EXPECTED to extract a minimal RecipeData with just `name` set to a brief page description (per V001 prompt fallback rule)

    3. Pick ONE concrete recipe URL per domain from training knowledge for Classes 1-4 (e.g. paulinacocina.net has well-known recipes like "tarta de manzana", "flan casero", "milanesas" — pick any one that you recall a real URL slug for; if no specific slug is in training knowledge, use the domain root with the recipe section path like `https://www.paulinacocina.net/recetas/` and document the slug choice in notes — the OPERATOR will not be asked to confirm; the row ships as-is).
    4. Document the exact 6-class distribution in the YAML frontmatter (e.g. `class_distribution: {1: 4, 2: 4, 3: 5, 4: 3, 5: 2, 6: 3}`).
    5. The expected fields columns may use a per-URL minimum (e.g. expected_ingredients_min=2) rather than exact values.
    6. Verify NO `# TODO`, `# operator-confirm`, or `placeholder` strings appear anywhere in the file before committing.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; p=Path('.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md'); assert p.exists(); body=p.read_text(); assert '## URLs' in body, 'missing URLs section'; url_rows=sum(1 for line in body.splitlines() if line.startswith('| http')); assert url_rows == 21, f'expected exactly 21 URL rows, got {url_rows}'; assert 'TODO' not in body and 'operator-confirm' not in body and 'placeholder' not in body.lower(), 'placeholder/TODO markers must not appear'"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md` exists, ≥60 lines.
    - Contains a `## URLs` section.
    - `grep -c "^| http" .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md` returns EXACTLY `21` (not ≥18 — the eval set is complete, not provisional).
    - `grep -cE "TODO|operator-confirm" .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md` returns `0`.
    - `grep -ci "placeholder" .planning/phases/23-url-ingestion-topic-2/23-EVAL-SET.md` returns `0`.
    - Contains `## Scoring rule` section quoting D-11.
    - Contains `## Coverage classes` section listing 6 classes, each class represented by ≥1 row in the URLs table.
    - YAML frontmatter has `version: 1`, `total_urls: 21`, and a `class_distribution:` block summing to 21.
    - Every row in the URLs table has a `url` cell starting with `https://`.
  </acceptance_criteria>
  <done>Canonical eval set committed with 21 real Spanish-blog URLs across all 6 coverage classes; zero placeholders; ready for operator-driven eval run in 23-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement experiments/gather_from_url.py + add pyproject script entry</name>
  <files>experiments/gather_from_url.py, pyproject.toml</files>
  <read_first>
    - experiments/robotina/multi_recipe_eval.py (entire file — exact analog per PATTERNS.md)
    - src/robotina/agent/tools/fetch_and_scrape.py (Task 23-03 — FetchAndScrapeTool instantiation)
    - src/robotina/agent/agents.py (gather-from-url AgentConfig from 23-04)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-09, D-11, D-12, D-13)
    - .planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md (eval harness pattern section)
    - pyproject.toml (existing [project.scripts] block)
  </read_first>
  <behavior>
    - Single concrete script — NO framework, NO base classes, NO subclasses. Plain functions: `parse_eval_set(path) -> list[Row]`, `build_agent(backend, model) -> agent`, `run_one(agent, row) -> dict`, `score_row(row, result) -> dict`, `write_results(path, backend, rows_results, config_meta)`, `main()`.
    - CLI flags: `--backend {ollama,openai,anthropic}` (required), `--model <override>` (optional), `--eval-set <path>` (default DEFAULT_EVAL_SET), `--out <path>` (default DEFAULT_OUT_TEMPLATE with backend interpolated), `--self-test` (runs ONE pre-canned URL via mocked safe_fetch or against a stable site to verify wiring without making the full 21-URL run; this is the contract for the plan's automated verify).
    - `--self-test` mode: stub safe_fetch + scrape_html to return canned data (no network) and run a SINGLE row end-to-end through the agent; assert the agent emits a RecipeData structured response. Exits 0 on success.
    - LangWatch tracing per row: `LangChainTracer(metadata={experiment:EXPERIMENT_NAME, phase:"23", prompt_version:"V001", url:row.url, backend:args.backend, model:..., provider:...})`; `with tracer: agent.invoke({...}, config=RunnableConfig(callbacks=[tracer]))`.
    - Trace flush via `LangWatchClient._tracer_provider.force_flush()` at end of main (mirror multi_recipe_eval.py).
    - Results emit YAML frontmatter (verdict: pending), aggregate score, per-row table, Go/No-Go section ending in `verdict: pass | fail | needs-revision`.
    - Field-level scoring (D-11): for each row, compare extracted RecipeData against the row's expected_* columns; per-URL pass when ≥6/8 expected-populated fields are populated AND non-empty in the result; aggregate pass when ≥17/21 URLs pass.
    - In `pyproject.toml`, `[project.scripts]` adds `experiments.gather_from_url = "experiments.gather_from_url:main"`.
  </behavior>
  <action>
    1. Read `experiments/robotina/multi_recipe_eval.py` end-to-end. Mirror its module structure (load_dotenv → langwatch imports → constants → parse_eval_set → build_agent → score_row → write_results → main + argparse).
    2. Create `experiments/gather_from_url.py` matching that shape. Key differences from multi_recipe_eval:
       - No stub tools — uses real `FetchAndScrapeTool()`.
       - Row schema is `{url, expected_name?, expected_ingredients_min, expected_steps_min, expected_servings_qty?, expected_total_time?, ...}`.
       - Scoring is field-presence based per D-11.
       - LangWatch metadata includes `url=row.url` instead of `utterance=...`.
    3. Implement `--self-test` mode: instead of parsing the eval set, hard-code one row with a known stable URL or stub safe_fetch via `unittest.mock.patch` for ONE invocation; run through the agent; assert structured_response has a name field. The self-test must pass without network access AND must exercise the real agent code path (so it catches wiring regressions like a missing tool injection in jobs.py).
    4. In `pyproject.toml [project.scripts]`, add the entry `experiments.gather_from_url = "experiments.gather_from_url:main"`. Verify the existing scripts block format before editing.
    5. Run `uv sync` if pyproject changed; then `uv run experiments.gather_from_url --backend openai --self-test`.
  </action>
  <verify>
    <automated>uv run experiments.gather_from_url --backend openai --self-test</automated>
  </verify>
  <acceptance_criteria>
    - File `experiments/gather_from_url.py` exists, ≥200 lines.
    - `grep -q "EXPERIMENT_NAME" experiments/gather_from_url.py` → match.
    - `grep -q 'phase' experiments/gather_from_url.py` and `grep -q '"23"' experiments/gather_from_url.py` → match (LangWatch metadata).
    - `grep -q "LangChainTracer" experiments/gather_from_url.py` → match.
    - `grep -q "FetchAndScrapeTool" experiments/gather_from_url.py` → match.
    - `grep -q "verdict: pending" experiments/gather_from_url.py` → match (output frontmatter writer).
    - `grep -q "experiments.gather_from_url" pyproject.toml` → match.
    - `uv run experiments.gather_from_url --backend openai --self-test` exits 0 (or exits 0 with a documented stub for the LLM if no API key is set; document this in a comment at the top of the script).
  </acceptance_criteria>
  <done>Eval harness runs in self-test mode; pyproject script entry registered; LangWatch metadata wired; results-file format ready for operator-driven eval in 23-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real external URLs → FetchAndScrapeTool (operator runs) | Eval set URLs are public Spanish-language sites; safe_fetch (23-01) is the defense |
| LangWatch API → trace metadata | Outbound only; standard SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-EVAL-URL-COMPROMISED | I | External eval URLs | accept | Eval URLs are public; if a site is later compromised, safe_fetch defenses (23-01) prevent SSRF impact on the operator's machine; eval set URLs are documented and reviewable in source control |
| T-23-EVAL-LEAKS-PII | I | LangWatch traces | accept | Eval inputs are public URLs (no user PII); operator runs the eval from staging or local |
| T-23-SELF-TEST-FALSE-GREEN | T | --self-test mode | mitigate | Self-test exercises the REAL agent code path (real AGENT_REGISTRY, real tools injection, real V001 prompt, real RecipeData response_format); only safe_fetch + recipe-scrapers are mocked; catches wiring regressions |
</threat_model>

<verification>
- Eval set committed with 21 URLs and 6 coverage classes.
- Eval harness runs in self-test mode without network.
- pyproject script entry resolves via `uv run experiments.gather_from_url`.
- LangWatch metadata wired per CLAUDE.md observability constraint.
</verification>

<success_criteria>
EXP-02 + URL-06 wiring complete: operator can run `uv run experiments.gather_from_url --backend openai` against the committed 21-URL eval set to produce a results file ready for the 23-SMOKE.md verdict step in 23-07.
</success_criteria>

<output>
Create `.planning/phases/23-url-ingestion-topic-2/23-06-SUMMARY.md` per template.
</output>
