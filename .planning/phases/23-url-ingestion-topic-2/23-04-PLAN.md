---
phase: 23-url-ingestion-topic-2
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - src/robotina/agent/prompts/gather-from-url/V001.md
  - src/robotina/agent/agents.py
  - src/robotina/queue/jobs.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
  - .env.example
  - tests/agents/test_gather_from_url_agent.py
autonomous: true
requirements: [URL-02, URL-04]
tags: [agent, prompt, registry, overrides]

must_haves:
  truths:
    - "AGENT_REGISTRY contains 'gather-from-url' with response_format_model=RecipeData"
    - "gather-from-url AgentConfig.prompt_path resolves to gather-from-url/V001.md"
    - "All three overrides/*.json files contain a 'gather-from-url' block (atomic sync per feedback_overrides_in_sync)"
    - "run_task injects FetchAndScrapeTool for the gather-from-url task type"
    - "The agent passes through scraped_recipe when present; extracts from html_text otherwise; response_format=RecipeData enforces output schema"
    - "An automated test using a stubbed BaseChatModel drives the agent through the html_text branch end-to-end (URL-04 LLM fallback path is covered, not just grepped)"
  artifacts:
    - path: "src/robotina/agent/prompts/gather-from-url/V001.md"
      provides: "Pass-through-or-extract prompt (English body)"
    - path: "src/robotina/agent/agents.py"
      contains: "gather-from-url"
    - path: "tests/agents/test_gather_from_url_agent.py"
      provides: "Agent integration test with stubbed BaseChatModel covering both branches"
  key_links:
    - from: "src/robotina/agent/agents.py"
      to: "src/robotina/agent/prompts/gather-from-url/V001.md"
      via: "prompt_path"
      pattern: "gather-from-url/V001.md"
    - from: "src/robotina/queue/jobs.py"
      to: "FetchAndScrapeTool"
      via: "tools injection elif branch"
      pattern: "FetchAndScrapeTool"
---

<objective>
Register the `gather-from-url` LLM agent end-to-end: V001 prompt, AGENT_REGISTRY entry, all three `overrides/*.json` files (atomic per memory feedback_overrides_in_sync — the Phase 21 D-12 CI guard will fail otherwise), `run_task` tool injection branch, and a unit test that exercises the agent's two branches (pass-through vs extract).

Purpose: Implements URL-02 (final wiring) + URL-04 (LLM fallback extraction). Schema parity is enforced by `response_format=RecipeData`.
Output: Working agent registered, prompt committed, overrides synced.

Schema-push note: No new tables/columns. No Alembic migration required.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md
@.planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md
@.planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md
@src/robotina/agent/agents.py
@src/robotina/agent/prompts/recipe-research-gather/V005.md
@src/robotina/queue/jobs.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json

<interfaces>
PATTERNS.md provides verbatim shape for AgentConfig (`recipe-research-gather` block, agents.py:88-101). Apply to gather-from-url:
- api_key_env: "GATHER_FROM_URL_API_TOKEN"
- prompt_path: "src/robotina/agent/prompts/gather-from-url/V001.md"
- tools: [] (FetchAndScrapeTool injected per-job in run_task)
- response_format_model: RecipeData

Per Phase 4 convention, run_task in `src/robotina/queue/jobs.py` has an elif chain that injects task-specific tools per task_type. The gather-from-url branch must inject `FetchAndScrapeTool()` (no constructor args — like WebSearchTool).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write V001 prompt + register agent + sync overrides</name>
  <files>src/robotina/agent/prompts/gather-from-url/V001.md, src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json, .env.example</files>
  <read_first>
    - src/robotina/agent/prompts/recipe-research-gather/V005.md (closest sibling — same response_format=RecipeData)
    - src/robotina/agent/agents.py (existing recipe-research-gather AgentConfig)
    - overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json (existing per-task-type blocks; copy the recipe-research-gather block shape)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-03, D-23)
    - .planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md (Pattern 5 prompt content + Spanish-vs-English rules)
  </read_first>
  <behavior>
    - V001 prompt is short (≤ 30 lines body), English (per memory feedback_prompts_language), and instructs:
      1. "Call `fetch-and-scrape` EXACTLY ONCE with the URL from `input.url`."
      2. "If the tool result's `scraped_recipe` field is non-null, pass it through verbatim as your RecipeData response (preserve `source_url`). Do not re-extract; do not modify field values."
      3. "If `scraped_recipe` is null and `html_text` is non-null, extract a complete `RecipeData` from `html_text`. Required: `name`, ≥2 ingredients, ≥1 step. Set `source_url` to the tool result's `source_url`."
      4. "Never fabricate ingredients, quantities, or steps. If the page is clearly not a recipe, emit a minimal RecipeData with just `name` set to a brief description of what the page IS."
      5. "Do not call any tool more than once."
    - AgentConfig entry in `AGENT_REGISTRY` dict named `"gather-from-url"` with the fields above.
    - All three `overrides/*.json` files gain a `"gather-from-url"` block mirroring the `recipe-research-gather` shape in each (different model per backend per existing convention).
    - `.env.example` adds `GATHER_FROM_URL_API_TOKEN=` (empty) — per memory feedback_env_example.
  </behavior>
  <action>
    1. Create `src/robotina/agent/prompts/gather-from-url/V001.md` per the prompt content above. Use the same header conventions as `recipe-research-gather/V005.md` (read it first; mirror the front-matter + section structure if present).
    2. In `src/robotina/agent/agents.py`: add a new AgentConfig entry to `AGENT_REGISTRY` mirroring the `recipe-research-gather` shape:
       ```python
       "gather-from-url": AgentConfig(
           task_type="gather-from-url",
           model_config={
               "provider": "ollama",
               "url": "http://localhost:11434",
               "model": "gpt-oss:20b",
               "api_key_env": "GATHER_FROM_URL_API_TOKEN",
               "reasoning": True,
           },
           prompt_path="src/robotina/agent/prompts/gather-from-url/V001.md",
           skills=[],
           tools=[],  # FetchAndScrapeTool injected per-job in run_task()
           response_format_model=RecipeData,
       ),
       ```
       Use `RecipeData` from existing import; if not yet imported in agents.py, add the import.
    3. In EACH of `overrides/anthropic.json`, `overrides/openai.json`, `overrides/staging.ollama.json`: add a `"gather-from-url"` block with the same shape as the existing `recipe-research-gather` block in that file (each backend's own model/provider/api_key_env). Same commit — Phase 21 D-12 CI guard requires this.
    4. In `.env.example`: add `GATHER_FROM_URL_API_TOKEN=` near other `*_API_TOKEN=` entries.
  </action>
  <verify>
    <automated>uv run python -c "from robotina.agent.agents import AGENT_REGISTRY; cfg=AGENT_REGISTRY['gather-from-url']; print(cfg.task_type, cfg.prompt_path, cfg.response_format_model.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/robotina/agent/prompts/gather-from-url/V001.md` exists.
    - `grep -q '"gather-from-url"' src/robotina/agent/agents.py` → match.
    - `grep -q 'gather-from-url/V001.md' src/robotina/agent/agents.py` → match.
    - `grep -q 'GATHER_FROM_URL_API_TOKEN' src/robotina/agent/agents.py` → match.
    - `grep -q '"gather-from-url"' overrides/anthropic.json` → match.
    - `grep -q '"gather-from-url"' overrides/openai.json` → match.
    - `grep -q '"gather-from-url"' overrides/staging.ollama.json` → match.
    - `grep -q 'GATHER_FROM_URL_API_TOKEN' .env.example` → match.
    - `uv run python -c "from robotina.agent.agents import AGENT_REGISTRY; assert 'gather-from-url' in AGENT_REGISTRY"` exits 0.
    - All existing AGENT_REGISTRY ↔ overrides CI guard tests still pass: `uv run pytest tests/ -q -k 'agent_registry or overrides_sync'` exits 0.
  </acceptance_criteria>
  <done>Agent registered everywhere; overrides synced atomically; prompt committed; env var declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire FetchAndScrapeTool injection in run_task + agent integration test with stubbed BaseChatModel</name>
  <files>src/robotina/queue/jobs.py, tests/agents/test_gather_from_url_agent.py</files>
  <read_first>
    - src/robotina/queue/jobs.py (existing run_task elif chain — find the task_type dispatch + tool-injection pattern; look for WebSearchTool / recipe-research-gather branch)
    - src/robotina/agent/tools/fetch_and_scrape.py (Task 23-03 — FetchAndScrapeTool constructor signature)
    - tests/unit/test_web_search_tool.py + any existing agent integration test (mirror shape; look for prior uses of `FakeMessagesListChatModel`, `FakeListChatModel`, or any in-repo stub of `BaseChatModel`)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-20)
  </read_first>
  <behavior>
    - run_task adds an `elif task_type == "gather-from-url":` branch that constructs `FetchAndScrapeTool()` and includes it in the agent's tools list (mirror how WebSearchTool is injected for recipe-research-gather).
    - Integration test exercises BOTH branches of the gather-from-url agent end-to-end through `langchain.agents.create_agent`, using a stubbed `BaseChatModel` (NOT a grep-only contract test):
      - `test_gather_from_url_passes_through_scraped_recipe`: FetchAndScrapeTool patched to return `{"scraped_recipe":{"name":"X","ingredients":[...],"steps":[...],"source_url":"..."}, "html_text":None}`. The stubbed chat model is scripted to (a) emit a tool call to `fetch-and-scrape`, then (b) emit a `RecipeData`-shaped response that mirrors the scraped_recipe payload. Assert: agent's final structured_response is a `RecipeData` instance with name/ingredients/steps/source_url preserved.
      - `test_gather_from_url_extracts_from_html_text` (URL-04 coverage — mandatory): FetchAndScrapeTool patched to return `{"scraped_recipe":None, "html_text":"<plain text recipe content with name, ingredients, steps>"}`. The stubbed chat model is scripted to (a) emit a tool call, then (b) emit a `RecipeData`-shaped response derived from `html_text` (name + ≥2 ingredients + ≥1 step + source_url). Assert: agent's structured_response has all four populated; tool was called exactly once.
    - Stub-LLM mechanism: use `langchain_core.language_models.fake.FakeMessagesListChatModel` (or `FakeListChatModel` if simpler) preloaded with the scripted `AIMessage` sequence (tool_call → final response with structured output). If `response_format=RecipeData` requires a specific shape from the fake model, either (a) use a small custom `BaseChatModel` subclass that returns canned `AIMessage` with `tool_calls=[...]` then an `AIMessage` whose content/tool_args validate against RecipeData, or (b) bypass `response_format` enforcement in the test by injecting the stub model directly into `create_agent` and asserting on the raw final message structure.
    - The grep-only fallback path is REMOVED. URL-04 (LLM fallback extraction) MUST be covered by an automated test that drives the agent through the html_text branch with a stubbed LLM end-to-end. No `@pytest.mark.skip`; no "if stub-LLM path is unavailable" escape hatch.
  </behavior>
  <action>
    1. In `src/robotina/queue/jobs.py`: locate the elif chain that injects task-specific tools (search for `task_type ==` or similar; the existing pattern for WebSearchTool / recipe-research-gather is the analog). Add a new branch:
       ```python
       elif task_type == "gather-from-url":
           from robotina.agent.tools.fetch_and_scrape import FetchAndScrapeTool
           tools.append(FetchAndScrapeTool())
       ```
       Match the existing local style for tool import location (lazy inside dispatch is consistent with the rest of jobs.py per Phase 4 lazy-import convention).
    2. Create `tests/agents/test_gather_from_url_agent.py` with the two test functions named in the behavior section. Implementation outline:
       - Import `FakeMessagesListChatModel` from `langchain_core.language_models.fake` (verify exact import path at test-write time — if not available, write a 10-line `BaseChatModel` subclass in the test file that returns a queue of canned `AIMessage` objects with `tool_calls`).
       - Patch `robotina.agent.tools.fetch_and_scrape.FetchAndScrapeTool._run` (or whichever method the tool exposes — read the file first) with `unittest.mock.patch` to return the canned dicts for each branch.
       - Build the agent via `langchain.agents.create_agent(model=fake_model, tools=[FetchAndScrapeTool()], response_format=RecipeData, prompt=...)` using the V001 prompt loaded from disk and the AGENT_REGISTRY config — DO NOT bypass the real prompt, that's part of URL-04.
       - Invoke the agent with `{"input": {"url": "https://example.test/recipe"}}` (mirror real run_task input shape) and assert on the result's `structured_response` field.
       - Both tests assert: (a) the `fetch-and-scrape` mock was called exactly once, (b) the structured_response is a RecipeData instance, (c) the expected fields are populated per branch.
    3. Run `uv run pytest tests/agents/test_gather_from_url_agent.py -q` and the AGENT_REGISTRY/overrides guard tests once more.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/test_gather_from_url_agent.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "gather-from-url" src/robotina/queue/jobs.py` → match.
    - `grep -q "FetchAndScrapeTool" src/robotina/queue/jobs.py` → match.
    - `grep -cE '^def test_' tests/agents/test_gather_from_url_agent.py` ≥ 2 (BOTH branches covered; no skip markers permitted).
    - `grep -q "test_gather_from_url_extracts_from_html_text" tests/agents/test_gather_from_url_agent.py` → match (URL-04 LLM fallback branch is named and exists).
    - `grep -q -E "FakeMessagesListChatModel|FakeListChatModel|class .*ChatModel" tests/agents/test_gather_from_url_agent.py` → match (a stubbed BaseChatModel drives the test; no grep-only fallback).
    - `grep -cE "@pytest.mark.skip|pytest.skip\(" tests/agents/test_gather_from_url_agent.py` = 0 (no skips).
    - `uv run pytest tests/agents/test_gather_from_url_agent.py -q` exits 0 with both test functions passing (not skipped, not xfailed).
    - `uv run pytest tests/agents/test_gather_from_url_agent.py::test_gather_from_url_extracts_from_html_text -q` exits 0 (URL-04 branch specifically green).
    - `uv run pytest tests/ -q -k 'overrides_sync or agent_registry'` exits 0.
  </acceptance_criteria>
  <done>FetchAndScrapeTool injected for the new task type; agent integration test green with both branches driven by a stubbed BaseChatModel; URL-04 LLM-fallback path has a real automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → response_format=RecipeData | LLM-emitted structured response validated against RecipeData schema |
| Prompt body → LLM | Prompt is trusted (committed code), not user input |
| FetchAndScrapeTool result → LLM context | Trafilatura-cleaned text capped at 200k chars (23-03) limits prompt-injection blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-LLM-HALLUCINATE-FIELDS | T | gather-from-url agent | mitigate | response_format=RecipeData (Phase 11) enforces schema; V001 prompt explicit "Never fabricate" rule |
| T-23-TOOL-LOOP | D | gather-from-url agent | mitigate | V001 prompt: "Do not call any tool more than once"; create_agent default tool-call cap also applies |
| T-23-OVERRIDES-DRIFT | T | overrides/*.json sync | mitigate | All three overrides files updated in same commit (D-23); Phase 21 D-12 CI guard catches drift |
| T-23-PROMPT-INJECT-HTML | E | trafilatura html_text → LLM context | accept | Risk acknowledged: trafilatura cleans nav/footer but body text can contain "ignore previous instructions" patterns. Mitigation: response_format=RecipeData constrains output shape so injection cannot exfil arbitrary content; 200k char cap limits payload. Vision-LLM "is this a recipe?" check deferred to Phase 24 per CONTEXT.md Deferred Ideas |
</threat_model>

<verification>
- Agent registered and configured.
- All three overrides files updated.
- Tool injection wired in jobs.py.
- Agent integration test green, both branches (pass-through AND html_text/LLM-fallback) exercised through a stubbed BaseChatModel.
- Existing AGENT_REGISTRY ↔ overrides CI guard still passes.
</verification>

<success_criteria>
URL-02 + URL-04 implemented: a `gather-from-url` task runs end-to-end, producing a structured RecipeData either via direct pass-through of scraper results OR via LLM extraction from cleaned HTML text. URL-04's LLM-fallback branch has a real automated test (stubbed BaseChatModel), not a grep-only contract test.
</success_criteria>

<output>
Create `.planning/phases/23-url-ingestion-topic-2/23-04-SUMMARY.md` per template.
</output>
