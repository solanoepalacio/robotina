---
phase: 23-url-ingestion-topic-2
plan: 05
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - src/robotina/agent/prompts/robotina/V007.md
  - src/robotina/agent/agents.py
  - tests/agents/test_handle_incoming_message_agent.py
autonomous: true
requirements: [URL-05]
tags: [prompt, robotina, url-detection]

must_haves:
  truths:
    - "V007.md exists, forked verbatim from V006 with URL handling sections appended"
    - "V006.md still exists for rollback (D-25)"
    - "agents.py handle-incoming-message prompt_path points to V007.md"
    - "V007 instructs LLM to route URLs via add-recipe-from-url, text via add-recipe-from-query"
    - "V007 instructs LLM to emit one start-workflow call per URL/recipe item"
    - "V007 instructs LLM to clarify ambiguous bare hostnames via respond()+terminate()"
  artifacts:
    - path: "src/robotina/agent/prompts/robotina/V007.md"
      provides: "Robotina prompt with URL detection + routing rules"
    - path: "src/robotina/agent/prompts/robotina/V006.md"
      provides: "Retained for rollback per D-25"
  key_links:
    - from: "src/robotina/agent/agents.py handle-incoming-message AgentConfig"
      to: "src/robotina/agent/prompts/robotina/V007.md"
      via: "prompt_path"
      pattern: "robotina/V007.md"
---

<objective>
Fork V006 to V007 and add URL-detection + per-source routing rules. The LLM (not regex) detects URLs, emits one `start-workflow(workflow_type="add-recipe-from-url", input={"url":"..."})` per URL and one `start-workflow(workflow_type="add-recipe-from-query", input={"value":"..."})` per text-only recipe item. Mixed messages produce N calls (one per item, each routed to its variant). Ambiguous bare hostnames trigger a clarification turn via `respond()+terminate()`.

Purpose: Implements URL-05 (Robotina detects URLs and routes) per D-05, D-06, D-07. Composes on top of Phase 22's multi-recipe behavior.
Output: V007 prompt + agents.py path bump + unit test asserting prompt sections.

Schema-push note: No code/schema changes beyond the prompt path bump in agents.py. No Alembic migration.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md
@.planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md
@.planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md
@src/robotina/agent/prompts/robotina/V006.md
@src/robotina/agent/agents.py

<interfaces>
The Phase 22 V006 prompt is the verbatim base. Phase 23 changes:
1. Remove any V006 section that deflects URLs as unsupported (RESEARCH.md flags this at line 1138 — "Todavía no manejo enlaces directos…" or similar).
2. Add a `## URL handling` section per RESEARCH.md lines 1085-1136 with worked examples for the four cases below.
3. Keep ALL existing V006 sections (multi-recipe, soft-cap-at-5, ambiguity, etc.) intact.

Phase 21 / 22 contracts that V007 inherits and MUST preserve:
- The tool surface is `respond(text)`, `start-workflow(workflow_type, input)`, `terminate()` with `return_direct=True` ONLY on terminate.
- `start-workflow.input` is one of `{value: str}` (query) or `{url: str}` (URL), discriminated by `workflow_type`.
- The soft cap of 5 (combined across query + URL items in one message) is enforced at prompt level.

The four URL handling cases V007 must teach (worked examples in the prompt body):
1. Single URL: user says "agregá esta receta: <URL>" → one `start-workflow(workflow_type="add-recipe-from-url", input={url:"<URL>"})`.
2. Mixed text+URL: "agregá <URL> y también lentejas" → two start-workflow calls (one URL variant, one query variant).
3. Multi-URL: "<URL1> <URL2>" → two start-workflow URL-variant calls (one per URL).
4. Ambiguous bare hostname (e.g. "agregá receta de recetas.com"): respond("¿Es un enlace o un nombre de receta? Mandame el enlace completo si querés que lo busque ahí.") + terminate(). No start-workflow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork V006 to V007 with URL handling sections + bump prompt_path</name>
  <files>src/robotina/agent/prompts/robotina/V007.md, src/robotina/agent/agents.py, tests/agents/test_handle_incoming_message_agent.py</files>
  <read_first>
    - src/robotina/agent/prompts/robotina/V006.md (entire file — copy verbatim as starting point)
    - src/robotina/agent/agents.py (handle-incoming-message AgentConfig — find current prompt_path line)
    - .planning/phases/23-url-ingestion-topic-2/23-CONTEXT.md (D-05, D-06, D-07, D-25)
    - .planning/phases/23-url-ingestion-topic-2/23-RESEARCH.md (URL handling examples, lines 1085-1136)
    - .planning/phases/23-url-ingestion-topic-2/23-PATTERNS.md (V007 fork section)
    - tests/ (look for existing handle-incoming-message agent or prompt tests to extend — grep tests for "V006" or "robotina_prompt")
  </read_first>
  <behavior>
    - V007.md is byte-for-byte identical to V006.md EXCEPT:
      (a) any V006 "URL not supported" deflection line is removed (RESEARCH.md flags this; if no such line exists in V006, skip this step — verify by grepping V006 for `URL`/`enlace`/`http`).
      (b) a new `## URL handling` section is appended, containing the four worked examples above with explicit example tool-call JSON for each.
      (c) the Literal values in any examples that reference `workflow_type="add-recipe"` are updated to `"add-recipe-from-query"` (matching the hard rename in 23-02).
    - The new URL handling section uses English instruction body but Spanish strings in any `respond()` example payloads (per memory feedback_prompts_language).
    - URL detection rule (LLM-judgment per D-05): "Treat a token as a URL if it begins with `http://` or `https://`. Tokens that look like bare hostnames (e.g., `recetas.com`) without scheme are AMBIGUOUS — ask the user to clarify."
    - Routing rule (D-07): "For each recipe item the user mentions, emit ONE `start-workflow` call. URL items use `workflow_type='add-recipe-from-url'` with `input={url:...}`; text items use `workflow_type='add-recipe-from-query'` with `input={value:...}`."
    - Soft cap rule (Phase 22 D-01 inherited): "If the user names more than 5 items total (queries + URLs combined), either ask them to split or proceed with the first 5 and note the cap. This soft-cap is across all items, not per-type."
    - agents.py prompt_path bump from V006.md to V007.md for handle-incoming-message AgentConfig.
    - V006.md remains in the repo (D-25 retention for rollback).
  </behavior>
  <action>
    1. Read `src/robotina/agent/prompts/robotina/V006.md` in full.
    2. Create `src/robotina/agent/prompts/robotina/V007.md` as a VERBATIM copy of V006.md.
    3. In V007.md:
       - Grep for "URL", "enlace", "link", "http" — if any line deflects URLs as unsupported, remove it.
       - Update any in-prompt example that uses `workflow_type="add-recipe"` to `"add-recipe-from-query"`.
       - Append a `## URL handling` section with the four worked examples described in behavior. Each example shows the user message + the exact tool-call sequence Robotina should produce.
       - Example for case 1 (single URL):
         ```
         User: "agregá esta receta: https://recetasdesa.com/canelones-de-choclo"
         Tool calls:
           respond(text="Listo, voy con esa receta. Te aviso cuando termine.")
           start-workflow(workflow_type="add-recipe-from-url", input={"url":"https://recetasdesa.com/canelones-de-choclo"})
           terminate()
         ```
       - Example for case 4 (ambiguous bare hostname):
         ```
         User: "agregá receta de recetas.com"
         Tool calls:
           respond(text="¿Es un enlace o un nombre de receta? Mandame el enlace completo si querés que lo busque ahí.")
           terminate()
         ```
    4. In `src/robotina/agent/agents.py`: find the handle-incoming-message AgentConfig prompt_path (currently `src/robotina/agent/prompts/robotina/V006.md` per ROADMAP / PATTERNS); change to `src/robotina/agent/prompts/robotina/V007.md`.
    5. Add or extend `tests/agents/test_handle_incoming_message_agent.py` (create if absent, mirroring shape of any existing prompt-version test you find via grep). Required tests:
       - test_handle_incoming_message_prompt_path_is_v007: assert AGENT_REGISTRY['handle-incoming-message'].prompt_path endswith "V007.md".
       - test_v007_contains_url_handling_section: open V007.md, assert "URL handling" or "## URL handling" substring present.
       - test_v007_contains_add_recipe_from_url_example: assert `add-recipe-from-url` substring present.
       - test_v007_contains_add_recipe_from_query_example: assert `add-recipe-from-query` substring present.
       - test_v007_drops_legacy_add_recipe_literal: assert no occurrence of `workflow_type="add-recipe"` (without `-from-...` suffix); use a strict regex.
       - test_v006_still_exists_for_rollback: assert path V006.md exists (D-25).
  </action>
  <verify>
    <automated>uv run pytest tests/agents/test_handle_incoming_message_agent.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/robotina/agent/prompts/robotina/V007.md` succeeds.
    - `test -f src/robotina/agent/prompts/robotina/V006.md` succeeds (D-25 retention).
    - `grep -q "robotina/V007.md" src/robotina/agent/agents.py` → match.
    - `! grep -q "robotina/V006.md" src/robotina/agent/agents.py` (no active path still points at V006).
    - `grep -q "add-recipe-from-url" src/robotina/agent/prompts/robotina/V007.md` → match.
    - `grep -q "add-recipe-from-query" src/robotina/agent/prompts/robotina/V007.md` → match.
    - `grep -q "URL handling" src/robotina/agent/prompts/robotina/V007.md` → match.
    - `grep -E '"add-recipe"[^-]' src/robotina/agent/prompts/robotina/V007.md | wc -l` returns 0 (no bare "add-recipe" literal remains as a workflow_type value).
    - `uv run pytest tests/agents/test_handle_incoming_message_agent.py -q` exits 0.
  </acceptance_criteria>
  <done>V007 prompt committed with URL detection + per-source routing; agents.py path bumped; prompt-contract tests green; V006 retained for rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed URL → Robotina prompt context | Untrusted URL string flows into the LLM's tool-call args |
| Prompt body → LLM | Trusted (committed code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-URL-DETECT-MISS | T | V007 URL detection rule | mitigate | LLM-judgment rule (D-05) with explicit worked examples in prompt; ambiguous bare hostnames trigger clarification (case 4); regex-based detection rejected per D-05 because of punycode/IDN/percent-encoding edge cases |
| T-23-CROSS-VARIANT-MISROUTE | T | V007 routing rule | mitigate | Worked examples per case 1-4; downstream `@model_validator` in StartWorkflowArgs (23-02) catches mismatch and ToolMessage(error) forces retry |
| T-23-LEGACY-LITERAL-LEAK | T | V007 prompt content | mitigate | Test_v007_drops_legacy_add_recipe_literal asserts no bare "add-recipe" workflow_type in prompt body |
| T-23-PROMPT-INJECT-USER-MSG | E | User message → V007 context | accept | User messages always reach the agent; standard LLM-API guidance + response_format constraints on downstream agents mitigate; out-of-scope for this plan (would need separate user-input sanitization phase) |
</threat_model>

<verification>
- V007.md exists, is forked from V006, has URL handling section.
- agents.py points to V007.
- All prompt-contract tests green.
- V006.md retained for rollback.
</verification>

<success_criteria>
URL-05 implemented: Robotina detects URLs in user messages and routes them through `add-recipe-from-url`; text-only items continue through `add-recipe-from-query`; mixed messages produce N parallel calls; ambiguous bare hostnames trigger a clarification turn.
</success_criteria>

<output>
Create `.planning/phases/23-url-ingestion-topic-2/23-05-SUMMARY.md` per template.
</output>
