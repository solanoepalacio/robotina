---
phase: 20
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/queue/test_workflow_runner.py
  - tests/queue/test_jobs.py
  - tests/queue/test_finalize_outcome.py
  - tests/queue/test_reconcile.py
  - tests/queue/test_task_types.py
  - tests/dashboard/test_detail_view.py
  - tests/conftest.py
autonomous: true
requirements: [WAKE-01, WAKE-02, WAKE-03, WAKE-04, WAKE-05, DASH-10, DASH-12]
requirements_addressed: []
gap_closure: false

must_haves:
  truths:
    - "Pytest collects (no syntax errors) after Wave 0 changes"
    - "RED test for WakeInvocationInput, WorkflowOutcomeSummary, FinalizeOutcomeInput Pydantic shapes exists and FAILS because classes do not yet exist"
    - "RED test for _check_and_dispatch_wake helper signature exists and FAILS because function does not yet exist"
    - "RED tests for wake helper happy/partial/idempotency/queue=None paths exist and FAIL (helper missing)"
    - "RED test for finalize-outcome run_task branch exists and FAILS (no branch yet)"
    - "RED test for run_task branching on invocation.trigger exists and FAILS (no branch yet)"
    - "RED test for InvocationStatus transitions in run_task exists and FAILS (no transitions yet)"
    - "RED test for reconcile_invocations function exists and FAILS (module/function missing)"
    - "RED tests for dashboard conversation_id row + outcome cell exist and FAIL (template not updated)"
    - "Shared wake_test_fixtures (wake_invocation_factory, workflow_run_factory_with_outcome) live in tests/conftest.py and are usable across phase tests"
  artifacts:
    - path: "tests/queue/test_task_types.py"
      provides: "RED tests for WakeInvocationInput, WorkflowOutcomeSummary, FinalizeOutcomeInput Pydantic shapes"
      contains: "test_wake_invocation_input_shape"
    - path: "tests/queue/test_workflow_runner.py"
      provides: "RED tests for _check_and_dispatch_wake helper — happy/partial/idempotency/queue=None/failed-terminal"
      contains: "test_check_and_dispatch_wake_happy_path"
    - path: "tests/queue/test_jobs.py"
      provides: "RED tests for run_task branching on invocation.trigger + InvocationStatus transitions"
      contains: "test_run_task_branches_on_invocation_trigger"
    - path: "tests/queue/test_finalize_outcome.py"
      provides: "RED test for finalize-outcome run_task branch composing AddRecipeOutcome from artifacts"
      contains: "test_finalize_outcome_writes_success_outcome"
    - path: "tests/queue/test_reconcile.py"
      provides: "RED tests for reconcile_invocations — orphan, non-orphan, no-op"
      contains: "test_reconcile_re_enqueues_orphan_invocation"
    - path: "tests/dashboard/test_detail_view.py"
      provides: "RED tests for conversation_id row + outcome cell (success/failure/null branches)"
      contains: "test_detail_view_renders_conversation_id_row"
    - path: "tests/conftest.py"
      provides: "wake_invocation_factory + workflow_run_factory_with_outcome fixtures"
      contains: "wake_invocation_factory"
  key_links:
    - from: "tests/queue/test_task_types.py"
      to: "src/robotina/queue/task_types.py::WakeInvocationInput"
      via: "Pydantic ValidationError on missing previous_invocation_id"
      pattern: "WakeInvocationInput"
    - from: "tests/queue/test_workflow_runner.py"
      to: "src/robotina/queue/workflow_runner.py::_check_and_dispatch_wake"
      via: "AttributeError on missing helper"
      pattern: "_check_and_dispatch_wake"
    - from: "tests/queue/test_reconcile.py"
      to: "src/robotina/queue/reconcile.py::reconcile_invocations"
      via: "ImportError on missing module"
      pattern: "reconcile_invocations"
---

<objective>
Wave 0 lock-test step: write all RED-state tests that encode Phase 20 contracts BEFORE any source change. Every contract in 20-CONTEXT.md (wake helper semantics, finalize-outcome composition, run_task branching, reconciler behavior, dashboard rendering) is captured as an executable RED test. Waves 1–3 flip them GREEN.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/robotina/queue/models.py
@src/robotina/queue/task_types.py
@src/robotina/queue/workflow_runner.py
@src/robotina/queue/jobs.py
@src/robotina/dashboard/templates/workflow.html
</context>

<tasks>

<task id="20-01-1" type="execute">
<read_first>
- tests/conftest.py
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-04, D-08, D-17)
- .planning/phases/18-robotinainvocation-entity/18-01-PLAN.md (existing invocation_factory pattern)
- src/robotina/queue/models.py::RobotinaInvocation (existing schema after Phase 18)
</read_first>
<action>
Extend `tests/conftest.py` with two new fixtures:

1. `wake_invocation_factory(session, conversation_id, parent_invocation_id=None, status="pending")` — inserts a `RobotinaInvocation` row with `trigger=WORKFLOW_COMPLETION` and `trigger_ref_id=parent_invocation_id`. Returns the inserted row. Idempotency-friendly (caller supplies uniqueness).

2. `workflow_run_factory_with_outcome(session, conversation_id, triggered_by_invocation_id, status="done", outcome=None)` — inserts a `WorkflowRun` row with the given terminal status and (optionally) JSON outcome dict already populated.

Both fixtures use the existing `invocation_factory` (Phase 18) as a model — same SQLAlchemy session pattern, same return shape (the inserted ORM object).
</action>
<acceptance_criteria>
- `tests/conftest.py` contains `def wake_invocation_factory(`
- `tests/conftest.py` contains `def workflow_run_factory_with_outcome(`
- Both fixtures decorated with `@pytest.fixture`
- `uv run pytest tests/conftest.py --collect-only -q` exits 0 (fixtures collect cleanly)
</acceptance_criteria>
</task>

<task id="20-01-2" type="execute">
<read_first>
- tests/queue/test_task_types.py (if exists; create if not)
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-03, D-06, D-08)
</read_first>
<action>
Create or extend `tests/queue/test_task_types.py` with RED tests for the three new Pydantic models. Each test imports the symbol from `robotina.queue.task_types` — import errors are the RED signal until Wave 1 lands.

```python
def test_wake_invocation_input_shape():
    from robotina.queue.task_types import WakeInvocationInput, WorkflowOutcomeSummary
    inp = WakeInvocationInput(
        previous_invocation_id="inv-abc",
        conversation_id="conv-xyz",
        outcomes=[],
    )
    assert inp.previous_invocation_id == "inv-abc"
    assert inp.outcomes == []
    # extra="forbid"
    with pytest.raises(ValidationError):
        WakeInvocationInput(previous_invocation_id="x", conversation_id="y", outcomes=[], extra_field="nope")

def test_workflow_outcome_summary_shape():
    from robotina.queue.task_types import WorkflowOutcomeSummary, AddRecipeOutcome
    s = WorkflowOutcomeSummary(
        workflow_run_id="run-1",
        workflow_type="add-recipe",
        status="done",
        outcome=AddRecipeOutcome(status="success", recipe_id="r1", recipe_name="Lentejas", recipe_slug="lentejas"),
    )
    assert s.status == "done"
    # FAILED workflows carry outcome=None
    s2 = WorkflowOutcomeSummary(workflow_run_id="run-2", workflow_type="add-recipe", status="failed", outcome=None)
    assert s2.outcome is None

def test_finalize_outcome_input_shape():
    from robotina.queue.task_types import FinalizeOutcomeInput
    fi = FinalizeOutcomeInput(metadata={"recipe_name": "x"}, load={"recipe_id": "y"})
    assert fi.metadata == {"recipe_name": "x"}
    with pytest.raises(ValidationError):
        FinalizeOutcomeInput(metadata={}, load={}, extra="nope")
```
</action>
<acceptance_criteria>
- `grep -c "def test_wake_invocation_input_shape" tests/queue/test_task_types.py` is at least 1
- `grep -c "def test_workflow_outcome_summary_shape" tests/queue/test_task_types.py` is at least 1
- `grep -c "def test_finalize_outcome_input_shape" tests/queue/test_task_types.py` is at least 1
- `uv run pytest tests/queue/test_task_types.py -k "wake_invocation_input_shape or workflow_outcome_summary or finalize_outcome_input" --co -q` collects 3 tests
- Running the tests fails with `ImportError` or `ModuleNotFoundError` (RED state) — verify with: `uv run pytest tests/queue/test_task_types.py::test_wake_invocation_input_shape 2>&1 | grep -E "ImportError|cannot import"`
</acceptance_criteria>
</task>

<task id="20-01-3" type="execute">
<read_first>
- tests/queue/test_workflow_runner.py
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-04, D-05, D-17, D-18)
- src/robotina/queue/workflow_runner.py (current on_step_complete and on_step_failed)
</read_first>
<action>
Append to `tests/queue/test_workflow_runner.py` a `class TestCheckAndDispatchWake:` with RED tests:

1. `test_check_and_dispatch_wake_happy_path_single_workflow` — One DONE WorkflowRun under a parent invocation → helper inserts a child invocation with `trigger=workflow_completion`, `trigger_ref_id=parent.id`, `rq_job_id` set; parent's `wake_dispatched_at` is set.
2. `test_check_and_dispatch_wake_multi_workflow_partial` — 2 DONE + 1 PENDING → helper does NOT insert child, parent's `wake_dispatched_at` stays NULL.
3. `test_check_and_dispatch_wake_multi_workflow_all_done` — 3 DONE → helper inserts exactly one child.
4. `test_check_and_dispatch_wake_failed_terminal` — 1 FAILED → helper inserts child (failure is terminal too).
5. `test_check_and_dispatch_wake_idempotent` — Call helper twice with same state → second call no-ops (count of `trigger=workflow_completion` rows for `trigger_ref_id=parent.id` is 1, not 2).
6. `test_check_and_dispatch_wake_queue_none_skips_enqueue` — `queue=None` → DB-side state mutates (wake_dispatched_at + child row), but `queue.enqueue` is not called.

All tests import `from robotina.queue.workflow_runner import _check_and_dispatch_wake` (RED — function doesn't exist yet). Tests use `wake_invocation_factory` and `workflow_run_factory_with_outcome` fixtures from conftest.

Mark integration-heavy tests `@pytest.mark.integration` if they touch real Postgres; pure-logic tests can stay unmarked.
</action>
<acceptance_criteria>
- `grep -c "class TestCheckAndDispatchWake" tests/queue/test_workflow_runner.py` is 1
- All 6 test method names appear: `grep -cE "def test_check_and_dispatch_wake_(happy_path_single_workflow|multi_workflow_partial|multi_workflow_all_done|failed_terminal|idempotent|queue_none_skips_enqueue)" tests/queue/test_workflow_runner.py` returns 6
- `uv run pytest tests/queue/test_workflow_runner.py::TestCheckAndDispatchWake --co -q` collects 6 tests
- All 6 tests fail with `ImportError` / `AttributeError` on `_check_and_dispatch_wake` (RED state)
</acceptance_criteria>
</task>

<task id="20-01-4" type="execute">
<read_first>
- tests/queue/test_jobs.py (create if needed)
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-07, D-10, D-19)
- src/robotina/queue/jobs.py::run_task (current handle-incoming-message branch)
</read_first>
<action>
Create or extend `tests/queue/test_jobs.py` with RED tests for the refactored `run_task` branching + invocation status transitions:

1. `test_run_task_user_message_path_transitions_status` — invocation with `trigger=USER_MESSAGE`, status=PENDING → after `run_task` returns: status=DONE, started_at set, completed_at set.
2. `test_run_task_wake_path_dispatches_with_wake_input` — invocation with `trigger=WORKFLOW_COMPLETION` and `task_input=WakeInvocationInput(...)` → `run_task` constructs the agent with the wake input (assert via mock on the agent factory). Status transitions to DONE.
3. `test_run_task_exception_marks_failed` — invocation runs, mock agent raises → status=FAILED, completed_at set, exception re-raised.
4. `test_run_task_missing_invocation_raises` — `meta['invocation_id']` points at non-existent row → `RuntimeError`.
5. `test_run_task_branches_on_invocation_trigger` — meta-level assertion: with two invocations differing only by trigger, the agent input shape differs (one IncomingMessageInput, one WakeInvocationInput).

Mock the agent invocation throughout (don't run an LLM). Use existing test patterns from `tests/queue/test_jobs.py` if present.
</action>
<acceptance_criteria>
- `grep -cE "def test_run_task_(user_message_path_transitions_status|wake_path_dispatches_with_wake_input|exception_marks_failed|missing_invocation_raises|branches_on_invocation_trigger)" tests/queue/test_jobs.py` returns 5
- `uv run pytest tests/queue/test_jobs.py -k "run_task_user_message or run_task_wake_path or run_task_exception or run_task_missing or run_task_branches" --co -q` collects 5 tests
- All 5 tests fail in RED state (either ImportError on `WakeInvocationInput`, or assertion failure because branching/status-transition logic doesn't exist yet)
</acceptance_criteria>
</task>

<task id="20-01-5" type="execute">
<read_first>
- tests/queue/test_finalize_outcome.py (create)
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-01, D-02, D-03, D-20)
- src/robotina/queue/jobs.py (line 90 — send-notification deterministic branch pattern)
</read_first>
<action>
Create `tests/queue/test_finalize_outcome.py` with RED tests for the `finalize-outcome` task type:

1. `test_finalize_outcome_writes_success_outcome` — given a `FinalizeOutcomeInput(metadata={...}, load={"recipe_id": "abc", "recipe_name": "Lentejas", "recipe_slug": "lentejas"})` and a real WorkflowRun row, the task composes `AddRecipeOutcome(status="success", recipe_id="abc", recipe_name="Lentejas", recipe_slug="lentejas", image_present=False)` and writes it to `run.outcome` (JSON dict on the row).
2. `test_finalize_outcome_writes_failure_outcome_when_load_missing` — given `FinalizeOutcomeInput(metadata={}, load=None, failure_reason="...")` → composes `AddRecipeOutcome(status="failure", failure_reason="...")`.
3. `test_finalize_outcome_run_task_branch_dispatches` — calls `run_task` with `task_type='finalize-outcome'` meta, asserts the deterministic branch wrote to `run.outcome`. RED on missing branch.

Tests use `workflow_run_factory_with_outcome` (or its non-outcome variant) and import `from robotina.queue.jobs import run_task`.
</action>
<acceptance_criteria>
- `tests/queue/test_finalize_outcome.py` exists
- `grep -cE "def test_finalize_outcome_(writes_success_outcome|writes_failure_outcome|run_task_branch_dispatches)" tests/queue/test_finalize_outcome.py` returns 3
- `uv run pytest tests/queue/test_finalize_outcome.py --co -q` collects 3 tests
- All 3 fail in RED state (no finalize-outcome branch / no FinalizeOutcomeInput)
</acceptance_criteria>
</task>

<task id="20-01-6" type="execute">
<read_first>
- tests/queue/test_reconcile.py (create)
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-11, D-21)
</read_first>
<action>
Create `tests/queue/test_reconcile.py` with RED tests for `reconcile_invocations(session, queue)`:

1. `test_reconcile_re_enqueues_orphan_invocation` — Insert a `RobotinaInvocation` with `status=PENDING`, `wake_dispatched_at=<set>`, `rq_job_id="job-abc"`. Mock Redis so `Job.exists("job-abc")` returns False. Call `reconcile_invocations(session, queue)`. Assert `queue.enqueue` was called with `job_id="job-abc"`.
2. `test_reconcile_no_op_when_job_exists` — Same setup, but `Job.exists("job-abc")` returns True. Assert `queue.enqueue` was NOT called.
3. `test_reconcile_skips_pending_user_message_invocations` — Insert a `RobotinaInvocation(trigger=USER_MESSAGE, status=PENDING, wake_dispatched_at=None)`. Assert `queue.enqueue` was NOT called (reconciler only touches rows where `wake_dispatched_at IS NOT NULL`).
4. `test_reconcile_returns_count` — Inserts 2 orphans + 1 healthy → returns or logs `reconciled=2`.

All tests import `from robotina.queue.reconcile import reconcile_invocations` (RED — module doesn't exist).
</action>
<acceptance_criteria>
- `tests/queue/test_reconcile.py` exists
- `grep -cE "def test_reconcile_(re_enqueues_orphan_invocation|no_op_when_job_exists|skips_pending_user_message_invocations|returns_count)" tests/queue/test_reconcile.py` returns 4
- `uv run pytest tests/queue/test_reconcile.py --co -q` collects 4 tests
- All 4 fail with `ImportError` on `robotina.queue.reconcile` (RED state)
</acceptance_criteria>
</task>

<task id="20-01-7" type="execute">
<read_first>
- tests/dashboard/test_detail_view.py (after Phase 18)
- .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (D-12, D-13, D-22)
- src/robotina/dashboard/templates/workflow.html (current kv-grid)
</read_first>
<action>
Append to `tests/dashboard/test_detail_view.py` four RED tests:

1. `test_detail_view_renders_conversation_id_row` — Render the template with a WorkflowRun whose `conversation_id="conv-abc"` → output contains `<dt>Conversation</dt>` AND `conv-abc`.
2. `test_detail_view_renders_outcome_success` — `run.outcome = {"status": "success", "recipe_name": "Lentejas", "recipe_id": "abc", "image_present": False}` → output contains `Lentejas` AND a success indicator (✓ or `class="outcome-success"`) AND `sin imagen`.
3. `test_detail_view_renders_outcome_failure` — `run.outcome = {"status": "failure", "failure_reason": "insufficient gather"}` → output contains `Falló` AND `insufficient gather`.
4. `test_detail_view_renders_outcome_null_dash` — `run.outcome = None` → output contains the em-dash placeholder for the outcome row.

All four fail RED because the template doesn't have the new rows yet.
</action>
<acceptance_criteria>
- `grep -cE "def test_detail_view_renders_(conversation_id_row|outcome_success|outcome_failure|outcome_null_dash)" tests/dashboard/test_detail_view.py` returns 4
- `uv run pytest tests/dashboard/test_detail_view.py -k "conversation_id_row or outcome_success or outcome_failure or outcome_null_dash" --co -q` collects 4 tests
- All 4 fail in RED state (template missing the rows)
</acceptance_criteria>
</task>

</tasks>

<verification>
- All 7 tasks complete in order
- `uv run pytest tests/queue tests/dashboard --co -q` collects all new tests without error
- `uv run pytest tests/queue tests/dashboard -q 2>&1 | tail -20` shows new tests as failing (RED), no syntax errors
- No source files in `src/robotina/` were modified by this plan (RED state is achieved purely via test additions)
- `git status` shows changes in `tests/` only
</verification>

<commit_message>
test(20-01): RED tests for wake rule + outcome plumbing contracts

Adds RED-state tests encoding every Phase 20 contract:
- WakeInvocationInput, WorkflowOutcomeSummary, FinalizeOutcomeInput shapes
- _check_and_dispatch_wake helper (happy/partial/idempotent/queue=None)
- run_task branching on invocation.trigger + status transitions
- finalize-outcome task type (success/failure composition)
- reconcile_invocations (orphan/healthy/USER_MESSAGE-skip)
- dashboard conversation_id row + outcome cell (success/failure/null)

Waves 1-3 flip these RED tests to GREEN.
</commit_message>
