---
phase: 10-langchain-1-x-agent-api-migration
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/decisions/agent-12-migrate-to-create-agent.md
autonomous: false
requirements:
  - AGENT-12
must_haves:
  truths:
    - "CLAUDE.md technology stack table reflects `langchain >=1.2`, `langchain-core >=1.2`, and `langgraph >=1.0` (demoted from the agent API surface)."
    - "CLAUDE.md 'Alternatives Considered' table has its `create_react_agent` row polarity inverted — `langchain.agents.create_agent` is now recommended, `create_react_agent` is the alternative."
    - "CLAUDE.md 'What NOT to Use' table contains a row for `langgraph.prebuilt.create_react_agent`."
    - "CLAUDE.md 'Sources' section reflects that LangChain 1.x exposes `create_agent` in `langchain.agents` (the old 'moved to langgraph.prebuilt' line is gone)."
    - "STATE.md line 128 reads as superseded — the new decision references AGENT-12 and the parity verification."
    - "PROJECT.md 'Key Decisions' table has a new row recording the migration with rationale linking to Phases 11 and 12."
    - "A new file `.planning/decisions/agent-12-migrate-to-create-agent.md` exists, mirroring the section layout of `switch-to-simple-worker.md`."
    - "After the human end-to-end checkpoint passes, AGENT-12 is marked `[x]` in REQUIREMENTS.md and 'In Progress' becomes 'Complete' in the traceability table."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated tech stack tables reflecting LangChain 1.x agent API"
      contains: "langchain.agents.create_agent"
    - path: ".planning/STATE.md"
      provides: "Updated Decisions log line for Phase 4 entry"
      contains: "AGENT-12"
    - path: ".planning/PROJECT.md"
      provides: "New Key Decisions row for AGENT-12"
      contains: "create_agent"
    - path: ".planning/decisions/agent-12-migrate-to-create-agent.md"
      provides: "Phase 10 decision record mirroring switch-to-simple-worker.md"
      contains: "# Decision: Migrate from"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AGENT-12 marked Complete after end-to-end verification"
      contains: "AGENT-12"
  key_links:
    - from: ".planning/decisions/agent-12-migrate-to-create-agent.md"
      to: "src/robotina/llm/__init__.py"
      via: "Files to change section"
      pattern: "src/robotina/llm/__init__.py"
    - from: "PROJECT.md Key Decisions row"
      to: "AGENT-12 in REQUIREMENTS.md"
      via: "decision identifier"
      pattern: "create_agent.*langchain.agents"
---

<objective>
Roll over documentation and decision records to reflect the completed migration, then perform the manual end-to-end add-recipe Telegram run that closes success criterion 4. This plan has one auto task (docs rollover) and one checkpoint task (human verification + AGENT-12 completion mark).

Purpose: Update CLAUDE.md, STATE.md, PROJECT.md, REQUIREMENTS.md, and create the new decision record so that future onboarding reads the correct stance ("use `create_agent`, never `create_react_agent`"). Then verify end-to-end that nothing semantic regressed — the add-recipe workflow runs to completion on a real Telegram message. After the human confirms, flip AGENT-12 from `In Progress` to `Complete`.
Output: 4 doc edits + 1 new decision file + 1 manual checkpoint + a finalized AGENT-12 status.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/solanoe/code/robotina-gsd/CLAUDE.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/decisions/switch-to-simple-worker.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-RESEARCH.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-PATTERNS.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-VALIDATION.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Update CLAUDE.md, STATE.md, PROJECT.md, and create the decision record file</name>
  <files>
    CLAUDE.md
    .planning/STATE.md
    .planning/PROJECT.md
    .planning/decisions/agent-12-migrate-to-create-agent.md
  </files>
  <read_first>
    - /home/solanoe/code/robotina-gsd/CLAUDE.md (entire file — find these table rows: in "## Recommended Stack › Core Technologies" the rows for LangChain and langgraph; in "## Alternatives Considered" the row starting with "LangGraph `create_react_agent`"; in "## What NOT to Use" the section header to add a new row beneath `AgentExecutor`; in "## Confidence Notes" the row "LangChain package split (core / langgraph)"; in "## Sources" the line at line 141 that begins with "- LangChain migration guide (training data, Aug 2025):")
    - /home/solanoe/code/robotina-gsd/.planning/STATE.md (line 128 — the AGENT-11/D-03 decision log entry)
    - /home/solanoe/code/robotina-gsd/.planning/PROJECT.md (lines 56-64 — the "## Key Decisions" table)
    - /home/solanoe/code/robotina-gsd/.planning/decisions/switch-to-simple-worker.md (entire file — analog for the new decision record format)
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-RESEARCH.md (lines 462-509 — "CLAUDE.md Update Diff Sketch" with verbatim BEFORE/AFTER for all 4 CLAUDE.md edits; lines 529-551 — "AGENT-11 / D-03 Decision Record Update")
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-PATTERNS.md (lines 585-779 — full CLAUDE.md edits + STATE.md edit + PROJECT.md row + the full content for the new decision record file)
  </read_first>
  <action>
**FILE 1: `CLAUDE.md`** — six table/line edits.

**Edit 1.1 — In the "## Recommended Stack › Core Technologies" table, find and rewrite the LangChain row:**

BEFORE:
```markdown
| LangChain | `langchain>=0.3`, `langchain-core>=0.3` | Agent orchestration | 0.3 stabilized the `create_react_agent` API from `langgraph` and removed most of the deprecated `AgentExecutor` path. The spec's `LLMBackend` Protocol wraps `BaseChatModel` which is `langchain-core` — keep `langchain` as thin as possible. |
```

AFTER:
```markdown
| LangChain | `langchain>=1.2`, `langchain-core>=1.2` | Agent orchestration via `langchain.agents.create_agent` | LangChain 1.x is the current major. `create_agent` (the agent factory) lives in `langchain.agents`, not `langgraph.prebuilt`. `LLMBackend.create_agent()` wraps this factory. |
```

**Edit 1.2 — In the same table, rewrite the langgraph row:**

BEFORE:
```markdown
| langgraph | `>=0.2` | `create_react_agent` implementation | As of LangChain 0.2+, `create_react_agent` lives in `langgraph` (not `langchain`). The spec's `create_agent()` method returns a LangGraph runnable. Required for the ReAct agent pattern. |
```

AFTER:
```markdown
| langgraph | `>=1.0` | Underlying graph engine for `create_agent` | `create_agent` is built on `langgraph` (`CompiledStateGraph`, `ToolNode`, `StateGraph`). Pinned as a direct dep to document the floor; not the agent API surface anymore. |
```

**Edit 1.3 — In the "## Alternatives Considered" table, find the row that begins with `LangGraph `create_react_agent`` and rewrite it (polarity flip):**

BEFORE:
```markdown
| LangGraph `create_react_agent` | LangChain `AgentExecutor` | Never — `AgentExecutor` is deprecated as of LangChain 0.2 and scheduled for removal. The spec's `LLMBackend.create_agent()` must use LangGraph's `create_react_agent`. |
```

AFTER:
```markdown
| LangChain `langchain.agents.create_agent` | LangGraph `create_react_agent` | Never — `create_react_agent` is deprecated in LangGraph V1.0 (emits `LangGraphDeprecatedSinceV10`, removal in V2.0). `LLMBackend.create_agent()` must use `langchain.agents.create_agent`. `AgentExecutor` is still forbidden (long-standing). |
```

**Edit 1.4 — In the "## What NOT to Use" table, add a NEW ROW beneath the existing `AgentExecutor (langchain legacy)` row.**

Find the row starting with `| `AgentExecutor` (langchain legacy) |`. Add this new row directly below it:
```markdown
| `langgraph.prebuilt.create_react_agent` | Deprecated in LangGraph V1.0, removal in V2.0. Emits `LangGraphDeprecatedSinceV10` at call time. | `langchain.agents.create_agent` |
```

**Edit 1.5 — In the "## Confidence Notes" table, rewrite the `LangChain package split` row:**

BEFORE:
```markdown
| LangChain package split (core / langgraph) | HIGH | Well-documented migration that happened LangChain 0.2 → 0.3. `create_react_agent` is in `langgraph.prebuilt`. |
```

AFTER:
```markdown
| LangChain package split (core / langgraph / agents) | HIGH | `langchain.agents.create_agent` is the current factory as of LangChain 1.x (verified empirically against installed `langchain 1.2.13`, 2026-05-12). `create_react_agent` is deprecated in `langgraph.prebuilt`. |
```

**Edit 1.6 — In the "## Sources" section, rewrite line 141 in place.**

Find the line that begins with `- LangChain migration guide (training data, Aug 2025):` and reads exactly:

BEFORE:
```markdown
- LangChain migration guide (training data, Aug 2025): `create_react_agent` moved to `langgraph.prebuilt` in LangChain 0.2+
```

AFTER:
```markdown
- LangChain 1.x agent API: `create_agent` lives in `langchain.agents` as of LangChain 1.x; `langgraph.prebuilt.create_react_agent` is the prior-generation API that this project migrated away from in Phase 10.
```

Rationale: leaving the old "moved TO langgraph.prebuilt" line in the Sources section contradicts Edits 1.1–1.5 above (which collectively reframe `create_react_agent` as superseded). Phase success criterion 5 requires CLAUDE.md to coherently reflect the new stance; this edit closes the last inconsistent reference.

---

**FILE 2: `.planning/STATE.md`** — rewrite line 128 in place.

BEFORE (line 128):
```
- [Phase 04-llm-module-and-agent-infrastructure]: Use create_react_agent from langgraph.prebuilt despite LangGraphDeprecatedSinceV10 warning — locked per AGENT-11/D-03, API remains functional in v1.1.3
```

AFTER:
```
- [Phase 04-llm-module-and-agent-infrastructure]: AGENT-11/D-03 superseded in Phase 10 by AGENT-12 — all agents now use `langchain.agents.create_agent` (LangGraph V1.0 deprecation; removal in V2.0). Behavior parity (return_direct, state shape, callbacks) verified during Phase 10.
```

---

**FILE 3: `.planning/PROJECT.md`** — add a new row to the "## Key Decisions" table.

Find the table starting at line 58 (`| Decision | Rationale | Outcome |`). The existing rows end at line 64. Append this new row to the table (insert directly after the line `| Skills use lazy loading (index pre-loaded, sub-files on demand) | Avoids context bloat for tasks that only need part of a skill | — Pending |`):

```markdown
| `create_agent` from `langchain.agents` is used for all agents | LangGraph deprecated `create_react_agent` (V1.0; removal in V2.0). The new factory is required to unlock `response_format` (Phase 11) and middleware (Phase 12). Behavior parity verified empirically during Phase 10. | — Active |
```

Confirm the pipe alignment matches the existing rows in the table (use 3 columns exactly).

---

**FILE 4: `.planning/decisions/agent-12-migrate-to-create-agent.md`** — CREATE this new file.

Mirror the section layout of `.planning/decisions/switch-to-simple-worker.md`. Use the following exact content (lifted from RESEARCH.md "AGENT-11 / D-03 Decision Record Update" and PATTERNS.md "Files Touched"):

```markdown
# Decision: Migrate from `langgraph.prebuilt.create_react_agent` to `langchain.agents.create_agent`

## Context

The `LLMBackend.create_agent()` method in `src/robotina/llm/__init__.py` previously
wrapped `create_react_agent` from `langgraph.prebuilt`. This API is deprecated in
LangGraph V1.0 — calling it emits `LangGraphDeprecatedSinceV10`, and the
deprecation decorator (`langgraph/prebuilt/chat_agent_executor.py:274-308`)
points to `langchain.agents.create_agent` as the replacement.

The repo's `uv.lock` already pinned `langchain 1.2.13` and `langchain-core 1.2.22`,
so the new factory was already available in the venv. AGENT-11/D-03 (Phase 4
decision) explicitly deferred the migration to a "future phase" — Phase 10 was
that phase.

Two near-term phases also depend on this:
- Phase 11 needs `response_format=...` on the factory (only on `create_agent`).
- Phase 12 needs `middleware=[...]` on the factory (only on `create_agent`).

## What `create_react_agent` actually buys us

- It works today. Strict behavior we rely on (`return_direct=True`,
  `{"messages": [...]}` state shape, strict-args→ToolMessage(status='error'),
  callback delivery via `RunnableConfig`) is intact in `langgraph 1.1.3`.
- It was already wired into 5 source files and 4 test files with parity tests.

## Why those benefits don't apply here

- `langchain.agents.create_agent` provides byte-for-byte parity for every
  behavior the project relies on. Verified empirically against `langchain 1.2.13`
  on 2026-05-12 (return_direct short-circuit, system_prompt SystemMessage
  prepending, strict-args ToolMessage, callbacks via RunnableConfig — all
  identical).
- Staying on `create_react_agent` blocks Phases 11 and 12 from accessing
  `response_format=` and `middleware=`, which are the planned mitigations for
  the canelones-de-choclo parse failure class.
- Eventual removal in LangGraph V2.0 forces the migration anyway.

## Proposed change

1. Replace the import in `src/robotina/llm/__init__.py` from
   `from langgraph.prebuilt import create_react_agent` to
   `from langchain.agents import create_agent as _create_agent`. The alias
   prevents self-recursion with the protocol method of the same name.
2. Switch the three adapter call sites (`OllamaBackend`, `AnthropicBackend`,
   `OpenAIBackend`) from `create_react_agent(model=..., tools=..., prompt=...)`
   to `_create_agent(model=..., tools=..., system_prompt=...)`.
3. Update the four test files that construct real agent graphs
   (`test_llm_backend.py`, `test_queue_tool.py`, `test_start_workflow_tool.py`,
   `test_household_manager_api_tool.py`) to import and call `create_agent`.
4. Rename three tests for grep-discoverability: `..._short_circuits_create_react_agent`
   → `..._short_circuits_create_agent` (×2);
   `test_create_react_agent_used_not_agent_executor` →
   `test_create_agent_used_not_agent_executor` and invert its source-grep
   assertions to lock the migration direction.
5. Sweep stale comments/docstrings in 5 source files and 2 experiment files
   that mention `create_react_agent` or `langgraph.prebuilt`.
6. Update CLAUDE.md, PROJECT.md (Key Decisions), REQUIREMENTS.md (add
   AGENT-12, mark AGENT-11 superseded), and STATE.md (rewrite line 128).

## Files to change

- `src/robotina/llm/__init__.py` — import + 3 adapter call sites + Protocol
  docstring
- `tests/unit/test_llm_backend.py` — patch target rename, source-grep test
  inversion, per-adapter test docstrings
- `tests/unit/test_queue_tool.py` — import + factory call + test rename +
  module docstring
- `tests/unit/test_start_workflow_tool.py` — import + factory call + test
  rename + module docstring
- `tests/unit/test_household_manager_api_tool.py` — import + factory call (no
  test rename — name is behavior-based)
- `src/robotina/queue/jobs.py`,
  `src/robotina/queue/workflow_runner.py`,
  `src/robotina/agent/tools/queue.py`,
  `src/robotina/agent/tools/start_workflow.py`,
  `tests/test_workflow_runner.py`,
  `experiments/recipe_research.py`,
  `experiments/recipe_load.py` — comment/docstring sweep
- `CLAUDE.md` — 4 table-row edits (Core Technologies, Alternatives Considered,
  What NOT to Use, Confidence Notes)
- `.planning/REQUIREMENTS.md` — add AGENT-12, mark AGENT-11 superseded, add
  AGENT-12 to traceability
- `.planning/STATE.md` — rewrite line 128 decision log entry
- `.planning/PROJECT.md` — add row to Key Decisions table

## Risk

Low. The migration is a 1:1 API rename with empirically verified behavior parity.
Total functional diff is ~18 LOC plus ~20 LOC of doc/comment hygiene. Rollback
is a single `git revert` per affected commit; no data, no env vars, no lockfile
bumps. The only interaction-level risk is LangWatch trace delivery under the
new factory — covered by the end-to-end add-recipe checkpoint in Plan 03.
```
  </action>
  <acceptance_criteria>
1. CLAUDE.md edits landed — each must appear EXACTLY ONCE:
   `grep -c "langchain>=1.2" CLAUDE.md` must output ≥ `1`
   `grep -c "langgraph | \`>=1.0\`" CLAUDE.md` must output ≥ `1` (the `>=1.0` cell)
   `grep -c "LangChain \`langchain.agents.create_agent\` | LangGraph \`create_react_agent\`" CLAUDE.md` must output `1`
   `grep -c "\`langgraph.prebuilt.create_react_agent\` | Deprecated in LangGraph V1.0" CLAUDE.md` must output `1`
   `grep -c "LangChain package split (core / langgraph / agents)" CLAUDE.md` must output `1`

2. CLAUDE.md "Sources" section line 141 rewritten (Edit 1.6):
   `grep -c "create_agent\` lives in \`langchain.agents\`" CLAUDE.md` must output `1` (presence — new wording landed)
   `grep -c "create_react_agent\` moved to \`langgraph.prebuilt" CLAUDE.md` must output `0` (absence — old wording gone)

3. STATE.md line 128 rewritten:
   `grep -c "AGENT-11/D-03 superseded in Phase 10 by AGENT-12" .planning/STATE.md` must output `1`
   `grep -c "Use create_react_agent from langgraph.prebuilt despite LangGraphDeprecatedSinceV10" .planning/STATE.md` must output `0`

4. PROJECT.md Key Decisions row added:
   `grep -c "\`create_agent\` from \`langchain.agents\` is used for all agents" .planning/PROJECT.md` must output `1`

5. Decision-record file created with expected sections:
   `test -f .planning/decisions/agent-12-migrate-to-create-agent.md` exits 0
   `grep -c "^# Decision: Migrate from " .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## Context" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## What \`create_react_agent\` actually buys us" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## Why those benefits don't apply here" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## Proposed change" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## Files to change" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`
   `grep -c "^## Risk" .planning/decisions/agent-12-migrate-to-create-agent.md` must output `1`

6. The full test suite is still green after the docs rollover (nothing accidentally edited in code):
   `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && grep -q "AGENT-11/D-03 superseded in Phase 10 by AGENT-12" .planning/STATE.md && grep -q "langchain>=1.2" CLAUDE.md && grep -q "langgraph.prebuilt.create_react_agent" CLAUDE.md && grep -q "create_agent\` lives in \`langchain.agents\`" CLAUDE.md && [ "$(grep -c "create_react_agent\` moved to \`langgraph.prebuilt" CLAUDE.md)" = "0" ] && test -f .planning/decisions/agent-12-migrate-to-create-agent.md && grep -q "^# Decision: Migrate from" .planning/decisions/agent-12-migrate-to-create-agent.md && echo OK</automated>
  </verify>
  <done>
CLAUDE.md tech-stack tables AND its Sources section reflect LangChain 1.x as the agent API surface (Edits 1.1–1.6), STATE.md records AGENT-11/D-03 supersession, PROJECT.md gains a Key Decisions row, and a new agent-12-migrate-to-create-agent.md decision record exists mirroring the switch-to-simple-worker.md format. The full test suite is still green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.2: Manual end-to-end add-recipe Telegram run (Success Criterion 4)</name>
  <what-built>
The full Phase 10 migration is complete:
- All three LLMBackend adapters use `langchain.agents.create_agent` (Plan 02 Task 2.1).
- The four real-agent test files use the new factory and the parity tests confirm `return_direct=True` semantics (Plan 02 Task 2.2).
- Doc/comment sweep across 7 secondary files is done (Plan 02 Task 2.3).
- CLAUDE.md, STATE.md, PROJECT.md, REQUIREMENTS.md, and the new decision record are updated (Plan 03 Task 3.1).
- `uv run pytest -q` is green.

What remains: confirm at run-time, end-to-end, that the `add-recipe` workflow still works on a real Telegram message — the production-side parity check that the unit tests can only approximate. This closes Success Criterion 4 and locks down Assumption A1 from RESEARCH.md (LangWatch trace delivery under the new factory).
  </what-built>
  <how-to-verify>
1. **Start the local stack:**
   ```bash
   docker compose up postgres redis -d
   ```
   Wait for "ready to accept connections" from postgres.

2. **In a separate terminal, start the gateway and the worker:**
   ```bash
   uv run gateway   # if a separate gateway entrypoint exists; otherwise skip
   uv run agent     # task runner
   ```
   Confirm both processes show no startup errors. In particular:
   - The agent process must NOT emit `LangGraphDeprecatedSinceV10` warnings (those would indicate stale create_react_agent calls).
   - LangWatch setup line should appear in stdout.

3. **From your Telegram client connected to the bot:** send the message:
   ```
   agrega receta de tortilla de papa
   ```
   (Or another recipe name of your choice — pick something the bot has not loaded before.)

4. **Observe in the worker logs:**
   - `acknowledge-add-recipe` agent runs first → emits a "trabajando en eso" type ack.
   - `recipe-research` sub-agents run sequentially (gather → instructions → ingredients → metadata).
   - `recipe-load` agent resolves ingredient names and creates the recipe via the household-manager API.
   - `send-notification` agent delivers a final confirmation back to your Telegram.

5. **Confirm in Telegram:**
   - You received the initial ack.
   - You received the final recipe confirmation (with a link or description).
   - No dead-letter notification arrived.

6. **Confirm in LangWatch UI:**
   - Open the LangWatch project this stack is configured against.
   - Find the trace for this run.
   - Confirm the trace contains spans for each agent invocation (research sub-agents + load + ack + notify).
   - Confirm token usage / model name are populated where available.

**Pass criteria:**
- All three workflow steps complete (research → load → notify).
- The user receives the final confirmation message.
- A LangWatch trace appears in the correct collection.
- No `LangGraphDeprecatedSinceV10` warnings in worker logs.

**Fail criteria — STOP and report:**
- Any agent step fails or times out.
- The user does not receive a confirmation.
- LangWatch trace is missing or empty.
- `LangGraphDeprecatedSinceV10` appears anywhere in the worker logs (indicates a missed call site).
  </how-to-verify>
  <resume-signal>
Type one of:
- **"approved"** — the end-to-end run succeeded, LangWatch trace appeared, no deprecation warning. The executor will then mark AGENT-12 complete in REQUIREMENTS.md (see post-checkpoint action below).
- **"failed: <symptom>"** — describe what went wrong. The executor will surface to the user and stop the phase; remediation will be planned via `/gsd-plan-phase 10 --gaps`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3.3 (post-checkpoint): Mark AGENT-12 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /home/solanoe/code/robotina-gsd/.planning/REQUIREMENTS.md (find the AGENT-12 entry and the AGENT-12 traceability row added by Plan 01)
  </read_first>
  <action>
**Run ONLY AFTER the human approves Task 3.2** (the manual end-to-end checkpoint).

Make two final edits to `.planning/REQUIREMENTS.md`:

**Edit 1 — Flip the AGENT-12 checkbox from `[ ]` to `[x]`:**

BEFORE:
```
- [ ] **AGENT-12**: All agents use `create_agent` from `langchain.agents` (supersedes AGENT-11)...
```

AFTER:
```
- [x] **AGENT-12**: All agents use `create_agent` from `langchain.agents` (supersedes AGENT-11)...
```

(Leave the rest of the bullet content unchanged.)

**Edit 2 — Update the AGENT-12 traceability row:**

BEFORE:
```
| AGENT-12 | Phase 10 | In Progress |
```

AFTER:
```
| AGENT-12 | Phase 10 | Complete |
```

**Edit 3 — Update the file's footer last-updated marker:**

BEFORE (last line of file):
```
*Last updated: 2026-03-25 after roadmap creation — traceability updated with individual requirement-to-phase mappings*
```

AFTER:
```
*Last updated: 2026-05-12 after Phase 10 — added AGENT-12 (LangChain 1.x agent API migration), marked AGENT-11 superseded*
```

(Use the actual phase completion date if different from 2026-05-12.)
  </action>
  <acceptance_criteria>
1. AGENT-12 is checked:
   `grep -c "^- \[x\] \*\*AGENT-12\*\*" .planning/REQUIREMENTS.md` must output `1`
   `grep -c "^- \[ \] \*\*AGENT-12\*\*" .planning/REQUIREMENTS.md` must output `0`

2. Traceability row marks Complete:
   `grep -cE "^\| AGENT-12 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md` must output `1`
   `grep -cE "^\| AGENT-12 \| Phase 10 \| In Progress \|" .planning/REQUIREMENTS.md` must output `0`

3. Footer marker updated:
   `grep -c "after Phase 10 — added AGENT-12" .planning/REQUIREMENTS.md` must output `1`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^- \[x\] \*\*AGENT-12\*\*" .planning/REQUIREMENTS.md && grep -qE "^\| AGENT-12 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
AGENT-12 is marked complete in both the agent infrastructure section and the traceability table. The footer reflects the Phase 10 completion.
  </done>
</task>

</tasks>

<verification>
1. CLAUDE.md tech-stack tables updated (5 grep gates from Task 3.1 acceptance criterion 1).
2. CLAUDE.md "Sources" section line 141 rewritten (2 grep gates — presence + absence — from Task 3.1 acceptance criterion 2).
3. STATE.md line 128 rewritten (1 grep gate).
4. PROJECT.md Key Decisions table has the new row (1 grep gate).
5. `.planning/decisions/agent-12-migrate-to-create-agent.md` exists with all 6 expected sections (7 grep gates).
6. The full `uv run pytest -q` is still green after docs rollover.
7. Human approval of the end-to-end Telegram add-recipe run (gating Task 3.3).
8. AGENT-12 marked complete in REQUIREMENTS.md (3 grep gates).
</verification>

<success_criteria>
- CLAUDE.md technology stack table reflects `langchain >=1.2`, `langchain-core >=1.2`, `langgraph >=1.0`. The "What NOT to Use" table contains a row for `langgraph.prebuilt.create_react_agent`. The "Alternatives Considered" table polarity is flipped. The "Sources" section line 141 reframes `create_agent` as the current LangChain 1.x agent API (no residual "moved TO langgraph.prebuilt" wording). (Success criterion 5 in the phase brief.)
- The AGENT-11/D-03 decision record is updated (STATE.md line 128) and a new `.planning/decisions/agent-12-migrate-to-create-agent.md` exists (success criterion 1 in the phase brief).
- PROJECT.md "Key Decisions" gains a row recording the migration.
- The end-to-end `add-recipe` workflow runs to completion on at least one real Telegram message with no semantic regression and no deprecation warnings (success criterion 4 in the phase brief).
- AGENT-12 is marked `[x]` and `Complete` in REQUIREMENTS.md.
</success_criteria>

<output>
After completion, create `.planning/phases/10-langchain-1-x-agent-api-migration/10-03-SUMMARY.md` documenting:
- The 4 doc edits + 1 new decision file from Task 3.1
- The human verification result from Task 3.2 (approved / failed:<symptom>)
- The AGENT-12 completion mark from Task 3.3
- The Telegram message used in the end-to-end run + the LangWatch trace URL (if available)
- Final phase verification: full `uv run pytest -q` green AND no `create_react_agent`/`langgraph.prebuilt` references in src/ tests/ experiments/ AND no `LangGraphDeprecatedSinceV10` warnings in worker logs
</output>
