---
phase: 10-langchain-1-x-agent-api-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/unit/test_llm_backend.py
autonomous: true
requirements:
  - AGENT-12
must_haves:
  truths:
    - "AGENT-12 exists as an active requirement entry under '### Agent Infrastructure' in REQUIREMENTS.md, and AGENT-11 carries a supersession note."
    - "AGENT-12 appears in the REQUIREMENTS.md traceability table mapped to Phase 10."
    - "test_llm_backend.py has the renamed test `test_create_agent_used_not_agent_executor` whose assertions REQUIRE the new import and FORBID the deprecated one."
    - "Running the renamed source-grep test against the as-yet-unchanged src/robotina/llm/__init__.py FAILS — proving the lock is real (this failure is expected at the end of Plan 01; Plan 02 resolves it)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "AGENT-12 requirement entry, AGENT-11 supersession note, traceability row"
      contains: "AGENT-12"
    - path: "tests/unit/test_llm_backend.py"
      provides: "Renamed source-grep test with inverted assertions"
      contains: "def test_create_agent_used_not_agent_executor"
  key_links:
    - from: "tests/unit/test_llm_backend.py::test_create_agent_used_not_agent_executor"
      to: "src/robotina/llm/__init__.py"
      via: "inspect.getfile(llm_module) + source string grep"
      pattern: "from langchain.agents import create_agent"
---

<objective>
Add AGENT-12 to REQUIREMENTS.md (mark AGENT-11 superseded) and rename + invert the source-grep test in test_llm_backend.py so it locks the migration direction. This plan establishes the contract (the new requirement) and the lock-test (the assertion that future commits cannot regress the migration).

Purpose: AGENT-12 must exist as a requirement before any plan can reference it in frontmatter. The source-grep test must be inverted FIRST so that Plan 02's source change is verifiable end-to-end — the test must fail at the end of this plan (proving it locks the direction) and pass after Plan 02 (proving the source migration landed).
Output: Updated REQUIREMENTS.md (AGENT-12 added, AGENT-11 superseded note, traceability row), renamed source-grep test with inverted assertions in test_llm_backend.py.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/solanoe/code/robotina-gsd/CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-RESEARCH.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-PATTERNS.md
@.planning/phases/10-langchain-1-x-agent-api-migration/10-VALIDATION.md
@tests/unit/test_llm_backend.py

<interfaces>
<!-- The source-grep test reads robotina.llm's module source via inspect.getfile()
     and asserts string presence/absence. The new assertions must match the import
     statement that Plan 02 will introduce: `from langchain.agents import create_agent as _create_agent`.

     Existing test (lines 107-118 of tests/unit/test_llm_backend.py):

     def test_create_react_agent_used_not_agent_executor():
         """AGENT-11: create_react_agent from langgraph.prebuilt is used, not AgentExecutor."""
         import robotina.llm as llm_module
         import inspect

         source_path = inspect.getfile(llm_module)
         with open(source_path) as f:
             source = f.read()

         assert "AgentExecutor" not in source, "AgentExecutor must not be used in robotina.llm"
         assert "create_react_agent" in source, "create_react_agent must be imported from langgraph.prebuilt"
         assert "from langgraph.prebuilt import create_react_agent" in source
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Add AGENT-12 to REQUIREMENTS.md and mark AGENT-11 superseded</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - /home/solanoe/code/robotina-gsd/.planning/REQUIREMENTS.md (entire file — locate AGENT-11 entry at line 62, the AGENT-11 row in the Traceability table around line 172, and the "v1 requirements: 68 total" coverage summary around line 212)
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-RESEARCH.md (section "## AGENT-12 Requirement Candidate (for REQUIREMENTS.md)" lines 511-527 — VERBATIM text to add)
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-PATTERNS.md (lines 601-624 — exact diff including "BEFORE / AFTER" form)
  </read_first>
  <action>
Make three edits to `.planning/REQUIREMENTS.md`:

**Edit 1 — Replace the existing AGENT-11 line (line 62) IN PLACE:**

Find this line (line 62):
```
- [x] **AGENT-11**: `create_react_agent` from `langgraph.prebuilt` is used for all agents
```

Replace it with:
```
- [x] **AGENT-11**: `create_react_agent` from `langgraph.prebuilt` is used for all agents *(superseded by AGENT-12 in Phase 10)*
```

**Edit 2 — Insert a new AGENT-12 bullet immediately after the (now-modified) AGENT-11 line:**

Insert this exact line directly after AGENT-11 (verbatim from RESEARCH.md lines 515-517):
```
- [ ] **AGENT-12**: All agents use `create_agent` from `langchain.agents` (supersedes AGENT-11). The deprecated `langgraph.prebuilt.create_react_agent` is no longer imported anywhere in `src/` or `tests/`. The three `LLMBackend` adapters (Ollama, Anthropic, OpenAI) call `langchain.agents.create_agent(model=…, tools=…, system_prompt=…)` with strict behavior parity — `return_direct=True` short-circuit, message state shape, callback delivery, and strict-args→ToolMessage(status='error') flow are all preserved.
```

Leave the checkbox unchecked (`- [ ]`) for now — Plan 03 marks it complete after the manual end-to-end verification passes.

**Edit 3 — Add AGENT-12 to the Traceability table immediately after the AGENT-11 row (around line 172):**

Find the AGENT-11 traceability row:
```
| AGENT-11 | Phase 4 | Complete |
```

Insert this row directly below it:
```
| AGENT-12 | Phase 10 | In Progress |
```

(Plan 03 will flip "In Progress" to "Complete" once the manual end-to-end run passes.)

**Edit 4 — Update the coverage summary at the bottom of the file (around line 212).**

Find:
```
**Coverage:**
- v1 requirements: 68 total
- Mapped to phases: 68
- Unmapped: 0 ✓
```

Replace with:
```
**Coverage:**
- v1 requirements: 69 total
- Mapped to phases: 69
- Unmapped: 0 ✓
```

Do NOT touch the `*Last updated:* 2026-03-25` footer line — that's a historical marker and Plan 03 will update it as part of the docs rollover.
  </action>
  <acceptance_criteria>
Run these grep commands; each must produce the exact line count shown.

1. AGENT-12 entry present:
   `grep -c "^- \[ \] \*\*AGENT-12\*\*: All agents use \`create_agent\` from \`langchain.agents\`" .planning/REQUIREMENTS.md` must output `1`

2. AGENT-11 supersession marker present:
   `grep -c "AGENT-11.*superseded by AGENT-12 in Phase 10" .planning/REQUIREMENTS.md` must output `1`

3. AGENT-12 traceability row present:
   `grep -cE "^\| AGENT-12 \| Phase 10 \| In Progress \|" .planning/REQUIREMENTS.md` must output `1`

4. Coverage count updated:
   `grep -c "v1 requirements: 69 total" .planning/REQUIREMENTS.md` must output `1`
   AND `grep -c "v1 requirements: 68 total" .planning/REQUIREMENTS.md` must output `0`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "AGENT-12" .planning/REQUIREMENTS.md | grep -qE '^[3-9]$|^[1-9][0-9]+$' && grep -q "superseded by AGENT-12 in Phase 10" .planning/REQUIREMENTS.md && grep -q "v1 requirements: 69 total" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
REQUIREMENTS.md contains an AGENT-12 entry (unchecked), an AGENT-11 supersession note, an AGENT-12 traceability row marked "In Progress", and the coverage counter shows 69 total v1 requirements.
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Rename source-grep test and invert assertions to lock the migration direction</name>
  <files>tests/unit/test_llm_backend.py</files>
  <read_first>
    - /home/solanoe/code/robotina-gsd/tests/unit/test_llm_backend.py (entire file — locate lines 107-118 for `test_create_react_agent_used_not_agent_executor`)
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-RESEARCH.md (lines 181-191 — BEFORE / AFTER for the source-grep test)
    - /home/solanoe/code/robotina-gsd/.planning/phases/10-langchain-1-x-agent-api-migration/10-PATTERNS.md (lines 287-324 — exact BEFORE / AFTER block)
  </read_first>
  <action>
Edit `tests/unit/test_llm_backend.py` and rewrite lines 107-118 IN PLACE.

**BEFORE (current lines 107-118):**
```python
def test_create_react_agent_used_not_agent_executor():
    """AGENT-11: create_react_agent from langgraph.prebuilt is used, not AgentExecutor."""
    import robotina.llm as llm_module
    import inspect

    source_path = inspect.getfile(llm_module)
    with open(source_path) as f:
        source = f.read()

    assert "AgentExecutor" not in source, "AgentExecutor must not be used in robotina.llm"
    assert "create_react_agent" in source, "create_react_agent must be imported from langgraph.prebuilt"
    assert "from langgraph.prebuilt import create_react_agent" in source
```

**AFTER (paste verbatim — note: function rename, docstring rewrite, three assertion inversions, one new negative assertion):**
```python
def test_create_agent_used_not_agent_executor():
    """AGENT-12: create_agent from langchain.agents is used, not AgentExecutor or the deprecated create_react_agent."""
    import robotina.llm as llm_module
    import inspect

    source_path = inspect.getfile(llm_module)
    with open(source_path) as f:
        source = f.read()

    assert "AgentExecutor" not in source, "AgentExecutor must not be used in robotina.llm"
    assert "from langchain.agents import create_agent" in source, (
        "robotina.llm must import create_agent from langchain.agents"
    )
    assert "create_react_agent" not in source, (
        "robotina.llm must not reference the deprecated create_react_agent"
    )
    assert "from langgraph.prebuilt" not in source, (
        "robotina.llm must not import from the deprecated langgraph.prebuilt module"
    )
```

DO NOT touch any other test in this file in this task. The per-adapter test docstrings (lines 17, 35, 58) and patch targets (lines 24, 44, 67) are updated in Plan 02 alongside the source change — they would fail this whole plan if updated here.

After the edit, the renamed test will FAIL when run against the current (unchanged) `src/robotina/llm/__init__.py` because the source still contains `from langgraph.prebuilt import create_react_agent`. THIS FAILURE IS EXPECTED AT THE END OF PLAN 01 — it proves the assertion truly locks the migration direction. Plan 02 will resolve it by editing the source.
  </action>
  <acceptance_criteria>
1. Renamed test exists and old name is gone:
   `grep -c "^def test_create_agent_used_not_agent_executor" tests/unit/test_llm_backend.py` must output `1`
   AND `grep -c "^def test_create_react_agent_used_not_agent_executor" tests/unit/test_llm_backend.py` must output `0`

2. All four assertions present (the new positive-import assertion + the two new negative assertions + the unchanged AgentExecutor assertion):
   `grep -c '"AgentExecutor" not in source' tests/unit/test_llm_backend.py` must output `1`
   `grep -c '"from langchain.agents import create_agent" in source' tests/unit/test_llm_backend.py` must output `1`
   `grep -c '"create_react_agent" not in source' tests/unit/test_llm_backend.py` must output `1`
   `grep -c '"from langgraph.prebuilt" not in source' tests/unit/test_llm_backend.py` must output `1`

3. The renamed test is collectible by pytest (syntax valid):
   `uv run pytest tests/unit/test_llm_backend.py::test_create_agent_used_not_agent_executor --collect-only -q` must exit 0 and list the test.

4. EXPECTED-FAILURE GATE (proves the lock is real): Running the renamed test against unchanged source MUST fail with an AssertionError mentioning either "create_react_agent must not reference" or "from langgraph.prebuilt must not import". Verify with:
   `uv run pytest tests/unit/test_llm_backend.py::test_create_agent_used_not_agent_executor -x 2>&1 | grep -E "AssertionError|FAILED"` must produce at least one match.

   Note for the executor: A FAILURE on this exact test, on this exact plan boundary, is the success signal — do NOT "fix" the source in this plan. Plan 02 owns that change. Record the expected failure in the plan SUMMARY.md.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_llm_backend.py::test_create_agent_used_not_agent_executor --collect-only -q 2>&1 | grep -q "test_create_agent_used_not_agent_executor" && grep -q '"create_react_agent" not in source' tests/unit/test_llm_backend.py && ! grep -q "def test_create_react_agent_used_not_agent_executor" tests/unit/test_llm_backend.py && echo OK</automated>
  </verify>
  <done>
Source-grep test is renamed to `test_create_agent_used_not_agent_executor`, assertions are inverted to lock the new import direction, and the test fails against current (unchanged) source as expected — confirming the lock is real.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "AGENT-12" .planning/REQUIREMENTS.md` produces a count ≥ 3 (entry + supersession note in AGENT-11 + traceability row).
2. `grep "v1 requirements: 69 total" .planning/REQUIREMENTS.md` matches.
3. `grep "^def test_create_agent_used_not_agent_executor" tests/unit/test_llm_backend.py` matches.
4. The renamed test is collectible (`uv run pytest tests/unit/test_llm_backend.py::test_create_agent_used_not_agent_executor --collect-only` succeeds).
5. The renamed test FAILS when executed (expected — the source hasn't been migrated yet; this proves the lock is real). Document this in SUMMARY.md.
6. No other tests in test_llm_backend.py have been modified (`grep -n "robotina.llm.create_react_agent" tests/unit/test_llm_backend.py` still shows three occurrences at lines 24, 44, 67 — Plan 02 fixes those).
</verification>

<success_criteria>
- AGENT-12 exists as an active requirement in REQUIREMENTS.md with the verbatim text from RESEARCH.md.
- AGENT-11 is marked superseded.
- AGENT-12 appears in the traceability table mapped to Phase 10 (status: In Progress).
- The source-grep test in test_llm_backend.py is renamed and has the four assertions in place.
- The renamed test demonstrably FAILS against the unchanged source (proving the lock is real and verifying that Plan 02's source change is necessary to make it pass).
</success_criteria>

<output>
After completion, create `.planning/phases/10-langchain-1-x-agent-api-migration/10-01-SUMMARY.md` documenting:
- The two edits made (REQUIREMENTS.md + test_llm_backend.py)
- The expected failure of `test_create_agent_used_not_agent_executor` at this plan boundary
- A note that Plan 02 is the next plan and will resolve the expected failure
</output>
