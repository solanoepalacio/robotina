---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - src/robotina/agent/prompts/recipe-research-metadata/V003.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/recipe-research-metadata/V003.md exists with the six skeleton sections"
    - "V003.md ## Output defers to RecipeResearchMetadataOutput in src/robotina/queue/task_types.py"
    - "V003.md preserves V002 behavior: estimates prep_time, cook_time, total_time (never null); servings_qty defaults to 4 when missing; servings_unit is always 'porciones'; final RecipeData re-emits instructions+ingredients faithfully without invention"
    - "AGENT_REGISTRY['recipe-research-metadata'].prompt_path == '...recipe-research-metadata/V003.md'"
    - "No overrides/*.json carries a prompt_path key for recipe-research-metadata"
    - "uv run pytest is green"
---

<objective>
Bump `recipe-research-metadata` from V002 to V003 adopting the standardized skeleton. Zero behavioral change. `## Output` defers to `RecipeResearchMetadataOutput` (Pydantic).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/recipe-research-metadata/V002.md
@src/robotina/agent/agents.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Write recipe-research-metadata/V003.md</name>
  <files>src/robotina/agent/prompts/recipe-research-metadata/V003.md</files>
  <read_first>
    src/robotina/agent/prompts/recipe-research-metadata/V002.md
    src/robotina/queue/task_types.py
  </read_first>
  <behavior>
    `# Recipe Research — Metadata — V003`

    `## Role` — one paragraph: "You are the metadata step of the recipe-research pipeline. You combine the draft instructions, the verified ingredients, and the per-source recipes from the gather step into a final, fully-populated recipe with estimated times and servings."

    `## Inputs` — bullet list:
    - The draft instructions emitted by the instructions step (name, description, step list).
    - The verified ingredients emitted by the ingredients step.
    - The per-source recipes from the gather step (used to inform time and serving estimates).

    `## Tools` — one line: "This agent uses no tools — produce the result directly from the inputs in the user message."

    `## Process` — numbered steps:
    1. Read the metadata of every per-source recipe from the gather step (times, servings) plus the draft instructions and verified ingredients.
    2. Estimate `prep_time` in minutes (based on ingredient count and complexity), `cook_time` in minutes (based on instruction techniques and temperatures), and `total_time = prep_time + cook_time`.
    3. Estimate `servings_qty` as an integer. If the per-source recipes do not specify a value, default to `4`. Set `servings_unit` to `"porciones"`.
    4. Assemble the final `RecipeData`: re-emit instructions and ingredients faithfully from the prior step outputs (do NOT invent new steps or ingredients here) and combine with your metadata estimates. `source_url` is the primary source page from the gather step.

    `## Rules` — bullet list:
    - `prep_time`, `cook_time`, `total_time` must NEVER be null — always estimate (use instruction complexity as a guide; e.g., pasta with 10 ingredients ≈ 30 min cook).
    - `servings_unit` is always `"porciones"`.
    - When the source recipes do not agree on servings, default `servings_qty` to `4`.
    - All user-facing content (recipe name, description, step bodies, ingredient names) is in Argentine / Latin American Spanish. Only this instructional prompt stays in English.
    - Do NOT invent new ingredients or steps in this stage — only re-emit prior outputs.

    `## Output` — short paragraph: "Return data shaped by `RecipeResearchMetadataOutput` in `src/robotina/queue/task_types.py`. Populate `recipe` as a fully-populated `RecipeData`. Field types, required/optional status, and `RecipeData` shape live on the Pydantic model — refer to it; do not re-describe fields here."
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - All six section headers appear exactly once.
    - `grep -c 'RecipeResearchMetadataOutput' ...V003.md` returns 1.
    - `grep -c 'porciones' ...V003.md` returns at least 2 (Rules + Process).
    - `grep -c 'Argentine / Latin American Spanish' ...V003.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/recipe-research-metadata/V003.md && grep -q 'RecipeResearchMetadataOutput' src/robotina/agent/prompts/recipe-research-metadata/V003.md</automated>
  </verify>
  <done>V003.md exists, defers schema to Pydantic class.</done>
</task>

<task type="auto">
  <name>Task 6.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['recipe-research-metadata'].prompt_path` from `V002.md` to `V003.md`. Strip any stale `prompt_path` keys.
  </behavior>
  <action>
    1. Edit `agents.py` for `recipe-research-metadata`: `V002.md` → `V003.md`.
    2. Review overrides; delete `prompt_path` keys if present.
    3. `uv run pytest` green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/recipe-research-metadata/V003.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-06): bump recipe-research-metadata V002 -> V003 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'recipe-research-metadata/V003.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'recipe-research-metadata/V002.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `recipe-research-metadata`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['recipe-research-metadata'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V003.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `recipe-research-metadata/V003.md` follows the standardized skeleton.
2. Registry points at V003.md.
3. Atomic commit includes prompt + registry + overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>Create `14-06-SUMMARY.md` documenting any deviations and confirming behavioral parity.</output>
