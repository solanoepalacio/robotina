---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - src/robotina/agent/prompts/recipe-load/V004.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/recipe-load/V004.md exists with the six skeleton sections"
    - "V004.md ## Output defers to RecipeLoadOutput in src/robotina/queue/task_types.py"
    - "V004.md preserves V003 behavior: snake_case→camelCase mapping table; resolve food_name via GET /api/foods?name=; resolve unit_name via GET /api/units?name=; never invent foodId/unitId; only fill output fields from a 2xx POST /api/recipes response; missing_ingredients lists dropped names; retry POST on non-2xx"
    - "V004.md preserves the 'do not hallucinate recipe_id' guardrail prose (single occurrence in ## Rules)"
    - "AGENT_REGISTRY['recipe-load'].prompt_path == '...recipe-load/V004.md'"
    - "No overrides/*.json carries a prompt_path key for recipe-load (note: anthropic.json has no recipe-load entry at all — left untouched, which is correct per CONTEXT.md scope: prompt+registry+overrides only; cross-override coverage is out of scope)"
    - "uv run pytest is green"
---

<objective>
Bump `recipe-load` from V003 to V004 adopting the standardized skeleton. Zero behavioral change. `## Output` defers to `RecipeLoadOutput` (Pydantic). Critically, preserve the hallucination guardrail — V003's "the only failure mode that matters" prose — but consolidate it into the standardized `## Rules` section.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/recipe-load/V003.md
@src/robotina/agent/agents.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 7.1: Write recipe-load/V004.md</name>
  <files>src/robotina/agent/prompts/recipe-load/V004.md</files>
  <read_first>
    src/robotina/agent/prompts/recipe-load/V003.md
    src/robotina/queue/task_types.py
  </read_first>
  <behavior>
    `# Recipe Load — V004`

    `## Role` — one paragraph: "You take a fully-formed `RecipeData` and create the recipe in household-manager by calling `POST /api/recipes`. Your output is a receipt of what the API returned — never something you make up."

    `## Inputs` — bullet list:
    - A `RecipeData` (in the user message) with `name`, `description`, `ingredients[]` (each with `food_name`, `unit_name`, `quantity`, `note`), `steps[]`, `servings_qty`, `servings_unit`, `prep_time`, `cook_time`, `total_time`, `source_url`.

    `## Tools` — bullet list:
    - `household-manager-api` — call the household-manager REST API. Parameters: `method`, `path`, `body` (object or null), `query` (object or null).
    - `read-skill` — read internal documentation files. The two relevant ones for this agent are `household-manager/recipes_create.md` (schema and field rules for `POST /api/recipes`) and `household-manager/shared.md` (base URL conventions and error semantics). Read both before your first API call.

    `## Process` — numbered steps:
    1. Read the two skill files via `read-skill` before any API call. They are the contract.
    2. Apply the input→API field-name map below to translate the recipe payload's snake_case keys into the API's camelCase keys. Names not in the table (`name`, `description`, `ingredients`, `steps`) stay unchanged.

       | Input             | API           |
       |-------------------|---------------|
       | `servings_qty`    | `servingsQty` |
       | `servings_unit`   | `servingsUnit`|
       | `prep_time`       | `prepTime`    |
       | `cook_time`       | `cookTime`    |
       | `total_time`      | `totalTime`   |
       | `source_url`      | `sourceUrl`   |
    3. For every distinct `food_name`, call `household-manager-api(GET, /api/foods?name={food_name})`. Resolution rules: exactly one result → use it; several results with a case-insensitive exact name match → use the exact match; several results with no exact match → choose the most plausible for the ingredient context; zero results → the ingredient cannot be added — drop it from the request body and remember its `food_name` for `missing_ingredients`.
    4. For each ingredient that has a non-null/non-empty `unit_name`, call `household-manager-api(GET, /api/units?name={unit_name})` and apply the same rules. Zero results → omit `unitId` from that ingredient (still include the ingredient, just without a unit).
    5. Build the `POST /api/recipes` body: top-level recipe metadata (with the field renames above); `ingredients[]` containing only ingredients whose `foodId` resolved successfully — each item has `foodId` (required), `unitId` (only if resolved), `quantity` (from input), `note` (from input); `steps[]` passed through from input.
    6. Call `household-manager-api(POST, /api/recipes, body=<the object above>)` and wait for the response.
    7. After a 2xx response, copy fields from the response body verbatim into the output: `recipe_id` ← `id`; `recipe_name` ← `name`; `recipe_description` ← `description` (or `null`); `recipe_slug` ← `slug` (or `""`); `missing_ingredients` ← the list of `food_name` values you dropped in step 3 (or `[]`).
    8. If `POST /api/recipes` returns a non-2xx status, read the error body, fix the most common cause (a field-rename slip or an unresolved `foodId` that snuck through), and retry. Do not give up after a single failure. Do not emit output until the POST succeeds.

    `## Rules` — bullet list:
    - All user-facing recipe content (name, description, step bodies, ingredient names) is in Argentine / Latin American Spanish. Only this instructional prompt stays in English.
    - The only legitimate source of `recipe_id`, `recipe_name`, `recipe_slug`, `recipe_description` is a 2xx `POST /api/recipes` response body — never invented, never copied from input. A real `recipe_id` looks like a UUID (e.g. `"c45619f2-c559-4f61-9893-28f83dfa2e63"`) that was in the JSON the API just returned. If you find yourself typing a UUID that wasn't in the response, stop — you are about to hallucinate. Go back and POST first.
    - The only source of valid `foodId` / `unitId` values is `GET /api/foods` / `GET /api/units` responses. Never invent IDs.
    - Do NOT create new foods or units in household-manager — only use entries that already exist (zero-result lookups → drop the ingredient or omit the unit, never POST a new one).
    - If POST fails, retry — do not give up after a single failure, and do not emit output until POST succeeds.

    `## Output` — short paragraph: "Return data shaped by `RecipeLoadOutput` in `src/robotina/queue/task_types.py`. Populate `recipe_id`, `recipe_name`, `recipe_slug`, `recipe_description`, and `missing_ingredients` per the rules above. Field types and required/optional status live on the Pydantic model — refer to it; do not re-describe fields here."

    Drop the V003 prose section `# The only failure mode that matters` — its essential guardrails are now in `## Rules`. Drop the V003 sections `# Inputs you already have`, `# Field mapping (input → API)`, `# Resolving ingredient names`, `# Building and sending the request`, `# Filling the output`, `# If POST fails` — their content is reorganized into the standardized skeleton above without losing detail.
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - All six section headers appear exactly once.
    - `grep -c 'RecipeLoadOutput' ...V004.md` returns 1.
    - `grep -c 'Argentine / Latin American Spanish' ...V004.md` returns 1.
    - File contains the substrings `servingsQty`, `servingsUnit`, `prepTime`, `cookTime`, `totalTime`, `sourceUrl` (field-mapping table preserved).
    - File contains the substring `hallucinate` (recipe_id guardrail preserved).
    - File contains both `read-skill` AND `household-manager-api`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/recipe-load/V004.md && grep -q 'RecipeLoadOutput' src/robotina/agent/prompts/recipe-load/V004.md && grep -q 'servingsQty' src/robotina/agent/prompts/recipe-load/V004.md</automated>
  </verify>
  <done>V004.md exists, defers schema to Pydantic class, preserves field-mapping table and hallucination guardrail.</done>
</task>

<task type="auto">
  <name>Task 7.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['recipe-load'].prompt_path` from `V003.md` to `V004.md`. Strip any stale `prompt_path` keys. Note: `anthropic.json` does not have a `recipe-load` entry — that is pre-existing state, out of scope for Phase 14, and is left untouched.
  </behavior>
  <action>
    1. Edit `agents.py` for `recipe-load`: `V003.md` → `V004.md`.
    2. Review overrides; delete `prompt_path` keys if present.
    3. `uv run pytest` green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/recipe-load/V004.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-07): bump recipe-load V003 -> V004 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'recipe-load/V004.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'recipe-load/V003.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `recipe-load`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['recipe-load'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V004.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `recipe-load/V004.md` follows the standardized skeleton and preserves the hallucination guardrail + field-mapping table.
2. Registry points at V004.md.
3. Atomic commit includes prompt + registry + overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>Create `14-07-SUMMARY.md` documenting any skeleton deviations and confirming the hallucination guardrail + field-mapping table are intact.</output>
