---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/robotina/agent/prompts/recipe-research-gather/V004.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/recipe-research-gather/V004.md exists with the six skeleton sections"
    - "V004.md defers ## Output to RecipeResearchGatherOutput in src/robotina/queue/task_types.py — no per-field re-description"
    - "V004.md preserves V003 behavior: adaptive Spanish queries via web-search until ≥1 usable Argentine/Latin American source, single-source entries, no synthesis across sources, no fixed cap on queries"
    - "AGENT_REGISTRY['recipe-research-gather'].prompt_path == 'src/robotina/agent/prompts/recipe-research-gather/V004.md'"
    - "No overrides/*.json carries a prompt_path key for recipe-research-gather"
    - "uv run pytest is green"
---

<objective>
Bump `recipe-research-gather` from V003 to V004 adopting the standardized skeleton. Zero behavioral change. `## Output` defers to `RecipeResearchGatherOutput` (Pydantic) in `src/robotina/queue/task_types.py` instead of re-describing the schema in prose.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/recipe-research-gather/V003.md
@src/robotina/agent/agents.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Write recipe-research-gather/V004.md (standardized skeleton)</name>
  <files>src/robotina/agent/prompts/recipe-research-gather/V004.md</files>
  <read_first>
    src/robotina/agent/prompts/recipe-research-gather/V003.md
    src/robotina/queue/task_types.py
  </read_first>
  <behavior>
    `# Recipe Research — Gather — V004`

    `## Role` — one paragraph: "You are the gather step of the recipe-research pipeline. You find candidate recipes on the web using Spanish search queries and extract structured per-source data so downstream steps (instructions, ingredients, metadata) can build a final recipe by consensus."

    `## Inputs` — bullet list:
    - The user's recipe query in Spanish (e.g. `"pasta bolognesa"`, `"guiso de lentejas"`) — passed via `shared_context.recipe_query`.

    `## Tools` — bullet list:
    - `web-search` — Tavily-backed web search. Pass a Spanish query. Each call returns up to 3 results with fields: `title`, `url`, `content` (short summary), `raw_content` (full HTML, may be `null`), `score`.

    `## Process` — numbered steps:
    1. Search adaptively until you have at least one usable Argentine or Latin American source. Issue one `web-search` call at a time; after each, evaluate the results. If a result is Argentine/Latin American and has enough data to extract a usable recipe, stop searching. Otherwise, formulate a new query and try again. There is no fixed cap on the number of queries — keep searching until you have a usable source or have exhausted reasonable phrasing variations.
    2. Use natural Spanish phrases an Argentine home cook would type — vary phrasing across queries (regional dish names, casual home-cook phrasing, cookbook-style names). Examples for "Pasta Bolognesa": `"Pasta Bolognesa facil de preparar"`, `"pasta bolognesa deliciosa"`, `"receta casera de salsa bolognesa"`.
    3. For each result that has `raw_content`, extract structured per-source recipe data (title, ingredients, instructions, times, servings). If `raw_content` is missing or unparseable, fall back to the `content` summary.
    4. Skip any individual result that has no usable data. Only fail (return an empty list) if no query has produced any usable result after exhausting reasonable phrasing variations.

    `## Rules` — bullet list:
    - All search queries are in Spanish — natural phrases as an Argentine cook would type.
    - All recipe content emitted by the model (titles, ingredients, instructions, descriptions) is in Argentine / Latin American Spanish. Only this instructional prompt stays in English.
    - Prioritize Argentine and Latin American recipe sites.
    - One usable source is enough to continue the pipeline; partial per-source data is acceptable.
    - Each output entry is grounded in a single source page — do NOT synthesize across sources at this stage. Consensus is the next step's job.

    `## Output` — short paragraph: "Return data shaped by `RecipeResearchGatherOutput` in `src/robotina/queue/task_types.py`. Populate `recipes` with one entry per usable source. Field types, required/optional status, and `RecipeResearchEntry` shape live on the Pydantic model — refer to it for the schema; do not re-describe fields here."

    Omit any prose enumeration of `RecipeResearchGatherOutput` fields. Omit any restated language-rule banner.
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - All six section headers appear exactly once.
    - `grep -c 'RecipeResearchGatherOutput' ...V004.md` returns 1.
    - `grep -c 'Argentine / Latin American Spanish' ...V004.md` returns 1.
    - `grep -c '^## Output$' ...V004.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/recipe-research-gather/V004.md && grep -q 'RecipeResearchGatherOutput' src/robotina/agent/prompts/recipe-research-gather/V004.md</automated>
  </verify>
  <done>V004.md exists, defers schema to Pydantic class.</done>
</task>

<task type="auto">
  <name>Task 3.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['recipe-research-gather'].prompt_path` from `V003.md` to `V004.md`. Strip any stale `prompt_path` keys from override blocks.
  </behavior>
  <action>
    1. Edit `src/robotina/agent/agents.py`: change the `prompt_path` line in `recipe-research-gather` AgentConfig from `V003.md` to `V004.md`.
    2. Review `overrides/*.json`. Delete `prompt_path` from `recipe-research-gather` override blocks if present.
    3. Run `uv run pytest`. Must be green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/recipe-research-gather/V004.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-03): bump recipe-research-gather V003 -> V004 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'recipe-research-gather/V004.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'recipe-research-gather/V003.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `recipe-research-gather`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['recipe-research-gather'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V004.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `recipe-research-gather/V004.md` follows the standardized skeleton.
2. Registry points at V004.md.
3. Atomic commit includes prompt + registry + overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>
Create `14-03-SUMMARY.md` documenting any skeleton deviations and confirming behavioral parity with V003.
</output>
