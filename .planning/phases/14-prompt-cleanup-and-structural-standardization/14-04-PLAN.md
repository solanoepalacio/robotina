---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/robotina/agent/prompts/recipe-research-instructions/V003.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/recipe-research-instructions/V003.md exists with the six skeleton sections"
    - "V003.md ## Output defers to RecipeResearchInstructionsOutput in src/robotina/queue/task_types.py"
    - "V003.md preserves V002 behavior: majority-consensus heuristic across gathered recipes, single-action Argentine-Spanish steps, ties resolved toward inclusion"
    - "AGENT_REGISTRY['recipe-research-instructions'].prompt_path == '...recipe-research-instructions/V003.md'"
    - "No overrides/*.json carries a prompt_path key for recipe-research-instructions"
    - "uv run pytest is green"
---

<objective>
Bump `recipe-research-instructions` from V002 to V003 adopting the standardized skeleton. Zero behavioral change. `## Output` defers to `RecipeResearchInstructionsOutput` (Pydantic).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/recipe-research-instructions/V002.md
@src/robotina/agent/agents.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Write recipe-research-instructions/V003.md</name>
  <files>src/robotina/agent/prompts/recipe-research-instructions/V003.md</files>
  <read_first>
    src/robotina/agent/prompts/recipe-research-instructions/V002.md
    src/robotina/queue/task_types.py
  </read_first>
  <behavior>
    `# Recipe Research — Instructions — V003`

    `## Role` — one paragraph: "You are the instructions step of the recipe-research pipeline. You receive every per-source recipe from the gather step and produce a single base recipe (name, description, step list) by consensus."

    `## Inputs` — bullet list:
    - The full list of per-source recipes emitted by the gather step (in the user message).

    `## Tools` — one line: "This agent uses no tools — produce the instructions directly from the recipes in the user message."

    `## Process` — numbered steps:
    1. Read every recipe and identify steps that appear in the majority of sources, shared cooking techniques, and the typical order of preparation.
    2. Apply a "majority wins" consensus heuristic. Example: if 6 of 10 recipes salt the meat first, include that step. If only 1 recipe mentions an unusual step, drop it. On a tie, include the step (better to have more information than less).
    3. Write clear, concise instruction steps. Each step describes a single, specific action and includes approximate timings when relevant (e.g., `"cocinar 5 minutos"`).
    4. Compose a Spanish recipe name and a 1–2 sentence appetizing description.

    `## Rules` — bullet list:
    - All output content (name, description, step bodies) is in Argentine / Latin American Spanish. Only this instructional prompt stays in English.
    - Use Argentine / Latin American kitchen vocabulary.
    - Steps must be single-action, specific, and clear.

    `## Output` — short paragraph: "Return data shaped by `RecipeResearchInstructionsOutput` in `src/robotina/queue/task_types.py`. Populate `draft_name`, `draft_description`, and `draft_instructions` (an ordered list of `RecipeStep` entries). Field types, required/optional status, and `RecipeStep` shape live on the Pydantic model — refer to it; do not re-describe fields here."
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - All six section headers appear exactly once.
    - `grep -c 'RecipeResearchInstructionsOutput' ...V003.md` returns 1.
    - `grep -c 'Argentine / Latin American Spanish' ...V003.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/recipe-research-instructions/V003.md && grep -q 'RecipeResearchInstructionsOutput' src/robotina/agent/prompts/recipe-research-instructions/V003.md</automated>
  </verify>
  <done>V003.md exists, defers schema to Pydantic class.</done>
</task>

<task type="auto">
  <name>Task 4.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['recipe-research-instructions'].prompt_path` from `V002.md` to `V003.md`. Strip any stale `prompt_path` keys.
  </behavior>
  <action>
    1. Edit `agents.py`: change the path from `V002.md` to `V003.md` in the `recipe-research-instructions` entry.
    2. Review overrides; delete `prompt_path` keys if present.
    3. `uv run pytest` green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/recipe-research-instructions/V003.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-04): bump recipe-research-instructions V002 -> V003 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'recipe-research-instructions/V003.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'recipe-research-instructions/V002.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `recipe-research-instructions`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['recipe-research-instructions'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V003.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `recipe-research-instructions/V003.md` follows the standardized skeleton.
2. Registry points at V003.md.
3. Atomic commit includes prompt + registry + overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>Create `14-04-SUMMARY.md` documenting any skeleton deviations and confirming behavioral parity.</output>
