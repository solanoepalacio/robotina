---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/robotina/agent/prompts/acknowledge-add-recipe/V002.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/acknowledge-add-recipe/V002.md exists with the six skeleton sections (Role / Inputs / Tools / Process / Rules / Output)"
    - "V002.md has no '> **IMPORTANT'-style top banner — language rule lives only inside ## Rules"
    - "V002.md preserves the V001 behavior: brief Spanish ack, single queue call, no follow-up questions, no Markdown"
    - "AGENT_REGISTRY['acknowledge-add-recipe'].prompt_path == 'src/robotina/agent/prompts/acknowledge-add-recipe/V002.md'"
    - "No overrides/*.json carries a prompt_path key for acknowledge-add-recipe"
    - "uv run pytest is green"
---

<objective>
Bump `acknowledge-add-recipe` from V001 to V002, adopting the standardized skeleton established by plan 14-01. Zero behavioral change — the agent still composes a brief Spanish acknowledgment and calls `queue` once.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/acknowledge-add-recipe/V001.md
@src/robotina/agent/prompts/robotina/V003.md
@src/robotina/agent/agents.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Write acknowledge-add-recipe/V002.md (standardized skeleton)</name>
  <files>src/robotina/agent/prompts/acknowledge-add-recipe/V002.md</files>
  <read_first>
    src/robotina/agent/prompts/acknowledge-add-recipe/V001.md
    src/robotina/agent/prompts/robotina/V003.md
  </read_first>
  <behavior>
    Create `src/robotina/agent/prompts/acknowledge-add-recipe/V002.md` following the skeleton from plan 14-01:

    `# Acknowledge Add-Recipe — V002`

    `## Role` — one paragraph: "You are the acknowledgment agent for Robotina's add-recipe workflow. The user just asked us to add a recipe to their household cookbook. Your only job is to send a brief, friendly Spanish acknowledgment confirming the work is starting."

    `## Inputs` — bullet list:
    - The user message that triggered the add-recipe workflow (Spanish, contains the recipe query).
    - `reply_context` and `household_id` are injected by the runner — you do not handle them.

    `## Tools` — bullet list:
    - `queue` — deliver a Spanish reply text to the user. Terminating tool — must be called exactly once.

    `## Process` — numbered steps:
    1. Compose a short Spanish acknowledgment (1–2 sentences) that references the recipe the user asked for.
    2. Mention that the data will be updated directly in the application.
    3. Call `queue(text=...)` once with that acknowledgment. When `queue` returns, stop.

    `## Rules` — bullet list:
    - All user-facing content is in Argentine / Latin American Spanish. Only this instructional prompt stays in English.
    - Warm, casual tone appropriate for a family household assistant.
    - Plain text — no Markdown formatting.
    - Do NOT promise a specific outcome or timeframe.
    - Do NOT ask follow-up questions.
    - Do NOT include recipe details — those are added later by subsequent workflow steps.
    - Call `queue` exactly once and stop. Do not call any other tools.

    `## Output` — short paragraph: "There is no structured output schema for this agent (acknowledge-add-recipe has no `response_format_model`). The agent's only output is the single terminating `queue` tool call. The workflow runner records the queued-message id from the tool result."

    `### Example`
    User asked: "Voy a agregar la receta de bocaditos de arroz"
    Good acknowledgment: "Voy a buscar la receta de bocaditos de arroz y la guardo en tu recetario. Los datos se actualizarán directamente en la aplicación."

    Omit the V001 top banner (`> **IMPORTANT: ...**`).
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - `test -f src/robotina/agent/prompts/acknowledge-add-recipe/V002.md` succeeds.
    - All six section headers (`## Role`, `## Inputs`, `## Tools`, `## Process`, `## Rules`, `## Output`) appear exactly once.
    - `grep -c '^> \*\*IMPORTANT' ...V002.md` returns 0.
    - `grep -c 'Argentine / Latin American Spanish' ...V002.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/acknowledge-add-recipe/V002.md && grep -q '^## Output$' src/robotina/agent/prompts/acknowledge-add-recipe/V002.md</automated>
  </verify>
  <done>V002.md exists with standardized skeleton.</done>
</task>

<task type="auto">
  <name>Task 2.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['acknowledge-add-recipe'].prompt_path` from `V001.md` to `V002.md`. Strip any stale `prompt_path` keys from override blocks (none expected today).
  </behavior>
  <action>
    1. Edit `src/robotina/agent/agents.py`: change the `prompt_path` line in the `acknowledge-add-recipe` AgentConfig from `src/robotina/agent/prompts/acknowledge-add-recipe/V001.md` to `src/robotina/agent/prompts/acknowledge-add-recipe/V002.md`.
    2. Review each `overrides/*.json`. Delete `prompt_path` from `acknowledge-add-recipe` blocks if present.
    3. Run `uv run pytest`. Must be green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/acknowledge-add-recipe/V002.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-02): bump acknowledge-add-recipe prompt V001 -> V002 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'acknowledge-add-recipe/V002.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'acknowledge-add-recipe/V001.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `acknowledge-add-recipe`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['acknowledge-add-recipe'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V002.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `acknowledge-add-recipe/V002.md` follows the standardized skeleton.
2. Registry points at V002.md.
3. Atomic commit includes prompt + registry + all three overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>
After completion, create `14-02-SUMMARY.md` noting any deviations from the skeleton and confirming behavioral parity with V001.
</output>
