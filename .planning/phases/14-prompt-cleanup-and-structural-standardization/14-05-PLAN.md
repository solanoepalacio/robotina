---
phase: 14-prompt-cleanup-and-structural-standardization
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/robotina/agent/prompts/recipe-research-ingredients/V003.md
  - src/robotina/agent/agents.py
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
autonomous: true
requirements: []
tags:
  - prompts
  - standardization
  - refactor

must_haves:
  truths:
    - "src/robotina/agent/prompts/recipe-research-ingredients/V003.md exists with the six skeleton sections"
    - "V003.md ## Output defers to RecipeResearchIngredientsOutput in src/robotina/queue/task_types.py"
    - "V003.md preserves V002 behavior: extract Spanish ingredient names from draft instructions; verify each via GET /api/foods?name=...; substitute from other sources if missing; drop otherwise; never create new foods"
    - "AGENT_REGISTRY['recipe-research-ingredients'].prompt_path == '...recipe-research-ingredients/V003.md'"
    - "No overrides/*.json carries a prompt_path key for recipe-research-ingredients"
    - "uv run pytest is green"
---

<objective>
Bump `recipe-research-ingredients` from V002 to V003 adopting the standardized skeleton. Zero behavioral change. `## Output` defers to `RecipeResearchIngredientsOutput` (Pydantic).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-CONTEXT.md
@.planning/phases/14-prompt-cleanup-and-structural-standardization/14-01-PLAN.md
@src/robotina/agent/prompts/recipe-research-ingredients/V002.md
@src/robotina/agent/agents.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Write recipe-research-ingredients/V003.md</name>
  <files>src/robotina/agent/prompts/recipe-research-ingredients/V003.md</files>
  <read_first>
    src/robotina/agent/prompts/recipe-research-ingredients/V002.md
    src/robotina/queue/task_types.py
  </read_first>
  <behavior>
    `# Recipe Research — Ingredients — V003`

    `## Role` — one paragraph: "You are the ingredients step of the recipe-research pipeline. You extract every ingredient from the draft instructions and verify each one against the household-manager food catalog before emitting the verified list for the loader step."

    `## Inputs` — bullet list:
    - The draft instructions emitted by the instructions step (in the user message).
    - The full list of per-source recipes from the gather step (in the user message) — used as fallback for substitutes when an extracted ingredient is missing from household-manager.

    `## Tools` — bullet list:
    - `household-manager-api` — call the household-manager REST API. For this step you use `GET /api/foods?name={food_name}` to look up foods by their Spanish name (e.g. `GET /api/foods?name=cebolla`).

    `## Process` — numbered steps:
    1. Read the draft instructions and identify every distinct ingredient. For each, capture the Spanish food name, a numeric quantity, an optional Spanish unit name (e.g. `"unidad"`, `"cucharada"`, `"g"`, `"ml"`, `"taza"`), and an optional Spanish note (e.g. `"picada finamente"`).
    2. For each ingredient, call `household-manager-api(GET, /api/foods?name={food_name})` with the Spanish name to verify the food exists.
    3. If an ingredient does not exist in household-manager, re-check the other recipes from the gather step for a substitute. If a substitute that exists in household-manager is found, use it. Otherwise, drop the ingredient entirely.
    4. Never create new foods in household-manager. Only emit ingredients that resolved against an existing food.

    `## Rules` — bullet list:
    - Use Spanish food names when calling `household-manager-api`.
    - Do NOT create new foods in the household-manager — only use foods that already exist.
    - Only include ingredients you have verified against household-manager.
    - All ingredient-facing strings in the output are in Argentine / Latin American Spanish. Only this instructional prompt stays in English.

    `## Output` — short paragraph: "Return data shaped by `RecipeResearchIngredientsOutput` in `src/robotina/queue/task_types.py`. Populate `ingredients` with one `RecipeIngredient` per verified ingredient. Field names (`food_name`, `unit_name`, `quantity`, `note`), required/optional status, and downstream resolution rules live on the Pydantic model — refer to it; do not re-describe fields here."
  </behavior>
  <action>Write the file per the behavior above.</action>
  <acceptance_criteria>
    - All six section headers appear exactly once.
    - `grep -c 'RecipeResearchIngredientsOutput' ...V003.md` returns 1.
    - `grep -c 'Argentine / Latin American Spanish' ...V003.md` returns 1.
    - File contains `household-manager-api`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/recipe-research-ingredients/V003.md && grep -q 'RecipeResearchIngredientsOutput' src/robotina/agent/prompts/recipe-research-ingredients/V003.md</automated>
  </verify>
  <done>V003.md exists, defers schema to Pydantic class.</done>
</task>

<task type="auto">
  <name>Task 5.2: Update registry + overrides; commit atomically</name>
  <files>src/robotina/agent/agents.py, overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    src/robotina/agent/agents.py
    overrides/anthropic.json
    overrides/openai.json
    overrides/staging.ollama.json
  </read_first>
  <behavior>
    Update `AGENT_REGISTRY['recipe-research-ingredients'].prompt_path` from `V002.md` to `V003.md`. Strip any stale `prompt_path` keys.
  </behavior>
  <action>
    1. Edit `agents.py` for `recipe-research-ingredients`: `V002.md` → `V003.md`.
    2. Review overrides; delete `prompt_path` keys if present.
    3. `uv run pytest` green.
    4. Atomic commit:
       ```bash
       git add src/robotina/agent/prompts/recipe-research-ingredients/V003.md \
               src/robotina/agent/agents.py \
               overrides/anthropic.json \
               overrides/openai.json \
               overrides/staging.ollama.json
       git commit -m "refactor(14-05): bump recipe-research-ingredients V002 -> V003 (standardized skeleton)"
       ```
  </action>
  <acceptance_criteria>
    - `grep -c 'recipe-research-ingredients/V003.md' src/robotina/agent/agents.py` returns 1.
    - `grep -c 'recipe-research-ingredients/V002.md' src/robotina/agent/agents.py` returns 0.
    - No `overrides/*.json` carries `prompt_path` for `recipe-research-ingredients`.
    - `uv run pytest` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run python -c "from pathlib import Path; from robotina.agent.agents import AGENT_REGISTRY; assert Path(AGENT_REGISTRY['recipe-research-ingredients'].prompt_path).is_file()"</automated>
  </verify>
  <done>Registry references V003.md, atomic commit landed, pytest green.</done>
</task>

</tasks>

<success_criteria>
1. `recipe-research-ingredients/V003.md` follows the standardized skeleton.
2. Registry points at V003.md.
3. Atomic commit includes prompt + registry + overrides.
4. `uv run pytest` is green.
</success_criteria>

<output>Create `14-05-SUMMARY.md` documenting any deviations and confirming behavioral parity.</output>
