---
phase: 01-developer-tooling-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-06

must_haves:
  truths:
    - "`docker compose up -d` starts without errors and all three services reach healthy/running state"
    - "Postgres 15 is reachable on host port 5432"
    - "Redis 7 is reachable on host port 6379 and has AOF `appendfsync always` active"
    - "RQ Dashboard HTTP server responds on port 9181"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Three-service compose stack: postgres, redis, rq-dashboard"
      contains: "appendonly yes --appendfsync always"
  key_links:
    - from: "docker-compose.yml rq-dashboard service"
      to: "redis container"
      via: "RQ_DASHBOARD_REDIS_URL env var on internal Docker network"
      pattern: "redis://redis:6379"
---

<objective>
Create `docker-compose.yml` that starts Postgres 15, Redis 7 (with AOF persistence), and RQ Dashboard as a single `docker compose up` command. This is the only deliverable — no Python code in this plan.

Purpose: Every developer and every subsequent phase depends on these services being available locally with a single command.
Output: `docker-compose.yml` at the project root.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-developer-tooling-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-developer-tooling-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml with Postgres, Redis AOF, and RQ Dashboard</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/01-developer-tooling-and-infrastructure/01-CONTEXT.md (D-03: eoranged/rq-dashboard image on port 9181)
    - .planning/phases/01-developer-tooling-and-infrastructure/01-RESEARCH.md (Pattern 4: Docker Compose with Redis AOF; Pattern 5: Alembic config)
  </read_first>
  <action>
Create `docker-compose.yml` at the project root with the following exact content:

```yaml
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: robotina
      POSTGRES_USER: robotina
      POSTGRES_PASSWORD: robotina
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U robotina -d robotina"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    command: redis-server --appendonly yes --appendfsync always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  rq-dashboard:
    image: eoranged/rq-dashboard:latest
    ports:
      - "9181:9181"
    environment:
      RQ_DASHBOARD_REDIS_URL: redis://redis:6379
    depends_on:
      - redis

volumes:
  postgres_data:
  redis_data:
```

Key requirements to verify:
- Redis command MUST include BOTH `--appendonly yes` AND `--appendfsync always` (per D-03 discretion guidance and CLAUDE.md constraint)
- RQ Dashboard image MUST be `eoranged/rq-dashboard:latest` (per D-03 locked decision)
- RQ Dashboard MUST connect to Redis via internal Docker hostname `redis://redis:6379` (not `localhost`)
- Ports 5432, 6379, and 9181 MUST be exposed to host so `uv run agent` and `uv run migrate` on the host can connect
- Named volumes `postgres_data` and `redis_data` MUST be declared under `volumes:` at top level
  </action>
  <verify>
    <automated>
      grep "appendonly yes --appendfsync always" /home/solanoe/code/robotina-gsd/docker-compose.yml && grep "eoranged/rq-dashboard" /home/solanoe/code/robotina-gsd/docker-compose.yml && grep "redis://redis:6379" /home/solanoe/code/robotina-gsd/docker-compose.yml && grep "postgres:15" /home/solanoe/code/robotina-gsd/docker-compose.yml && echo "docker-compose.yml structure verified"
    </automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at project root
    - File contains `image: postgres:15`
    - File contains `image: redis:7`
    - File contains `redis-server --appendonly yes --appendfsync always` on the redis command line
    - File contains `image: eoranged/rq-dashboard:latest`
    - File contains `RQ_DASHBOARD_REDIS_URL: redis://redis:6379`
    - File contains `"5432:5432"`, `"6379:6379"`, `"9181:9181"` port mappings
    - File contains top-level `volumes:` block declaring `postgres_data:` and `redis_data:`
    - `docker compose config` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>
    `docker-compose.yml` is syntactically valid and contains all three services with correct images, AOF configuration, port bindings, and volume declarations. Running `docker compose up -d` would start the full stack without manual intervention.
  </done>
</task>

</tasks>

<verification>
After creating docker-compose.yml:
1. `docker compose config` — validates YAML syntax (exits 0)
2. `grep "appendfsync always" docker-compose.yml` — confirms AOF is fully configured
3. `grep "eoranged/rq-dashboard" docker-compose.yml` — confirms locked decision D-03 honored
4. `grep "9181:9181" docker-compose.yml` — confirms RQ Dashboard port exposed

Full integration smoke (run if Docker daemon is accessible):
- `docker compose up -d`
- `docker compose ps` — all three services in "running" state
- `docker exec $(docker compose ps -q redis) redis-cli CONFIG GET appendfsync` — must return `always`
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:9181` — must return 200
</verification>

<success_criteria>
- `docker-compose.yml` exists and passes `docker compose config`
- Redis AOF `appendfsync always` is set via command-line args (not mounted config)
- RQ Dashboard uses `eoranged/rq-dashboard:latest` image on port 9181 (per D-03)
- All three services have host port bindings allowing local `uv run` commands to connect
</success_criteria>

<output>
After completion, create `.planning/phases/01-developer-tooling-and-infrastructure/01-01-SUMMARY.md`
</output>
