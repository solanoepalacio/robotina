---
phase: 04-llm-module-and-agent-infrastructure
plan: "06"
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - src/robotina/agent/prompts/hello-world/V001.md
  - tests/unit/test_prompts.py
autonomous: false
requirements:
  - AGENT-08
  - AGENT-11

user_setup:
  - service: langwatch
    why: "Manual verification of LangWatch traces requires a live LangWatch account and LANGWATCH_API_KEY + LANGWATCH_ENDPOINT env vars"
    env_vars:
      - name: LANGWATCH_API_KEY
        source: "LangWatch Dashboard → Project Settings → API Keys"
      - name: LANGWATCH_ENDPOINT
        source: "LangWatch Dashboard → Project Settings → Endpoint URL"
    dashboard_config:
      - task: "Note your project name for trace verification"
        location: "LangWatch Dashboard → Projects"
  - service: ollama
    why: "Manual end-to-end verification requires Ollama running locally with llama3.2 model for hello-world task type"
    env_vars:
      - name: HELLO_WORLD_API_TOKEN
        source: "Set to any value for Ollama (Ollama is unauthenticated; the env var must be present even if unused)"
    dashboard_config:
      - task: "Pull llama3.2 model"
        location: "Terminal: ollama pull llama3.2"

must_haves:
  truths:
    - "src/robotina/agent/prompts/hello-world/V001.md exists with non-empty content"
    - "Prompt file is loaded by run_task via AgentConfig.prompt_path"
    - "Full pipeline proven end-to-end: hello-world job → agents.py lookup → LLM → response logged"
  artifacts:
    - path: "src/robotina/agent/prompts/hello-world/V001.md"
      provides: "placeholder system prompt for hello-world task type"
      min_lines: 3
  key_links:
    - from: "AgentConfig.prompt_path"
      to: "src/robotina/agent/prompts/hello-world/V001.md"
      via: "Path(config.prompt_path).read_text() in run_task()"
      pattern: "src/robotina/agent/prompts/hello-world/V001.md"
---

<objective>
Create the `hello-world/V001.md` system prompt file, complete the prompt-related unit tests, and perform a manual end-to-end checkpoint verifying the full pipeline works (agents.py lookup → LLM backend → prompt loading → agent invocation → LangWatch trace).

Purpose: Closes out Phase 4 by proving the complete pipeline end-to-end with the hello-world placeholder task type. The checkpoint confirms that a real agent run produces observable output before Phase 5 workflow integration begins.
Output: Prompt file at canonical path; all unit tests green; manual pipeline verification complete.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/solanoe/code/robotina-gsd/.planning/PROJECT.md
@/home/solanoe/code/robotina-gsd/.planning/ROADMAP.md
@/home/solanoe/code/robotina-gsd/.planning/phases/04-llm-module-and-agent-infrastructure/04-CONTEXT.md
@/home/solanoe/code/robotina-gsd/.planning/phases/04-llm-module-and-agent-infrastructure/04-RESEARCH.md
</context>

<interfaces>
<!-- From Plan 03 output: AgentConfig for hello-world -->
AgentConfig(
    task_type="hello-world",
    prompt_path="src/robotina/agent/prompts/hello-world/V001.md",
    skills=[],
    tools=[],
    model_config={"provider": "ollama", "url": "http://localhost:11434",
                  "model": "llama3.2", "api_key_env": "HELLO_WORLD_API_TOKEN"}
)

<!-- run_task() reads prompt via: -->
prompt_text = Path(config.prompt_path).read_text()
# Path is relative to CWD (project root) — prompt_path must be a valid relative path
# from the project root when running uv run agent
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hello-world prompt file and complete prompt tests</name>
  <files>src/robotina/agent/prompts/hello-world/V001.md, tests/unit/test_prompts.py</files>
  <read_first>
    - tests/unit/test_prompts.py (stubs to fill in)
    - src/robotina/agent/agents.py (verify prompt_path value for hello-world entry)
    - .planning/phases/04-llm-module-and-agent-infrastructure/04-CONTEXT.md (D-14: placeholder prompt purpose)
  </read_first>
  <behavior>
    - test_prompt_file_exists_for_hello_world: `Path("src/robotina/agent/prompts/hello-world/V001.md").exists()` is True when run from project root
    - test_prompt_loaded_from_agent_config_path: `get_agent_config("hello-world").prompt_path` is `"src/robotina/agent/prompts/hello-world/V001.md"`; `Path(that_path).read_text()` returns a non-empty string
    - test_skill_index_appended_to_prompt: With a mock SkillSet that has `index_content = "skill content"`, the combined prompt passed to `backend.create_agent()` contains both the original prompt text AND "skill content" (verify in run_task logic using mock)
  </behavior>
  <action>
Create the directory `src/robotina/agent/prompts/hello-world/` and the file `src/robotina/agent/prompts/hello-world/V001.md` with this content:

```markdown
# Hello World Agent (Phase 4 Placeholder)

You are a test agent used to verify the Robotina agent pipeline is working correctly.

When invoked, respond with a brief greeting that confirms you are operational. Include:
1. A confirmation that you received the user's message
2. A brief "hello world" acknowledgment

This agent is a placeholder. It will be removed in Phase 6 when the send-notification
agent is added. See CONTEXT.md D-06.
```

Then fill in `tests/unit/test_prompts.py` with real assertions per the behavior spec. For `test_prompt_file_exists_for_hello_world`, use `pathlib.Path` directly. For `test_skill_index_appended_to_prompt`, mock `make_backend`, `get_agent_config`, and `get_current_job` to inject a fake SkillSet with known `index_content`, then assert the `system_prompt` argument passed to `backend.create_agent()` contains the skill content.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/test_prompts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/robotina/agent/prompts/hello-world/V001.md` exists
    - `src/robotina/agent/prompts/hello-world/V001.md` contains `Hello World Agent`
    - `src/robotina/agent/prompts/hello-world/V001.md` contains `Phase 4 Placeholder`
    - `src/robotina/agent/prompts/hello-world/V001.md` has at least 5 lines
    - `uv run pytest tests/unit/test_prompts.py -q` exits 0 with 0 failures
  </acceptance_criteria>
  <done>Prompt file created; all test_prompts.py tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run full unit test suite — verify all Phase 4 tests pass</name>
  <files>
    tests/unit/test_llm_backend.py,
    tests/unit/test_agents_registry.py,
    tests/unit/test_agent_runner.py,
    tests/unit/test_skills.py,
    tests/unit/test_prompts.py,
    tests/unit/test_observability.py
  </files>
  <read_first>
    - .planning/phases/04-llm-module-and-agent-infrastructure/04-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
Run the full unit test suite and the existing integration tests to confirm no regressions:

```bash
cd /home/solanoe/code/robotina-gsd
uv run pytest tests/unit/ -q
uv run pytest tests/ -q --ignore=tests/unit/
```

If any test fails, investigate and fix the root cause. Do NOT skip or xfail tests to make the suite pass.

The unit suite must show 0 failures (tests may be PASSED or SKIPPED — no FAILED, no ERROR).
The integration suite may have pre-existing failures unrelated to Phase 4 — document any pre-existing failures found but do not fix them unless they were introduced by Phase 4 changes.
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/ -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/unit/ -q` exits 0 with 0 FAILED and 0 ERROR
    - `uv run pytest tests/test_gateway.py tests/test_runner.py -q` exits 0 (no regressions in previously passing tests)
    - All 6 unit test files show results (not collection errors)
  </acceptance_criteria>
  <done>Full unit suite green; no regressions in gateway or runner tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end pipeline verification</name>
  <action>Human verifies the complete Phase 4 pipeline by starting the worker and confirming it starts cleanly with LangWatch status logged. Optionally enqueues a hello-world job to confirm the full agent execution path.</action>
  <what-built>
Full Phase 4 pipeline: LLMBackend Protocol + adapters, agents.py registry, run_task() universal job function, SkillSet + ReadSkillTool, hello-world prompt, LangWatch init, per-module logging, gateway enqueue fix.
  </what-built>
  <how-to-verify>
Manual end-to-end pipeline verification:

**Step 1 — Start infrastructure:**
```bash
docker compose up -d
```

**Step 2 — Verify gateway enqueue string is updated:**
```bash
grep "robotina.queue.jobs.run_task" src/robotina/gateway/handler.py
```
Expected: line with `"robotina.queue.jobs.run_task"` present.

**Step 3 — Start the task runner:**
```bash
uv run agent
```
Expected console output:
- `LangWatch credentials not set` (if LANGWATCH_API_KEY not set) OR `LangWatch instrumentation initialized`
- `Starting task runner worker (concurrency=1)...`

**Step 4 — (Optional, requires Ollama) Enqueue a hello-world job:**
```bash
export HELLO_WORLD_API_TOKEN=not-needed-for-ollama
uv run python -c "
from redis import Redis
from rq import Queue
from robotina.queue.task_types import IncomingMessageInput
from datetime import datetime, timezone

r = Redis.from_url('redis://localhost:6379')
q = Queue('agent-tasks', connection=r)
task_input = IncomingMessageInput(
    message_id='test-001',
    platform='telegram',
    received_at=datetime.now(timezone.utc),
    chat_id='99001',
    user_id='55001',
    household_id='test',
    text='hello world test',
    history=[],
)
q.enqueue(
    'robotina.queue.jobs.run_task',
    task_input,
    at_front=True,
    result_ttl=-1,
    failure_ttl=-1,
    meta={'task_type': 'hello-world'},
)
print('Enqueued hello-world job')
"
```
Expected worker console output:
- `[agent-tasks] job <id> starting | task_type=hello-world`
- `LLM stream start | model=...`
- `[agent-tasks] job <id> finished | task_type=hello-world`

**Step 5 — Confirm all 6 unit test files pass:**
```bash
uv run pytest tests/unit/ -v
```
Expected: 0 failures.
  </how-to-verify>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run agent` starts and logs LangWatch status (either warning or initialized)
    - `uv run pytest tests/unit/ -v` exits 0
  </acceptance_criteria>
  <done>Worker starts cleanly; all unit tests green; Phase 4 pipeline verified end-to-end.</done>
  <resume-signal>
Type "approved" if the pipeline works (worker starts, jobs process, unit tests pass).
Describe any issues if verification failed.
  </resume-signal>
</task>

</tasks>

<verification>
```bash
cd /home/solanoe/code/robotina-gsd && uv run pytest tests/unit/ tests/test_gateway.py tests/test_runner.py -q
```
Full unit suite green; gateway and runner tests pass without regression.
</verification>

<success_criteria>
- `uv run pytest tests/unit/ -q` exits 0 with 0 failures
- `src/robotina/agent/prompts/hello-world/V001.md` exists with "Phase 4 Placeholder" content
- `uv run pytest tests/test_gateway.py tests/test_runner.py -q` exits 0
- Manual checkpoint: worker starts, logs LangWatch status, processes hello-world job end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-llm-module-and-agent-infrastructure/04-06-SUMMARY.md`
</output>
