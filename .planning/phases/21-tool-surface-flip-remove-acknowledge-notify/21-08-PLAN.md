---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 08
type: execute
wave: 3
depends_on: [21-04, 21-05, 21-06, 21-07]
files_modified:
  - .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md
  - .planning/REQUIREMENTS.md
  - PROJECT.md
  - README.md
  - pyproject.toml
autonomous: false
requirements: [EXP-05, EVAL-01, EVAL-02, EVAL-03]
must_haves:
  truths:
    - "21-SMOKE.md exists with 5-8 utterances spanning the EVAL-02 coverage matrix (1 single + 2 multi + 1 compound + 1 ambiguous + 1 over-cap, planner-selected within that envelope)"
    - "21-SMOKE.md contains a Go/No-Go decision line filled in by the operator"
    - "REQUIREMENTS.md has TOOLS-01..05, DASH-11, EXP-05, EVAL-01..03 ticked (checkbox or equivalent)"
    - "PROJECT.md experiment list no longer mentions acknowledge_add_recipe (was never present per CONTEXT.md grep — verify and update if needed)"
    - "pyproject.toml [project.scripts] has no acknowledge_add_recipe entry"
    - "Phase 21 final verification routes as human_needed until 21-SMOKE.md is marked verdict: pass"
  artifacts:
    - path: ".planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md"
      provides: "Manual multi-call smoke results (EVAL-01..03)"
      min_lines: 30
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 21 requirement IDs ticked"
  key_links:
    - from: "21-SMOKE.md"
      to: "Phase 21 verification gate"
      via: "verdict: pass line"
      pattern: "verdict.*pass"
---

<objective>
Wrap Phase 21: commit the manual smoke template, run the smoke against both Ollama and OpenAI backends, capture results, tick REQUIREMENTS.md, and update PROJECT.md / README / pyproject.toml for the doc-only EXP-05 cleanup.

This plan is `autonomous: false` because the smoke execution (D-13/D-14/D-16) is operator-driven. Claude commits the template + does the documentation edits autonomously; the operator runs the smoke and fills in the table.

Per D-15, if OpenAI staging shows unacceptable multi-call reliability, the operator triggers the pivot (list-form `start-workflow(actions=[...])`). The pivot is a follow-up cycle; Phase 21 stays open until the smoke passes one way or another.

Output: `21-SMOKE.md` committed with operator results; REQUIREMENTS.md ticks; doc updates.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@PROJECT.md
@README.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 21-SMOKE.md template</name>
  <files>.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md</files>
  <read_first>
    - .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md (D-13/14 template)
    - .planning/REQUIREMENTS.md (EVAL-02 utterance coverage requirements)
  </read_first>
  <behavior>
    - File exists at the documented path.
    - Contains the 5-row utterance table from D-13, expanded if needed to satisfy EVAL-02 (planner picks utterances within the coverage envelope: 1 single + 2 multi (2-3 items) + 1 compound + 1 ambiguous + 1 over-cap).
    - Contains backend identification line, date, operator placeholders.
    - Contains Go/No-Go section with verdict placeholder.
    - `verdict:` line is initially empty/`pending` — operator fills it.
  </behavior>
  <action>
    Create `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md`:

    ```markdown
    ---
    verdict: pending
    date: <YYYY-MM-DD — operator fills>
    operator: <name — operator fills>
    ---

    # Phase 21 Multi-call Smoke Results

    **Backends:** Ollama gpt-oss:20b (local dev), OpenAI (staging)
    **V005 prompt:** src/robotina/agent/prompts/robotina/V005.md
    **Tool surface:** RespondTool + StartWorkflowTool (multi-call, return_direct=False) + TerminateTool (return_direct=True)

    ## Utterance Set (EVAL-02 coverage)

    | # | Utterance (Spanish) | Coverage | Expected N workflows | Ollama N | Ollama OK? | OpenAI N | OpenAI OK? | LangWatch trace IDs | Notes |
    |---|---|---|---|---|---|---|---|---|---|
    | 1 | agregá lentejas | single-recipe | 1 | | | | | | |
    | 2 | agregá canelones y pollo al horno | multi (2) | 2 | | | | | | |
    | 3 | agregá canelones, pollo y arroz con verduras | multi (3) | 3 | | | | | | |
    | 4 | agregá pollo al horno con papas | compound dish (ambiguous: 1 or 2) | 1-2 | | | | | | note which |
    | 5 | agregá algo rico para esta noche | ambiguous (no specific recipe) | 0 or ask | | | | | | should clarify, not start |
    | 6 | agregá canelones, pollo, arroz, lentejas, milanesas, salmón | over-cap (>5) | should split or reject | | | | | | |

    ## Tool-call Hygiene Checks (per backend, per utterance)

    For each row, ALSO verify:
    - [ ] `terminate()` was called as the LAST tool call.
    - [ ] No trailing AI free text in the final assistant message (PITFALL 4 mitigation).
    - [ ] `respond()` was called BEFORE `terminate()`, with Spanish text.
    - [ ] N start-workflow calls match expected N (or operator notes the deviation in the row).

    ## Go / No-Go

    **Ollama:** <PASS / FAIL — operator notes>
    **OpenAI:** <PASS / FAIL — operator notes>

    **Decision:** <GO with current `{workflow_type, input}` schema / NO-GO — pivot to list-form `actions=[...]` per D-15 before merge>

    **Verdict (set in frontmatter):** pending → pass / pivot / fail

    ## Operator runbook

    1. Start the worker locally: `uv run agent`.
    2. For each utterance, send the message via Telegram (or test gateway) once on each backend.
    3. Capture the LangWatch trace ID for each turn (Ollama dashboard URL + OpenAI dashboard URL).
    4. Inspect the trace: confirm tool call sequence, confirm `terminate()` is last, confirm no trailing AI text.
    5. Record N (number of `start-workflow` calls observed) per backend.
    6. Mark OK/FAIL per row.
    7. Fill in Go/No-Go.
    8. Set `verdict:` in the frontmatter to `pass` (merge), `pivot` (apply D-15 list-form refactor before merge), or `fail` (re-open phase planning).
    9. Commit this file with message: `test(21): manual multi-call smoke <verdict>`.

    ## Pivot path (D-15)

    If OpenAI shows unacceptable reliability (e.g. <80% correct N), pivot StartWorkflowTool to list-form:

    ```python
    class StartWorkflowArgs(BaseModel):
        actions: list[StartWorkflowAction]  # one entry per workflow to start

    class StartWorkflowAction(BaseModel):
        workflow_type: Literal["add-recipe"]
        input: AddRecipeQueryInput
    ```

    Update V005 prompt examples to use the list form. Re-run the smoke. Update this file's verdict accordingly.

    ## References

    - D-13, D-14, D-15, D-16, D-24 in `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md`.
    - EVAL-01, EVAL-02, EVAL-03 in `.planning/REQUIREMENTS.md`.
    - PITFALL 4 (trailing AI text), PITFALL 5 (parallel tool calls) in `.planning/research/PITFALLS.md`.
    ```

    Commit the file with the verdict still `pending`. The operator step (Task 2) fills it in.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md`
    - `grep -c "verdict" .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md` >= 1
    - `grep -c "Ollama\|OpenAI" .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md` >= 2
    - Utterance count: `grep -cE "^\| [0-9]+ \|" .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md` >= 5
    - `grep -c "PITFALL 4\|trailing AI" .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md` >= 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md && grep -q "verdict" .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-SMOKE.md</automated>
  </verify>
  <done>Smoke template committed with `verdict: pending`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs manual multi-call smoke and fills 21-SMOKE.md</name>
  <what-built>
    Phase 21 code changes (plans 21-01 through 21-07) all landed:
    - RespondTool + TerminateTool added.
    - StartWorkflowTool refactored to multi-call surface.
    - QueueTool, acknowledge-add-recipe, notify step, dead-letter block all deleted.
    - V005 prompt loaded.
    - Dashboard task-type label map live.
    - CI guard for AGENT_REGISTRY ↔ overrides green.
    - 21-SMOKE.md template committed with `verdict: pending`.
  </what-built>
  <how-to-verify>
    Run the smoke per the `Operator runbook` section of 21-SMOKE.md:

    1. Start the worker: `uv run agent` (Ollama backend, local dev).
    2. Send each of the 5-6 utterances via Telegram (or your test gateway).
    3. Capture LangWatch trace IDs for each turn.
    4. Inspect each trace and fill in the row: N workflows, OK/FAIL, trace URL.
    5. Switch backend to OpenAI (staging) and repeat.
    6. Per-utterance hygiene checklist: terminate() is last, no trailing AI text, respond() in Spanish, N matches expected.
    7. Fill in the Go/No-Go section.
    8. Set frontmatter `verdict:` to one of:
       - `pass` — merge Phase 21.
       - `pivot` — apply D-15 list-form refactor, re-run smoke, then merge.
       - `fail` — re-open phase planning.
    9. Commit `21-SMOKE.md` with: `test(21): manual multi-call smoke <verdict>`.

    If `pivot`: apply the schema refactor described in 21-SMOKE.md's Pivot path section, re-run the smoke, re-record results in a new section or new file, then proceed to Task 3.

    Approval gate: Task 3 (REQUIREMENTS.md ticks + doc updates) MUST NOT run until verdict is `pass` (or `pivot` followed by a passing re-run).
  </how-to-verify>
  <resume-signal>Type "approved" once `21-SMOKE.md` is committed with `verdict: pass` (or describe issues to redirect).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Tick REQUIREMENTS.md + update PROJECT.md / README / pyproject.toml</name>
  <files>.planning/REQUIREMENTS.md, PROJECT.md, README.md, pyproject.toml</files>
  <read_first>
    - .planning/REQUIREMENTS.md (find TOOLS-01..05, DASH-11, EXP-05, EVAL-01..03 — note current tick format)
    - PROJECT.md (find experiment list or agent set, if present)
    - README.md (find experiment / agent references)
    - pyproject.toml (find [project.scripts] — verify no acknowledge_add_recipe entry)
  </read_first>
  <behavior>
    - REQUIREMENTS.md: TOOLS-01, TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05, DASH-11, EXP-05, EVAL-01, EVAL-02, EVAL-03 each marked complete (checkbox ticked or status field updated, matching existing format in the file).
    - PROJECT.md: any experiment list / agent surface mention is current (no acknowledge_add_recipe, no QueueTool); active experiments are recipe_research, recipe_load (per CONTEXT.md `<specifics>` block).
    - README.md: same — no stale references.
    - pyproject.toml [project.scripts]: confirmed no `experiments.acknowledge_add_recipe` entry; if any, remove it.
    - All four files commit atomically with message: `docs(21): tick Phase 21 requirements; update experiment list`.
  </behavior>
  <action>
    1. **REQUIREMENTS.md:** locate each of the ten requirement IDs and update the status field / checkbox to match the file's existing convention (e.g. `- [x]` if checkboxes, or `status: done` if YAML, or `✓` if symbols). Read the file first to learn the convention. Reference Phase 21 in the tick (e.g. `- [x] TOOLS-01 — multi-call surface (Phase 21)` if the convention adds phase tags).

    2. **PROJECT.md:** locate the "experiments" or "agents" section if present. Remove any mention of acknowledge_add_recipe (was never in `experiments/` per CONTEXT.md, but check). If the active-experiment list is enumerated, ensure it lists `recipe_research`, `recipe_load` and notes that Phase 23 will add `gather_from_url`, Phase 24 will add `recipe_image`.

    3. **README.md:** grep for `acknowledge`, `QueueTool`, `V004` references. If any exist describing the old surface, update them to V005 + new tool surface. If no references exist, no change required.

    4. **pyproject.toml:** grep `[project.scripts]` section. Verify no `experiments.acknowledge_add_recipe` entry. If present, remove. If absent, no change (just confirm).

    Reference EXP-05 + memory `feedback_overrides_in_sync` in commit message.
  </action>
  <acceptance_criteria>
    - `grep -c "TOOLS-01\|TOOLS-02\|TOOLS-03\|TOOLS-04\|TOOLS-05\|DASH-11\|EXP-05\|EVAL-01\|EVAL-02\|EVAL-03" .planning/REQUIREMENTS.md` >= 10 (all ten IDs present)
    - REQUIREMENTS.md format check: ten requirement entries display the "complete" marker (whatever the file's convention is — checkbox, status field, etc.). Operator-visible.
    - `grep -c "acknowledge_add_recipe\|acknowledge-add-recipe" PROJECT.md` == 0
    - `grep -c "acknowledge_add_recipe\|acknowledge-add-recipe" README.md` == 0
    - `grep -c "acknowledge_add_recipe" pyproject.toml` == 0
    - `grep -c "QueueTool" README.md PROJECT.md` == 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn "acknowledge_add_recipe\|acknowledge-add-recipe" PROJECT.md README.md pyproject.toml .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Requirements ticked, docs scrubbed of legacy references, pyproject.toml verified clean.</done>
</task>

</tasks>

<verification>
- Phase 21 success criteria #1-6 from ROADMAP all met by the combined plan set 21-01..21-08.
- Manual smoke verdict committed.
- No legacy references remain in src/ tests/ overrides/ docs.
</verification>

<success_criteria>
- 21-SMOKE.md verdict: pass (or pivot → pass).
- All ten requirement IDs ticked in REQUIREMENTS.md.
- PROJECT.md / README / pyproject.toml clean of legacy references.
- Phase 21 ready for `/gsd:verify-phase 21` (which will route to human_needed until smoke verdict is set).
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-08-SUMMARY.md` documenting the smoke verdict and final doc state.
</output>
