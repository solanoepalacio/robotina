---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/robotina/agent/tools/respond.py
  - tests/unit/test_respond_tool.py
autonomous: true
requirements: [TOOLS-02]
must_haves:
  truths:
    - "RespondTool exists as a LangChain BaseTool subclass at src/robotina/agent/tools/respond.py"
    - "RespondTool enqueues a send-notification job with at_front=True and returns the job_id"
    - "RespondTool is non-terminal (return_direct=False) so Robotina can call it before start-workflow and continue"
  artifacts:
    - path: "src/robotina/agent/tools/respond.py"
      provides: "RespondTool class — Spanish-text reply via send-notification enqueue (at_front=True)"
      contains: "class RespondTool"
    - path: "tests/unit/test_respond_tool.py"
      provides: "RespondTool unit tests (D-17)"
      contains: "def test_respond_tool"
  key_links:
    - from: "src/robotina/agent/tools/respond.py"
      to: "robotina.queue.task_types.SendNotificationInput"
      via: "import + instantiate inside _run"
      pattern: "SendNotificationInput\\("
    - from: "src/robotina/agent/tools/respond.py"
      to: "Redis queue (send-notification)"
      via: "enqueue at_front=True (mirrors QueueTool)"
      pattern: "at_front=True"
---

<objective>
Add `RespondTool` — a non-terminal LangChain tool that enqueues a `send-notification` job at the front of the queue with the Spanish text Robotina wants the user to see. Per D-01.

Purpose: This tool is half of the new "Robotina-as-decider" surface. It replaces `QueueTool` (deleted in plan 21-04). Non-terminal so Robotina can `respond(...)` → `start-workflow(...)` → `terminate()` in one turn.

Output: New tool file + unit tests.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/research/PITFALLS.md
@src/robotina/agent/tools/queue.py
@src/robotina/queue/task_types.py
@CLAUDE.md

<interfaces>
From src/robotina/agent/tools/queue.py (the model — RespondTool mirrors this shape):
- Class subclasses `langchain_core.tools.BaseTool`
- Constructor-injected: `chat_id: int`, `user_id: int`, `platform: str`, `household_id: NonEmptyHouseholdId`
- `name`, `description`, `args_schema` class attrs
- `_run(self, **args) -> str` builds `SendNotificationInput(...)`, enqueues, returns `job_id`
- Uses `at_front=True` (memory `feedback_queue_at_front` — load-bearing)

From src/robotina/queue/task_types.py:
- `SendNotificationInput(chat_id: int, user_id: int, platform: str, text: str)` — KEEP, reused as-is.
- `NonEmptyHouseholdId` — Phase 16 pattern, reused via constructor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RespondTool</name>
  <files>src/robotina/agent/tools/respond.py</files>
  <read_first>
    - src/robotina/agent/tools/queue.py (entire file — mirror its structure)
    - src/robotina/queue/task_types.py (SendNotificationInput class)
    - src/robotina/agent/tools/start_workflow.py (constructor-injection pattern for NonEmptyHouseholdId)
  </read_first>
  <behavior>
    - When `_run(text="Hola")` is called, builds SendNotificationInput(chat_id, user_id, platform, text="Hola") and enqueues via the same path as QueueTool with at_front=True.
    - Returns the enqueued job_id as a string.
    - Is non-terminal: `return_direct: bool = False` on the class.
    - Constructor accepts chat_id, user_id, platform, household_id and stores them as Pydantic v2 fields (matches QueueTool shape).
    - args_schema has a single field `text: str` (Pydantic v2, `extra="forbid"`).
  </behavior>
  <action>
    Create `src/robotina/agent/tools/respond.py` defining `class RespondTool(BaseTool)`:

    Class attributes (mirror QueueTool):
    - `name: str = "respond"`
    - `description: str = "..."` — Spanish-replier description: "Envía un mensaje al usuario en español. No termina el turno: podés llamar a otras herramientas después."
    - `args_schema: type[BaseModel] = RespondArgs` where `class RespondArgs(BaseModel)` has `model_config = ConfigDict(extra="forbid")` and one field `text: str = Field(..., description="Mensaje en español para enviar al usuario")`.
    - `return_direct: bool = False` (NON-TERMINAL — per D-01).

    Constructor-injected fields (Pydantic v2 attrs on the BaseTool subclass, matching QueueTool):
    - `chat_id: int`
    - `user_id: int`
    - `platform: str`
    - `household_id: NonEmptyHouseholdId` (import the existing type from wherever QueueTool imports it)

    `_run(self, text: str, **kwargs) -> str`:
    1. Build `SendNotificationInput(chat_id=self.chat_id, user_id=self.user_id, platform=self.platform, text=text)`.
    2. Enqueue via the same helper QueueTool uses (likely `from robotina.queue.queue import enqueue` or whatever QueueTool imports — read queue.py and reuse the same import and the same `at_front=True` invocation).
    3. Return the resulting `job_id` as a string.

    Do NOT implement `_arun` (sync only, matching QueueTool).

    Implementation MUST exactly mirror QueueTool's enqueue call (same module, same task_type="send-notification", same `at_front=True`, same `result_ttl=-1`, same `failure_ttl=-1` — whatever QueueTool does). Per D-01 invariants.

    Reference D-01 in a docstring comment for traceability ("# Per D-01: non-terminal Spanish-replier; enqueues send-notification at_front=True (mirrors retired QueueTool).").
  </action>
  <acceptance_criteria>
    - `grep -c "class RespondTool" src/robotina/agent/tools/respond.py` == 1
    - `grep -c "return_direct.*False" src/robotina/agent/tools/respond.py` >= 1
    - `grep -c "at_front=True" src/robotina/agent/tools/respond.py` >= 1
    - `grep -c "SendNotificationInput" src/robotina/agent/tools/respond.py` >= 1
    - `grep -c "name.*=.*\"respond\"" src/robotina/agent/tools/respond.py` >= 1
    - `python -c "from robotina.agent.tools.respond import RespondTool; t = RespondTool(chat_id=1, user_id=1, platform='telegram', household_id='hh-x'); assert t.return_direct is False; assert t.name == 'respond'"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.agent.tools.respond import RespondTool; t = RespondTool(chat_id=1, user_id=1, platform='telegram', household_id='hh-x'); assert t.return_direct is False and t.name == 'respond'"</automated>
  </verify>
  <done>RespondTool importable, constructable, non-terminal, exposes `text: str` arg schema, mirrors QueueTool enqueue pattern with at_front=True.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for RespondTool</name>
  <files>tests/unit/test_respond_tool.py</files>
  <read_first>
    - tests/unit/ (list contents; find existing tool test for pattern reference, e.g. test_start_workflow_tool.py if it exists)
    - src/robotina/agent/tools/queue.py (for matching test patterns)
    - src/robotina/agent/tools/respond.py (just created)
  </read_first>
  <behavior>
    - test_respond_tool_constructs: instantiates with chat_id=42, user_id=7, platform="telegram", household_id="hh-1" and confirms field propagation.
    - test_respond_tool_is_non_terminal: asserts `RespondTool(...).return_direct is False`.
    - test_respond_tool_name_and_schema: asserts `name == "respond"`, args_schema accepts `{text: "hola"}`, rejects extra fields.
    - test_respond_tool_run_enqueues_at_front: monkeypatches the enqueue helper RespondTool imports; calls `_run(text="hola")`; asserts the helper was called with at_front=True and a SendNotificationInput whose text == "hola"; asserts the return value equals the mocked job_id.
    - test_respond_tool_empty_text_rejected: feeding empty text via args_schema validation raises ValidationError (assumes the agent validates via args_schema; if `text: str` doesn't enforce non-empty, accept empty — test then asserts the call still goes through). Pick whichever Pydantic semantics RespondTool actually enforces, do not invent new constraints.
  </behavior>
  <action>
    Create `tests/unit/test_respond_tool.py` with the 4-5 tests described above using pytest + monkeypatch. Use the same mocking style as existing QueueTool tests if present (grep `tests/` for `QueueTool` first).

    The `_run` test MUST patch the SAME symbol RespondTool imports (e.g. `robotina.agent.tools.respond.enqueue` — patch where it's used, not where it's defined).

    Reference D-17 in a top-of-file docstring.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_respond_tool.py -v` passes
    - Test file contains at least 4 distinct `def test_` functions
    - `grep -c "at_front=True" tests/unit/test_respond_tool.py` >= 1
    - `grep -c "return_direct" tests/unit/test_respond_tool.py` >= 1
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_respond_tool.py -v</automated>
  </verify>
  <done>RespondTool unit tests pass and exercise enqueue path + non-terminal flag.</done>
</task>

</tasks>

<verification>
- RespondTool importable, constructable, non-terminal.
- Enqueue path mirrors QueueTool with at_front=True (PITFALL 13 — no inline Telegram call).
- Unit tests cover construction, schema, enqueue, return_direct=False.
</verification>

<success_criteria>
- `src/robotina/agent/tools/respond.py` exists with the documented contract.
- `uv run pytest tests/unit/test_respond_tool.py` passes.
- No edits to other modules in this plan (pure additive).
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-01-SUMMARY.md` per the standard template after completion.
</output>
