---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 07
type: execute
wave: 3
depends_on: [21-04]
files_modified:
  - src/robotina/dashboard/templates/_macros.html
  - src/robotina/dashboard/templates/workflow.html
  - tests/dashboard/test_task_type_labels.py
autonomous: true
requirements: [DASH-11]
must_haves:
  truths:
    - "A Jinja macro task_type_label(t) exists and returns Spanish label for known task types, raw enum for unknown ones"
    - "TASK_TYPE_LABELS has entries for gather, instructions, ingredients, metadata, load, finalize-outcome, handle-incoming-message, send-notification (Spanish)"
    - "TASK_TYPE_LABELS has NO entry for acknowledge-add-recipe or notify (retired)"
    - "workflow.html renders step.task_type via the macro, not raw"
    - "Template tests pin label rendering and the unknown-task-type fallback"
  artifacts:
    - path: "src/robotina/dashboard/templates/_macros.html"
      provides: "TASK_TYPE_LABELS dict + task_type_label macro (Jinja-side, no Python import — D-11 / Phase 13 D-01)"
      contains: "task_type_label"
    - path: "src/robotina/dashboard/templates/workflow.html"
      provides: "Wraps step.task_type renders in task_type_label()"
    - path: "tests/dashboard/test_task_type_labels.py"
      provides: "Label rendering tests + unknown-task-type fallback test (D-22)"
  key_links:
    - from: "src/robotina/dashboard/templates/workflow.html"
      to: "src/robotina/dashboard/templates/_macros.html"
      via: "Jinja macro import"
      pattern: "task_type_label"
---

<objective>
Introduce the dashboard task-type label map (DASH-11) as a Jinja-side macro per D-11 + Phase 13 D-01 (module isolation — no Python-side import from outside the dashboard).

Initial Spanish labels:
- gather → Búsqueda
- instructions → Instrucciones
- ingredients → Ingredientes
- metadata → Metadatos
- load → Guardar
- finalize-outcome → Cierre del flujo
- handle-incoming-message → Robotina (mensaje)
- send-notification → Notificación

NO entries for acknowledge-add-recipe or notify (retired in Phase 21).

Unknown task types fall back to raw enum value (so missing-label regressions are visible).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/phases/13-queue-visibility-dashboard/13-CONTEXT.md
@src/robotina/dashboard/templates/workflow.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _macros.html with TASK_TYPE_LABELS + task_type_label macro</name>
  <files>src/robotina/dashboard/templates/_macros.html</files>
  <read_first>
    - src/robotina/dashboard/templates/ (list contents — confirm _macros.html does not exist)
    - src/robotina/dashboard/templates/workflow.html (current render of step.task_type)
  </read_first>
  <behavior>
    - New file `_macros.html` in dashboard templates dir.
    - Contains `{% set TASK_TYPE_LABELS = { ... } %}` with the 8 entries.
    - Defines macro `task_type_label(t)` returning the label or raw enum fallback.
    - No Python-side import (Phase 13 D-01).
  </behavior>
  <action>
    Create `src/robotina/dashboard/templates/_macros.html` containing:
    - A Jinja comment block referencing Phase 21 D-11 / DASH-11 / Phase 13 D-01.
    - A `{% set TASK_TYPE_LABELS = {...} %}` with these eight key/value pairs (Spanish values, double-quoted): "gather"→"Búsqueda", "instructions"→"Instrucciones", "ingredients"→"Ingredientes", "metadata"→"Metadatos", "load"→"Guardar", "finalize-outcome"→"Cierre del flujo", "handle-incoming-message"→"Robotina (mensaje)", "send-notification"→"Notificación".
    - A macro: `{% macro task_type_label(t) -%}{{ TASK_TYPE_LABELS[t] if t in TASK_TYPE_LABELS else t }}{%- endmacro %}`

    Do NOT include keys for acknowledge-add-recipe or notify. Future phases (23/24) extend the dict with gather-from-url / recipe-image.
  </action>
  <acceptance_criteria>
    - `test -f src/robotina/dashboard/templates/_macros.html`
    - `grep -c "task_type_label" src/robotina/dashboard/templates/_macros.html` >= 1
    - `grep -c "TASK_TYPE_LABELS" src/robotina/dashboard/templates/_macros.html` >= 1
    - `grep -c "Búsqueda" src/robotina/dashboard/templates/_macros.html` == 1
    - `grep -c "Cierre del flujo" src/robotina/dashboard/templates/_macros.html` == 1
    - `grep -c "acknowledge-add-recipe" src/robotina/dashboard/templates/_macros.html` == 0
    - `uv run python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('src/robotina/dashboard/templates')); e.get_template('_macros.html')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('src/robotina/dashboard/templates')); e.get_template('_macros.html')"</automated>
  </verify>
  <done>_macros.html created, valid Jinja, contains 8 labels and the macro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire task_type_label macro into workflow.html</name>
  <files>src/robotina/dashboard/templates/workflow.html</files>
  <read_first>
    - src/robotina/dashboard/templates/workflow.html (find every place step.task_type is rendered raw — likely in a step list and/or kv-grid)
    - src/robotina/dashboard/templates/_macros.html (just created)
  </read_first>
  <behavior>
    - workflow.html imports the macro at the top: `{% from '_macros.html' import task_type_label %}` (or whatever import style the rest of the codebase uses).
    - Every render of `{{ step.task_type }}` (or equivalent direct enum render) is replaced with `{{ task_type_label(step.task_type) }}`.
    - Other template logic untouched.
    - Template still renders without error.
  </behavior>
  <action>
    Edit `src/robotina/dashboard/templates/workflow.html`:

    1. At the top of the file (after any existing `{% extends %}` and before the first block), add: `{% from '_macros.html' import task_type_label %}`.
    2. Find every `{{ step.task_type }}` and replace with `{{ task_type_label(step.task_type) }}`.
    3. If there are sibling templates (e.g. a step row partial) that also render task_type raw, update them in the same pass — grep `src/robotina/dashboard/templates/` for `step.task_type` to find them all.

    Do NOT remove any other markup. Do NOT alter the Conversation / Outcome rows from Phase 20 D-13.
  </action>
  <acceptance_criteria>
    - `grep -c "from '_macros.html' import task_type_label\|from \"_macros.html\" import task_type_label" src/robotina/dashboard/templates/workflow.html` >= 1
    - `grep -c "task_type_label(step.task_type)" src/robotina/dashboard/templates/workflow.html` >= 1
    - `grep -c "{{ step.task_type }}" src/robotina/dashboard/templates/workflow.html` == 0 (every raw render replaced)
    - The Conversation row and Outcome row from Phase 20 (kv-grid entries) still present: `grep -c "Conversation\|conversation" src/robotina/dashboard/templates/workflow.html` >= 1
    - Existing dashboard tests still pass: `uv run pytest tests/dashboard/ -v`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/dashboard/ -v</automated>
  </verify>
  <done>workflow.html (and any sibling step-render templates) uses task_type_label() everywhere; existing dashboard tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add task_type label rendering tests (D-22)</name>
  <files>tests/dashboard/test_task_type_labels.py</files>
  <read_first>
    - tests/dashboard/ (current tests for fixture / app-client patterns)
    - src/robotina/dashboard/ (Flask/FastAPI route definition for the workflow detail page; identify the URL)
  </read_first>
  <behavior>
    - test_known_task_type_renders_spanish_label: rendering a WorkflowRun with a step where `task_type="gather"` produces "Búsqueda" in the HTTP response body.
    - test_finalize_outcome_label: a step with `task_type="finalize-outcome"` renders "Cierre del flujo".
    - test_unknown_task_type_falls_back_to_raw: a step with `task_type="unknown-task-xyz"` renders "unknown-task-xyz" (not an empty string, not an exception).
    - test_acknowledge_legacy_falls_back_to_raw: a step with `task_type="acknowledge-add-recipe"` (somehow surfaced in legacy data) falls into the fallback path — proves the legacy label is gone.
    - All tests use the same dashboard test client / fixture style as existing tests/dashboard/*.py.
  </behavior>
  <action>
    Create `tests/dashboard/test_task_type_labels.py`:

    1. Copy the test-client fixture style from existing tests/dashboard/*.py (read them first).
    2. Use a factory or in-memory DB fixture to create a WorkflowRun with WorkflowRunSteps that have controlled task_type values.
    3. Issue a GET to the workflow detail route; parse response body.
    4. Assert the four behaviors above.

    If the existing dashboard test setup is too heavy and direct Jinja-environment rendering is cleaner, render the `_macros.html` macro directly:

    ```python
    from jinja2 import Environment, FileSystemLoader

    def make_env():
        return Environment(loader=FileSystemLoader("src/robotina/dashboard/templates"))

    def test_known_task_type_renders_spanish_label():
        env = make_env()
        tpl = env.from_string("{% from '_macros.html' import task_type_label %}{{ task_type_label('gather') }}")
        assert tpl.render().strip() == "Búsqueda"

    def test_finalize_outcome_label():
        env = make_env()
        tpl = env.from_string("{% from '_macros.html' import task_type_label %}{{ task_type_label('finalize-outcome') }}")
        assert tpl.render().strip() == "Cierre del flujo"

    def test_unknown_task_type_falls_back_to_raw():
        env = make_env()
        tpl = env.from_string("{% from '_macros.html' import task_type_label %}{{ task_type_label('unknown-task-xyz') }}")
        assert tpl.render().strip() == "unknown-task-xyz"

    def test_acknowledge_legacy_falls_back_to_raw():
        env = make_env()
        tpl = env.from_string("{% from '_macros.html' import task_type_label %}{{ task_type_label('acknowledge-add-recipe') }}")
        assert tpl.render().strip() == "acknowledge-add-recipe"
    ```

    The Jinja-direct approach is preferred since it isolates the macro logic from route/fixture complexity. Use it unless project convention strongly prefers full route tests.

    Reference D-22 / DASH-11 in top-of-file docstring.
  </action>
  <acceptance_criteria>
    - `test -f tests/dashboard/test_task_type_labels.py`
    - `uv run pytest tests/dashboard/test_task_type_labels.py -v` passes (4 tests)
    - `grep -c "Búsqueda" tests/dashboard/test_task_type_labels.py` >= 1
    - `grep -c "acknowledge-add-recipe" tests/dashboard/test_task_type_labels.py` >= 1 (fallback test asserts legacy label is gone)
    - `grep -c "task_type_label" tests/dashboard/test_task_type_labels.py` >= 4
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/dashboard/test_task_type_labels.py -v</automated>
  </verify>
  <done>Four label-rendering tests pass; legacy-label fallback proven.</done>
</task>

</tasks>

<verification>
- Macro is the single source of truth for task_type display.
- Unknown labels fall back to raw enum (not empty string).
- Phase 13 D-01 module isolation preserved (Jinja-side only, no Python import).
</verification>

<success_criteria>
- DASH-11 delivered.
- ROADMAP success-criterion: dashboard task-type label map landed with Spanish labels for active types only.
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-07-SUMMARY.md`.
</output>
