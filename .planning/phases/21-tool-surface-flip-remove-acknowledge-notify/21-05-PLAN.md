---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 05
type: execute
wave: 2
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - src/robotina/agent/prompts/robotina/V005.md
autonomous: true
requirements: [TOOLS-02, TOOLS-03]
must_haves:
  truths:
    - "V005.md exists at src/robotina/agent/prompts/robotina/V005.md and is loaded by handle-incoming-message (after plan 21-04 task 2 bumps the prompt_path)"
    - "V005 teaches three tools: respond(text), start-workflow(workflow_type, input), terminate()"
    - "V005 explicitly forbids user-facing text in the assistant's final message — respond() is the only user-visible channel (PITFALL 4 mitigation)"
    - "V005 includes a happy-path example AND a wake-context example, both single-recipe (multi-recipe is Phase 22)"
    - "V005 prompt body is English; user-facing examples in respond() calls are Spanish (per memory feedback_prompts_language)"
    - "V004.md is RETAINED untouched for rollback"
  artifacts:
    - path: "src/robotina/agent/prompts/robotina/V005.md"
      provides: "Robotina V005 system prompt teaching new tool surface"
      min_lines: 50
  key_links:
    - from: "src/robotina/agent/agents.py (handle-incoming-message entry)"
      to: "src/robotina/agent/prompts/robotina/V005.md"
      via: "prompt_path string (set in plan 21-04 task 2)"
      pattern: "robotina/V005.md"
---

<objective>
Create the V005 Robotina system prompt per D-09. V005 = V004 forked with new tool-surface sections that teach `respond()` + `start-workflow()` (multi-call) + `terminate()`, with strict output rules to prevent trailing AI text (PITFALL 4).

Output: New prompt file. V004 stays for rollback.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md
@src/robotina/agent/prompts/robotina/V004.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork V004.md to V005.md and add new tool surface sections</name>
  <files>src/robotina/agent/prompts/robotina/V005.md</files>
  <read_first>
    - src/robotina/agent/prompts/robotina/V004.md (entire file — the V005 base)
    - .planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md (D-09 spec)
    - .planning/research/PITFALLS.md (Pitfall 4 — termination)
  </read_first>
  <action>
    Create `src/robotina/agent/prompts/robotina/V005.md` by copying V004.md and then applying the following changes. DO NOT modify V004.md.

    **Sections to ADD or REPLACE in V005:**

    1. **Available tools section** (replace whatever V004 said about QueueTool / start-workflow). Add a clear three-tool list:
       - `respond(text: str)` — send a Spanish message to the user. NON-terminal. You can call it before or after other tools.
       - `start-workflow(workflow_type, input)` — start a household workflow. NON-terminal. `workflow_type` is currently only `"add-recipe"`. `input` is `{value: <recipe query string>}`. You may call this multiple times in one turn to start N workflows in sequence (one per recipe). Per Phase 21 D-03.
       - `terminate()` — signals end of turn. No arguments. Call this AFTER your last tool call. After terminate(), the turn ends.

    2. **Strict Output Rule** (new section, load-bearing for PITFALL 4):
       > "All user-facing messages MUST go through `respond()`. DO NOT write any user-facing text in your final assistant message — `respond()` is the ONLY user-visible channel. After your last tool call, call `terminate()`. Never write conversational text outside of `respond()` arguments."

    3. **Happy-path example (single-recipe, USER_MESSAGE trigger):**
       ```
       User: "agregá lentejas"
       Tool calls:
         1. respond(text="Listo, voy con lentejas")
         2. start-workflow(workflow_type="add-recipe", input={"value": "lentejas"})
         3. terminate()
       ```

    4. **Wake-context happy-path example (WAKE_RULE trigger with outcomes summary):**
       ```
       Wake preamble describes: outcome status="success", workflow_type="add-recipe", final-output recipe_id=abc123, title="Lentejas guisadas"
       Tool calls:
         1. respond(text="Lista guardada: Lentejas guisadas (id abc123)")
         2. terminate()
       ```

    5. **Wake-context failure-path example:**
       ```
       Wake preamble describes: outcome status="failure", workflow_type="add-recipe", failure_reason="No se encontró la receta"
       Tool calls:
         1. respond(text="No pude guardar la receta: no encontré información suficiente. ¿Probamos con otro nombre?")
         2. terminate()
       ```

    6. **Wake-context multi-outcome rule:** "When the wake preamble contains multiple outcomes, compose a SINGLE `respond()` that summarizes all of them in one Spanish message. Do NOT call `respond()` once per outcome — that produces message spam."

    7. **Forbidden behaviors** (new bulleted list):
       - Writing recipe data (ingredients, instructions, IDs you weren't told) inside `respond()`. You only know what the wake preamble told you.
       - Calling `respond()` AFTER `terminate()`.
       - Omitting `terminate()` at the end of your turn.
       - Writing any free-form text in your final assistant message.
       - Calling QueueTool (it no longer exists).

    8. **Multi-recipe note (minimal — Phase 22 will expand):**
       > "If the user lists multiple recipes in one message (e.g. 'agregá lentejas y pollo'), emit ONE `start-workflow` per recipe. Order them as the user said. Worked examples for multi-recipe extraction land in V006 (Phase 22)."

    9. **Phase 20 wake-context reversal note (in the section that V004 said 'you may reflect but cannot speak'):**
       > "Phase 21 update: when invoked with the wake-context preamble, you CAN and SHOULD speak via `respond()`. The wake preamble exists so you can compose a Spanish reply summarizing what happened."

    10. **Language rule** (carry forward V004's stance, restate for emphasis):
        > "Prompt body is English. All user-facing strings inside `respond()` arguments MUST be Spanish."

    Reference D-09, D-10, PITFALL 4 in a top-of-file comment block (HTML or Markdown comment, however V004 does it).

    **Do NOT** add multi-recipe worked examples — keep V005 focused on the tool-surface flip per D-09.
    **Do NOT** add wake-rule cron examples (Phase 25+).
    **Do NOT** include URL ingestion examples (Phase 23).
  </action>
  <acceptance_criteria>
    - File exists: `test -f src/robotina/agent/prompts/robotina/V005.md`
    - V004 untouched: `test -f src/robotina/agent/prompts/robotina/V004.md` AND `diff` against pre-Phase-21 V004 shows no changes (verify via git diff)
    - File contains all three tool names: `grep -c "respond" src/robotina/agent/prompts/robotina/V005.md` >= 3 (multiple references), `grep -c "start-workflow" src/robotina/agent/prompts/robotina/V005.md` >= 2, `grep -c "terminate" src/robotina/agent/prompts/robotina/V005.md` >= 3
    - Strict output rule present: `grep -ic "only user-visible channel\|final assistant message" src/robotina/agent/prompts/robotina/V005.md` >= 1
    - At least one Spanish example string: `grep -c "Listo\|lentejas\|guardada" src/robotina/agent/prompts/robotina/V005.md` >= 1
    - No reference to QueueTool: `grep -c "QueueTool" src/robotina/agent/prompts/robotina/V005.md` == 0
    - No reference to acknowledge-add-recipe: `grep -c "acknowledge-add-recipe" src/robotina/agent/prompts/robotina/V005.md` == 0
    - File is at least 50 lines: `[ $(wc -l < src/robotina/agent/prompts/robotina/V005.md) -ge 50 ]`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/robotina/agent/prompts/robotina/V005.md && test -f src/robotina/agent/prompts/robotina/V004.md && [ $(wc -l < src/robotina/agent/prompts/robotina/V005.md) -ge 50 ] && ! grep -q "QueueTool\|acknowledge-add-recipe" src/robotina/agent/prompts/robotina/V005.md && grep -q "terminate" src/robotina/agent/prompts/robotina/V005.md</automated>
  </verify>
  <done>V005.md exists, teaches the new tool surface, forbids trailing AI text, retains Spanish-user / English-body rule, and V004.md is untouched for rollback.</done>
</task>

</tasks>

<verification>
- Loaded by plan 21-04 task 2 (prompt_path bump in agents.py).
- Strict Output Rule enforces PITFALL 4 mitigation at prompt level (engine-level enforcement is the TerminateTool from plan 21-02).
- V004 retained for rollback per D-09.
</verification>

<success_criteria>
- V005 prompt file in place.
- All three tools introduced.
- Strict output rule present.
- Spanish user-facing / English body rule preserved.
- ROADMAP success-criterion #2 (V005 system prompt deployed).
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-05-SUMMARY.md`.
</output>
