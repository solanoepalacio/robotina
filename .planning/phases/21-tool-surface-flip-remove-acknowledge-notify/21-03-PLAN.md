---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/robotina/agent/tools/start_workflow.py
  - src/robotina/queue/task_types.py
  - tests/unit/test_start_workflow_tool.py
autonomous: true
requirements: [TOOLS-01]
must_haves:
  truths:
    - "StartWorkflowTool is non-terminal (return_direct=False) — multi-call enabled per TOOLS-01"
    - "StartWorkflowTool args schema is {workflow_type: Literal['add-recipe'], input: AddRecipeQueryInput}"
    - "AddRecipeQueryInput {value: str} lives in robotina.queue.task_types alongside other input models"
    - "Constructor-injection of invocation_id / conversation_id / household_id is unchanged (Phase 18 D-13)"
  artifacts:
    - path: "src/robotina/agent/tools/start_workflow.py"
      provides: "Refactored StartWorkflowTool — multi-call surface"
      contains: "return_direct.*False"
    - path: "src/robotina/queue/task_types.py"
      provides: "AddRecipeQueryInput {value: str}"
      contains: "class AddRecipeQueryInput"
    - path: "tests/unit/test_start_workflow_tool.py"
      provides: "Updated tests covering new schema + multi-call behavior"
      contains: "workflow_type"
  key_links:
    - from: "src/robotina/agent/tools/start_workflow.py"
      to: "robotina.queue.task_types.AddRecipeQueryInput"
      via: "args_schema field type"
      pattern: "AddRecipeQueryInput"
---

<objective>
Refactor `StartWorkflowTool` to enable the multi-call surface (TOOLS-01) per D-03:

1. Drop `return_direct=True` (was terminal). Set `return_direct=False`.
2. Replace the flat `recipe_query: str` arg with a typed `{workflow_type: Literal["add-recipe"], input: AddRecipeQueryInput}` shape.
3. Add `AddRecipeQueryInput {value: str}` to `task_types.py`.
4. Update unit tests.

Constructor injection of `invocation_id` (Phase 18 D-13), `conversation_id` (Phase 17 D-03/04), and `household_id` (Phase 16 NonEmptyHouseholdId) is UNCHANGED.

Output: Refactored tool + new input model + updated tests.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@src/robotina/agent/tools/start_workflow.py
@src/robotina/queue/task_types.py

<interfaces>
Today (pre-Phase-21):
```python
class StartWorkflowArgs(BaseModel):
    workflow_type: Literal["add-recipe"]
    recipe_query: str

class StartWorkflowTool(BaseTool):
    return_direct: bool = True
    args_schema = StartWorkflowArgs
    # constructor-injected: invocation_id, conversation_id, household_id
```

Target (Phase 21 per D-03):
```python
class AddRecipeQueryInput(BaseModel):
    model_config = ConfigDict(extra="forbid")
    value: str  # the recipe query

class StartWorkflowArgs(BaseModel):
    model_config = ConfigDict(extra="forbid")
    workflow_type: Literal["add-recipe"]
    input: AddRecipeQueryInput

class StartWorkflowTool(BaseTool):
    return_direct: bool = False  # FLIP
    args_schema = StartWorkflowArgs
    # constructor injection UNCHANGED
```

`_run` unwraps: `recipe_query = input.value`, then enqueues exactly as before (same workflow-start path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AddRecipeQueryInput to task_types.py</name>
  <files>src/robotina/queue/task_types.py</files>
  <read_first>
    - src/robotina/queue/task_types.py (entire file — locate existing input models like RecipeResearchInput, SendNotificationInput, AcknowledgeAddRecipeInput for placement and style)
  </read_first>
  <behavior>
    - New class `AddRecipeQueryInput(BaseModel)` with `model_config = ConfigDict(extra="forbid")` and one field `value: str`.
    - Co-located with other typed input models (alongside RecipeResearchInput).
    - Importable as `from robotina.queue.task_types import AddRecipeQueryInput`.
  </behavior>
  <action>
    Add to `src/robotina/queue/task_types.py` (place near RecipeResearchInput / SendNotificationInput — do NOT modify AcknowledgeAddRecipeInput here; that deletion lives in plan 21-04):

    ```python
    class AddRecipeQueryInput(BaseModel):
        """Per D-03: typed input for StartWorkflowTool when workflow_type='add-recipe'.

        Replaces the legacy flat `recipe_query: str` arg on StartWorkflowTool's
        args_schema. The tool's _run unwraps `input.value` to get the recipe query.
        Phase 23 will make this part of a discriminated union with URL input.
        """
        model_config = ConfigDict(extra="forbid")
        value: str
    ```

    Use the same `BaseModel` and `ConfigDict` imports already present in the file. If the file lacks `ConfigDict`, add it to the existing pydantic import line.
  </action>
  <acceptance_criteria>
    - `grep -c "class AddRecipeQueryInput" src/robotina/queue/task_types.py` == 1
    - `grep -c 'value: str' src/robotina/queue/task_types.py` >= 1
    - `uv run python -c "from robotina.queue.task_types import AddRecipeQueryInput; m=AddRecipeQueryInput(value='lentejas'); assert m.value=='lentejas'"` exits 0
    - AcknowledgeAddRecipeInput still present (deleted in plan 21-04, NOT here): `grep -c "class AcknowledgeAddRecipeInput" src/robotina/queue/task_types.py` == 1
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.queue.task_types import AddRecipeQueryInput; m=AddRecipeQueryInput(value='lentejas'); assert m.value=='lentejas'"</automated>
  </verify>
  <done>AddRecipeQueryInput exists, importable, validates a `value: str` field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor StartWorkflowTool to multi-call surface</name>
  <files>src/robotina/agent/tools/start_workflow.py</files>
  <read_first>
    - src/robotina/agent/tools/start_workflow.py (current implementation — note args_schema fields, _run body, constructor-injected fields)
    - src/robotina/queue/task_types.py (after task 1 lands — AddRecipeQueryInput now available)
    - .planning/research/ARCHITECTURE.md §2.6
  </read_first>
  <behavior>
    - `StartWorkflowTool.return_direct is False` — non-terminal.
    - args_schema is `{workflow_type: Literal["add-recipe"], input: AddRecipeQueryInput}` with `extra="forbid"` on the outer schema.
    - `_run(workflow_type, input)` unwraps `recipe_query = input.value` and enqueues exactly as the previous implementation did.
    - Constructor injection of `invocation_id`, `conversation_id`, `household_id` UNCHANGED — same field names, same types, same instantiation pattern in jobs.py.
    - Calling the tool twice in sequence produces two independent enqueues (no shared mutable state added in this refactor).
  </behavior>
  <action>
    Edit `src/robotina/agent/tools/start_workflow.py`:

    1. Import `AddRecipeQueryInput` from `robotina.queue.task_types` alongside the existing imports.
    2. Modify `StartWorkflowArgs` (or whatever the args_schema class is named):
       - Remove the `recipe_query: str` field.
       - Add `input: AddRecipeQueryInput` field.
       - Keep `workflow_type: Literal["add-recipe"]`.
       - Ensure `model_config = ConfigDict(extra="forbid")` is set.
    3. Change `return_direct: bool = True` → `return_direct: bool = False` on the tool class.
    4. Update `_run` signature: replace the `recipe_query: str` parameter with `input: AddRecipeQueryInput`. Inside the body, extract `recipe_query = input.value` as the very first line, then leave the rest of the enqueue logic byte-identical (same shared_context build, same workflow-start enqueue, same return).
    5. Update the tool's `description` to reflect that it's now multi-call-safe: "Inicia un flujo de tipo workflow_type con el input dado. Podés llamarme varias veces en un mismo turno para iniciar N flujos. No termino el turno — usá terminate() cuando hayas terminado."
    6. Do NOT touch constructor-injected fields (`invocation_id`, `conversation_id`, `household_id`). Reference D-03 + Phase 18 D-13 in a docstring comment for traceability.
  </action>
  <acceptance_criteria>
    - `grep -c "return_direct.*False" src/robotina/agent/tools/start_workflow.py` >= 1
    - `grep -c "return_direct.*True" src/robotina/agent/tools/start_workflow.py` == 0
    - `grep -c "AddRecipeQueryInput" src/robotina/agent/tools/start_workflow.py` >= 1
    - `grep -c "recipe_query: str" src/robotina/agent/tools/start_workflow.py` == 0 (flat field gone from args_schema)
    - `grep -c "invocation_id" src/robotina/agent/tools/start_workflow.py` >= 1 (constructor injection preserved)
    - `grep -c "input: AddRecipeQueryInput" src/robotina/agent/tools/start_workflow.py` >= 1
    - `uv run python -c "from robotina.agent.tools.start_workflow import StartWorkflowTool; assert StartWorkflowTool.model_fields"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.agent.tools.start_workflow import StartWorkflowTool; from robotina.queue.task_types import AddRecipeQueryInput; t = StartWorkflowTool(invocation_id='inv-1', conversation_id='conv-1', household_id='hh-1'); assert t.return_direct is False"</automated>
  </verify>
  <done>StartWorkflowTool is non-terminal, args_schema uses {workflow_type, input}, AddRecipeQueryInput-typed input field, constructor injection unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update StartWorkflowTool unit tests</name>
  <files>tests/unit/test_start_workflow_tool.py</files>
  <read_first>
    - Find existing test file via `grep -rln "StartWorkflowTool\|start_workflow_tool" tests/` and read it
    - src/robotina/agent/tools/start_workflow.py (post-refactor)
  </read_first>
  <behavior>
    - test_start_workflow_tool_is_non_terminal: asserts `return_direct is False` (was True).
    - test_start_workflow_tool_args_schema: instantiates `StartWorkflowArgs(workflow_type="add-recipe", input={"value": "lentejas"})` and confirms `args.input.value == "lentejas"`.
    - test_start_workflow_tool_rejects_flat_recipe_query: passing `recipe_query="lentejas"` at the args_schema level raises ValidationError (the flat field is gone).
    - test_start_workflow_tool_rejects_extra_fields: `extra="forbid"` rejects unknown keys.
    - test_start_workflow_tool_run_unwraps_input: monkeypatch the enqueue helper; call `_run(workflow_type="add-recipe", input=AddRecipeQueryInput(value="lentejas"))`; assert the recorded enqueue payload's recipe_query == "lentejas".
    - test_start_workflow_tool_multi_call_independent: call `_run` twice with different values; assert two separate enqueues happened with the right values (smoke-level test that no mutable state leaks between calls).
    - test_start_workflow_tool_constructor_injection_unchanged: instantiates with invocation_id/conversation_id/household_id; asserts attribute propagation.
  </behavior>
  <action>
    Locate the existing StartWorkflowTool tests (path may be `tests/unit/test_start_workflow_tool.py` or similar — find via grep). Rewrite or add tests to cover the 7 behaviors above.

    If an existing test file exists, EDIT it; do not duplicate. If no existing file, CREATE `tests/unit/test_start_workflow_tool.py`.

    Remove or update any existing assertion that says `return_direct is True` for StartWorkflowTool — that's the legacy invariant, must be flipped.

    Remove or update any existing assertion that uses the flat `recipe_query` field at the args_schema layer.

    Multi-call test: instantiate one tool with mocked enqueue, call `_run` twice with distinct AddRecipeQueryInput values, assert mock recorded two calls with the distinct values.

    Reference D-03 in a top-of-file docstring.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_start_workflow_tool.py -v` passes
    - `grep -c "return_direct is False\|return_direct.*False" tests/unit/test_start_workflow_tool.py` >= 1
    - `grep -c "AddRecipeQueryInput" tests/unit/test_start_workflow_tool.py` >= 1
    - `grep -c "multi" tests/unit/test_start_workflow_tool.py` >= 1
    - No test asserts `return_direct is True` for StartWorkflowTool
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_start_workflow_tool.py -v</automated>
  </verify>
  <done>StartWorkflowTool tests pass, cover new schema, multi-call shape, return_direct=False.</done>
</task>

</tasks>

<verification>
- Refactor is wire-compatible: jobs.py's existing StartWorkflowTool injection in plan 21-04 will work without changes to constructor injection.
- args_schema flip is breaking — any caller still passing `recipe_query` will fail loudly (good, fail-fast).
</verification>

<success_criteria>
- Tool flips to non-terminal multi-call surface.
- AddRecipeQueryInput added.
- All existing StartWorkflowTool tests pass with new schema.
- No edits to jobs.py / agents.py / workflows.py here — those land in plan 21-04.
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-03-SUMMARY.md`.
</output>
