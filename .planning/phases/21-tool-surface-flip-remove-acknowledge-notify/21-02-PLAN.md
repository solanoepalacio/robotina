---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/robotina/agent/tools/terminate.py
  - tests/unit/test_terminate_tool.py
autonomous: true
requirements: [TOOLS-03]
must_haves:
  truths:
    - "TerminateTool exists as a no-arg LangChain BaseTool subclass at src/robotina/agent/tools/terminate.py"
    - "TerminateTool is return_direct=True (engine-enforced termination point — PITFALL 4 mitigation)"
    - "Calling terminate() returns a sentinel string that the engine ignores due to return_direct=True"
  artifacts:
    - path: "src/robotina/agent/tools/terminate.py"
      provides: "TerminateTool class — no-arg, return_direct=True termination signal"
      contains: "class TerminateTool"
    - path: "tests/unit/test_terminate_tool.py"
      provides: "TerminateTool unit tests (D-17)"
      contains: "def test_terminate_tool"
  key_links:
    - from: "TerminateTool"
      to: "LangChain agent loop"
      via: "return_direct=True signals end of turn"
      pattern: "return_direct.*True"
---

<objective>
Add `TerminateTool` — a no-argument LangChain tool whose only purpose is engine-enforced termination of the agent turn. Per D-02.

Purpose: PITFALL 4 mitigation. Without an explicit `terminate()` tool with `return_direct=True`, the LLM can emit trailing free text after its last tool call. `terminate()` makes the termination point explicit and machine-checkable.

Output: New tool file + unit tests.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/research/PITFALLS.md
@src/robotina/agent/tools/queue.py
@CLAUDE.md

<interfaces>
TerminateTool shape (per D-02):
- Subclasses `langchain_core.tools.BaseTool`
- `name = "terminate"`
- `description` — English: "Signals the end of the assistant turn. Call this AFTER your last respond() / start-workflow() tool call. Takes no arguments. Do not write any user-facing text in your final message — respond() is the only user-visible channel."
- `args_schema` — empty Pydantic BaseModel (no fields, `extra="forbid"`)
- `return_direct: bool = True`
- `_run(self) -> str` returns a sentinel string like `"turn-terminated"` (engine ignores it because of return_direct=True)
- No constructor-injected fields (the tool is pure)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TerminateTool</name>
  <files>src/robotina/agent/tools/terminate.py</files>
  <read_first>
    - src/robotina/agent/tools/queue.py (for BaseTool import path and class shape)
    - src/robotina/agent/tools/start_workflow.py (for args_schema/Pydantic v2 patterns in this codebase)
  </read_first>
  <behavior>
    - `TerminateTool()` constructs with no args.
    - `TerminateTool().return_direct is True`.
    - `TerminateTool().name == "terminate"`.
    - `TerminateTool()._run()` returns a non-empty string sentinel (engine ignores it).
    - args_schema rejects any extra kwargs (Pydantic v2 `extra="forbid"`).
  </behavior>
  <action>
    Create `src/robotina/agent/tools/terminate.py`:

    ```python
    """Per D-02: engine-enforced termination tool. PITFALL 4 mitigation."""
    from langchain_core.tools import BaseTool
    from pydantic import BaseModel, ConfigDict

    class TerminateArgs(BaseModel):
        model_config = ConfigDict(extra="forbid")

    class TerminateTool(BaseTool):
        name: str = "terminate"
        description: str = (
            "Signals the end of the assistant turn. Call this AFTER your last "
            "respond() / start-workflow() tool call. Takes no arguments. Do not "
            "write any user-facing text in your final assistant message — "
            "respond() is the only user-visible channel."
        )
        args_schema: type[BaseModel] = TerminateArgs
        return_direct: bool = True

        def _run(self, **kwargs) -> str:
            return "turn-terminated"
    ```

    Use whatever BaseTool import path the rest of the project uses (grep `from langchain_core.tools import BaseTool` to confirm).
  </action>
  <acceptance_criteria>
    - `grep -c "class TerminateTool" src/robotina/agent/tools/terminate.py` == 1
    - `grep -c "return_direct.*True" src/robotina/agent/tools/terminate.py` >= 1
    - `grep -c "name.*=.*\"terminate\"" src/robotina/agent/tools/terminate.py` >= 1
    - `uv run python -c "from robotina.agent.tools.terminate import TerminateTool; t=TerminateTool(); assert t.return_direct is True and t.name=='terminate' and t._run()"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.agent.tools.terminate import TerminateTool; t=TerminateTool(); assert t.return_direct is True and t.name=='terminate' and t._run()"</automated>
  </verify>
  <done>TerminateTool importable, constructable with no args, return_direct=True, _run returns non-empty sentinel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for TerminateTool</name>
  <files>tests/unit/test_terminate_tool.py</files>
  <read_first>
    - src/robotina/agent/tools/terminate.py (just created)
    - tests/unit/ (list contents for test patterns)
  </read_first>
  <behavior>
    - test_terminate_tool_constructs_no_args
    - test_terminate_tool_return_direct_true
    - test_terminate_tool_name_is_terminate
    - test_terminate_tool_run_returns_sentinel — non-empty string
    - test_terminate_tool_rejects_extra_args — calling with kwargs via args_schema validation fails (Pydantic ValidationError)
  </behavior>
  <action>
    Create `tests/unit/test_terminate_tool.py` with the five test functions above using pytest. Reference D-17 + PITFALL 4 in a top-of-file docstring.

    The "rejects extra args" test should instantiate `TerminateArgs(foo="bar")` (importing TerminateArgs from the tool module) and assert it raises pydantic.ValidationError. Do NOT test the tool's `_run` with extra kwargs (BaseTool's invoke validates via args_schema; testing at the schema level is more deterministic).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/unit/test_terminate_tool.py -v` passes
    - File contains at least 4 `def test_` functions
    - `grep -c "return_direct" tests/unit/test_terminate_tool.py` >= 1
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/unit/test_terminate_tool.py -v</automated>
  </verify>
  <done>TerminateTool unit tests pass.</done>
</task>

</tasks>

<verification>
- TerminateTool exists, is no-arg, return_direct=True.
- Unit tests cover construction, return_direct, sentinel, schema rejection.
</verification>

<success_criteria>
- `src/robotina/agent/tools/terminate.py` exists with the documented contract.
- `uv run pytest tests/unit/test_terminate_tool.py` passes.
- Pure additive — no other file edits.
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-02-SUMMARY.md`.
</output>
