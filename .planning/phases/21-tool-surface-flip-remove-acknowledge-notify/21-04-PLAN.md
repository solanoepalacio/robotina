---
phase: 21-tool-surface-flip-remove-acknowledge-notify
plan: 04
type: execute
wave: 2
depends_on: [21-01, 21-02, 21-03]
files_modified:
  - src/robotina/queue/jobs.py
  - src/robotina/agent/agents.py
  - src/robotina/agent/workflows.py
  - src/robotina/queue/task_types.py
  - src/robotina/queue/workflow_runner.py
  - src/robotina/agent/tools/queue.py
  - src/robotina/agent/prompts/acknowledge-add-recipe/
  - overrides/anthropic.json
  - overrides/openai.json
  - overrides/staging.ollama.json
  - tests/test_workflows.py
  - tests/test_workflow_runner.py
autonomous: true
requirements: [TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05]
must_haves:
  truths:
    - "QueueTool is deleted; RespondTool + TerminateTool are injected for handle-incoming-message"
    - "acknowledge-add-recipe agent, prompt directory, task type, registry entry, overrides entries, and jobs.py elif branch are GONE"
    - "WORKFLOW_REGISTRY['add-recipe'].steps is exactly 6 step_keys in order: gather, instructions, ingredients, metadata, load, finalize-outcome — NO acknowledge, NO notify"
    - "Dead-letter send-notification block in on_step_failed is GONE; the try/except around _check_and_dispatch_wake stays but the except branch is log-only"
    - "send-notification task_type branch in jobs.py:90 STAYS (RespondTool's delivery mechanism — D-07)"
    - "`grep -rn acknowledge-add-recipe src/ tests/ overrides/ experiments/` returns zero hits"
    - "Single-recipe happy-path regression test passes with new 6-step list and new tool injection"
  artifacts:
    - path: "src/robotina/queue/jobs.py"
      provides: "Tool injection swap: QueueTool → RespondTool + TerminateTool; acknowledge-add-recipe elif deleted; send-notification branch retained"
      contains: "RespondTool"
    - path: "src/robotina/agent/workflows.py"
      provides: "add-recipe workflow with 6 steps (no acknowledge, no notify)"
    - path: "src/robotina/agent/agents.py"
      provides: "AGENT_REGISTRY with no acknowledge-add-recipe; V005 prompt path for handle-incoming-message"
  key_links:
    - from: "src/robotina/queue/jobs.py (handle-incoming-message branch)"
      to: "RespondTool, TerminateTool, StartWorkflowTool, HouseholdManagerApiTool"
      via: "per-job tool injection"
      pattern: "RespondTool\\(|TerminateTool\\("
    - from: "src/robotina/queue/workflow_runner.py::on_step_failed"
      to: "_check_and_dispatch_wake"
      via: "try/except log-only"
      pattern: "_check_and_dispatch_wake"
---

<objective>
The load-bearing coupled deletion plan. Per D-06 (PITFALL 10), every reference to `acknowledge-add-recipe` AND the `notify` workflow step AND the dead-letter `send-notification` block AND `QueueTool` is removed in lockstep, with the new tool injection (`RespondTool` + `TerminateTool`) wired in jobs.py and `handle-incoming-message` bumped to `V005.md`.

Purpose: Single-commit-per-logical-group deletion to avoid orphan dependencies. Repo grep gate at end. Single-recipe regression smoke updated.

CRITICAL non-deletions (do NOT touch):
- `if task_type == "send-notification":` branch in jobs.py:90 STAYS (D-07 — RespondTool's delivery).
- Constructor-injected `invocation_id` / `conversation_id` / `household_id` on tools STAYS.
- `_check_and_dispatch_wake` itself STAYS; only the dead-letter block inside `on_step_failed` is deleted.
- `SendNotificationInput` in task_types.py STAYS (RespondTool uses it).
- V005.md prompt file is CREATED in plan 21-05; this plan only updates the agents.py `prompt_path` to point at it (the path string can land before the file exists; plan 21-05 lands the file content before merge).

Output: Codebase with the new tool surface live and all legacy artifacts gone; grep gate passes; existing happy-path test passes against the new step list.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md
@src/robotina/queue/jobs.py
@src/robotina/agent/agents.py
@src/robotina/agent/workflows.py
@src/robotina/queue/workflow_runner.py
@src/robotina/queue/task_types.py
@overrides/anthropic.json
@overrides/openai.json
@overrides/staging.ollama.json

<interfaces>
Existing tool injection for handle-incoming-message (jobs.py lines ~191-280, current state):
- Imports `QueueTool` and instantiates with chat_id/user_id/platform/household_id.
- Imports `StartWorkflowTool` and instantiates with invocation_id/conversation_id/household_id (Phase 18 D-13).
- Imports `HouseholdManagerApiTool` and instantiates.

Target (Phase 21):
- Import `RespondTool` (from robotina.agent.tools.respond) — same constructor args as QueueTool today.
- Import `TerminateTool` (from robotina.agent.tools.terminate) — no constructor args.
- Keep `StartWorkflowTool` (already refactored in plan 21-03; same constructor signature).
- Keep `HouseholdManagerApiTool`.
- DELETE: QueueTool import + instantiation.

acknowledge-add-recipe elif branch in jobs.py (~line 315 onwards) — DELETE entire elif clause.

workflows.py WORKFLOW_REGISTRY['add-recipe'] target step order:
1. gather
2. instructions
3. ingredients
4. metadata
5. load
6. finalize-outcome
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap tool injection in jobs.py and delete acknowledge-add-recipe elif</name>
  <files>src/robotina/queue/jobs.py</files>
  <read_first>
    - src/robotina/queue/jobs.py (entire file — line 90 send-notification branch, lines ~134-280 handle-incoming-message branch, lines ~315 onwards acknowledge-add-recipe elif)
    - src/robotina/agent/tools/respond.py (plan 21-01 — constructor signature)
    - src/robotina/agent/tools/terminate.py (plan 21-02)
    - src/robotina/agent/tools/start_workflow.py (post plan 21-03)
  </read_first>
  <behavior>
    - `handle-incoming-message` branch injects: RespondTool(chat_id, user_id, platform, household_id), TerminateTool(), StartWorkflowTool(invocation_id, conversation_id, household_id), HouseholdManagerApiTool().
    - QueueTool import and all its instantiation lines are deleted from this file.
    - The `elif task_type == "acknowledge-add-recipe":` block is deleted entirely (including the QueueTool import inside it).
    - The `if task_type == "send-notification":` branch at line 90 is UNTOUCHED (D-07).
    - Any comments / docstrings referring to "QueueTool", "acknowledge-add-recipe", or "V004" within this file are updated to reflect the new state (or deleted if they only described retired behavior).
  </behavior>
  <action>
    Edit `src/robotina/queue/jobs.py`:

    1. In the `handle-incoming-message` branch (around lines 134-280):
       - Replace `from robotina.agent.tools.queue import QueueTool` with two imports:
         ```python
         from robotina.agent.tools.respond import RespondTool
         from robotina.agent.tools.terminate import TerminateTool
         ```
       - Replace each `tools.append(QueueTool(...))` call with both:
         ```python
         tools.append(RespondTool(
             chat_id=...,
             user_id=...,
             platform=...,
             household_id=...,
         ))
         tools.append(TerminateTool())
         ```
         using the SAME chat_id/user_id/platform/household_id values that the QueueTool call was using.
       - Update inline comments that referenced "V004 instructs..." to mention V005 (or just delete the comment if it's now obsolete).
       - Per D-10: agents.py prompt_path bumps to V005 — verify the comment block in jobs.py doesn't claim "V004"; if it does, update to V005.

    2. Delete the `elif task_type == "acknowledge-add-recipe":` block entirely (starts ~line 315, runs ~50-80 lines). Include the inner `from robotina.agent.tools.queue import QueueTool` import inside that branch.

    3. DO NOT touch the `if task_type == "send-notification":` branch around line 90. Verify after edits with: `grep -A2 'task_type == "send-notification"' src/robotina/queue/jobs.py` — it MUST still be present and unchanged.

    4. DO NOT touch the StartWorkflowTool injection — Phase 18 D-13 invocation_id constructor injection stays exactly as-is. The tool itself was refactored in plan 21-03 to have new args_schema, but the construction call in jobs.py is unchanged.

    5. Add a docstring comment near the handle-incoming-message branch: `# Per Phase 21 D-01/D-02/D-05: QueueTool retired; RespondTool + TerminateTool replace it.`

    Reference D-06 / D-07 in commit message for traceability.
  </action>
  <acceptance_criteria>
    - `grep -c "QueueTool" src/robotina/queue/jobs.py` == 0
    - `grep -c "RespondTool" src/robotina/queue/jobs.py` >= 1
    - `grep -c "TerminateTool" src/robotina/queue/jobs.py` >= 1
    - `grep -c "acknowledge-add-recipe" src/robotina/queue/jobs.py` == 0
    - `grep -c 'task_type == "send-notification"' src/robotina/queue/jobs.py` >= 1 (D-07 — send-notification branch preserved)
    - `grep -c "StartWorkflowTool" src/robotina/queue/jobs.py` >= 1
    - `uv run python -c "import robotina.queue.jobs"` exits 0 (file imports clean)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import robotina.queue.jobs" && grep -c "QueueTool\|acknowledge-add-recipe" src/robotina/queue/jobs.py</automated>
  </verify>
  <done>jobs.py uses new tool surface; acknowledge-add-recipe branch gone; send-notification branch intact; module imports without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete acknowledge-add-recipe from AGENT_REGISTRY + bump handle-incoming-message to V005</name>
  <files>src/robotina/agent/agents.py</files>
  <read_first>
    - src/robotina/agent/agents.py (entire file — line 84 V004 path for handle-incoming-message, line 180+ acknowledge-add-recipe entry)
  </read_first>
  <behavior>
    - The `"acknowledge-add-recipe": AgentConfig(...)` dict entry (around lines 180-195) is fully deleted.
    - The `handle-incoming-message` entry's `prompt_path` changes from `.../robotina/V004.md` to `.../robotina/V005.md`.
    - All other entries unchanged.
    - File imports cleanly.
  </behavior>
  <action>
    Edit `src/robotina/agent/agents.py`:

    1. Delete the entire `"acknowledge-add-recipe": AgentConfig(...)` dict entry (including the trailing comma — clean dict literal).
    2. Update line 84 (handle-incoming-message's prompt_path) from `"src/robotina/agent/prompts/robotina/V004.md"` to `"src/robotina/agent/prompts/robotina/V005.md"`. Per D-10.
    3. Update the inline comment on line 86 `# HouseholdManagerApiTool, QueueTool, StartWorkflowTool injected per-job` to: `# HouseholdManagerApiTool, RespondTool, TerminateTool, StartWorkflowTool injected per-job in run_task() — per Phase 21 D-01/D-02`.
    4. Do NOT delete any other registry entry.

    Reference D-06 / D-10 in commit message.
  </action>
  <acceptance_criteria>
    - `grep -c "acknowledge-add-recipe" src/robotina/agent/agents.py` == 0
    - `grep -c "robotina/V005.md" src/robotina/agent/agents.py` >= 1
    - `grep -c "robotina/V004.md" src/robotina/agent/agents.py` == 0
    - `uv run python -c "from robotina.agent.agents import AGENT_REGISTRY; assert 'acknowledge-add-recipe' not in AGENT_REGISTRY; assert 'handle-incoming-message' in AGENT_REGISTRY; assert AGENT_REGISTRY['handle-incoming-message'].prompt_path.endswith('V005.md')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.agent.agents import AGENT_REGISTRY; assert 'acknowledge-add-recipe' not in AGENT_REGISTRY and AGENT_REGISTRY['handle-incoming-message'].prompt_path.endswith('V005.md')"</automated>
  </verify>
  <done>AGENT_REGISTRY has no acknowledge entry; handle-incoming-message points at V005.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete acknowledge + notify steps from add-recipe workflow</name>
  <files>src/robotina/agent/workflows.py</files>
  <read_first>
    - src/robotina/agent/workflows.py (entire file — WORKFLOW_REGISTRY['add-recipe'] structure, step list at lines 102-167)
  </read_first>
  <behavior>
    - `WORKFLOW_REGISTRY["add-recipe"].steps` after edit is exactly 6 entries in this order: gather, instructions, ingredients, metadata, load, finalize-outcome.
    - No `step_key="acknowledge"` entry remains.
    - No `step_key="notify"` entry remains.
    - finalize-outcome step is unchanged (Phase 20 D-02 placement at the tail preserved).
    - Comments referring to the deleted steps are removed or updated.
  </behavior>
  <action>
    Edit `src/robotina/agent/workflows.py`:

    1. Delete the WorkflowStep entry with `step_key="acknowledge"` (around line 102 — typically a few-line WorkflowStep(...) literal).
    2. Delete the WorkflowStep entry with `step_key="notify"` (around line 157).
    3. Verify the remaining order is: gather → instructions → ingredients → metadata → load → finalize-outcome.
    4. Update any module-level comment block describing the workflow's step order (if present) to reflect the new 6-step list.

    Reference D-06 in commit message.
  </action>
  <acceptance_criteria>
    - `grep -c 'step_key="acknowledge"' src/robotina/agent/workflows.py` == 0
    - `grep -c "step_key='acknowledge'" src/robotina/agent/workflows.py` == 0
    - `grep -c 'step_key="notify"' src/robotina/agent/workflows.py` == 0
    - `grep -c "step_key='notify'" src/robotina/agent/workflows.py` == 0
    - `grep -c 'step_key="finalize-outcome"\|step_key=.finalize-outcome.' src/robotina/agent/workflows.py` >= 1
    - `uv run python -c "from robotina.agent.workflows import WORKFLOW_REGISTRY; keys = [s.step_key for s in WORKFLOW_REGISTRY['add-recipe'].steps]; assert keys == ['gather','instructions','ingredients','metadata','load','finalize-outcome'], keys"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.agent.workflows import WORKFLOW_REGISTRY; keys=[s.step_key for s in WORKFLOW_REGISTRY['add-recipe'].steps]; assert keys==['gather','instructions','ingredients','metadata','load','finalize-outcome'], keys"</automated>
  </verify>
  <done>add-recipe workflow has exactly 6 steps in the documented order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Delete AcknowledgeAddRecipeInput from task_types.py</name>
  <files>src/robotina/queue/task_types.py</files>
  <read_first>
    - src/robotina/queue/task_types.py (entire file — locate AcknowledgeAddRecipeInput class)
    - Verify SendNotificationInput is present and untouched (D-07)
  </read_first>
  <behavior>
    - `AcknowledgeAddRecipeInput` class is gone from task_types.py.
    - `SendNotificationInput` class is UNCHANGED (D-07).
    - `AddRecipeQueryInput` (added in plan 21-03) is still present.
    - File imports cleanly.
  </behavior>
  <action>
    Edit `src/robotina/queue/task_types.py`:
    - Delete the entire `class AcknowledgeAddRecipeInput(...)` block.
    - Do NOT touch SendNotificationInput.
    - Do NOT touch AddRecipeQueryInput.

    If any other module imports AcknowledgeAddRecipeInput, those imports must be removed (the global grep gate in task 9 catches them).
  </action>
  <acceptance_criteria>
    - `grep -c "AcknowledgeAddRecipeInput" src/robotina/queue/task_types.py` == 0
    - `grep -c "class SendNotificationInput" src/robotina/queue/task_types.py` == 1 (D-07)
    - `grep -c "class AddRecipeQueryInput" src/robotina/queue/task_types.py` == 1
    - `uv run python -c "from robotina.queue.task_types import SendNotificationInput, AddRecipeQueryInput"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from robotina.queue.task_types import SendNotificationInput, AddRecipeQueryInput; import robotina.queue.task_types as t; assert not hasattr(t, 'AcknowledgeAddRecipeInput')"</automated>
  </verify>
  <done>AcknowledgeAddRecipeInput deleted; other input models intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Delete dead-letter block in workflow_runner.on_step_failed</name>
  <files>src/robotina/queue/workflow_runner.py</files>
  <read_first>
    - src/robotina/queue/workflow_runner.py (entire file — focus on `on_step_failed` function around line 556; dead-letter block roughly lines 650-745)
    - .planning/phases/20-wake-rule-outcome-plumbing/20-CONTEXT.md (Phase 20 D-05 set this up as a fallback; Phase 21 D-08 reverses it)
  </read_first>
  <behavior>
    - The fallback `send-notification` enqueue block inside `on_step_failed` is deleted entirely.
    - The try/except wrapping `_check_and_dispatch_wake` is RETAINED, but the except branch becomes log-only (no fallback enqueue).
    - The "FAILED status flip + commit" logic is RETAINED.
    - The call to `_check_and_dispatch_wake` is RETAINED.
    - Any reply_context-keyed dead-letter logic is RETAINED only as logging if it was outside the dead-letter enqueue block (otherwise deleted).
    - Doc comments referencing "dead-letter" are removed.
  </behavior>
  <action>
    Edit `src/robotina/queue/workflow_runner.py::on_step_failed`:

    1. Locate the dead-letter block. Per grep at planning time, the structure is:
       ```python
       try:
           _check_and_dispatch_wake(...)
       except Exception:
           logger.exception("Wake dispatch failed; falling back to dead-letter notify | run_id=%s", run_id)
           # dead-letter send-notification enqueue block follows (lines ~700-745)
           ...
           meta={"task_type": "send-notification"}
           ...
       ```

    2. Replace the entire except branch body with:
       ```python
       except Exception:
           logger.exception("Wake dispatch failed in on_step_failed | run_id=%s", run_id)
           # Phase 21 D-08: dead-letter fallback removed. Wake-respond path supersedes
           # transitional fallback. Reconciler (Phase 20 D-11) handles structural
           # wake-enqueue failures.
       ```

    3. Delete the entire dead-letter enqueue block (lines ~700-745 in current state) — anything that built a `send-notification` job inside on_step_failed. This includes:
       - The `meta={"task_type": "send-notification"}` enqueue.
       - The reply_context-based message-build code (if it was used only for dead-letter).
       - Any "Workflow failed without reply_context; skipping dead-letter" log line (no longer reachable).

    4. The docstring on `on_step_failed` (around line 570) currently says "...a `send-notification` job at the front of the queue with a Spanish apology..." — UPDATE it to reflect Phase 21 reality: the function now (a) marks WorkflowRun FAILED, (b) calls _check_and_dispatch_wake, (c) commits, with the wake-respond path handling user-facing apologies via V005.

    5. Reference D-08 + "supersedes Phase 20 D-05" in commit message.
  </action>
  <acceptance_criteria>
    - `grep -c 'send-notification' src/robotina/queue/workflow_runner.py` <= 1 (only allowed match: docstring referencing the historical pattern; if no match remains, even better)
    - `grep -c "dead-letter" src/robotina/queue/workflow_runner.py` == 0
    - `grep -c "_check_and_dispatch_wake" src/robotina/queue/workflow_runner.py` >= 1 (wake helper call preserved)
    - `grep -c "def on_step_failed" src/robotina/queue/workflow_runner.py` == 1 (function still exists)
    - `uv run python -c "import robotina.queue.workflow_runner"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import robotina.queue.workflow_runner" && test "$(grep -c dead-letter src/robotina/queue/workflow_runner.py)" -eq 0</automated>
  </verify>
  <done>Dead-letter block deleted; wake-helper call preserved with log-only except; on_step_failed simpler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Delete QueueTool file + acknowledge-add-recipe prompt directory</name>
  <files>src/robotina/agent/tools/queue.py, src/robotina/agent/prompts/acknowledge-add-recipe/</files>
  <read_first>
    - src/robotina/agent/tools/queue.py (confirm contents)
    - src/robotina/agent/prompts/acknowledge-add-recipe/ (list contents — typically V002.md)
  </read_first>
  <behavior>
    - `src/robotina/agent/tools/queue.py` file is deleted.
    - `src/robotina/agent/prompts/acknowledge-add-recipe/` directory is deleted (with all its contents).
    - No other files in `src/robotina/agent/tools/` or `src/robotina/agent/prompts/` are touched.
  </behavior>
  <action>
    Use git to delete the file and directory cleanly:

    ```bash
    git rm src/robotina/agent/tools/queue.py
    git rm -r src/robotina/agent/prompts/acknowledge-add-recipe/
    ```

    If git rm is unavailable, use plain rm and confirm tracking changes. Reference D-05 + D-06 in commit message.

    BEFORE deleting queue.py, run `grep -rln "from robotina.agent.tools.queue import\|QueueTool" src/ tests/` and verify no remaining imports exist (task 1 already cleared jobs.py; nothing else should import it). If there are stragglers, fix them in this task too.
  </action>
  <acceptance_criteria>
    - `test -f src/robotina/agent/tools/queue.py` returns non-zero (file gone)
    - `test -d src/robotina/agent/prompts/acknowledge-add-recipe` returns non-zero (directory gone)
    - `grep -rln "from robotina.agent.tools.queue\|QueueTool" src/ tests/` returns zero matches
    - `uv run python -c "import robotina.agent.tools"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/robotina/agent/tools/queue.py && test ! -d src/robotina/agent/prompts/acknowledge-add-recipe && ! grep -rln "QueueTool" src/ tests/</automated>
  </verify>
  <done>QueueTool file and acknowledge-add-recipe prompt directory removed from the working tree and git.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Remove acknowledge-add-recipe entries from overrides/*.json (all 3 files)</name>
  <files>overrides/anthropic.json, overrides/openai.json, overrides/staging.ollama.json</files>
  <read_first>
    - overrides/anthropic.json
    - overrides/openai.json
    - overrides/staging.ollama.json
  </read_first>
  <behavior>
    - Each of the three override files has its top-level `acknowledge-add-recipe` key (and the entire value object) removed.
    - JSON remains valid (trailing-comma clean).
    - No other keys are modified.
    - After this task, the set of keys in each file == `set(AGENT_REGISTRY.keys())` (which excludes acknowledge-add-recipe after task 2).
  </behavior>
  <action>
    For each of the three files:
    1. Parse the JSON.
    2. Remove the `"acknowledge-add-recipe"` top-level key and its value object.
    3. Write back with consistent formatting (2-space indent, trailing newline — match the file's existing style).

    Use jq or a small Python script. Do NOT hand-edit if it risks JSON corruption. Reference D-06 + memory `feedback_overrides_in_sync` in the commit message.

    Suggested approach:
    ```bash
    for f in overrides/anthropic.json overrides/openai.json overrides/staging.ollama.json; do
      jq 'del(."acknowledge-add-recipe")' "$f" > "$f.tmp" && mv "$f.tmp" "$f"
    done
    ```
  </action>
  <acceptance_criteria>
    - `grep -l "acknowledge-add-recipe" overrides/` returns nothing (zero matches across all three files)
    - All three JSON files still parse: `python -c "import json; [json.load(open(f)) for f in ['overrides/anthropic.json','overrides/openai.json','overrides/staging.ollama.json']]"` exits 0
    - `python -c "import json; from robotina.agent.agents import AGENT_REGISTRY; [exec('assert set(json.load(open(f)).keys()) == set(AGENT_REGISTRY.keys()), f') for f in ['overrides/anthropic.json','overrides/openai.json','overrides/staging.ollama.json']]"` exits 0 (each override matches AGENT_REGISTRY)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import json; from robotina.agent.agents import AGENT_REGISTRY; [(__import__('sys').exit(1) if set(json.load(open(f)).keys()) != set(AGENT_REGISTRY.keys()) else None) for f in ['overrides/anthropic.json','overrides/openai.json','overrides/staging.ollama.json']]"</automated>
  </verify>
  <done>All three overrides files have acknowledge-add-recipe gone; keys match AGENT_REGISTRY exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Update regression tests for new 6-step workflow + new tool injection</name>
  <files>tests/test_workflows.py, tests/test_workflow_runner.py</files>
  <read_first>
    - tests/test_workflows.py (locate the add-recipe happy-path test; locate any assertion about step count / step keys / acknowledge / notify)
    - tests/test_workflow_runner.py (locate dead-letter assertions per Phase 20 — Phase 21 simplifies them)
  </read_first>
  <behavior>
    - tests/test_workflows.py: any test asserting the add-recipe workflow step list updates to assert the new 6-step order (gather, instructions, ingredients, metadata, load, finalize-outcome). Per D-19.
    - tests/test_workflows.py: any test referencing acknowledge-add-recipe agent / notify step / AcknowledgeAddRecipeInput is updated or removed.
    - tests/test_workflow_runner.py: tests that asserted "dead-letter fires under wake-failure" become "wake-helper exception is logged and swallowed; no fallback enqueue" (D-08, mirrors Discretion note in CONTEXT.md).
    - All tests pass after edits.
  </behavior>
  <action>
    Locate and edit:

    1. tests/test_workflows.py:
       - Find the existing happy-path test for add-recipe (likely `test_add_recipe_happy_path` or similar — grep for `add-recipe` in the file).
       - Update step-list assertions to `["gather", "instructions", "ingredients", "metadata", "load", "finalize-outcome"]`.
       - Delete or update any test asserting acknowledge / notify step presence.
       - If a test asserted acknowledge agent invocation or QueueTool injection for the acknowledge branch, delete that test.

    2. tests/test_workflow_runner.py:
       - Find dead-letter-related tests (Phase 20 D-05 / 20-03 deviation note mentions them).
       - For each: rewrite to assert (a) when wake-helper raises, exception is logged (use caplog), (b) NO send-notification enqueue happens, (c) FAILED status is still committed.
       - Mock _check_and_dispatch_wake to raise; assert no Queue interactions related to a fallback notification.

    3. Run the affected test files after edits: `uv run pytest tests/test_workflows.py tests/test_workflow_runner.py -v`.

    4. If any other test imports from deleted modules (e.g. `from robotina.agent.tools.queue import QueueTool` or `AcknowledgeAddRecipeInput`), remove or update. Run full test discovery: `uv run pytest --collect-only -q` and fix any collection errors.

    Reference D-19 + D-25 + D-08 in commit message.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_workflows.py tests/test_workflow_runner.py -v` passes
    - `grep -c "acknowledge-add-recipe\|AcknowledgeAddRecipeInput\|QueueTool" tests/test_workflows.py tests/test_workflow_runner.py` == 0
    - `grep -c "dead-letter\|dead_letter" tests/test_workflow_runner.py` == 0
    - At least one test in tests/test_workflows.py asserts the 6-step ordered list explicitly: `grep -c "finalize-outcome" tests/test_workflows.py` >= 1
    - `uv run pytest --collect-only -q 2>&1 | grep -c "error"` == 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_workflows.py tests/test_workflow_runner.py -v</automated>
  </verify>
  <done>Regression tests reflect the new 6-step workflow and the simplified on_step_failed; full collection succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: Repo-wide grep gate + full test sweep</name>
  <files>(no file edits — verification only)</files>
  <read_first>
    - All tasks 1-8 must have landed.
  </read_first>
  <behavior>
    - `grep -rn "acknowledge-add-recipe" src/ tests/ overrides/ experiments/` returns ZERO hits.
    - `grep -rn "QueueTool" src/ tests/` returns ZERO hits.
    - `grep -rn "AcknowledgeAddRecipeInput" src/ tests/` returns ZERO hits.
    - `grep -rn "step_key=.acknowledge.\|step_key=.notify." src/ tests/` returns ZERO hits.
    - Full pytest run passes: `uv run pytest`.
    - Module import smoke: `uv run python -c "import robotina.queue.jobs; import robotina.agent.agents; import robotina.agent.workflows; import robotina.queue.workflow_runner; import robotina.queue.task_types"` exits 0.
  </behavior>
  <action>
    Run the four grep gates and the test sweep. If any returns non-zero hits / failures, STOP and surface the remaining offenders for a follow-up edit in tasks 1-8 of this plan (do not punt to a later plan — Phase 21 D-06 says single-PR removes everything).

    The grep gate is the load-bearing exit criterion for the PITFALL 10 mitigation per ROADMAP success-criterion #3.
  </action>
  <acceptance_criteria>
    - `! grep -rn "acknowledge-add-recipe" src/ tests/ overrides/ experiments/` (no matches)
    - `! grep -rn "QueueTool" src/ tests/` (no matches)
    - `! grep -rn "AcknowledgeAddRecipeInput" src/ tests/` (no matches)
    - `! grep -rn 'step_key=.acknowledge.' src/ tests/` (no matches)
    - `! grep -rn 'step_key=.notify.' src/ tests/` (no matches)
    - `uv run pytest` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -rn "acknowledge-add-recipe\|QueueTool\|AcknowledgeAddRecipeInput" src/ tests/ overrides/ && uv run pytest -q</automated>
  </verify>
  <done>All grep gates pass; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → run_task | User message enters; new RespondTool path delivers reply via queue (PITFALL 13 mitigation) |
| LLM → tool surface | LLM emits tool calls; engine-enforced termination via TerminateTool (PITFALL 4 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T (tampering) | jobs.py tool injection swap | mitigate | Grep gate ensures no QueueTool import survives; unit + regression tests pin new injection shape. |
| T-21-02 | I (information disclosure) | LLM trailing free text after last tool call | mitigate | TerminateTool return_direct=True + V005 prompt rule (plan 21-05) + existing _extract_task_output strict path catches violations. PITFALL 4. |
| T-21-03 | D (denial of service) | Dead-letter block removal | accept | Phase 20 D-11 reconciler structurally catches wake-enqueue failures; V005 prompt teaches Robotina to respond on outcome.status="failure". Acceptable degradation: a transient wake failure produces a logged exception with no user-visible apology (extremely rare). |
| T-21-04 | E (elevation) | overrides drift from AGENT_REGISTRY | mitigate | Plan 21-06 ships the bidirectional CI guard test. |
</threat_model>

<verification>
- All 9 tasks complete, grep gates clean, full pytest green.
- send-notification task_type branch (D-07) preserved.
- WorkflowRun add-recipe step list is exactly the documented 6-key order.
</verification>

<success_criteria>
- Phase 21 ROADMAP success-criterion #3 verbatim: "grep for acknowledge-add-recipe / QueueTool returns zero hits in src/, tests/, overrides/, experiments/".
- ROADMAP success-criterion #4: add-recipe workflow has 6 steps in order.
- ROADMAP success-criterion #5: dead-letter block removed; on_step_failed simpler.
- TOOLS-02, TOOLS-03, TOOLS-04, TOOLS-05 delivered.
</success_criteria>

<output>
Write `.planning/phases/21-tool-surface-flip-remove-acknowledge-notify/21-04-SUMMARY.md` documenting every file deleted/modified, with a final grep-gate confirmation.
</output>
