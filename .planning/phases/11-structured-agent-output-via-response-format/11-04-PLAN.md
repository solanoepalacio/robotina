---
phase: 11
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - .planning/decisions/response-format-adoption.md
  - .planning/STATE.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md
autonomous: false
requirements:
  - RRECIPE-07
  - RLOAD-07
  - WF-10
must_haves:
  truths:
    - "Decision record under .planning/decisions/ explains response_format adoption rationale, the Ollama AutoStrategy trap, and the 5 named agents in scope"
    - "STATE.md Locked Decisions section gains Phase 11 entries (per-provider Strategy mapping, non-overridable response_format_model, removed free-text parse ladder)"
    - "CLAUDE.md stack table notes that response_format is in active use on 5 agents and references the decision record"
    - "11-VERIFICATION.md contains the 3-query manual checkpoint template with explicit pass/fail criteria and LangWatch tag verification"
    - "RRECIPE-07, RLOAD-07, WF-10 flip from In Progress to Complete in REQUIREMENTS.md ONLY after manual checkpoint approval"
  artifacts:
    - path: ".planning/decisions/response-format-adoption.md"
      provides: "Phase 11 decision record mirroring agent-12-migrate-to-create-agent.md format"
      contains: "ToolStrategy"
    - path: ".planning/STATE.md"
      provides: "Updated Locked Decisions section with Phase 11 entries + roadmap evolution note"
    - path: "CLAUDE.md"
      provides: "Stack-table note + What NOT to Use update for response_format"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RRECIPE-07, RLOAD-07, WF-10 marked Complete after manual sign-off"
    - path: ".planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md"
      provides: "Manual 3-query checkpoint template + sign-off block"
  key_links:
    - from: ".planning/decisions/response-format-adoption.md"
      to: "CLAUDE.md stack table"
      via: "Cross-reference in the 'What NOT to Use' / 'Recommended Stack' rows"
      pattern: "response-format-adoption.md"
    - from: "STATE.md Locked Decisions"
      to: ".planning/decisions/response-format-adoption.md"
      via: "Decision record reference"
      pattern: "Phase 11"
---

<objective>
Close out Phase 11 with documentation + the manual 3-query end-to-end checkpoint. Create a decision record mirroring `agent-12-migrate-to-create-agent.md`. Update STATE.md Locked Decisions to capture the Phase 11 choices. Update CLAUDE.md stack table notes. Run the manual 3-query Telegram end-to-end checkpoint and record the result in `11-VERIFICATION.md`. After user approval, flip RRECIPE-07 / RLOAD-07 / WF-10 from In Progress → Complete in REQUIREMENTS.md.

Purpose: Phase 11's success criteria #3 and #4 in ROADMAP.md are end-to-end verification gates — only the user can sign off on them. This plan packages the docs work that any executor can do autonomously (decision record, STATE.md, CLAUDE.md, VERIFICATION.md template) AND defers the requirement-flip until after the gated checkpoint. Without this gating, the requirement table would lie about Phase 11 completion before real verification happened.

Output: Decision record committed; STATE.md and CLAUDE.md cross-reference it; VERIFICATION.md captures the manual checkpoint's outcome; REQUIREMENTS.md ticks the three new entries Complete (post-checkpoint).
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/decisions/agent-12-migrate-to-create-agent.md
@.planning/phases/11-structured-agent-output-via-response-format/11-CONTEXT.md
@.planning/phases/11-structured-agent-output-via-response-format/11-RESEARCH.md
@.planning/phases/11-structured-agent-output-via-response-format/11-01-PLAN.md
@.planning/phases/11-structured-agent-output-via-response-format/11-02-PLAN.md
@.planning/phases/11-structured-agent-output-via-response-format/11-03-PLAN.md

<interfaces>
<!-- Decision record format precedent — mirror this structure. -->
<!-- File: .planning/decisions/agent-12-migrate-to-create-agent.md -->
<!-- Sections: Context, What X actually buys us, Why those benefits don't apply here, Proposed change, ... -->

<!-- Locked Decisions in STATE.md follow this format: -->
<!-- - [Phase NN-name]: Decision text -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Write decision record + update STATE.md + update CLAUDE.md + draft VERIFICATION.md template</name>
  <files>.planning/decisions/response-format-adoption.md, .planning/STATE.md, CLAUDE.md, .planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md</files>
  <read_first>
    - .planning/decisions/agent-12-migrate-to-create-agent.md (format precedent — read the whole file before drafting)
    - .planning/STATE.md (find the "Locked Decisions" section + Roadmap Evolution block)
    - CLAUDE.md (find the Recommended Stack table + What NOT to Use table)
  </read_first>
  <action>
    **Step 1: Write the decision record** at `.planning/decisions/response-format-adoption.md`. Mirror the structure of `agent-12-migrate-to-create-agent.md` (sections: Context / What … actually buys us / Why those benefits don't apply here / Proposed change / Alternatives / Risks / Verification).

    Concrete content:

    ```markdown
    # Decision: Adopt response_format on the 5 artifact-producing agents

    **Status:** Adopted (Phase 11)
    **Date:** 2026-05-13

    ## Context

    The 2026-05-13 canelones-de-choclo incident exposed a structural weakness in
    Robotina's artifact-extraction pipeline. The recipe-research-gather agent
    emitted valid recipe JSON wrapped in a prose preamble + ```json fence + a
    closing remark. `_extract_task_output` in
    `src/robotina/queue/workflow_runner.py` ran a four-step fallback ladder —
    `content.strip("```")`, `json.loads(content)`, scan-for-first-`{`,
    `json.loads(content[idx:])` — and still could not recover the JSON. The
    workflow dead-lettered 5 downstream steps and the user got a Spanish apology
    instead of the recipe.

    The same parse ladder lives in every other artifact-producing agent
    (recipe-research-instructions, recipe-research-ingredients,
    recipe-research-metadata, recipe-load). Any one of them can emit
    prose-wrapped JSON on any run, and the failure mode is silent — the
    workflow runner cannot tell "agent emitted garbage" from "agent emitted
    legitimate output the parser couldn't read."

    LangChain 1.x ships `response_format=` on `langchain.agents.create_agent`,
    which populates `state["structured_response"]` with a Pydantic instance
    instead of relying on free-text content. Phase 10 (`AGENT-12`) unblocked
    this API surface — Phase 11 puts it to work.

    ## What the free-text parse ladder actually buys us

    - It works most of the time. Defensive scanning recovered ~99% of well-formed
      LLM outputs across Phases 6–10.
    - It is provider-agnostic — every backend (Ollama, Anthropic, OpenAI) emits
      the same free-text shape, so one parser covers all three.
    - No new dependencies. The schema lives in `task_types.py`; the parser is
      pure Python.

    ## Why those benefits don't apply here

    - The ~1% prose-wrap failure rate is not a tail risk; in production it
      manifested as a dead-lettered workflow within a week of Phase 9 ship.
      Every additional recipe query is another lottery ticket.
    - LangChain 1.x natively wraps the schema concern. `ProviderStrategy` uses
      OpenAI/Anthropic strict mode (token-level enforcement); `ToolStrategy`
      uses a synthesized emit tool whose args are validated against the Pydantic
      schema (tool-call-level enforcement on Ollama). Both replace the parser
      with an enforcement guarantee — the agent CAN'T emit prose-wrapped JSON
      because the channel that carries the artifact is not free text.
    - The schemas already exist (`RecipeResearchGatherOutput`,
      `RecipeResearchInstructionsOutput`, `RecipeResearchIngredientsOutput`,
      `RecipeResearchMetadataOutput`, `RecipeLoadOutput` in
      `src/robotina/queue/task_types.py`). Binding them costs one field on
      `AgentConfig`, one kwarg on `LLMBackend.create_agent`, and one branch on
      `_extract_task_output`.

    ## Proposed change

    1. **Per-provider Strategy mapping.** Ollama → `ToolStrategy`. Anthropic /
       OpenAI → `ProviderStrategy`. Strategy is selected INSIDE each
       `LLMBackend.create_agent()` adapter — `run_task` passes a raw Pydantic
       class, the adapter wraps it.

       **Why explicit per-provider mapping.** `gpt-oss` is in
       `FALLBACK_MODELS_WITH_STRUCTURED_OUTPUT`
       (`langchain/agents/factory.py:148–158`), so AutoStrategy on Ollama
       resolves to ProviderStrategy and calls
       `model.bind_tools(strict=True, response_format={...})` on ChatOllama —
       which Ollama does not honor. ToolStrategy is the correctness path on
       Ollama, not an aesthetic preference. Anthropic and OpenAI have
       provider-native strict-schema support, so ProviderStrategy is correct
       there.

    2. **`AgentConfig.response_format_model: type[BaseModel] | None = None`.**
       New optional field. NOT overridable via `AGENT_OVERRIDES_FILEPATH` —
       schema is a code contract, not config. Populated for the 5 named
       artifact-producing agents.

    3. **Workflow-runner refactor.** `_extract_task_output` reads
       `result["structured_response"]` when the executing agent has
       `response_format_model` bound; raises `ValueError` on missing. The
       prose-strip / markdown-fence / first-`{`-scan ladder is removed. The
       `return_direct` tool-message branch (Phase 07.1, used by
       handle-incoming-message and acknowledge-add-recipe) is preserved.

    4. **Prompt bumps.** 5 new prompt versions (V003 for gather; V002 for the
       other four) strip "respond with valid JSON only" / schema-mirroring
       boilerplate — schema is now token-enforced; instructing the model to
       also "respond with JSON" can confuse the model into wrapping the
       structured emit in extra prose or double-emitting.

    ## Alternatives considered

    - **Tighten the existing parser (e.g. add a more aggressive prose-strip
      heuristic).** Rejected: every heuristic addition is another lottery
      ticket, not a closed loop. Token-level / tool-arg-level enforcement is
      categorically stronger.
    - **Centralize Strategy selection in `run_task` based on
      `config.model_config["provider"]`.** Rejected: the adapter already knows
      its provider; centralizing the decision in `jobs.py` leaks provider
      knowledge into the caller and creates two-place coupling.
    - **Add `response_format_model` to the overridable fields in
      `get_agent_config`.** Rejected: an override file could break artifact
      extraction silently. Schema is a code contract — the override system
      should not touch it.

    ## Risks

    - **Ollama 500 "error parsing tool call" on the synthesized emit tool.**
      Existing risk class (Phase 10 documented `_RetryingChatOllama` for this).
      With ToolStrategy adding a structured-emit tool whose args are large,
      gpt-oss has more opportunity to emit malformed tool-call JSON. Mitigation:
      the retry wrapper already covers this path (the emit-tool call is a
      normal `model.invoke` step). If retries are exhausted the existing
      FAILED-step + dead-letter flow runs — same behavior as today.
    - **OpenAI strict mode rejects `additionalProperties: true`.**
      `RecipeResearchGatherOutput.recipes: list[dict]` is intentionally loose;
      if the OpenAI override is ever activated for gather, the API will 400 at
      first invoke. Inert under Ollama-only deployment. Documented in
      Phase 11 RESEARCH.md Pitfall 2.
    - **Schema validation failure raises late, not at construction.**
      `create_agent(response_format=BadSchema)` does not raise at build time;
      a typo'd schema only surfaces at first invoke. The 5 schemas are
      battle-tested; mitigation is documented but no construction-time guard
      added (out of phase scope).

    ## Verification

    - Unit: `tests/test_llm_backend.py` (5 tests, adapter Strategy-wrap);
      `tests/test_agents.py` (10 tests, registry bindings + non-overridability);
      `tests/test_workflow_runner.py` (4 new tests, structured branch +
      defensive fail-loud).
    - Manual: 3 distinct Telegram add-recipe queries complete end-to-end with
      no dead-lettered steps, LangWatch traces tagged with prompt version and
      model. Captured in `.planning/phases/11-.../11-VERIFICATION.md`.

    ## References

    - Phase 11 CONTEXT.md (`.planning/phases/11-.../11-CONTEXT.md`) — locked decisions
    - Phase 11 RESEARCH.md (`.planning/phases/11-.../11-RESEARCH.md`) — verified API surface, pitfalls
    - `langchain.agents.factory.py:148–158` — `FALLBACK_MODELS_WITH_STRUCTURED_OUTPUT`
    - `langchain.agents.structured_output.py:194–304` — ToolStrategy / ProviderStrategy
    - `agent-12-migrate-to-create-agent.md` — Phase 10 prerequisite
    ```

    **Step 2: Update STATE.md.**

    Open `.planning/STATE.md`. Find the `### Decisions` section under `## Accumulated Context` (currently around line 99). After the last `[Phase 10-...]` decision entry (current line 184), append three new lines:

    ```
    - [Phase 11-structured-agent-output-via-response-format]: Per-provider Strategy mapping: Ollama → ToolStrategy (correctness — gpt-oss is in FALLBACK_MODELS_WITH_STRUCTURED_OUTPUT so AutoStrategy would silently route to ProviderStrategy and call bind_tools(strict=True, response_format=...) which Ollama does not honor); Anthropic / OpenAI → ProviderStrategy. Strategy selected inside each LLMBackend adapter, not in run_task.
    - [Phase 11-structured-agent-output-via-response-format]: AgentConfig.response_format_model is NOT overridable via AGENT_OVERRIDES_FILEPATH — schema is a code contract, not config. get_agent_config only propagates model_config and prompt_path; the response_format_model field flows directly from AGENT_REGISTRY.
    - [Phase 11-structured-agent-output-via-response-format]: _extract_task_output in workflow_runner.py rewritten — prefers result['structured_response'] when expects_structured=True (resolved by on_step_complete via get_agent_config(step.task_type).response_format_model is not None); raises ValueError loudly on missing. The prose-strip / markdown-fence / first-{-scan / json.loads fallback ladder + TEMP DIAGNOSTIC logger.error block are REMOVED. handle-incoming-message and acknowledge-add-recipe continue to use the return_direct tool-message branch (out of scope for Phase 11).
    ```

    Then find the `### Roadmap Evolution` block (currently lines 93–98). Add one new bullet:
    ```
    - Phase 11 complete: response_format adopted on 5 named agents; canelones-class parse failures structurally eliminated for those agents
    ```

    Then find the frontmatter at the top. Update:
    - `status: Phase 11 complete` (was `Phase 10 complete`)
    - `stopped_at: "Phase 11 complete: ... [summarize what 11-04 SUMMARY records]"`
    - `last_updated:` to today's ISO timestamp (2026-05-13)
    - `progress.completed_phases: 10` (was 9)
    - `progress.total_plans: 44` (was 40 — Phase 11 adds 4 plans)
    - `progress.completed_plans: 39` (was 35)
    - `progress.percent: 89` (recompute: 39/44 ≈ 89%)

    Update the `## Performance Metrics` table by appending 4 new rows for Phase 11 (use the same `| Phase 11-... PNN | XXmin | N tasks | N files |` format, with realistic counts from the SUMMARY files of plans 11-01 through 11-04). If 11-04 is being written before runtime metrics are known, use placeholder `| Phase 11-structured-agent-output-via-response-format PNN | TBD | N tasks | N files |` and the next plan-completion will fill in.

    Update `## Current Position`:
    - `Phase: 11 — COMPLETE`
    - `Plan: 4 of 4 (all plans complete; pending manual checkpoint approval in 11-VERIFICATION.md)`

    Update `Last activity` and `Last session` lines at the bottom.

    **Step 3: Update CLAUDE.md.**

    Open `CLAUDE.md`. The technology stack tables are quoted verbatim in this plan's frontmatter context. Make two targeted edits:

    1. In the **Core Technologies** table row for `LangChain`, append to the "Why Recommended" cell:
       > As of Phase 11, `create_agent(response_format=Schema)` is in active use on the 5 artifact-producing agents (recipe-research-gather/instructions/ingredients/metadata + recipe-load). See `.planning/decisions/response-format-adoption.md`.

    2. In the **What NOT to Use** table, append a new row:

       | Custom output parser that scans for prose, fences, JSON | This is exactly the canelones-class bug. LangChain's structured-output path (response_format=) is token-level (provider) or tool-arg-validated (Ollama). | `response_format=<Pydantic class>` on `langchain.agents.create_agent` (Phase 11) |

    Do NOT touch other sections of CLAUDE.md.

    **Step 4: Write the VERIFICATION.md template** at `.planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md`:

    ```markdown
    # Phase 11 — Verification

    **Status:** Pending manual checkpoint
    **Owner:** Solano (project user)
    **Created:** 2026-05-13

    ---

    ## Manual 3-query End-to-End Checkpoint

    **Goal:** Verify the 5 named agents emit structured output via the response_format / structured_response channel in production conditions; verify no canelones-class parse failures occur across 3 distinct recipe queries.

    **Setup (one-time before running queries):**
    1. Confirm Ollama daemon is running: `curl -s http://localhost:11434/api/tags | jq '.models[] | .name' | grep gpt-oss`
    2. Confirm `uv run agent` starts cleanly and worker logs show no `LangGraphDeprecatedSinceV10` warnings.
    3. Confirm LangWatch endpoint + API key are configured (`LANGWATCH_ENDPOINT`, `LANGWATCH_API_KEY` in env).
    4. Confirm the household-manager API is reachable (the recipe-load step needs `GET /api/foods` + `POST /api/recipes`).

    **Queries to run (one at a time, observe each end-to-end before queuing the next):**

    | # | Telegram message | Expected outcome |
    |---|------------------|------------------|
    | 1 | `agrega ñoquis de papa` | WorkflowRun completes all 6 steps; user receives Spanish confirmation with recipe name + app link |
    | 2 | `agrega tarta de zapallo` | Same — completes end-to-end |
    | 3 | `agrega milanesa de berenjena` | Same — completes end-to-end |

    **For each query, record:**

    - `workflow_run_id` (from worker logs — `Workflow queued | workflow_type=add-recipe run_id=<UUID>`)
    - `final state` of WorkflowRun (must be `DONE`)
    - LangWatch trace URL
    - Confirmation that each trace has `prompt_version` and `model` tags
    - Any unexpected `ValueError: structured_response missing` log lines (must be ZERO across all 3 runs)
    - Any Ollama 500 "error parsing tool call" retries (acceptable up to the 3-attempt budget; record count)

    ---

    ## Sign-Off

    **Query 1: `agrega ñoquis de papa`**

    - workflow_run_id: ___
    - final state: ___
    - LangWatch trace: ___
    - prompt_version + model tags present? [ ] yes / [ ] no
    - structured_response missing errors: ___
    - Ollama 500 retries: ___
    - Result: [ ] PASS / [ ] FAIL — notes:

    **Query 2: `agrega tarta de zapallo`**

    - workflow_run_id: ___
    - final state: ___
    - LangWatch trace: ___
    - prompt_version + model tags present? [ ] yes / [ ] no
    - structured_response missing errors: ___
    - Ollama 500 retries: ___
    - Result: [ ] PASS / [ ] FAIL — notes:

    **Query 3: `agrega milanesa de berenjena`**

    - workflow_run_id: ___
    - final state: ___
    - LangWatch trace: ___
    - prompt_version + model tags present? [ ] yes / [ ] no
    - structured_response missing errors: ___
    - Ollama 500 retries: ___
    - Result: [ ] PASS / [ ] FAIL — notes:

    ---

    ## Overall Sign-Off

    All 3 queries PASS: [ ] yes / [ ] no
    LangWatch trace tagging works: [ ] yes / [ ] no
    Zero canelones-class regressions: [ ] yes / [ ] no

    **Approval (write APPROVED if all three above are yes):** ___
    **Approved by:** ___
    **Approval date:** ___

    Only after APPROVED above, run Task 4.2 to flip RRECIPE-07 / RLOAD-07 / WF-10 to Complete in REQUIREMENTS.md.
    ```

    All three documentation files + the verification template are created by this task. Commit message suggestion: `docs(11): adopt response_format — decision record + STATE/CLAUDE.md updates + manual checkpoint template`.
  </action>
  <verify>
    <automated>test -f .planning/decisions/response-format-adoption.md && test -f .planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md && grep -c "Phase 11" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/decisions/response-format-adoption.md` exits 0
    - `test -f .planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md` exits 0
    - `grep -c "Phase 11" .planning/STATE.md` returns at least 4 (3 new decisions + roadmap evolution bullet)
    - `grep -c "response-format-adoption.md" CLAUDE.md` returns at least 1
    - `grep -c "response_format=" CLAUDE.md` returns at least 1 (in "What NOT to Use" → use-instead column)
    - `grep -n "ToolStrategy" .planning/decisions/response-format-adoption.md` exits 0 with at least 2 matches
    - `grep -n "FALLBACK_MODELS_WITH_STRUCTURED_OUTPUT" .planning/decisions/response-format-adoption.md` exits 0 (correctness rationale captured)
    - `grep -c "agrega ñoquis de papa\|agrega tarta de zapallo\|agrega milanesa de berenjena" .planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md` returns 3
  </acceptance_criteria>
  <done>Decision record exists in the standard format. STATE.md, CLAUDE.md cross-reference the decision and the new locked decisions are captured. VERIFICATION.md has the 3-query template ready for the user to fill in during the checkpoint task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.2 (CHECKPOINT): User runs the 3-query end-to-end verification, fills VERIFICATION.md, and signs off — only then flip REQUIREMENTS.md to Complete</name>
  <what-built>
    All Phase 11 code is in place (Plans 11-01 through 11-03):
    - `LLMBackend.create_agent` + 3 adapters accept `response_format=` and wrap in provider-correct Strategy (Plan 11-01)
    - `AgentConfig.response_format_model` field exists, non-overridable (Plan 11-01)
    - `_extract_task_output` rewritten with structured-response-first read + fail-loud (Plan 11-02)
    - `on_step_complete` resolves `expects_structured` from `AgentConfig.response_format_model` (Plan 11-02)
    - 5 named agents bound to their Pydantic Output models in `AGENT_REGISTRY` (Plan 11-03)
    - 5 new prompt version files written, JSON boilerplate stripped (Plan 11-03)
    - `run_task()` threads `response_format=` through (Plan 11-03)
    - Decision record + STATE.md + CLAUDE.md updated (Plan 11-04 Task 4.1)
    - VERIFICATION.md template ready (Plan 11-04 Task 4.1)

    Pre-checkpoint full-suite sanity check: `uv run pytest --ignore=tests/integration -x` MUST exit 0.
  </what-built>
  <how-to-verify>
    **Pre-flight (Claude runs):**
    1. Run `uv run pytest --ignore=tests/integration -x` — must be green. If not, fix before asking the user.
    2. Verify Ollama daemon is reachable: `curl -fs http://localhost:11434/api/tags | jq -r '.models[].name' | grep gpt-oss` — must include `gpt-oss:20b`. If not, surface to user that Ollama must be running with `gpt-oss:20b` pulled.
    3. Confirm `.env` / shell has `LANGWATCH_ENDPOINT` and `LANGWATCH_API_KEY` set (do not print the secret value; just confirm the env var is non-empty).

    **User actions (the user runs):**

    1. Start the agent: `uv run agent` in one terminal. Confirm clean startup — no `LangGraphDeprecatedSinceV10` warnings, no import errors.
    2. From Telegram, send these three messages one at a time, waiting for the final confirmation reply before sending the next:
       - `agrega ñoquis de papa`
       - `agrega tarta de zapallo`
       - `agrega milanesa de berenjena`
    3. For each, observe the worker logs and the Telegram reply. Confirm:
       - The workflow reaches `Workflow complete | run_id=...` (i.e., `WorkflowRun.status=DONE`)
       - No `ValueError: structured_response missing` lines anywhere in the logs
       - The final Telegram message is the recipe-confirmation Spanish text (with recipe name and app link)
    4. Open the LangWatch project UI. For each of the 3 traces, confirm `prompt_version` and `model` tags are present (the experiment-pinning code from Phase 8/9 should attach these via the standard pipeline).
    5. Fill in the sign-off blocks in `.planning/phases/11-structured-agent-output-via-response-format/11-VERIFICATION.md` with `workflow_run_id`, trace URLs, PASS/FAIL per query, and finally the overall sign-off line `Approval: APPROVED` (or `Approval: NEEDS REWORK` with notes).

    **If APPROVED:** Claude proceeds to flip REQUIREMENTS.md entries to Complete (see resume-signal below).
    **If NEEDS REWORK:** Claude does NOT flip REQUIREMENTS.md. Phase 11 stays at "In Progress." Surface the user's notes verbatim in the Plan 11-04 SUMMARY and stop. Address the reported issues via a follow-up plan or quick task.
  </how-to-verify>
  <resume-signal>
    When the user types "approved" (or pastes the filled VERIFICATION.md with `Approval: APPROVED`):

    Edit `.planning/REQUIREMENTS.md`:
    1. Flip `RRECIPE-07` line from `- [ ]` to `- [x]`
    2. Flip `RLOAD-07` line from `- [ ]` to `- [x]`
    3. Flip `WF-10` line from `- [ ]` to `- [x]`
    4. In the traceability table, change the `Status` column for `RRECIPE-07`, `RLOAD-07`, `WF-10` rows from `In Progress` to `Complete`
    5. Update the trailing `*Last updated:*` line to: `*Last updated: <today> — Phase 11 complete: RRECIPE-07 / RLOAD-07 / WF-10 marked Complete after manual 3-query checkpoint sign-off*`

    Then update ROADMAP.md Phase 11 entry from `- [ ] **Phase 11:** ...` to `- [x] **Phase 11:** ... (completed YYYY-MM-DD)` AND change the Plans count to `**Plans:** 4 plans` AND list each plan (11-01-PLAN.md, 11-02-PLAN.md, 11-03-PLAN.md, 11-04-PLAN.md) with checked boxes.

    Commit message: `docs(11): phase complete — RRECIPE-07/RLOAD-07/WF-10 ticked Complete post-checkpoint`

    Verify final state:
    - `grep -c "RRECIPE-07\|RLOAD-07\|WF-10" .planning/REQUIREMENTS.md | head -1` (sanity)
    - `grep -n "- \[x\] \*\*RRECIPE-07\*\*\|- \[x\] \*\*RLOAD-07\*\*\|- \[x\] \*\*WF-10\*\*" .planning/REQUIREMENTS.md` returns 3 lines
    - `grep -c "Phase 11.*Complete" .planning/REQUIREMENTS.md` returns at least 3
    - `grep -c "- \[x\] \*\*Phase 11:" .planning/ROADMAP.md` returns 1

    If the user typed anything other than approved (e.g. "needs rework, query 2 hung"), STOP. Do NOT touch REQUIREMENTS.md or ROADMAP.md. Record the user's feedback verbatim in the Plan 11-04 SUMMARY and surface as a blocker.
  </resume-signal>
</task>

</tasks>

<verification>
- All 4 doc files exist (`response-format-adoption.md`, STATE.md updates, CLAUDE.md updates, VERIFICATION.md template) — verified by acceptance grep gates
- The 3-query manual checkpoint is the gate on RRECIPE-07 / RLOAD-07 / WF-10 marking Complete
- ROADMAP.md Phase 11 entry transitions to `[x]` ONLY after user sign-off
</verification>

<success_criteria>
- Phase 11 decision rationale is permanently captured in `.planning/decisions/response-format-adoption.md` (not lost in a SUMMARY file)
- STATE.md Locked Decisions section now warns future-Claude that `response_format_model` is NOT overridable (preventing a future drift)
- CLAUDE.md stack table reflects the active use of response_format
- No requirement is silently marked Complete without manual checkpoint approval
- If the checkpoint fails, the user has a clear "NEEDS REWORK" path that does not contaminate the requirement tracking
</success_criteria>

<output>
After completion, create `.planning/phases/11-structured-agent-output-via-response-format/11-04-SUMMARY.md` documenting: documentation files created/updated, the 3-query checkpoint outcome (PASS / NEEDS REWORK + notes), the final REQUIREMENTS.md state (if APPROVED), and the ROADMAP.md state.
</output>
