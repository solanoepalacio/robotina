---
phase: quick-260509-m46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/robotina/agent/tools/household_manager_api.py
  - src/robotina/agent/tools/start_workflow.py
autonomous: true
requirements:
  - QUICK-260509-m46
must_haves:
  truths:
    - "When the agent reads the household-manager-api tool description, it sees explicit JSON-literal guidance covering null vs None/none, true/false vs True/False, and double-quoted strings."
    - "When the agent reads the start-workflow tool description, it sees the same explicit JSON-literal guidance."
    - "Importing each modified tool class and reading the .description attribute returns a string containing both the substring 'null' and the substring 'JSON'."
    - "No other tool's description string is changed by this work."
    - "All existing tests still pass — this is a description-only edit, no behavioral change."
  artifacts:
    - path: "src/robotina/agent/tools/household_manager_api.py"
      provides: "HouseholdManagerApiTool with JSON-literal guidance appended to description"
      contains: "null"
    - path: "src/robotina/agent/tools/start_workflow.py"
      provides: "StartWorkflowTool with JSON-literal guidance appended to description"
      contains: "null"
  key_links:
    - from: "HouseholdManagerApiTool.description"
      to: "LangGraph create_react_agent context"
      via: "tool binding (description injected into agent prompt every turn)"
      pattern: "null.*JSON|JSON.*null"
    - from: "StartWorkflowTool.description"
      to: "LangGraph create_react_agent context"
      via: "tool binding (description injected into agent prompt every turn)"
      pattern: "null.*JSON|JSON.*null"
---

<objective>
Add explicit JSON-literal guidance to the `description=` attribute of every BaseTool whose arguments include dict-shaped fields, so the LLM emits valid JSON (`null`, `true`, `false`, double-quoted strings) on tool calls instead of Python literals (`None`/`none`, `True`/`False`).

Purpose: Address the root cause of a workflow failure where the model emitted `{"query": none}` and tripped Ollama's tool-call validator (workaround `f801814` added retry; this is the upstream fix). Tool descriptions are injected into the agent context every turn the tool is bound — universal coverage with zero per-prompt overhead.

Output: Two modified tool files with augmented `description` strings. No other code, prompts, skills, env vars, or files are touched.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260509-m46-add-json-literal-guidance-to-dict-arg-to/260509-m46-BRIEF.md
@.planning/STATE.md
@./CLAUDE.md
@src/robotina/agent/tools/household_manager_api.py
@src/robotina/agent/tools/start_workflow.py

<interfaces>
<!-- Both tools are langchain_core.tools.BaseTool subclasses. The `description: str` -->
<!-- class attribute is what LangChain/LangGraph injects into the agent context every -->
<!-- turn the tool is bound. This is the only attribute being modified in this plan. -->

From src/robotina/agent/tools/household_manager_api.py (current description, lines 53–62):
```python
name: str = "household-manager-api"
description: str = (
    "Call the household-manager REST API. "
    "Read the household-manager skill before calling this tool to understand "
    "available endpoints, pagination, and error codes. "
    "Args: method (str) — HTTP method (GET/POST/PATCH/DELETE). "
    "path (str) — API path relative to base URL (e.g. /api/recipes). "
    "body (dict | None) — JSON request body for POST/PATCH. "
    "query (dict | None) — URL query parameters for filtering/pagination."
)
```

From src/robotina/agent/tools/start_workflow.py (current description, lines 54–61):
```python
name: str = "start-workflow"
description: str = (
    "Initiate a multi-step workflow. Creates a WorkflowRun and enqueues the first step.\n"
    "Args:\n"
    "  workflow_type (str): Workflow name, e.g. 'add-recipe'.\n"
    "  shared_context (dict): Task-specific fields (e.g. recipe_query). "
    "reply_context and household_id are injected automatically."
)
```

Required guidance content (per BRIEF — three rules MUST be explicit):
1. `null` (not `None` or `none`)
2. `true` / `false` (not `True` / `False`)
3. Strings must use double quotes

Suggested verbatim wording (BRIEF lines 26–28; adapt per tool but keep all three rules):
> Arguments are passed as JSON. Use JSON literals: `null` (not `None` or `none`), `true`/`false` (not `True`/`False`). Strings must use double quotes. Example: `{"query": null}`, not `{"query": none}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append JSON-literal guidance to household-manager-api and start-workflow tool descriptions</name>
  <files>src/robotina/agent/tools/household_manager_api.py, src/robotina/agent/tools/start_workflow.py</files>
  <behavior>
    - After edit, `HouseholdManagerApiTool().description` (or class attribute access) contains the substrings "JSON", "null", "true", "false", and a clear contrast against `None`/`None`/`True`/`False`.
    - After edit, `StartWorkflowTool().description` contains the same substrings.
    - The original args descriptions (method/path/body/query for household-manager-api; workflow_type/shared_context for start-workflow) remain present and intact — guidance is appended, not replaced.
    - No other file in `src/robotina/agent/tools/` is modified.
  </behavior>
  <action>
    Edit BOTH files to append JSON-literal guidance to the existing `description: str = (...)` literal. Do NOT replace the existing args documentation — keep it and append the new guidance.

    File 1: `src/robotina/agent/tools/household_manager_api.py`
    - Locate the `description: str = (` block at lines ~54-62 (after the `name: str = "household-manager-api"` line).
    - Append a final concatenated string segment INSIDE the parenthesized literal, AFTER the existing "query (dict | None) — URL query parameters for filtering/pagination." line.
    - The appended segment MUST explicitly state all three rules: (a) `null` not `None` or `none`, (b) `true`/`false` not `True`/`False`, (c) double-quoted strings. Include a concrete contrast example such as `{"query": null}` (correct) vs `{"query": none}` (wrong).
    - Suggested wording (you may adapt phrasing slightly but ALL THREE rules must remain explicit and the example must be present):
      `" Arguments are passed as JSON. Use JSON literals: null (not None or none), true/false (not True/False). Strings must use double quotes. Example: {\"query\": null}, not {\"query\": none}."`
    - Mind the leading space inside the new string segment — Python implicit string concatenation does NOT insert whitespace between adjacent literals.
    - Mind backslash-escaping: the example `{"query": null}` lives inside a double-quoted Python string, so the inner double quotes around `query` MUST be escaped (`\"query\"`).

    File 2: `src/robotina/agent/tools/start_workflow.py`
    - Locate the `description: str = (` block at lines ~55-61 (after the `name: str = "start-workflow"` line).
    - Append a final concatenated string segment INSIDE the parenthesized literal, AFTER the existing line ending in "reply_context and household_id are injected automatically.".
    - The appended segment MUST state the same three rules with a concrete example. Tailor the example to this tool's dict arg, e.g. `{"recipe_query": "lentil soup"}` (correct) vs single-quoted/Python-literal counter-example, OR reuse the same generic `{"key": null}` vs `{"key": none}` example. Keeping wording consistent across both tools is preferred so the model sees the same pattern twice.
    - Suggested wording (consistent with file 1):
      `"\nArguments are passed as JSON. Use JSON literals: null (not None or none), true/false (not True/False). Strings must use double quotes. Example: {\"recipe_query\": \"lentil soup\"}, not {'recipe_query': 'lentil soup'}."`
    - Note this file's existing description uses `\n` line breaks between sections — match that style: prefix the new segment with `\n` (or start a new "Notes:" line) so it renders as a separate line in the agent's view.

    Do NOT modify:
    - The tool's `name`, `args` schema, `_run`, `_arun`, or any other attribute/method.
    - The module docstring.
    - Any other tool file in `src/robotina/agent/tools/` (queue.py, read_skill.py, web_search.py — those have no dict args and are explicitly out of scope per BRIEF).
    - Any prompt, skill, or test file.

    Do NOT include the quick-task identifier `260509-m46` in any code, comment, or docstring. Per CLAUDE.md guidance, code must not reference current task IDs (they rot). Keep the identifier in the commit message and planning artifacts only.

    Both edits are a single logical change and must land in a single atomic commit. Project commit style is `fix(...)` or `feat(...)` with short scope (see `git log --oneline -10`); a suitable message is `fix(tools): add JSON-literal guidance to dict-arg tool descriptions`.
  </action>
  <verify>
    <automated>grep -n "null" src/robotina/agent/tools/household_manager_api.py src/robotina/agent/tools/start_workflow.py | grep -v '^#' | grep -c "null" | awk '{ if ($1 >= 2) print "PASS: null appears in both tool files"; else { print "FAIL: null missing"; exit 1 } }' &amp;&amp; uv run python -c "from robotina.agent.tools.household_manager_api import HouseholdManagerApiTool; from robotina.agent.tools.start_workflow import StartWorkflowTool; h = HouseholdManagerApiTool(household_id='test'); s = StartWorkflowTool(); assert 'null' in h.description and 'JSON' in h.description, f'household-manager-api missing guidance: {h.description!r}'; assert 'null' in s.description and 'JSON' in s.description, f'start-workflow missing guidance: {s.description!r}'; assert 'true' in h.description.lower() and 'false' in h.description.lower(), 'household-manager-api missing true/false guidance'; assert 'true' in s.description.lower() and 'false' in s.description.lower(), 'start-workflow missing true/false guidance'; print('PASS: both tool descriptions contain null, JSON, true, false')" &amp;&amp; uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - `src/robotina/agent/tools/household_manager_api.py` has its `description` attribute extended with JSON-literal guidance covering `null`, `true`/`false`, and double-quoted strings, plus a concrete example.
    - `src/robotina/agent/tools/start_workflow.py` has its `description` attribute extended with the same three rules and a concrete example.
    - The original args documentation in both descriptions is preserved (not replaced).
    - Importing each tool class and accessing `.description` returns a string containing `null`, `JSON`, `true`, and `false` (case-insensitive for the latter two).
    - The full pytest suite (`uv run pytest tests/`) passes — no behavioral regressions.
    - No other files are modified.
    - Single atomic commit lands on the working branch with a `fix(...)` or `feat(...)` style message.
    - The string `260509-m46` does NOT appear anywhere in the modified source files.
  </done>
</task>

</tasks>

<verification>
The single task's `<automated>` verify block covers all phase-level checks:

1. **String content check** — `grep -n "null"` confirms both files now mention `null` in their description strings.
2. **Runtime import check** — Imports each tool class, instantiates it, and asserts the `.description` attribute contains `null`, `JSON`, `true`, and `false`. This proves the LangChain-visible description (what the LLM actually sees at runtime) carries the new guidance, not just the source file.
3. **Regression check** — `uv run pytest tests/` confirms no existing test breaks. Since this is a description-only edit with no behavioral change, all tests must pass without modification.

Manual sanity check (optional, not blocking): visually diff each file against `git show HEAD:src/robotina/agent/tools/household_manager_api.py` to confirm only the description block changed and original args docs are preserved.
</verification>

<success_criteria>
- Both `HouseholdManagerApiTool.description` and `StartWorkflowTool.description` contain explicit JSON-literal guidance: `null` (vs `None`/`none`), `true`/`false` (vs `True`/`False`), double-quoted strings, and at least one concrete example contrasting JSON literal vs Python literal.
- All existing tests pass (`uv run pytest tests/`).
- No other files in the repo are modified — confirmed by `git diff --name-only` showing only the two target files.
- Single atomic commit with a conventional `fix(...)` or `feat(...)` message; the quick-task ID `260509-m46` appears in the commit message but NOT in the source files.
- The model, on its next workflow run that hits these tools, sees the new guidance in its bound-tool context and is more likely to emit `null` instead of `none` (this is the underlying motivation but not directly verifiable in this plan — the retry safety net from `f801814` remains in place either way).
</success_criteria>

<output>
After completion, create `.planning/quick/260509-m46-add-json-literal-guidance-to-dict-arg-to/260509-m46-SUMMARY.md` summarizing:
- Exact wording appended to each tool's description (paste both new segments verbatim).
- Confirmation that grep + import checks + pytest all passed.
- The commit hash of the single atomic commit.
- Any deviation from the suggested wording and why.
</output>
