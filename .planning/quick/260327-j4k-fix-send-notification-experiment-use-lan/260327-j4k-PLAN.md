---
phase: quick-260327-j4k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/send_notification.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Experiment runs inside a langwatch.Experiment context manager"
    - "Each case logs PASS or FAIL via evaluation.log()"
    - "Traces are flushed before the process exits"
  artifacts:
    - path: experiments/send_notification.py
      provides: "Updated experiment using Experiment context + evaluation.log + force_flush"
  key_links:
    - from: "main()"
      to: "langwatch.Experiment"
      via: "context manager wrapping the full test loop"
    - from: "run_experiment_case()"
      to: "evaluation.log()"
      via: "called after agent.invoke() with pass/fail outcome"
    - from: "main() exit"
      to: "trace.get_tracer_provider().force_flush()"
      via: "called in finally block before SystemExit"
---

<objective>
Fix the send_notification experiment to use the LangWatch Experiment API instead of
bare langwatch.trace() calls, record PASS/FAIL per case via evaluation.log(), and
flush OTel traces before the process exits.

Purpose: Traces currently may not be grouped under an experiment in LangWatch, and
force_flush is absent so traces can be silently dropped on process exit.
Output: experiments/send_notification.py with Experiment context, evaluation.log calls,
and force_flush teardown.
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor experiment to use langwatch.Experiment, evaluation.log, and force_flush</name>
  <files>experiments/send_notification.py</files>
  <action>
Rewrite experiments/send_notification.py with three targeted changes:

1. WRAP THE FULL LOOP in a langwatch.Experiment context manager in main():

    import langwatch
    from opentelemetry import trace

    with langwatch.Experiment(
        experiment_slug="send-notification",
        run_name=f"prompt-V001 model={config.model_config.get('model')}",
    ) as experiment:
        # existing for-loop runs here
        # (patch.object block stays inside the experiment context)

2. REPLACE per-case langwatch.trace() usage in run_experiment_case():
   - Remove the `with langwatch.trace() as trace:` block
   - Keep the agent.invoke() call and RunnableConfig with LangChainTracer
   - Add an `outcome` parameter (str) to the function signature
   - After agent.invoke(), call:
       experiment.log(
           input=text,
           output=formatted_text,  # pass this back up or restructure slightly
           passed=outcome == "PASS",
       )
   - Since evaluation.log needs the output, restructure so run_experiment_case
     returns the raw result and the caller determines pass/fail then logs:

     Restructure flow (caller in main()):
       result = run_experiment_case(agent, text, label, model_config, experiment)
       formatted = extract_formatted_text(result) or captured_outputs[0]
       issues = check_escaping(formatted) if formatted else ["tool not called"]
       status = "PASS" if not issues else f"WARN: {'; '.join(issues)}"
       experiment.log(
           input=text,
           output=formatted,
           passed=(not issues and bool(captured_outputs)),
       )

   - Inside run_experiment_case, keep langwatch.trace() only if langwatch.Experiment
     doesn't automatically trace child calls. If langwatch.Experiment already creates
     the trace context, remove the with langwatch.trace() block entirely and just
     invoke the agent directly. Pass `experiment` as a parameter to run_experiment_case
     so LangChainTracer can use the experiment's context if needed.

   The simplest correct approach:
   - run_experiment_case(agent, text, label, model_config) → returns result dict only
   - Remove `with langwatch.trace()` from run_experiment_case entirely
   - langwatch.Experiment handles the parent trace context
   - LangChainTracer() inside RunnableConfig remains (instruments LangChain calls)
   - Caller (main()) calls experiment.log() after extracting outcome

3. ADD force_flush before process exit at the bottom of main():

    finally:
        from opentelemetry import trace as otel_trace
        otel_trace.get_tracer_provider().force_flush()

   Structure main() with try/finally so force_flush runs even if a case raises:

    try:
        with langwatch.Experiment(...) as experiment:
            with patch.object(SendNotificationTool, "_run", capture_run):
                for i, case in enumerate(TEST_CASES, 1):
                    # ... existing loop body ...
                    experiment.log(input=..., output=..., passed=...)
    finally:
        from opentelemetry import trace as otel_trace
        otel_trace.get_tracer_provider().force_flush()

   Keep the existing error summary and SystemExit(1) logic inside the try block.
   Remove the top-level `import langwatch` from run_experiment_case (move to
   module-level imports at top of file alongside existing imports).
  </action>
  <verify>
    <automated>cd /home/solanoe/code/robotina-gsd && uv run python -c "import ast, sys; ast.parse(open('experiments/send_notification.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
    - experiments/send_notification.py passes syntax check
    - File contains `langwatch.Experiment` (not just `langwatch.trace`)
    - File contains `experiment.log(` call
    - File contains `force_flush()`
    - `with langwatch.trace()` is removed from run_experiment_case
  </done>
</task>

</tasks>

<verification>
grep -n "Experiment\|experiment\.log\|force_flush" /home/solanoe/code/robotina-gsd/experiments/send_notification.py
# Expected: lines showing langwatch.Experiment(...), experiment.log(...), force_flush()

grep -n "langwatch.trace()" /home/solanoe/code/robotina-gsd/experiments/send_notification.py
# Expected: no output (removed)
</verification>

<success_criteria>
- experiments/send_notification.py uses langwatch.Experiment as the outermost context
- Each test case calls experiment.log() with input, output, passed=bool
- force_flush() is called in a finally block before process exit
- No bare langwatch.trace() calls remain in run_experiment_case
- File is syntactically valid Python
</success_criteria>

<output>
After completion, create `.planning/quick/260327-j4k-fix-send-notification-experiment-use-lan/260327-j4k-SUMMARY.md`
</output>
