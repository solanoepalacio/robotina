---
phase: 260509-o56
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/robotina/agent/prompts/recipe-research-gather/V001.md
  - src/robotina/agent/prompts/recipe-research-instructions/V001.md
  - src/robotina/agent/prompts/recipe-research-ingredients/V001.md
  - src/robotina/agent/prompts/recipe-research-metadata/V001.md
  - src/robotina/agent/agents.py
  - src/robotina/agent/skills/recipe-research/index.md
  - src/robotina/agent/skills/recipe-research/gather.md
  - src/robotina/agent/skills/recipe-research/instructions.md
  - src/robotina/agent/skills/recipe-research/ingredients.md
  - src/robotina/agent/skills/recipe-research/metadata.md
  - tests/unit/test_skills.py
  - tests/unit/test_agents_registry.py
autonomous: true
requirements:
  - QUICK-260509-o56
must_haves:
  truths:
    - "Each of the 4 recipe-research sub-agent prompts is self-contained — no `read-skill` step, no reference to a recipe-research skill bundle, fully merges the per-step procedural content"
    - "agents.py shows `skills=[]` for all 4 recipe-research-* AgentConfigs"
    - "src/robotina/agent/skills/recipe-research/ no longer exists on disk"
    - "Test suite passes via `uv run pytest` (no test still asserts existence of recipe-research skill files or `skills==[\"recipe-research\"]`)"
    - "household-manager skill bundle is untouched — its directory and all 6 sub-files remain unchanged"
    - "Loader logic (read_skill.py, jobs.py) is untouched — the existing `if skill_sets:` guard at jobs.py:127 already handles `skills=[]` (no read-skill tool injected when skill list is empty)"
  artifacts:
    - path: "src/robotina/agent/prompts/recipe-research-gather/V001.md"
      provides: "Self-contained gather prompt with merged gather.md + index.md content"
      contains_not: "read-skill"
    - path: "src/robotina/agent/prompts/recipe-research-instructions/V001.md"
      provides: "Self-contained instructions prompt with consensus heuristic + JSON shape inlined"
      contains_not: "read-skill"
    - path: "src/robotina/agent/prompts/recipe-research-ingredients/V001.md"
      provides: "Self-contained ingredients prompt with food/unit verification rules + JSON shape inlined"
      contains_not: "read-skill"
    - path: "src/robotina/agent/prompts/recipe-research-metadata/V001.md"
      provides: "Self-contained metadata prompt with time/servings estimation + RecipeData JSON shape inlined"
      contains_not: "read-skill"
    - path: "src/robotina/agent/agents.py"
      provides: "AgentConfig registry with skills=[] for the 4 recipe-research-* agents"
      contains_not: "skills=[\"recipe-research\"]"
  key_links:
    - from: "src/robotina/queue/jobs.py:127"
      to: "AgentConfig.skills"
      via: "if skill_sets: guard — must skip read-skill tool injection when skills=[]"
      pattern: "if skill_sets:"
    - from: "tests/unit/test_skills.py"
      to: "src/robotina/agent/skills/recipe-research/"
      via: "test_recipe_research_skill_index_exists + test_recipe_research_skill_has_subfiles assert paths exist"
      pattern: "recipe-research"
    - from: "tests/unit/test_agents_registry.py"
      to: "AgentConfig.skills"
      via: "4 tests assert skills == [\"recipe-research\"]"
      pattern: "skills == \\[\"recipe-research\"\\]"
---

<objective>
Collapse the recipe-research skill bundle: inline each sub-file's content into its 1:1 sub-agent prompt, then delete the skill directory and set `skills=[]` for the four recipe-research-* agents in the registry.

Purpose: the recipe-research skill is per-agent procedural content masquerading as reusable knowledge — every sub-file is bound to exactly one agent and is never reused. The current "use read-skill as step 1" mandate forces every run to round-trip through a tool call only to load content that could ship in the prompt itself, and creates two sources of truth that drift. The household-manager skill bundle (used by 3 agents) is the genuine reference library and stays untouched.

Output:
- 4 self-contained sub-agent prompts at src/robotina/agent/prompts/recipe-research-{gather,instructions,ingredients,metadata}/V001.md
- agents.py with skills=[] for all 4 recipe-research-* agents
- src/robotina/agent/skills/recipe-research/ deleted
- Updated tests (test_skills.py, test_agents_registry.py) that no longer assert the deleted bundle
- `uv run pytest` passes
</objective>

<execution_context>
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/home/solanoe/code/robotina-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/solanoe/code/robotina-gsd/CLAUDE.md
@/home/solanoe/code/robotina-gsd/.planning/STATE.md

# Current sub-agent prompts (the ones being rewritten)
@/home/solanoe/code/robotina-gsd/src/robotina/agent/prompts/recipe-research-gather/V001.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/prompts/recipe-research-instructions/V001.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/prompts/recipe-research-ingredients/V001.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/prompts/recipe-research-metadata/V001.md

# Skill content to be inlined (then deleted)
@/home/solanoe/code/robotina-gsd/src/robotina/agent/skills/recipe-research/index.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/skills/recipe-research/gather.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/skills/recipe-research/instructions.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/skills/recipe-research/ingredients.md
@/home/solanoe/code/robotina-gsd/src/robotina/agent/skills/recipe-research/metadata.md

# Registry (locked except `skills=` field on the 4 recipe-research-* agents)
@/home/solanoe/code/robotina-gsd/src/robotina/agent/agents.py

# Loader (locked — DO NOT modify; the `if skill_sets:` guard already handles skills=[])
@/home/solanoe/code/robotina-gsd/src/robotina/queue/jobs.py
@/home/solanoe/code/robotina-gsd/src/robotina/agent/tools/read_skill.py

# Tests touched by this work
@/home/solanoe/code/robotina-gsd/tests/unit/test_skills.py
@/home/solanoe/code/robotina-gsd/tests/unit/test_prompts.py
@/home/solanoe/code/robotina-gsd/tests/unit/test_agents_registry.py

<scope_facts>
- The four sub-agent prompts are bound 1:1 to the four skill sub-files. There is no other consumer of the recipe-research skill — confirmed by `grep -rn "recipe-research" src/ tests/ experiments/`.
- The household-manager skill bundle (src/robotina/agent/skills/household-manager/) is the reference library used by 3 agents (handle-incoming-message, recipe-load, and historically others). It is OUT OF SCOPE — do not touch any file under src/robotina/agent/skills/household-manager/.
- jobs.py:127 already contains `if skill_sets:` — when AgentConfig.skills is `[]`, no SkillSet is constructed, no read-skill tool is injected, and the empty `skill_index` string is not appended to the prompt. So loader logic needs zero changes.
- The overrides/*.json files (anthropic.json, openai.json, staging.ollama.json) only override `model_config` for the recipe-research-* agents. They do not override `skills` (per agents.py:175-183 only model_config and prompt_path are overridable). Leave overrides files untouched — confirmed no-op.
- Existing test in test_prompts.py only asserts `prompt_path.exists()` and non-empty content; it does NOT assert prompt body content, so it stays green automatically once the prompts are rewritten in place.
- experiments/recipe_research.py references task_type strings only ("recipe-research-gather", etc.); it does NOT touch the skill bundle. No change needed.
- Filename versioning is preserved: the prompts stay at V001.md (per task description: "in-place clarification, not a behavior bump").
</scope_facts>

<interfaces>
<!-- The Argentine-Spanish register, JSON output rules, and the per-step JSON shapes
     are the load-bearing contracts the merged prompts must preserve verbatim from
     the skill files. Each contract is fully present in the skill files cited above. -->

JSON output rules (must appear in all 4 rewritten prompts, verbatim from skill files):
- Use the JSON literal `null` for missing optional fields — never the Python value `None`, never the bare word `none`, never an empty string in place of `null`.
- Booleans are `true` and `false`, lowercase.
- Numeric fields are bare numbers, never quoted.
- Do not omit optional fields when missing — emit them with `null`.

Output JSON shapes per agent (copy verbatim from the corresponding skill file):
- gather → {"recipes": [{"title", "url", "ingredients", "instructions", "prep_time", "cook_time", "total_time", "servings"}, ...]}
- instructions → {"draft_name", "draft_description", "draft_instructions": [{"body", "title"}]}
- ingredients → {"ingredients": [{"food_name", "unit_name", "quantity", "note"}]}
- metadata → {"recipe": {"name", "description", "servings_qty", "servings_unit", "prep_time", "cook_time", "total_time", "source_url", "ingredients": [...], "steps": [...]}}

Spanish register (must remain in all 4 prompts):
- All user-visible content (recipe names, ingredient names, step bodies, descriptions, search queries) MUST be in Argentine/Latin-American Spanish.
- Instructional/meta text in the prompt itself stays in English.

Tool availability per agent (after skills=[]):
- recipe-research-gather: web-search ONLY (no read-skill — `if skill_sets:` is false when skills=[])
- recipe-research-instructions: NO tools (no read-skill, no per-job tool — see jobs.py — there is no elif block for instructions)
- recipe-research-ingredients: household-manager-api ONLY (no read-skill)
- recipe-research-metadata: NO tools (no read-skill, no per-job tool — see jobs.py — there is no elif block for metadata)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline skill sub-files into the four sub-agent prompts</name>
  <files>
    src/robotina/agent/prompts/recipe-research-gather/V001.md,
    src/robotina/agent/prompts/recipe-research-instructions/V001.md,
    src/robotina/agent/prompts/recipe-research-ingredients/V001.md,
    src/robotina/agent/prompts/recipe-research-metadata/V001.md
  </files>
  <action>
    Rewrite all four sub-agent prompts in place (keep filename V001.md — this is in-place clarification, not a version bump). Each rewritten prompt must be FULLY SELF-CONTAINED — after this task an agent that reads only the prompt file should have everything it needs to do its job.

    Use the existing prompt as the skeleton (preserve its overall section ordering: role / available tools / process / rules / output format) and merge the corresponding skill sub-file's content into it. Apply these rules to ALL FOUR prompts:

    1. Remove the `read-skill` line from the "Available tools" section. Tools to list per agent:
       - gather: `web-search` only (Spanish search queries via Tavily)
       - instructions: NO "Available tools" section is needed (the agent has no tools after skills=[]); replace the section with one short sentence noting "This agent uses no tools — produce the JSON directly from the recipes in the user message."
       - ingredients: `household-manager-api` only (GET /api/foods?name= for food verification)
       - metadata: NO "Available tools" section is needed (same as instructions); replace with the same one-line statement.

    2. Remove "Use `read-skill` with `recipe-research/<step>.md`" from step 1 of the Process section in every prompt. Renumber the remaining steps so step 1 becomes the first real action.

    3. Inline the full per-step procedural content from the matching skill sub-file into the Process section. Specifically:
       - gather/V001.md ← merge ALL of skills/recipe-research/gather.md (Process steps 1-4: Build search queries with Argentine-Spanish examples, Search with web-search, Extract data from each result, Error handling) plus the relevant Argentine-Spanish register notes from skills/recipe-research/index.md.
       - instructions/V001.md ← merge ALL of skills/recipe-research/instructions.md (Process steps 1-3: Analyse the gathered recipes, Consensus "majority wins" heuristic with the 6/10 and tie-break examples, Write the instructions with Argentine kitchen vocabulary) plus the index.md Argentine-Spanish register note.
       - ingredients/V001.md ← merge ALL of skills/recipe-research/ingredients.md (Process steps 1-4: Extract ingredients with the food/quantity/unit/note structure and Spanish examples, Verify against household-manager via GET /api/foods?name=, Handle missing ingredients via substitute-from-other-recipes, "Do not create new foods" rule from line 28) plus the index.md Argentine-Spanish register note.
       - metadata/V001.md ← merge ALL of skills/recipe-research/metadata.md (Process steps 1-4: Review metadata sources, Estimate prep_time/cook_time/total_time, Estimate servings_qty/servings_unit with the default-of-4 fallback, Assemble final RecipeData) plus the "time fields are NEVER null" narrower constraint from metadata.md:48 plus the index.md Argentine-Spanish register note.

    4. Inline the full "Output format" JSON example from each skill sub-file verbatim — including the JSON shape and the **JSON output rules** paragraph (`null` literal not `None`/`none`/empty string, lowercase booleans, bare numbers not quoted, emit `null` for missing optional fields rather than omitting them). For metadata, also include the narrower "time fields must always be numeric — they are never null" constraint as a sub-bullet under the JSON output rules.

    5. Preserve the existing prompt's "Rules" section but extend it where the skill file added narrower constraints (e.g. metadata's "time fields are NEVER null", ingredients' "Do not create new foods", gather's "you need at least 1 usable recipe to succeed"). The existing "Rules" sections already cover most of this; cross-check each merged prompt against its skill file and add any missing rule.

    6. Where the existing prompt and the skill file say the same thing in different words, prefer the skill file's wording (it was the canonical source). The existing prompt's CORRECT/INCORRECT example block in gather/V001.md (lines 26-28) MUST be preserved — it's a defensive guard against the agent emitting a bare array.

    7. Keep all instructional/meta text in English; keep all user-facing content examples (recipe names, search queries, step bodies, ingredient names) in Argentine/Latin-American Spanish exactly as the skill files have them.

    DO NOT touch the household-manager skill bundle. DO NOT modify jobs.py or read_skill.py. DO NOT change the prompt filenames.
  </action>
  <verify>
    <automated>test ! -z "$(cat src/robotina/agent/prompts/recipe-research-gather/V001.md)" && test ! -z "$(cat src/robotina/agent/prompts/recipe-research-instructions/V001.md)" && test ! -z "$(cat src/robotina/agent/prompts/recipe-research-ingredients/V001.md)" && test ! -z "$(cat src/robotina/agent/prompts/recipe-research-metadata/V001.md)" && ! grep -l "read-skill" src/robotina/agent/prompts/recipe-research-*/V001.md && ! grep -l "recipe-research/.*\.md" src/robotina/agent/prompts/recipe-research-*/V001.md && grep -q "null" src/robotina/agent/prompts/recipe-research-gather/V001.md && grep -q "majority" src/robotina/agent/prompts/recipe-research-instructions/V001.md && grep -q "household-manager-api" src/robotina/agent/prompts/recipe-research-ingredients/V001.md && grep -q "porciones" src/robotina/agent/prompts/recipe-research-metadata/V001.md</automated>
  </verify>
  <done>
    All four V001.md prompt files are non-empty self-contained prompts. None of them contains the string `read-skill`. None of them references a `recipe-research/*.md` path. Each prompt contains the merged per-step content (consensus heuristic for instructions, food/unit verification rules for ingredients, time-and-servings estimation for metadata, query construction + extraction + error handling for gather), the inlined output JSON example block, the JSON output rules paragraph, and the Argentine-Spanish register guidance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set skills=[] in registry, delete the skill directory, update tests</name>
  <files>
    src/robotina/agent/agents.py,
    src/robotina/agent/skills/recipe-research/index.md,
    src/robotina/agent/skills/recipe-research/gather.md,
    src/robotina/agent/skills/recipe-research/instructions.md,
    src/robotina/agent/skills/recipe-research/ingredients.md,
    src/robotina/agent/skills/recipe-research/metadata.md,
    tests/unit/test_skills.py,
    tests/unit/test_agents_registry.py
  </files>
  <action>
    This task is atomic: registry + skill deletion + test updates MUST land in the same commit, otherwise the test suite breaks intermediately.

    Step 1 — Edit src/robotina/agent/agents.py at lines 79, 92, 105, 118: change `skills=["recipe-research"],` to `skills=[],` for all four recipe-research-* AgentConfigs. Do NOT touch any other field on those configs (model_config, prompt_path, tools, comments — locked). Do NOT touch the handle-incoming-message or recipe-load configs (their `skills=["household-manager"]` is correct and stays).

    Step 2 — Delete the entire src/robotina/agent/skills/recipe-research/ directory and all 5 files inside it (index.md, gather.md, instructions.md, ingredients.md, metadata.md). Use `rm -rf src/robotina/agent/skills/recipe-research/`. After deletion, confirm src/robotina/agent/skills/household-manager/ is still present and untouched.

    Step 3 — Update tests/unit/test_skills.py: delete the two tests that assert the recipe-research skill bundle exists:
      - `test_recipe_research_skill_index_exists` (lines 115-121)
      - `test_recipe_research_skill_has_subfiles` (lines 124-131)
    Leave every other test in the file untouched — the household-manager and generic ReadSkillTool tests remain.

    Step 4 — Update tests/unit/test_agents_registry.py: in the four tests
      - `test_recipe_research_gather_registered` (line ~117)
      - `test_recipe_research_instructions_registered` (line ~128)
      - `test_recipe_research_ingredients_registered` (line ~138)
      - `test_recipe_research_metadata_registered` (line ~148)
    change `assert config.skills == ["recipe-research"]` to `assert config.skills == []`. Leave docstrings, the model_config and prompt_path assertions, and every other test in the file untouched.

    Step 5 — Do NOT modify tests/unit/test_prompts.py — it only asserts prompt files exist and are non-empty, both of which are still true.

    Step 6 — Do NOT modify overrides/*.json. The recipe-research-* override entries only contain model_config (which is still overridable per agents.py:175-183); they have no skills field. Confirmed no-op.

    Do NOT modify src/robotina/queue/jobs.py, src/robotina/agent/tools/read_skill.py, src/robotina/agent/workflows.py, or experiments/recipe_research.py.
  </action>
  <verify>
    <automated>! test -d src/robotina/agent/skills/recipe-research && test -d src/robotina/agent/skills/household-manager && [ "$(grep -c 'skills=\["recipe-research"\]' src/robotina/agent/agents.py)" = "0" ] && [ "$(grep -c 'skills=\[\]' src/robotina/agent/agents.py)" -ge "5" ] && ! grep -q "test_recipe_research_skill_index_exists" tests/unit/test_skills.py && ! grep -q "test_recipe_research_skill_has_subfiles" tests/unit/test_skills.py && [ "$(grep -c 'config.skills == \["recipe-research"\]' tests/unit/test_agents_registry.py)" = "0" ] && [ "$(grep -c 'config.skills == \[\]' tests/unit/test_agents_registry.py)" -ge "5" ]</automated>
  </verify>
  <done>
    src/robotina/agent/skills/recipe-research/ no longer exists on disk. src/robotina/agent/skills/household-manager/ still exists and is unchanged. agents.py has zero occurrences of `skills=["recipe-research"]` and at least 5 occurrences of `skills=[]` (4 recipe-research-* + 1 acknowledge-add-recipe). tests/unit/test_skills.py has the two recipe-research bundle tests removed. tests/unit/test_agents_registry.py asserts `config.skills == []` for all 4 recipe-research-* agents.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the full test suite to verify the change</name>
  <files></files>
  <action>
    Run `uv run pytest` from the project root. The full suite must pass.

    Expected behaviour:
    - All previously-passing tests stay green.
    - The two deleted recipe-research-skill-bundle tests in test_skills.py are gone (so no FAILED for missing files).
    - The four recipe-research-* registry tests pass with the new `skills == []` assertion.
    - test_skill_index_appended_to_prompt (test_prompts.py) still passes — it uses handle-incoming-message which still has the household-manager skill, so the path is exercised.

    If any test fails:
    - If a test asserts the existence of recipe-research skill files or `skills == ["recipe-research"]`, it was missed in Task 2 — fix and re-run.
    - If jobs.py or read_skill.py raises because a recipe-research agent ran with skills=[], it means the `if skill_sets:` guard is missing or broken — read jobs.py:127 carefully; do NOT modify it without re-confirming the locked-loader constraint with the user.
    - Do not change the loader, the household-manager bundle, or the prompt filenames to make a test pass.

    Do not commit. The orchestrator handles commits.
  </action>
  <verify>
    <automated>uv run pytest 2>&1 | tail -20</automated>
  </verify>
  <done>
    `uv run pytest` exits 0. No tests are failing. No tests were skipped that were previously running. The two deleted tests do not appear in the output.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run these manual cross-checks (the orchestrator can do them as part of the summary):

1. `! test -d src/robotina/agent/skills/recipe-research` → true (directory gone)
2. `test -d src/robotina/agent/skills/household-manager` → true (untouched)
3. `grep -c "read-skill" src/robotina/agent/prompts/recipe-research-*/V001.md` → 0 (no prompt mentions read-skill)
4. `grep -c "recipe-research/" src/robotina/agent/prompts/recipe-research-*/V001.md` → 0 (no prompt references skill paths)
5. `grep -c 'skills=\["recipe-research"\]' src/robotina/agent/agents.py` → 0
6. `uv run pytest` → exit 0
7. `git diff src/robotina/queue/jobs.py src/robotina/agent/tools/read_skill.py` → empty (loader untouched)
8. `git diff src/robotina/agent/skills/household-manager/` → empty (other skill bundle untouched)
9. `git diff overrides/` → empty (overrides untouched — confirmed no-op)
</verification>

<success_criteria>
- 4 recipe-research-* sub-agent prompts are self-contained (no read-skill, no skill-path reference, full per-step procedural content + JSON shape + JSON output rules + Argentine-Spanish guidance inlined)
- agents.py has `skills=[]` for all 4 recipe-research-* agents (and only those — handle-incoming-message and recipe-load still have `skills=["household-manager"]`)
- src/robotina/agent/skills/recipe-research/ no longer exists
- household-manager skill bundle untouched
- Loader files (jobs.py, read_skill.py) untouched — the existing `if skill_sets:` guard handles skills=[] cleanly
- Test suite passes via `uv run pytest`
- Tests no longer assert the deleted recipe-research bundle (test_skills.py) and assert `skills == []` for the 4 agents (test_agents_registry.py)
</success_criteria>

<output>
After completion, create `.planning/quick/260509-o56-inline-recipe-research-skill-files-into-/260509-o56-SUMMARY.md` per the standard summary template.
</output>
